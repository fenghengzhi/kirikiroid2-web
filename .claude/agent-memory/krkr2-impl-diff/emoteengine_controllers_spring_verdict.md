---
name: emoteengine-controllers-spring-verdict
description: EmoteEngine subsystem vs libkrkr2.so. 2026-07-18 update: controller/spring/direct sinks/raw mirror/HM3 timeline chain-bind/clamp are live and source-structure aligned; supersedes the former P1 sink and decoded-timeline gaps.
metadata:
  type: project
---

> **2026-07-19 纠正：** 本文内所有 `_activeMotion` 数据源描述仅记录旧实现，已被
> +528/+1012 raw Variant 与 Engine raw controller/HM 链取代；不得再作为当前架构结论。

2026-06-06 FRESH full re-decompile (read-only) of: 0x671228 setVariable, 0x67D01C progress, 0x67D4D0 builder, 0x662768 springStep, 0x6689A4 bustChainStep, 0x67C560 chain-bind cascade, 0x67C8A8 clamp, 0x663BDC eye step, 0x666068 mouth step, 0x67D380 applyVarControllers(direct 4). SUPERSEDES 06-04/06-05 on two points (legacy dead-path now removed; new P1 gap found).

## VERDICT per audit dim
- setVariable 0x671228 ✅ (EmoteEngine.cpp:1646). HM6-index reader: _scalarHM6_1384.find -> EmoteVarRef{type,index} -> switch -> typed_deque[index] -> 5 enqueue handlers. cases0/1/2 _syncWaiting(+1159) gate ✅; case4 eye(deque4 16B)/case5 eyebrow(deque5 16B)/case6 mouth(deque6 24B DUAL-key: key==label->ctl+108=(int)value @0x6716a8; key==talkLabel->enqueue @0x6716a0)/case7 transition(deque8 24B, elem+16 flag gate)/case8 selector(deque9 48B, elem+16 flag gate) ✅; easeWeight 3-branch(0x671304: ==0->1.0, >0->d+1, <0->1/(1-d)) ✅; HM2(_labelToValueHM7) fallthrough ✅. Strides byte-verified vs binary (16/16/24/24/48).
- progress 0x67D01C ✅ (EmoteEngine.cpp:1777). dt==0 top gate(0x530a60) ✅; originalDt(v12) vs drained dt(v14) split ✅; step=fmin(dt,1.1) ✅; 6-deque step order incl. NON-OBVIOUS selector(engine+656)@0x67d1e0 BEFORE transition(engine+576)@0x67d240 ✅; mouth feeds TWO HM7 keys(label=beginFrame, talkLabel=current) ✅; deque#10 loop INLINE curve sampler reified into EmoteLoopController_step ✅; post-loop bind-loop(HM7@+1456)@0x67d3a4 ✅; clamp@0x67d3f8 ✅; sub_6D2A54(originalDt)@0x67d408 ✅; final physics gate `originalDt!=0 && !_syncWaiting`@0x67d414 + 3 ctl-steps(1104/1112/1120)+stepHairParts+2x stepBust(consts +1184/+1192) ✅.
- builder 0x67D4D0 ✅ — full key dispatch present (eye/eyebrow/mouth/transition/selector/loop/clamp/mirror/bust/hair/parts/timeline/variableList/instantVariableList). mirror byte +1157/+1158(v7!=v6) ✅, scale +1168 + 1176 denom ✅.
- spring 0x662768 ✅ EmoteSpring.cpp:31 byte-verified 1:1. firstFlag init/accum, k_a/k_b/drag*a8, rest-vec qword_1AB7E74{0,1}, atanf(.)/0.0392699082, all float literals(0.0451603944) exact. RAW float-bits field reads (no SCVTF). REAL sinf/cosf/atanf.
- chain-spring 0x6689A4 ✅ EmoteSpring.cpp:120 byte-verified 1:1. 2-seg loop(v28 0..1), per-seg constraint, +168 collision curve(128 entries stride12, scale+1556), velocity damp, dual atan outputs(a2/a3 seg-X, a4 last-Y). sqrtf/sinf/cosf/atanf REAL.
- chain-bind cascade 0x67C560 ✅ (2026-07-18 later correction): live receiver is `EmoteEngine`; it iterates active-label vector@+1040, HM3@+936, and `EmoteTimelineData80B::variableList`'s 56B track deque. HM3 owns the raw element plus timeline data; builder/play/stop/seek/query/fade/blend populate and consume the same raw state machine. The former Player decoded `_timelines/timelineControlByLabel/controlTrackValues` delegation is no longer on this Engine path.
- clamp 0x67C8A8 ✅ (2026-07-18 later correction): live body belongs to `EmoteEngine`, iterates raw `_clampControlDeque7`, reads Engine `_labelToValueHM7`, uses Engine raw mirror lookup, invokes the Engine HM3/+1040 `0x67C560` cascade, and binds through the embedded Player. Normalize/squircle/circle math and branch boundaries match.
- container ✅ — typed std::deque<EntryN> per controller(EmoteEngine.h:517-561) + HM6=_scalarHM6_1384(unordered_map<ttstr,EmoteVarRef>) + HM7=_labelToValueHM7(LabelValueMap). libstdc++ deque/unordered_map selection-aligned (no #pragma pack, per byte-layout methodology). Deque element layouts documented to match binary strides.

## 06-05 LEGACY DEAD-PATH: NOW REMOVED (correction to that memory)
The 06-05 "PlayerFrameProgress.cpp:2001-2014 stepControllerBucket over _type4..8ControllerAnimators legacy deques disguised as 0x67D01C-aligned" is GONE. grep confirms: NO _typeNControllerAnimators / stepControllerBucket anywhere. Line 2001-2014 is now an unrelated faithful firstFrame-gate comment. RESIDUE: 3 caller-less Player helpers remain (stepTimelineControlAnimatorsLike_0x67D01C / stepTimelineBlendAnimatorsLike_0x67D01C / refreshFixedControllerEvalOutputsLike_0x67D01C — PlayerFrameProgress.cpp:194/222/245; the last is explicitly marked caller-less). These are timeline-animator helpers (HM3 path), declared+defined but uninvoked. Low severity, oracle-inert, no longer misattributed to 0x67D01C step path (carried faithfully by EmoteEngine::progress typed-deque loop).

## 2026-06-06 RE-VERIFY of the 3 caller-less helpers (read-only, fresh 0x67D01C decompile) — VERDICT: DELETE all 3
- Caller-count CONFIRMED ZERO: grep over cpp/ (multiple terms: full names, partial stems stepTimelineControl/stepTimelineBlend/refreshFixedControllerEval, and TimelineControl/TimelineBlend/FixedControllerEval) -> each helper has exactly 2 hits = decl(Player.h:809/810/817) + def(PlayerFrameProgress.cpp:194/222/245). No 3rd reference anywhere. True caller-less.
- BINARY OWNERSHIP of controller stepping (fresh decompile 0x67D01C): v13=engine(*(*(a1+24)+8)). ALL stepping done by 6 EmoteEngine typed-deque loops (+256 deque4/+336/+416/+576/+656/+736), each -> Player_HM2_upsert_labelToValue(engine+1440,...) i.e. HM7. NO Player-side per-timeline animator step exists in binary. The 3 helpers operate on Player _timelines/_playingTimelineLabels/controlTrackAnimators/blendAnimator/fixedControllerOutputs — NONE of these has any counterpart in 0x67D01C. -> these 3 helpers have NO binary correspondent = pure 06-03 parallel-model residue. Same disposition as #5 type4-8 dead-container removal: DELETE (decl + def, all 3).
- **2026-07-19 纠正（取代上述旧 collateral 判断）**：Player timeline containers 并非
  live Android API。`Player_ncb_registerMembers@0x6D69C8` 无 timeline 成员；
  `0x672F70/0x67C2A0/0x673F98/0x66F80C` 的 receiver 均是 EmoteEngine。
  `_timelines/_playingTimelineLabels`、control/blend animator、decoded timeline 派生表及
  PlayerTimeline 公开面现已全部删除。旧“DO NOT delete”结论方向相反，禁止继续引用。
- VERIFICATION GAP: removal is oracle-inert (helpers never executed) -> logo 0-mismatch unaffected = non-regression guard only, cannot positively confirm. Confidence rests on (a) byte/term-exhaustive zero-caller grep, (b) fresh 0x67D01C showing engine-side ownership with no Player-timeline-step counterpart.
- IDB: 0x67D01C head comment added recording this verdict (idb_save'd 2026-06-06).
PRIOR (now superseded "保留以备厘清"): STUB_WARN macro defined EmoteEngine.cpp:20 but NEVER invoked. No TODO/FIXME in EmoteSpring/Emote*Controller/VarController/BlinkRng/WindEmitter/MeshResolver.

## 2026-06-07 RE-AUDIT (read-only, fresh full re-decompile of 0x671228/0x67D01C/0x67D4D0/0x662768/0x6689A4/0x67C560/0x67C8A8/0x663BDC/0x666068/0x67C6B0): subsystem now ESSENTIALLY 1:1.
- **P1 gap (applyVarControllers 4 sinks) is now RESOLVED/WIRED** (was open 06-06). EmoteEngine.cpp:281-324 now calls all 4 Player write-backs LIVE in binary order pos->color->scale->angle: setCoord(out0,out1)@0x6CCFF8, setColorWeight(packed ARGB)@0x6CD724, +1176 scale denom + setSlant(out0)@0x6C0F54, setAngleDeg(out0)@0x6C0F84. No more commented TODO. Pack/swizzle/order all match.
- DEAD-PATH _typeNControllerAnimators/stepControllerBucket/stepTimeline*/refreshFixedControllerEval*: grep CONFIRMS removed (only removal-note comment PlayerFrameProgress.cpp:194-195).
- float-bits: eye(EmoteBlinkController.cpp:261 trackPow)+mouth(EmoteMouthController.cpp:130 powField) std::memcpy raw-bits (NOT SCVTF); real std::pow; blink RNG real EmoteBlinkRng(sub_9F17D0/9F1A08). EmoteSpring.cpp byte-1:1 real sinf/cosf/atanf/sqrt/fmod + exact literals. NO STUB_WARN/TODO in any controller/spring file.

## OPEN GAPS (by severity) — only ONE 🟡 remains after 06-07
- **[RESOLVED 06-07] P1 applyVarControllers 4 sinks (0x67D380) — now WIRED, see above. Former text:** **applyVarControllers_pos_scale_color_angle (0x67D380) 4 direct-controller SINKS unwired.** EmoteEngine.cpp:266-306. Binary steps ctlPosition(+1072,cnt2)/ctlColor(+1088,cnt4)/ctlScale(+1080,cnt1)/ctlAngle(+1096) and IMMEDIATELY applies each: Player_setCoord(out0,out1)@0x6CCFF8, sub_6CD724(packed ARGB)@0x6CD724, Player_setSlant(out0,out0), Player_setAngleDeg(out0)@0x6C0F84. Binary order pos->color->scale->angle. Local steps all 4 controllers (internal state advances) + writes the +1176 scale denom (_meshDivisionRatioDup) LIVE, but the 4 Player write-backs are commented-out TODO(P1). VERIFIED the sinks EXIST locally and are callable: Player::setCoord(Player.h:756), setColorWeight(Player.h:351), setSlant(Player.h:494), setAngleDeg(Player.h:660). So "PLATFORM_BOUNDARY" label (06-04) is WRONG — sinks are implemented; this is a missing wire, fixable. The 4 emote direct-controller outputs (root coord/color/scale-slant/angle) never reach the Player each slice -> emote-driven whole-body coord/color/scale/angle animation inert. Per CLAUDE.md "sink not yet wired" is not a platform boundary.
- 🟡 MEDIUM sub_67C6B0 mirror-flag (RE-VERIFIED 2026-06-07): local Player::shouldMirrorEvalLabelLike_0x67C6B0 (PlayerVariable.cpp:162) uses EXACT std::find over _activeMotion->mirrorVariableMatchList. Binary 0x67C6B0: +1158 gate -> two memo HM lookups (+824 hit-cache @0x67c76c via sub_6899F0 / +880 miss-cache @0x67c804) -> on miss scan vector(+800/+808) with `ttstr_indexOf(label,entry,0) < 1` = SUBSTRING/PREFIX match -> memoize into +824(match)/+880(miss) via sub_68BF40. Local = exact-match, no memo. Divergence only for superstring/prefix mirror labels; motion-data dependent; logo-inert.
- CORRECTED 2026-07-12: 0x67d060 calls EmoteEngine_preProgress_guess@0x671764 with X0=engine/W1=0/V0=original dt. The former Player::frameProgress call was wrong and is removed; the EmoteEngine call is restored. DRACU START still hangs afterward, so this was a real topology repair but not the sole hang cause.
- 🟢 LOW bust/hair/parts physics deques inert on logo fixture (no metadata) — structure correct, just no data; targets/spring-consts (+1104/1112/1120, +1184/1192) zero until variableList path resolves them.

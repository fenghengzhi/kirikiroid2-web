# Frida version must match the frida-server binary pushed to the
# device (see README "One-time setup"). Bumping here is a two-sided
# change: update the CI download URL and verify fresh trace artifacts at
# the same time, because tracer output can evolve between releases.
frida==16.4.10
Pillow

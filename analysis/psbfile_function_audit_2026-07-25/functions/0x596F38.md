# `0x596F38` — `PSBValueDispatch_native_Invalidate_primary_nullsub`

## 节点身份

- canonical 地址：`0x596F38`
- canonical 名称：`PSBValueDispatch_native_Invalidate_primary_nullsub`
- IDA 当前名称：`nullsub_258`；这是 IDA 对空函数生成的标签，不作为原始 C++ 符号名证据。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。该边表示 parent 构造的对象安装了包含本入口的 primary
  vtable，不是 `0x597AD4 -> 0x596F38` 的直接调用。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596f38`
- 审计日期：2026-07-25
- 唯一权威来源：Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25，本 function subagent fresh 执行了 `decompile(0x596F38)`，并补充执行完整
`disasm(0x596F38)`、`xrefs_to(0x596F38)`、`lookup_funcs(0x596F38)`、primary/secondary
vtable 原始字节读取及 vtable 地址点 xref 查询。本报告为本轮新建，不是旧报告冒充本轮结果。

fresh decompile 的完整函数体为：

```c
void nullsub_258()
{
  ; /*0x596f38*/
}
```

补充证据：

- `lookup_funcs` 给出函数范围大小 `0x4`；完整反汇编恰好只有
  `0x596F38: RET` 一条指令。函数没有参数读取、内存访问、条件跳转、返回值构造、
  `BL/BLR`、尾调用或异常恢复路径。
- `xrefs_to(0x596F38)` 恰好只有一条 data xref：`0x1A0B4C8`，没有 code xref。
- primary vtable address point 是 `0x1A0B3D8`；
  `0x1A0B4C8 - 0x1A0B3D8 = 0xF0 = 30 * 8`，故该 data xref 精确落在 vslot 30。
- `get_bytes(0x1A0B4C0, 24)` 的三个连续 64 位表项依次为
  `0x597A30`、`0x596F38`、`0x597A28`，即 primary vtable slot 29、本节点 slot 30、
  slot 31。相邻表项只用于定位，不表示函数间调用。
- `xrefs_to(0x1A0B3D8)` 显示 parent `0x597AD4` 在 `0x597ADC` 引用/安装该 primary
  vtable；另有 `0x59673C`、`0x597A40` 的对象重置/释放上下文引用。没有任一引用证明这些
  函数直接调用本入口。
- secondary vtable `0x1A0B4E8` 的三个表项为 `0x597A38`、`0x596F3C`、
  `0x597A2C`；这只证明 `0x596F3C` 是 secondary slot 1 的独立 sibling/child 入口，
  其函数体与六维结论留给该地址自己的 fresh 审计。

## Android vtable / NCB / 调用链归属

本入口是 `PSBValueDispatch` primary vtable 的 vslot 30，对应其
`iTJSNativeInstance::Invalidate()` override 在 primary address point 中的入口。它不是 NCB
member wrapper、注册 callback 或业务 helper；正常到达方式是通过 native-instance 虚槽间接
分派。函数本体立即返回且没有 callee，因此调用链在本入口处终止。

## Android 关键伪代码（5 行）

```text
PSBValueDispatch::native_Invalidate_primary(self):
    // 不读取 self，不访问 dispatch、raw owner、node、valid 或引用计数
    // 不清理、不析构、不写回，也没有成功/失败返回值
    // 无条件、无默认值选择、无 callee、无局部异常处理
    return
```

## 本地精确实现与逐行对照

本地精确位置：

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 同时继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`，建立 Android 双 vtable 归属所对应的普通 C++
  结构。
- `cpp/plugins/psbfile/PSBDispatch.h:74-78`：在同一派生类中声明 `Construct`、本节点
  `void Invalidate() override`、`Destruct` 三个 native-instance 方法；本节点声明精确位于
  `:76`。
- `cpp/plugins/psbfile/main.cpp:476-479`：符号 `PSB::PSBValueDispatch::Invalidate()` 的
  独立 out-of-line 定义，body 为空；`:739-740` 仅记录 Android 两个入口地址，不产生
  运行时行为。
- `cpp/core/tjs2/tjsInterface.h:326-335`：`iTJSNativeInstance` 的虚接口顺序为
  `Construct`、`Invalidate`、`Destruct`，其中本节点对应声明在 `:332`。

| Android 伪代码步骤 | 本地逐行复刻 |
| --- | --- |
| 行 1，作为 native-instance `Invalidate` 虚槽进入 | `PSBDispatch.h:16-17,74-78` 保留双继承与独立 `Invalidate() override`；`tjsInterface.h:326-335` 保留接口槽语义和顺序。 |
| 行 2，不读取 `self` 或任何对象字段 | `main.cpp:476-479` 的空 body 没有字段、指针或引用计数访问。 |
| 行 3，不清理、不析构、不写回且无返回值 | 本地返回类型同为 `void`，空 body 不调用 `Release`、析构或状态重置。 |
| 行 4，无分支、默认值、callee 或异常包装 | 本地函数体没有 guard、helper、返回码或异常处理。 |
| 行 5，直接返回 | 空 `void` body 到达函数末尾，语义与 Android 单条 `RET` 一致。 |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留 primary vtable slot 30 的独立 4-byte 空入口；本地保留 `PSBValueDispatch` 双继承结构中的独立 out-of-line `void Invalidate() override` 空定义。stripped 二进制不能证明原始标识符拼写，`MATCH` 仅覆盖可证的虚槽/独立空函数结构。 |
| 数据流 | `MATCH` | Android 只有 `RET`，不读取隐式 `self`、不访问成员且无输出；本地空 body 同样没有读写、传递或返回数据。 |
| 调用链 | `MATCH` | Android 唯一函数 xref 是 primary vtable data 项，表明 vdispatch 间接到达；本体无 callee。parent 只安装 vtable，本地则通过 `iTJSNativeInstance::Invalidate` override 保留相同“虚分派进入、立即终止”的形状，没有把 parent 或 sibling 伪装成 direct call。 |
| 对象生命周期 | `MATCH` | 虽然本入口是生命周期回调，Android 明确不释放 raw owner、不改 dispatch refcount/valid、不析构或清空对象；本地 `Invalidate()` 也故意为空，保留完全相同的 no-op 生命周期效果。 |
| 内部容器实现 | `N/A` | 完整单指令入口不创建、遍历、查询或修改 packed/TJS/STL/自定义容器。 |
| 边界行为 | `MATCH` | Android 对所有正常进入该 emitted body 的调用均无条件立即返回，没有空值检查、fallback、错误码、局部异常或参数相关分支；本地空 `void` override 一致。本机器码事实不扩张为“以非法 `this` 进行 C++ 调用是受支持契约”。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整单指令反汇编、唯一 vtable data xref、primary slot 精确定位与本地
空 override 可证明范围内，未发现 `0x596F38` 的六维确定偏差。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 阶段 A 本轮未 spawn。
- 尚待阶段 B 派发的唯一 canonical 直接子节点：
  `0x596F3C PSBValueDispatch_native_Invalidate_secondary_nullsub [vtable-member]`。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x596F38` 条目。
- `@597AD4` 是安装包含本入口的 primary vtable 的 canonical parent，不是 direct caller；
  `@59673C` 与 `@597A40` 只是同一 vtable address point 的其他引用上下文。
- `@597A30`、`@597A28` 是相邻 primary vtable siblings；`@597A38`、`@597A2C` 是
  secondary vtable 中 `@596F3C` 的相邻 siblings。它们都不是本入口的 caller/callee，也不为
  它们创建重复 agent。

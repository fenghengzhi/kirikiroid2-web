# `0x596E0C` — `PSBValueDispatch_OperationByNum`

## 节点身份

- 审计日期：`2026-07-25`
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596e0c`
- 分组：`A`（PSBValueDispatch / packed dispatch ABI）
- canonical 地址：`0x596E0C`
- canonical 名称：`PSBValueDispatch_OperationByNum`
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 表示构造对象所安装 primary vtable 的成员归属，
  不表示 `0x597AD4` 直接调用本入口。
- TASK_TREE 直接子节点：无。
- 名称证据边界：Android stripped 二进制没有保留可独立证明原始 C++ 拼写的符号；
  本报告沿用 MANIFEST/TASK_TREE canonical 标签。归属由 primary vtable 槽位与
  `iTJSDispatch2` ABI 顺序证明，本地名称只用于实现对照，不能反向命名二进制。
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本新会话由上述 function agent 对唯一目标地址执行了以下 fresh IDA MCP
取证：

1. `decompile(addr="0x596E0C")`：fresh 结果为
   `__int64 PSBValueDispatch_OperationByNum() { return 4294966294LL; }`；返回值低
   32 位为 `0xFFFFFC16`，按 `tjs_error` 解释为 `-1002`。
2. `disasm(addr="0x596E0C", include_total=true)`：完整函数只有
   `0x596E0C: MOV W0, #0xFFFFFC16` 与 `0x596E10: RET`，共 2 条指令；没有参数读取、
   条件分支、调用、内存访问或输出写回。
3. `lookup_funcs(0x596E0C/0x596E14/0x596D90)`：本函数边界为
   `0x596E0C..0x596E13`、size `0x8`；相邻 `Operation@0x596E14` 与
   `NativeInstanceSupport@0x596D90` 是独立入口，没有函数合并。
4. `xrefs_to(0x596E0C)`：唯一引用是 data xref `0x1A0B498`，没有 code xref。
5. `get_bytes(0x1A0B480, 48)`：依次恢复
   `@596E24, @596E1C, @596E14, @596E0C, @596D90, @596D88`。已知 primary
   vtable address point 为 `0x1A0B3D8`，故 `0x1A0B498 = address point + 24*8`，
   精确证明本入口是 `PSBValueDispatch` primary `iTJSDispatch2` vslot 24，位于
   `Operation` 与 `NativeInstanceSupport` 之间。

本节点不是 NCB wrapper、注册 callback 或 helper，也没有 Android direct caller；运行期
经 primary vtable slot 24 间接进入。TASK_TREE 将它挂在构造节点 `@597AD4` 下的
`[vtable-member]` 关系准确，没有把 vtable sibling/归属伪装成直接调用。

## Android 伪代码（4 行）

```cpp
tjs_error PSBValueDispatch::OperationByNum(flag, num, result, param, objthis) {
    ignore(flag, num, result, param, objthis);
    return TJS_E_NOTIMPL; // -1002；无条件、默认值或其他失败分支
}
```

完整二指令体证明：任意 index、operation flag 与指针输入均走同一失败边界；函数不验证
`this/num/param/objthis`，不解引用空指针，不修改 `result`，也没有异常捕获或 callee
失败路径。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 直接实现
  `iTJSDispatch2`，形成本节点所属的 primary dispatch vtable。
- `cpp/plugins/psbfile/PSBDispatch.h:98-112`：在 `Reserved1`、`IsInstanceOfByNum`、
  `Operation` 后及 `ClassInstanceInfo` 前独立覆写接口；目标的五参数声明精确位于
  `:107-109`。
- `cpp/plugins/psbfile/main.cpp:538-542`：独立定义
  `PSBValueDispatch::OperationByNum(...)`；所有形参省略名称，函数体只返回
  `TJS_E_NOTIMPL`。
- `cpp/core/tjs2/tjsInterface.h:280-311`：`iTJSDispatch2` 依次声明
  `IsInstanceOfByNum`、`Operation`、`OperationByNum`、`NativeInstanceSupport`；目标完整
  ABI 参数位于 `:298-305`，与 Android vslot 22..25 的顺序一致。
- `cpp/core/tjs2/tjsTypes.h:35-42,57`：`tjs_error` 是 32 位有符号 `tjs_int32`；因此
  Android 写入 `W0` 的 `0xFFFFFC16` 正是 `-1002`。
- `cpp/core/tjs2/tjsErrorDefs.h:23-29`：`TJS_E_NOTIMPL` 精确定义为 `-1002`。
- 对照边界：`cpp/core/tjs2/tjsObject.cpp:214-220` 的通用
  `tTJSDispatch::OperationByNum` 会把数值索引转换成字符串后转发给 `Operation`；本地
  `PSBValueDispatch` 没有继承或复用该转换链，而是像 Android 一样保留独立、无条件
  `TJS_E_NOTIMPL` override。

逐项映射：

| Android 证据 | 本地源码 | 对照 |
| --- | --- | --- |
| vtable `0x1A0B498 -> 0x596E0C`，位于 `Operation` 与 `NativeInstanceSupport` 之间 | `PSBDispatch.h:98-112` 的同序独立 `OperationByNum` override | 虚槽结构一致 |
| `MOV W0,#0xFFFFFC16` | `main.cpp:541` 返回 `TJS_E_NOTIMPL`；`tjsErrorDefs.h:24` 为 `-1002` | 32 位返回值逐位一致 |
| 没有参数寄存器读取、内存访问或输出写回 | `main.cpp:538-540` 所有形参省略名称且函数体不使用它们 | 输入、对象与结果数据流一致 |
| 紧随其后的 `RET`；无 branch、`BL/BLR` | `main.cpp:538-542` 为单返回独立方法 | 控制流和副作用一致 |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留独立 emitted 的两指令 primary vslot 24；本地同样以 `PSBValueDispatch::OperationByNum` 独立覆写，没有复用 `tTJSDispatch` 的数值转字符串转发。原始拼写不可见，但当前二进制可证明的结构一致。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或任一参数，只向 `W0` 写 `-1002`；本地所有形参均未使用，只返回同值且不写 `result`。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 primary-vtable 数据项 `0x1A0B498`，没有 direct-call；本地通过 `iTJSDispatch2::OperationByNum` override 经虚分派进入。canonical parent 的 `[vtable-member]` 关系准确。 |
| 对象生命周期 | `N/A` | 完整函数不分配、析构、AddRef/Release、读取对象字段或接管任何参数；vptr 安装属于 parent 构造节点而非本函数体。 |
| 内部容器实现 | `N/A` | 完整二指令函数不创建、遍历或修改任何 STL/KiriKiri/TJS 容器。 |
| 边界行为 | `MATCH` | 对任意 flag/num，以及空或非空 `this/result/param/objthis`，均不解引用、不写回，并无条件返回 `TJS_E_NOTIMPL (-1002)`；本地完全相同。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能恢复原始源码参数名或精确方法拼写；IDA 将无类型入口
暂时显示成无参 `__int64`，但 vtable 位置、32 位 `W0` 返回和本地接口 ABI 足以证明本轮
六维结论。该类型恢复限制不是已证实的行为差异。

## Cross-reference 与阶段 A 派发状态

- `0x1A0B498` 是本函数唯一 Android xref，属于 canonical primary-vtable 归属关系，
  不是额外函数节点或 direct caller。
- TASK_TREE 未记录本节点的非 canonical cross-reference。
- 本节点没有 TASK_TREE canonical 直接子节点；阶段 A 未 spawn 任何 agent。

# `0x597ED0` — `ncbClassInfo_PSBFile_Set_guess`

## 身份与归属

- canonical 地址：`0x597ED0`
- canonical 名称：`ncbClassInfo_PSBFile_Set_guess`
- 分组：C / typed NCB class-info helper
- TASK_TREE parent：`0x597F24 ncbClassInfo_PSBFile_InfoCtor_guess`，父边类型为
  `[helper]`。这是同一 `ncbClassInfo<PSBFile>` 静态状态上的逻辑 helper 归属；fresh
  `xrefs_to(0x597ED0)` 没有发现 parent 或其他 retained caller，因此本关系不是
  `[direct-call]`。
- 审计 agent path：`/root/f_42cef8/f_597f24/f_597ed0`
- Android vtable / NCB / 调用链归属：本入口不是 vtable 槽、TJS member wrapper 或注册
  callback，而是 `ncbClassInfo<PSBFile>` 的 template-emitted 一次性 class-info setter。
  Android 注册上下文 `0x59AA84` 在创建 PSBFile native-class object 与取得 class id 后内联
  了相同的 initialized 检查及四字段提交；本报告只用该调用点确认本入口归属，不替
  `0x59AA84` 下审计结论。
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cef8/f_597f24/f_597ed0` fresh 执行了
`decompile(0x597ED0)`；随后执行完整 `disasm(0x597ED0)`（13 条指令）、
`xrefs_to(0x597ED0)`、同一地址段的 function entity 查询，以及四个 class-info 全局字段的
data-xref 查询。为确认 NCB 调用链归属，另以 `decompile(0x59AA84)` 只读核对内联调用点。
本次记录独立于旧报告：本轮开始时不存在 `functions/0x597ED0.md`。

- fresh decompile 当前 IDA 名称为 `PSBFile_ncbClassInfo_Set_guess`，函数范围为
  `0x597ED0..0x597F07`；TASK_TREE canonical 名称为
  `ncbClassInfo_PSBFile_Set_guess`。
- `0x597ED0..0x597EDC` 读取 `initialized@0x1AB50F8`；非零时走
  `0x597EE0: W0=0` 并于 `0x597EE4` 返回，没有执行任何字段 store。
- 首次设置路径在 `0x597EF4`、`0x597EF8`、`0x597EFC` 依次把原样传入的 `X0` name、
  `W1` id、`X2` class object 写至 `0x1AB5100`、`0x1AB5108`、`0x1AB5110`。
- `0x597F00` 最后才把 `initialized` 写为 `1`；成功返回值在任何字段写入前已经装入
  `W0=1`，并于 `0x597F04` 返回。
- 完整函数没有 `BL`、循环、异常处理、分配、引用计数、输入替代值或 null guard；所有
  指针与整数均按位原样保存。
- fresh `xrefs_to(0x597ED0)` 为空，证明当前 retained out-of-line body 没有直接引用者；
  这不能反推源码 helper 不存在。四字段 data xref 则显示注册上下文 `0x59AA84` 在
  `0x59AB10..0x59AB44` 内联同形逻辑：已置位时抛出“Already registerd class.”，否则按
  id/name/object/initialized 的机器调度写入同一四个槽。异常是 caller 对 `false` 结果的
  处理，不属于 `0x597ED0` 本体。

## Android 伪代码（8 行）

```text
Set(name, id, obj):
    if info.initialized: return false       // 唯一失败边界；字段全部保持原值
    info.name = name                        // 无默认替代；null 也原样保存
    info.id = id
    info.obj = obj                          // 无 AddRef/Release
    info.initialized = true                 // 三个载荷字段之后才提交
    return true
// 无 callee、异常、循环、参数检查或并发同步
```

## 本地精确对照

- class-info 共通类型：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 28–35 行，符号
  `ncbTypedefs::{NameT, IdentT, ClassObjectT}`；分别对应只读字符指针、32 位 class id 与
  native-class object 类型。
- class-info 模板及本函数：同文件第 73–114 行，符号 `ncbClassInfo<T>`；精确 setter 为
  第 88–94 行 `ncbClassInfo<T>::Set(NameT, IdentT, ClassObjectT *)`，静态 `InfoT` 字段声明
  位于第 103–111 行。
- 本地 NCB 注册调用点：同文件第 1855–1867 行，符号
  `ncbRegistNativeClass<CLASS>::RegistBegin()`；第 1863 行调用 `ClassInfoT::Set`，失败时第
  1864 行抛出 `Already registerd class.`，对应 Android caller 把 setter 的 `false` 转成
  异常，而不是把异常混入 setter。
- 模板显式实例化：同文件第 2160–2172 行，宏 `NCB_REGISTER_CLASS_COMMON` 与
  `NCB_REGISTER_CLASS`；第 2161 行显式实例化 `ncbClassInfo<cls>`。
- PSBFile 展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 751-755 行，符号
  `NCB_REGISTER_CLASS(PSBFile)`。

逐项对应：

1. Android `initialized != 0` 立即返回 `0`，本地第 89 行立即返回 `false`；两者都在此
   分支完全不写 name/id/obj/initialized。
2. Android 首次路径按 name → id → obj → initialized 顺序写入，本地第 90–93 行保持
   相同数据依赖与提交顺序；第 93 行赋值表达式返回 `true`。
3. Android name/object 不做引用计数或 null 替换，id 不做范围检查；本地使用相同非 owning
   指针与 `tjs_int32` 值直接赋值，没有增加“安全化”行为。
4. Android out-of-line helper 没有 callee，而注册路径把它内联；本地保留源代码层
   `RegistBegin → ClassInfoT::Set`，编译器可同样内联，因而不要求保留机器级 `BL`。

## 六维判定

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是三参数、静态 class-info 上的一次性 setter：先 guard，后写三个载荷字段，最后置位并返回 bool。对应本地 `ncbClassInfo<T>::Set` 第 88–94 行逐 token 保留这一结构；stripped 二进制不能证明私有模板与成员的原始拼写，`MATCH` 仅覆盖可证结构。 |
| 数据流 | `MATCH` | Android 失败路径零写入并返回 0；成功路径把 name/id/obj 原样写入，最后写 initialized=1 并返回 1。本地条件、输入、store 顺序、提交点和返回值完全一致。 |
| 调用链 | `MATCH` | 本入口自身没有 callee，out-of-line xref 为空；Android `0x59AA84` 内联同一 setter 并在失败结果上抛异常。本地源码层由 `ncbRegistNativeClass::RegistBegin` 调用 `ClassInfoT::Set`，再在 false 时抛同一异常；机器级内联不构成调用链 GAP。TASK_TREE parent 仅标成 `[helper]`，未伪装为 direct call。 |
| 对象生命周期 | `MATCH` | Android 不分配、不 AddRef/Release，仅把 name 与 class-object 的非 owning 指针保存进静态 class-info；initialized 使生命周期内的重复设置失败，清理由独立入口负责。本地保存相同非 owning 值并使用同一 one-shot 状态规则。本报告不替 Clear 入口下结论。 |
| 内部容器实现 | `N/A` | 本入口只访问一个 bool、一个 32 位整数和两个指针，不创建、遍历或修改任何容器。 |
| 边界行为 | `MATCH` | Android 唯一显式失败边界是 initialized 已为真：返回 false 且四字段不变；首次调用接受任意整数与 null 指针，既不替换也不拒绝，且没有原子/锁同步。本地 `Set` 保留完全相同的重复注册、null、值域和并发边界。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整反汇编、函数 xref、静态字段 data-xref 与 Android 注册内联调用点
可证明范围内，本地 `ncbClassInfo<PSBFile>::Set` 与 `0x597ED0` 的六维行为一致。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 尚待派发的 canonical 直接子节点：无（本节点为 leaf，本 turn 未 spawn child）。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x597ED0` 条目。
- `0x597F24` 仅是本节点的 canonical `[helper]` parent；fresh xref 没有 Android direct call，
  不为该 parent 或任何 class-info sibling 创建重复函数 agent。

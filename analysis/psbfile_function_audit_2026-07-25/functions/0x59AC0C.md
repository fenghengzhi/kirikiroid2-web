# `0x59AC0C` — `PSBFile_ncbInstanceAdaptor_Invalidate_guess`

> `LSDA-CALLSITE-TOPOLOGY`（2026-08-03）：FDE 的 LSDA `0x186E4CC` 只有 1 项，
> 是 action-1 null-type catch-all；无零 landing 或 cleanup-only 项，完整四元组由全量门禁固定。

## 审计身份

- canonical 地址：`0x59AC0C`
- canonical 名称：`PSBFile_ncbInstanceAdaptor_Invalidate_guess`
- IDA 当前标签：`PSBFile_ncbInstanceAdaptor_Invalidate_guess`。Android 二进制没有保留
  该模板专门化的精确 C++ 符号，因此名称继续保留 `_guess`。
- 分组：F（typed NCB wrapper、adaptor 与反注册）
- TASK_TREE parent：`0x59ABD8 PSBFile_instanceAdaptor_CreateEmpty_ctor_guess`
- parent 关系：`[vtable-member]`。parent 在 `0x59ABE8..0x59ABF4` 把
  `off_1A0B598` address-point 安装为新 adaptor 的 vptr，本入口位于该表的
  `Invalidate` 槽；parent 不直接调用本入口。
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_59aa84/f_59abd8/f_59ac0c`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只视为 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由
`/root/f_42cf28/f_59a8d8/f_59aa84/f_59abd8/f_59ac0c` fresh 调用 IDA MCP
`decompile(addr="0x59AC0C")`。随后只读补充
`disasm(addr="0x59AC0C", include_total=true)`、`xrefs_to(0x59AC0C)`、
`get_bytes(0x1A0B580, 64)`、`xrefs_to(0x1A0B598)`，并只把 parent `@59ABD8`
的 disasm、直接 callee `@A0DE90` 与异常终止 helper `@520FAC` 的 decompile 用作调用点
语义上下文。本轮目标报告原先不存在，因此不是旧报告冒充本轮结果；上下文函数不在此
替代其各自审计。

- IDA 函数范围为 `0x59AC0C..0x59AC7B`，完整 28 条指令。正常路径在
  `0x59AC74` 返回；末尾 `0x59AC78` 是本函数的 cold exception landing pad，调用
  `@520FAC`。后者执行 `__cxa_begin_catch` 后 `std::terminate()`，故这是 cleanup
  异常的终止边界，不是另一个函数的普通控制流。
- `0x59AC20..0x59AC2C` 读取 adaptor `+8` native pointer；pointer 为 null，或
  adaptor `+16` sticky byte 非零时，均跳过全部销毁步骤。
- 仅在 native pointer 非 null 且 sticky 为 false 时，`0x59AC30..0x59AC5C`
  读取 native `PSBFile` 首槽的 owner pointer。owner 非 null 时直接把其 32-bit
  非原子 refcount 减一；结果为零才以 owner `+0x58` 的 raw-data pointer 调
  `@A0DE90`，再 delete owner。无论 owner 是否为空或是否到零，随后都 delete native
  `PSBFile` allocation。
- `@A0DE90` 的上下文 decompile 为：读取 aligned pointer 前一 pointer-sized 槽中的
  original allocation，非 null 时 `operator delete[]`。这与本地
  `TJSAlignedDealloc` 的 allocator-family 数据流一致；本报告不审计该外部通用 helper。
- `0x59AC60..0x59AC64` 在所有分支汇合后无条件写
  `_instance=null`、`_sticky=false`。函数返回 `void`，没有状态码、默认替代对象、
  可恢复 catch、错误回滚或防御性 owner/data 修复；cleanup 异常进入上述
  `std::terminate()` landing pad。
- `xrefs_to(0x59AC0C)` 只有 `0x1A0B5A0` 的 data xref。原始 qword 序列显示
  `off_1A0B598` 的函数槽依次为 `@5242A8`、`@59AC0C`、`@5242B4`、
  `@59AC7C`、`@59AD08`；结合本地 `iTJSNativeInstance` 顺序，第二槽就是
  `Invalidate`。`xrefs_to(0x1A0B598)` 又显示 parent 在 `0x59ABF0` 安装该
  address-point。故 canonical parent 边是 `[vtable-member]`，不能写成 direct-call。

## Android 伪代码（9 行）

```text
Invalidate():
    native = this._instance
    if native != null && this._sticky == false:
        owner = native.owner
        if owner != null: --owner.refCount; if owner.refCount == 0: aligned_dealloc(owner.data); delete owner
        delete native
    this._instance = null
    this._sticky = false
    // void；null/sticky 仍复位；无 status/fallback，析构异常经 cold landing pad terminate
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/plugin/ncbind.hpp:118-148`，符号 `ncbInstanceAdaptor<T>`：第 120 行保持
  `public tTJSNativeInstance`；第 125-126 行构造/析构 adaptor；第 134 行 virtual
  `Invalidate()` 只调用 `_deleteInstance()`；第 138、141 行保持 `_instance` 后接
  `_sticky` 的语义字段顺序；第 144-148 行严格实现“非 null 且非 sticky 才 delete，
  然后无条件清 null/false”。这逐行对应伪代码第 1-8 行。
- `cpp/core/tjs2/tjsInterface.h:326-337`，符号 `iTJSNativeInstance`，以及
  `cpp/core/tjs2/tjsNative.h:42-54`，符号 `tTJSNativeInstance`：`Invalidate` 位于
  `Construct` 与 `Destruct` 之间并由 adaptor override，和 Android
  `off_1A0B598 + 8 -> @59AC0C` 的 vtable 槽归属一致。
- `cpp/core/plugin/ncbind.hpp:228-231`，符号
  `ncbInstanceAdaptor<T>::CreateEmptyAdaptor()`：parent 对应的 typed factory 以
  `new AdaptorT()` 创建具有该 virtual slot 的对象；它只建立 parent/child vtable
  归属，不是对 `Invalidate()` 的直接调用。
- `cpp/plugins/psbfile/main.cpp:751-755`，符号 `NCB_REGISTER_CLASS(PSBFile)`：
  真实实例化 `ncbInstanceAdaptor<PSBFile>` typed registration 路径，确认本地目标
  specialization，不从本地反推 Android 行为。
- `cpp/plugins/psbfile/PSBRawFile.h:31-42,58`，符号 `PSBRawOwner::Release()`：32-bit
  intrusive refcount 直接递减，只有新值为零才 `delete this`；对应 Android 伪代码第 5 行
  与 `0x59AC38..0x59AC54`。
- `cpp/plugins/psbfile/PSBRawFile.h:73-118`，符号 `PSBFile`：第 75、116-118 行
  保持仅含 owner pointer 的 holder；第 170-174 行 destructor 在 owner 非 null 时调用
  `Release()`。因此本地 `_deleteInstance()` 的 `delete _instance` 保留 Android
  `0x59AC30..0x59AC5C` 展开的“Release owner，再 delete native holder”调用链。
- `cpp/plugins/psbfile/PSBRawFile.cpp:141-145`，符号 `PSBRawOwner::~PSBRawOwner()`，
  以及 `cpp/core/tjs2/tjsUtils.cpp:258-274`，符号
  `TJSAlignedAlloc/TJSAlignedDealloc`：owner 最后一次 Release 使用配对 aligned
  deallocator；deallocator 同样读取 aligned pointer 前的 original allocation 后
  `delete[]`，对应 Android `0x59AC48..0x59AC54` 与 `@A0DE90`。

逐行对应：伪代码第 2-3 行对应 `ncbind.hpp:138-145` 与 Android
`0x59AC20..0x59AC2C`；第 4-5 行对应 `PSBRawFile.h:31-42,100-104`、
`PSBRawFile.cpp:141-145` 与 Android `0x59AC30..0x59AC54`；第 6 行对应
`ncbind.hpp:145` 与 `0x59AC58..0x59AC5C`；第 7-8 行对应 `ncbind.hpp:146-147`
与 `0x59AC60..0x59AC64`；第 9 行由正常返回 `0x59AC74`、cold landing pad
`0x59AC78 → @520FAC → std::terminate()` 及本地隐式 non-throwing destructor 规则共同
证明。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 `ncbInstanceAdaptor<PSBFile>` 的独立 `Invalidate` virtual slot，函数体呈现 `_deleteInstance` template helper 的完整内联 shape：条件 delete 后两个字段无条件复位。本地保留同一 template specialization、virtual override、两字段与独立 `_deleteInstance()` helper；ARM64 字段偏移仅是 ABI 结果，不要求 wasm32 硬凑。 |
| 数据流 | `MATCH` | Android 从 adaptor 读取 native/sticky，经 native 首槽取得 owner，再做 owner 非原子 refcount decrement、零值 raw-data dealloc/owner delete、native delete，最终统一写 null/false。本地 `Invalidate → _deleteInstance → delete PSBFile → owner Release → owner destructor → TJSAlignedDealloc` 保留相同输入、中间对象、顺序与输出状态。 |
| 调用链 | `MATCH` | Android parent 只安装包含本入口的 vptr，TJS 运行期经 `iTJSNativeInstance::Invalidate` vdispatch 进入本函数；目标内部仅在释放路径调用 aligned-dealloc helper/operator delete，没有 direct-call parent 或 vtable sibling。本地 virtual override 与 nested destructor/Release/deallocator 链一致，也未把 registration/vtable 关系简化成直接调用。 |
| 对象生命周期 | `MATCH` | Android 非 sticky adaptor 拥有 native holder：invalidate 销毁 holder，并在 owner 最后一份引用时销毁 owner/raw allocation；sticky adaptor 不拥有 native holder，invalidate 只丢弃 pointer；所有路径最终回到 empty non-sticky adaptor。local `_sticky` ownership gate、`delete _instance`、intrusive owner Release 与无条件 reset 完全一致。 |
| 内部容器实现 | `N/A` | 目标完整 28 指令只操作 adaptor、native holder、intrusive owner 与 raw allocation，没有 TJS Array、STL 或自定义容器；引用计数和对象字段属于生命周期数据，不是容器实现。 |
| 边界行为 | `MATCH` | instance null、sticky true、owner null、owner refcount 非零/归零五类边界均与本地一致：前两类跳过全部 delete 但仍复位；owner null 只 delete holder；共享 owner 仅减引用；最后 owner 配对释放 raw allocation。Android 的 cleanup 异常由 `0x59AC78` 进入 `std::terminate()`；本地 `PSBFile`/`PSBRawOwner` destructor 在无显式 exception spec 且子对象均 non-throwing 时同样具有隐式 non-throwing 边界。两端均无 status/fallback/可恢复 catch，也不为 aligned data null/损坏增加防御分支。 |

## 总判定

`ALIGNED`

本轮 fresh Android decompile、完整目标 disasm、唯一 vtable data xref、address-point 原始
字节与本地 typed adaptor/PSB holder 逐行对照，证明 `0x59AC0C` 在六维可证明范围内与本地
`ncbInstanceAdaptor<PSBFile>::Invalidate()` / `_deleteInstance()` 对齐。

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能唯一恢复 template helper 是否在原源码中显式命名为
`_deleteInstance`，也不能恢复精确源码 token 拼写；但保留的 CFG、vtable 槽和内联对象
生命周期足以证明本报告所判行为。这不构成确定差异。

## TASK_TREE 直接子节点与 cross-reference

- 本节点是 leaf；TASK_TREE 尚待派发的 canonical 直接子节点：空。本阶段未 spawn。
- `@1A0B5A0` 是本入口唯一 xref，属于 parent 安装的 adaptor vtable 数据槽；
  `@59ABD8` 与本节点关系为 `[vtable-member]`，不是 direct call。
- `@59AC7C`、`@59AD08` 是同一 address-point 中的析构 vtable siblings；其中
  `@59AD08` 的 canonical parent 也是 `@59ABD8`，`@59AC7C` 的 canonical parent 是
  `@59AD08 [lifecycle]`。它们不直接调用本入口，且必须由各自唯一 agent 审计；本节点不为
  sibling/lifecycle 关系重复创建 agent。
- TASK_TREE 全局 Cross-reference 节没有指向本节点的额外 non-canonical direct edge；这里
  仅记录上述 vtable/ABI 归属，不派生重复节点。

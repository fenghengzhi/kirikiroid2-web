# `0x597AC0` — `PSBValueDispatch_AddRef`

## 审计身份

- canonical 地址：`0x597AC0`
- canonical 名称：`PSBValueDispatch_AddRef`
- IDA 当前标签：`PSBValueDispatch_AddRef`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 构造并安装 primary vptr；本边只表示对象/vtable
  归属，不伪装成 `@597AD4` 对本入口的直接调用。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_597ac0`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。本入口不读取二进制中的 `PSBFile.dll` 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x597AC0")`，随后 fresh 调用
`disasm(addr="0x597AC0", include_total=true)`、`xrefs_to(addrs="0x597AC0")`、
`lookup_funcs(queries=["0x597AC0","0x597AD4"])`，并读取 primary vtable
`get_bytes(addr="0x1A0B3D8", size=16)`。本报告在这些 fresh 证据取得后新建；阶段 A 没有
审计或派发第二个函数。

- fresh decompile 恢复为 `unsigned int PSBValueDispatch_AddRef(PSBValueDispatch *self)`：
  读取 `self->refCount`，加一，写回并返回写回值；没有条件分支、默认替代值或 callee。
- 完整 disasm 覆盖 `0x597AC0..0x597AD0`，恰好 5 条指令：
  `LDR W8,[X0,#0x10]` → `ADD W8,W8,#1` → `STR W8,[X0,#0x10]` →
  `MOV W0,W8` → `RET`。W 寄存器证明运算、存储和返回都采用 32 位值；没有原子独占指令、
  饱和检查、zero gate 或异常路径。
- fresh `xrefs_to` 只返回 data xref `@1A0B3D8`，没有 code xref。该 address point 的前
  16 字节是 `0x597AC0, 0x597A40`，因此本入口是 `PSBValueDispatch` primary
  `iTJSDispatch2` vtable 的 slot 00 `AddRef`，相邻 slot 01 是 `Release`。调用经虚分派
  间接发生；没有 code xref 不能被误读成“没有运行时调用者”。
- 本入口不是 NCB wrapper、NCB callback、secondary-base thunk 或注册函数；它只实现
  primary dispatch ABI 的引用计数槽。canonical parent `@597AD4` 安装该 vtable，但本入口
  不调用 parent，parent 也没有保留 `BL @597AC0`。

## Android 伪代码（6 行）

```text
AddRef(self):
    if self is not readable/writable: fault/UB at the first load/store  // 无 null/valid guard
    next = uint32(self.refCount + 1)       // 非原子，按 W32 模 2^32 回绕
    self.refCount = next
    return next                            // 回绕为 0 也照常返回
    // 无 Release/delete、饱和、错误码或异常恢复
```

该伪代码覆盖本入口的全部数据操作以及 null/非法 `self`、并发更新丢失和计数溢出的失败/边界
行为；函数本身没有实际条件跳转。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:105-118`：`iTJSDispatch2` 的首个虚槽声明为
  `virtual tjs_uint AddRef() = 0`，下一槽为 `Release()`；这与 Android address point
  `@1A0B3D8` 的前两项顺序一致。
- `cpp/plugins/psbfile/PSBDispatch.h:16-24`：`PSBValueDispatch final` 以
  `iTJSDispatch2` 为 primary base，并显式声明 `tjs_uint AddRef() override`；对应 Android
  primary vtable slot 00，而不是 NCB wrapper 或 secondary thunk。
- `cpp/plugins/psbfile/PSBDispatch.h:130-132`：引用计数是独立成员
  `tjs_uint refCount_{1}`。Android 的 ARM64 对象偏移是 `+0x10`；本地源码正确表达字段语义，
  不硬编码跨 ABI 不可移植的 ARM64 偏移。
- `cpp/plugins/psbfile/main.cpp:94-97`：`PSBValueDispatch::AddRef()` 只执行
  `return ++refCount_;`，逐项对应 Android 的 load → add-one → store → return-new-value；没有
  guard、原子操作、饱和处理、zero gate 或额外调用。
- `cpp/core/tjs2/tjsTypes.h:51-52`：本地 `tjs_uint` 是 `unsigned int`，与反编译恢复的
  unsigned return 以及 Android W32 算术一致；unsigned 前置递增同样按类型宽度回绕。

逐行对应：伪代码第 2 行对应 Android `LDR/STR` 与本地对 `refCount_` 的直接访问；第 3-5 行
对应 `main.cpp:96` 的前置 `++` 和返回；第 6 行对应本地函数体没有任何其他语句。初值 1
属于 parent 构造/字段初始化，本入口只消费当前计数，不重置它。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一个独立 5-instruction primary vtable slot 00，语义是无辅助层的 `AddRef` override；本地在同一 primary `iTJSDispatch2` 继承面声明并 out-of-line 定义该 override，直接操作独立 `tjs_uint` 字段。`MATCH` 只覆盖二进制可见结构，不声称 stripped 符号能证明不可观察的原始排版或拼写。 |
| 数据流 | `MATCH` | 双方均读取当前 32 位计数、加一、把同一新值写回，再返回新值；不读取 `value_`/`valid_`，不产生临时 dispatch，也不改写其他字段。 |
| 调用链 | `MATCH` | Android 唯一静态引用是 primary vtable slot 00，运行时经 `iTJSDispatch2::AddRef` 虚分派进入；函数内部无 `BL/BLR`。本地同样通过 `override` 安装到首槽，函数体无 callee。parent 边正确记录为 `[vtable-member]`，没有伪装成 direct-call。 |
| 对象生命周期 | `MATCH` | 双方只增加 dispatch 自身的引用计数并返回新持有数，不 retain/release raw owner、不检查 valid、不在计数变为 0 时析构；归零删除属于独立 `Release` 槽，不属于本入口。 |
| 内部容器实现 | `N/A` | 本入口只访问一个标量引用计数字段；不创建、遍历或改变 packed、STL、TJS 或自定义容器。 |
| 边界行为 | `MATCH` | 双方都不检查 null/非法 `self`，采用非原子 unsigned 32 位递增，无锁、无饱和和无溢出兜底；`0xFFFFFFFF + 1` 回绕为 0 并返回 0，且不会触发 delete。并发竞态可丢失更新，函数不报告错误。 |

## 总判定

`ALIGNED`

Android 可见的 vtable 归属、标量数据流、调用边界、引用生命周期与全部边界行为均由本地
精确复刻；没有确定的结构或行为 GAP。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 直接子节点：无。阶段 A 未 spawn，亦无未成功派发节点。
- TASK_TREE 没有为本节点列出非 canonical function cross-reference。
- `@597AD4` 是 canonical `[vtable-member]` parent；它安装含本入口的 primary vptr，但不直接
  调用本函数。
- `@1A0B3D8` 是证明 slot 归属的 vtable 数据地址，不是函数节点；`@597A40` 只是相邻
  `Release` vtable member，不是本入口的 callee 或 child，均不得创建重复 agent。

# `0x597EB8` — `ncbClassInfo_PSBFile_GetClassObject_guess`

## 身份与归属

- canonical 地址：`0x597EB8`
- canonical 名称：`ncbClassInfo_PSBFile_GetClassObject_guess`
- 分组：C / PSBFile typed NCB 前段 / factory / root
- TASK_TREE parent：`0x597F24 ncbClassInfo_PSBFile_InfoCtor_guess`，关系为
  `[helper]`。这表示本 getter 与 parent 构造器共同归属于同一份
  `ncbClassInfo<PSBFile>` 静态状态；fresh xref 证明 parent 不调用本入口，因此该边不是
  `[direct-call]`。
- 审计 agent path：`/root/f_42cef8/f_597f24/f_597eb8`
- Android vtable / NCB / 调用链归属：本入口不是 vtable 槽、TJS member wrapper、注册
  callback 或生命周期 callback，而是 `ncbClassInfo<PSBFile>` 的 template-emitted class-object
  指针 getter。它读取 typed NCB class-info 状态，供 native-class/adaptor 基础设施消费；本报告
  不替那些独立入口下审计结论。
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cef8/f_597f24/f_597eb8` fresh 执行了
`decompile(0x597EB8)`；随后执行完整 `disasm(0x597EB8)`、`xrefs_to(0x597EB8)`、
`analyze_function(0x597EB8)`，并对其读取的 class-object 槽补做 xref 复核。此次记录独立于
任何旧报告：

- fresh decompile 当前 IDA 标签为 `PSBFile_ncbClassInfo_GetClassObject_guess`，函数范围为
  `0x597EB8..0x597EC7`；TASK_TREE canonical 名称采用
  `ncbClassInfo_PSBFile_GetClassObject_guess`。二进制没有保留可证明精确源码拼写的符号，故
  名称保留 `_guess`。
- `0x597EB8: ADRL X8, PSBFile_ncbClassInfo_initialized`：取得 class-info 静态状态基址
  `0x1AB50F8`。
- `0x597EC0: LDR X0, [X8,#0x18]`：从 `0x1AB5110` 读取 64 位 class-object 指针槽到返回
  寄存器 `X0`。
- `0x597EC4: RET`：原样返回该指针；完整函数总计 3 条指令、1 个 basic block，圈复杂度
  为 1。
- 完整函数没有参数、条件分支、callee、写入、分配、释放、引用计数、异常路径或显式失败
  返回；当前槽值即使为 null 也直接返回。
- fresh `xrefs_to(0x597EB8)` 没有记录到 retained caller 或数据引用。对
  `0x1AB5110` 的 xref 复核确认本函数在 `0x597EC0` 读取该槽，另有注册、清理和 adaptor
  路径直接访问同一状态；这里只把它们作为状态归属/内联上下文，不替那些函数作审计判定。

## Android 伪代码（4 行）

```text
GetClassObject():
    value = classInfo.obj              // 当前槽值；未初始化时也不另设默认值
    return value                       // null 原样返回
// 无参数、分支、callee、写入、引用计数、异常或错误码
```

## 本地精确对照

- class-object 类型：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 28–35 行，符号
  `ncbTypedefs::ClassObjectT`；第 35 行将其定义为 `tTJSNativeClassForPlugin`。
- 本函数的精确源码对照：同文件第 73–85 行，符号 `ncbClassInfo<T>` 与
  `ncbClassInfo<T>::GetClassObject()`；第 84 行无条件执行 `return _info.obj;`。
- 被读取状态的源代码结构：同文件第 103–114 行，符号 `ncbClassInfo<T>::InfoT` 与
  `ncbClassInfo<T>::_info`；第 104、109 行分别给 `obj` 设 null 初值并声明
  `ClassObjectT *obj`，第 114 行定义模板静态状态。
- 直接消费语义的共通 adaptor 源码：同文件第 202–208 行，符号
  `ncbInstanceAdaptor<T>::CreateAdaptor()`；第 204 行读取 `ClassInfoT::GetClassObject()`，随后
  的 null 分支属于 caller，不属于本 getter。
- 显式实例化入口：同文件第 2160–2172 行，符号 `NCB_REGISTER_CLASS_COMMON` 与
  `NCB_REGISTER_CLASS`；第 2161 行显式实例化 `ncbClassInfo<cls>`。
- PSBFile 展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 751-755 行，符号
  `NCB_REGISTER_CLASS(PSBFile)`。

逐项对应：

1. Android 唯一的数据读取是 class-info 基址 `+0x18` 的指针槽；本地第 84 行唯一的数据
   读取是同一语义字段 `_info.obj`。
2. Android 通过 `X0` 原样返回读取值；本地返回类型是 `ClassObjectT *`，没有转换、复制、
   AddRef 或 Release。
3. Android 没有把 `initialized` 作为 getter 门控，也不在 getter 内制造 null 默认值；本地
   同样不检查 `_info.initialized`，直接暴露 `_info.obj` 的当前值。
4. Android 的 `+0x18` 和 64 位 load 是 AArch64 ABI 产物；本地以普通 C++ 字段访问表达相同
   源码语义，由 wasm32 / 其他目标 ABI 自行计算布局，没有硬凑 Android 偏移。

## 六维判定

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是无参数、单字段读取并返回的 class-info getter；本地 `static inline ClassObjectT *GetClassObject()` 保留完全相同的函数 shape 和 `_info.obj` 字段归属。stripped 二进制不能证明原始私有字段/函数拼写，`MATCH` 仅覆盖可证结构。 |
| 数据流 | `MATCH` | Android 从静态状态 `0x1AB50F8 + 0x18` 读取 `0x1AB5110` 的指针到 `X0` 并原样返回；本地第 84 行直接返回 `_info.obj`，没有任何中间改写。 |
| 调用链 | `MATCH` | Android 本体无 callee，且 fresh xref 没有 retained caller；同一状态的其他 xref 显示消费路径可直接读取该槽。对应本地 getter 被声明为 `inline`，函数体自身同样不调用 helper、vdispatch 或注册 callback；canonical parent 的 `[helper]` 关系未被伪装成 direct call。 |
| 对象生命周期 | `MATCH` | Android 返回借用的现存 class-object 指针，不 AddRef、Release、构造或销毁对象，也不改写状态；本地裸指针返回保持相同非 owning 生命周期语义。 |
| 内部容器实现 | `N/A` | 本入口只读取一个静态指针字段，不创建、遍历或修改任何容器。 |
| 边界行为 | `MATCH` | Android 不检查 `initialized`，也不拦截 null；槽为 null 时返回 null，非 null 时原样返回。第 84 行本地实现完全相同，不增设异常、替代值或安全化分支。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 3 指令反汇编、函数 xref 和 class-object 槽 xref 可证明
范围内，本地 `ncbClassInfo<PSBFile>::GetClassObject()` 与 `0x597EB8` 的六维行为一致。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无；本节点为 leaf，本阶段未 spawn child。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x597EB8` 条目。
- `0x597F24` 仅是本节点的 canonical `[helper]` parent；fresh xref 未发现 parent 对本入口的
  retained direct call，因此不把该关系改写为 `[direct-call]`，也不为任何关系创建重复 agent。

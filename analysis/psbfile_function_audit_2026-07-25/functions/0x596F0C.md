# `0x596F0C` — `PSBValueDispatch_Invalidate_dispatch`

## 审计身份

- canonical 地址：`0x596F0C`
- canonical 名称：`PSBValueDispatch_Invalidate_dispatch`
- IDA 当前标签：`PSBValueDispatch_Invalidate`；Android stripped 二进制未保留精确 C++
  符号，canonical 标签用 `_dispatch` 与 `iTJSNativeInstance::Invalidate()` nullsub 区分。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`；这是构造器安装的 primary vtable 成员归属，不表示
  `@597AD4` 直接调用本入口。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596f0c`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制里的 `PSBFile.dll` 仅视为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596F0C")`；随后 fresh 调用
`disasm(addr="0x596F0C", include_total=true)`、`xrefs_to(addrs="0x596F0C")`，并读取
primary vtable 相邻 32 字节 `get_bytes(addr="0x1A0B438", size=32)` 复核槽位。本地址本轮
开始时不存在报告，本文件是 fresh 审计结果。

- fresh decompile 与完整 disasm 覆盖 `0x596F0C..0x596F34`，共 11 条指令。入口不建栈、
  不执行 `BL/BLR`，没有任何 callee、字符串或容器访问。
- `xrefs_to` 只返回一个 data xref：`0x1A0B448`。它相对 primary vtable
  `@1A0B3D8` 的偏移是 `0x70 = 14 * 8`，因此本函数确属 `iTJSDispatch2` primary vtable
  第 14 槽的 dispatch `Invalidate`，不是 NCB wrapper/callback，也不是 native-instance
  的无参 `Invalidate()`。
- vtable bytes `@1A0B438` 连续解码为 `@596F48, @596F40, @596F0C, @596F04`，即相邻
  slots 12..15；本入口没有保留的 direct-call xref，运行调用链是 TJS dispatch 的 vcall。
- ARM64 `X0` 是 `this`，`X2` 是接口签名中的 `membername`；`flag`、`hint`、`objthis`
  均未消费。`MOV W0,#0xFFFFFC16/#0xFFFFFC12` 的 32-bit 返回位型分别是
  `TJS_E_NOTIMPL (-1002)` 与 `TJS_E_INVALIDOBJECT (-1006)`。
- `0x596F1C` 读取 `this+0x28` 的 validity byte：为零则失败；任意非零则
  `0x596F28` 写零并返回 `TJS_S_OK`。函数不释放 owner、不改变 dispatch 引用计数，也不
  销毁对象。

## Android 伪代码（7 行）

```text
InvalidateDispatch(this, flag, membername, hint, objthis):
    if membername != null: return TJS_E_NOTIMPL       // -1002；此路径不读取 this
    if this.validByte == 0: return TJS_E_INVALIDOBJECT // -1006
    this.validByte = 0
    return TJS_S_OK                                   // 0
    // flag、hint、objthis 均忽略；无 callee、cleanup 或 fallback
    // this 在默认成员路径无效时，读取 validByte 即 fault/UB
```

伪代码覆盖全部两个条件分支、返回默认值、重复失效边界、参数忽略顺序及非法 `this`
边界。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:217-223`：`iTJSDispatch2::Invalidate(flag, membername,
  hint, objthis)` 的第 14 个接口槽签名，与 Android 对 `X2=membername` 的检查及其余三个
  参数不消费一致。
- `cpp/plugins/psbfile/PSBDispatch.h:15-16,80-87,130-132`：类注释把独立 validity byte
  关联到 `@596F0C`；`PSBValueDispatch` 声明同签名 override，并以普通 C++ `bool
  valid_{true}` 保存状态。源码没有硬凑 Android ARM64 的 `+0x28` ABI 偏移。
- `cpp/plugins/psbfile/main.cpp:486-503`：`PSBValueDispatch::IsValid` 读取同一 `valid_`；目标
  `PSBValueDispatch::Invalidate` 在 `754-765` 先检查 `membername != nullptr` 返回
  `TJS_E_NOTIMPL`，再检查 `!valid_` 返回 `TJS_E_INVALIDOBJECT`，随后仅写
  `valid_=false` 并返回 `TJS_S_OK`，逐句对应伪代码第 2-5 行。
- `cpp/core/tjs2/tjsErrorDefs.h:23-35`：本地常量固定为 `TJS_E_NOTIMPL=-1002`、
  `TJS_E_INVALIDOBJECT=-1006`、`TJS_S_OK=0`，与 Android W0 位型完全一致。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:83-84` 与 `tjsInterface.h:217-223`；第 2 行
对应 `main.cpp:495-497`；第 3 行对应 `main.cpp:498-500`；第 4-5 行对应
`main.cpp:501-502`；第 6 行由本地未命名参数和空函数体内无其他语句共同复刻。两边均不
在本函数中释放 `value_` owner、递减 `refCount_` 或调用 native `Invalidate()` nullsub。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 primary `iTJSDispatch2` vtable slot 14 的独立 11-instruction override；本地 `PSBValueDispatch` 声明并实现同签名 override，且与无参 native `Invalidate()` 保持两个独立入口。ARM64 字节偏移只用于证据记录，没有被写进 wasm 源码。 |
| 数据流 | `MATCH` | 两边都只读取 `membername` 和 validity state，并严格按“membername 非空立即失败 → validity 为零失败 → 写 false → 成功”流动；`flag/hint/objthis` 均忽略，失败路径均不写状态。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 primary vtable slot 数据项，函数无 callee，运行时只能经 dispatch vcall 进入；本地以 `iTJSDispatch2` virtual override 提供同一调用边界，没有额外 helper/native-invalidate 调用。canonical parent 仅是 `[vtable-member]` 归属。 |
| 对象生命周期 | `MATCH` | Android 与本地都只把仍存活 dispatch 的逻辑 validity 从 true 改为 false；不析构、不释放 retained owner、不改变 dispatch refcount。重复调用在 null membername 路径返回 `TJS_E_INVALIDOBJECT`，对象本身继续存活。 |
| 内部容器实现 | `N/A` | 本入口不创建、读取或修改 STL、TJS、packed table 或其他多元素容器；唯一状态是对象内单个 validity 标量。 |
| 边界行为 | `MATCH` | membername 非空优先返回 `TJS_E_NOTIMPL`，甚至不读取 `this`；null membername 下 validity 已零返回 `TJS_E_INVALIDOBJECT`，任意非零 Android byte 被清零并成功。正常构造状态只有 1/0，本地 `bool` 完整复刻可观察路径；两边都没有 null-this guard、异常恢复或 fallback。 |

## 总判定

`ALIGNED`

六个固定维度在 Android 当前可证明范围内没有确定偏差；独立 vtable 入口、分支优先级、
错误码、状态写入、无 callee 与不触发对象销毁的边界均与本地一一对应。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 直接子节点：无。阶段 A 未 spawn，也未审计任何第二函数。
- canonical parent `@597AD4` 通过安装 primary vptr 建立 `[vtable-member]` 归属；它不直接
  调用本函数。
- `@1A0B448` 是本入口唯一 Android xref（primary vtable slot 14 数据项），不是函数节点，
  不创建 agent。
- TASK_TREE 的非 canonical Cross-reference 清单没有指向本节点的其他 emitted-function
  边，因此没有重复派发对象。

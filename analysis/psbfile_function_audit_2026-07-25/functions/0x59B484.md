# `0x59B484` — `PSBFile_root_GetFlags_guess`

## 审计身份

- canonical 地址：`0x59B484`
- canonical 名称：`PSBFile_root_GetFlags_guess`
- IDA 当前标签：`PSBFile_rootProperty_GetFlags_guess`。Android stripped 二进制未保留
  该模板专门化的精确 C++ 符号，故 canonical 名称保留 `_guess`。
- 分组：F（typed NCB wrapper、adaptor 与反注册）
- TASK_TREE parent：`0x59B28C PSBFile_root_PropGet_guess`
- parent 关系：`[vtable-member]`。两者只是同一个 `root` Property wrapper 的 vtable
  sibling 归属；`0x59B28C` 不直接调用本入口，该边绝不能解释为 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b484`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅是 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x59B484")`；随后调用目标函数完整 `disasm`、`lookup_funcs`、
`xrefs_to` 与相关 vtable 原始字节读取。为确认 NCB 间接调用归属，另把
`@597F38/@59AEEC/@534DAC` 的指令仅作为上下文；没有替这些地址作函数审计结论。

- fresh decompile 精确得到：

  ```c
  unsigned int __fastcall PSBFile_rootProperty_GetFlags_guess(
      PSBFile_rootPropertyWrapper_arm64 *self)
  {
    return 0; /*0x59b488*/
  }
  ```

- `lookup_funcs(0x59B484)` 给出 size=`0x8`；完整 disasm 只有两条指令：
  `0x59B484 MOV W0, WZR`、`0x59B488 RET`。因此返回值精确为零；没有读取
  `this`，没有条件、callee、默认回退、失败返回或异常路径。写 `W0` 会按 AArch64
  规则清零整个 `X0`。2026-08-02 已按 vtable interface ABI 将 IDB 旧泛化
  `__int64()` 纠正为 `unsigned int(self)`；`self` 虽未被函数体读取，仍是该 vslot 的真实
  接口参数。
- `xrefs_to(0x59B484)` 只返回 data xref `0x1A0B7F8`，没有直接 code xref。
  `@597F38:0x597FD0..0x598038` 构造并以字面名 `root` 注册 Property wrapper：主
  vtable address-point 为 `0x1A0B6F0`，getter 为 `@5981F8`，setter 为 null。
  原始 qword 与 data xref 共同证明 `0x1A0B7F8 = 0x59B484`，即该主 vtable 的
  `+0x108` `GetFlags` 槽。
- 同一构造上下文把通用 `ncbIMethodObject` address-point `0x19FE1F8` 写入 wrapper
  内嵌 interface；其第二槽 `0x19FE200` 指向 `@534DAC`。该 helper 的 fresh context
  decompile/disasm 为：读取 `interface+8` 的 property object，再从其主 vtable
  `+0x108` 取函数指针并尾跳。这独立证明 `@534DAC` 是 interface `GetFlags` 转发层，
  最终 vdispatch 到当前 `@59B484`。
- registration consumer `@59AEEC:0x59B074..0x59B080` 从 item interface vtable
  `+8` 调用 `GetFlags`，随后 `0x59B080..0x59B098` 把所得 `W0` 作为 member flags
  传给 `ncb_registerMember`。这是 `@59AEEC → interface vdispatch @534DAC → property
  vdispatch @59B484` 的间接 NCB 数据流，不是 canonical parent `@59B28C` 对本函数的
  直接调用。

## Android 伪代码（2 行）

```text
GetFlags(self):
    return 0  // 无条件；不读 self；无默认/失败分支、callee 或异常边界
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/main.cpp:751-755`，`NCB_REGISTER_CLASS(PSBFile)`：第 1096 行
  `Property(TJS_W("root"), &PSBFile::GetRootDispatch, 0)` 创建实例 getter、null setter
  的 `root` Property；这决定 getter command 不是 static member command。
- `cpp/plugins/psbfile/PSBRawFile.h:75-107`，`PSBFile` 与
  `PSBFile::GetRootDispatch() const` 声明：getter 是 `PSBFile` 成员函数，进一步独立
  确认下述 `InvokeCommand::ClassT` 为 `PSBFile` 而非 `void`。
- `cpp/core/plugin/ncbind.hpp:28-34`，`ncbTypedefs`：`FlagsT` 为 `tjs_uint32`；Android
  `MOV W0, WZR` 正是 32-bit 零返回的 ABI 形态。
- `cpp/core/plugin/ncbind.hpp:1211-1227`，
  `InvokeType::InvokeCommand<REFCLASS, METHOD, ivtNormal>`：第 1224-1226 行仅在
  `METHOD` 的 `ClassT == void` 时令 `Flags=TJS_STATICMEMBER`，否则令 `Flags=0`。
  root getter 是 `PSBFile` 成员函数，所以本专门化的 `GetCommandT::Flags` 精确为零。
- `cpp/core/plugin/ncbind.hpp:1310-1315`，`PropertyCommand`：把 getter 的上述
  `InvokeCommand` 固定为 `GetCommandT`；`cpp/core/plugin/ncbind.hpp:1441-1450` 再用
  它实例化 `ncbNativeClassProperty`。
- `cpp/core/plugin/ncbind.hpp:1488-1490`，目标本地符号
  `ncbNativeClassProperty<PropCommandT>::GetFlags() const`：函数体唯一语句是
  `return GetCommandT::Flags;`，对 root 专门化即无条件 `return 0`，逐指令对应 Android
  的 `MOV W0, WZR; RET`。
- `cpp/core/plugin/ncbind.hpp:835-843,895-901`，`ncbIMethodObject` 与内嵌
  `iMethod`：interface `GetFlags()` 通过 `_this->GetFlags()` 转发，结构对应 Android
  `@534DAC` 的 `_this` 读取与主 vtable `+0x108` 尾分派。
- `cpp/core/plugin/ncbind.hpp:1720-1732`，`ncbRegistClass::Property`：保持 typed
  Property wrapper 层；`cpp/core/plugin/ncbind.hpp:1879-1887`，
  `ncbRegistNativeClass::RegistItem`：第 1885 行调用 `item->GetFlags()` 并把返回值交给
  `TJSNativeClassRegisterNCM`，对应 Android `@59AEEC` 的间接消费链。

逐行对应：伪代码第 1 行是 local/Android 虚函数槽签名；第 2 行对应
`ncbind.hpp:1490` 与 `ncbind.hpp:1224-1226`，机器码为 `0x59B484..0x59B488`。
interface 转发与 registrar 消费均发生在函数外，只用于证明本入口的 NCB 归属，未被
误写进两行目标伪代码。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留 root typed Property 专门化的独立 `GetFlags` vtable 入口，位于主 address-point `+0x108`，并保留通用 interface 转发层 `@534DAC`；本地同样由 `ncbNativeClassProperty::GetFlags` override 与内嵌 `iMethod::GetFlags` forwarder 两层组成。精确模板符号拼写不可由 stripped 二进制恢复，但可证明的类层次结构一致。 |
| 数据流 | `MATCH` | Android 不读取任何输入，只把零写入 `W0`；registrar 随后把该零作为 member flags。root getter 为实例成员时，本地 `GetCommandT::Flags` 也是常量零，并沿 `item->GetFlags()` 返回给 registrar。 |
| 调用链 | `MATCH` | Android registration 链为 `@59AEEC item->GetFlags vdispatch → @534DAC interface forwarder → property vtable +0x108 → @59B484`；本地为 `RegistItem → ncbIMethodObject::GetFlags → iMethod::_this->GetFlags → ncbNativeClassProperty::GetFlags`。canonical parent `@59B28C` 仅为 sibling 归属，两边都不存在 parent 对本入口的 direct call。 |
| 对象生命周期 | `N/A` | 当前两指令入口既不构造、销毁、retain/release，也不修改 wrapper/interface 状态；`this` 甚至未被读取。wrapper 的创建与 Release 属于外层 registration 函数，不是本函数行为。 |
| 内部容器实现 | `N/A` | 本入口不创建、访问或修改任何 STL/KiriKiri 容器、TJS Array、member table 或 autoreg 链；仅返回标量常量。 |
| 边界行为 | `MATCH` | Android 对所有可能的调用输入都无条件返回 `0`，没有 null 检查、条件默认、错误码、callee 或异常路径；本地专门化同样是无条件常量表达式返回，不添加任何“更安全”的分支。 |

## 总判定

`ALIGNED`

本轮 fresh decompile、完整两指令 disasm、唯一 data xref、property/interface vtable 证据、
间接 registrar 调用链及本地模板逐行对照共同证明：`0x59B484` 在六维可适用范围内与本地
root Property `GetFlags()` 专门化一致。

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能唯一恢复该模板专门化的原始 C++ 标识符拼写，故名称
保留 `_guess`；旧 IDB `__int64()` 已被 vtable ABI 与 `MOV W0, WZR` 证伪并纠正为
`unsigned int(self)`。这不改变函数不读取 self、无条件返回零的行为结论。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。本节点是 leaf，阶段 A 未 spawn 任何 agent。
- canonical parent `@59B28C` 与本节点的关系仅为 `[vtable-member]`：两者属于同一
  `root` Property wrapper，但 `PropGet` 不 call `GetFlags`。
- `@597F38` 是 wrapper 构造/注册上下文；`@59AEEC` 经通用 interface 间接消费 flags；
  `@534DAC` 是 manifest 外的通用 NCB interface forwarder。这些均为 cross-reference，
  不改变 canonical parent，也不得从本 leaf 重复派发函数 agent。
- `@59B700` 是另一个 `load` Method wrapper 的独立 `GetFlags` emitted 入口，不是本函数
  的 caller、callee 或 canonical child；不得因同样返回零而合并审计。

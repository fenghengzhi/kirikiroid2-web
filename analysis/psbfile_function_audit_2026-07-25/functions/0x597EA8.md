# `0x597EA8` — `ncbClassInfo_PSBFile_GetID_guess`

## 身份与归属

- canonical 地址：`0x597EA8`
- canonical 名称：`ncbClassInfo_PSBFile_GetID_guess`
- 分组：C / PSBFile typed NCB 前段 / factory / root
- TASK_TREE parent：`0x597F24 ncbClassInfo_PSBFile_InfoCtor_guess`，关系为
  `[helper]`。这表示同一 `ncbClassInfo<PSBFile>` 静态状态的构造器与读取 helper 的逻辑归属；
  fresh xref 没有显示 parent 或其他地址对本 out-of-line body 的 retained direct call，因此该边不
  伪装成 `[direct-call]`。
- 审计 agent path：`/root/f_42cef8/f_597f24/f_597ea8`
- Android vtable / NCB / 调用链归属：本入口不是 vtable 槽、TJS member wrapper 或注册
  callback，而是 `ncbClassInfo<PSBFile>` 的无参静态 ID getter。它读取 typed NCB 注册状态中
  位于 `0x1AB5108` 的 32 位 class ID；优化后的实际 NCB/adaptor 使用点可直接读取该全局字段，
  不必保留对本 emitted helper 的调用。本报告不替那些独立函数下审计结论。
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cef8/f_597f24/f_597ea8` fresh 执行了
`decompile(0x597EA8)`；随后执行完整 `disasm(0x597EA8)`（3 条指令）、
`xrefs_to(0x597EA8)`，并对静态字段 `0x1AB5108` 的数据 xref、静态初始化和 Clear/Set 写点
作调用上下文复核。此次记录不是沿用旧报告：

- fresh decompile 当前 IDA 标签为 `PSBFile_ncbClassInfo_GetID_guess`，函数范围为
  `0x597EA8..0x597EB7`；TASK_TREE canonical 名称采用
  `ncbClassInfo_PSBFile_GetID_guess`。stripped 二进制没有保留精确源码拼写，故保留 `_guess`。
- `0x597EA8: ADRL X8, PSBFile_ncbClassInfo_initialized`：取得同一静态 `Info` 状态的基址。
- `0x597EB0: LDR W0, [X8,#0x10]`：从 `PSBFile_ncbClassInfo_id @ 0x1AB5108`
  无条件读取 32 位值到返回寄存器。
- `0x597EB4: RET`：立即返回；没有条件分支、初始化检查、callee、分配、引用计数、异常路径
  或显式失败码。
- fresh `xrefs_to(0x597EA8)` 结果为空。对 `0x1AB5108` 的数据 xref 则显示优化后的使用点直接
  访问字段，包括 adaptor、NCB factory/property/method wrapper 等上下文；这解释了为何
  out-of-line template-emitted body 可以没有 retained caller，但不把这些数据 xref 宣称为本函数调用。
- 为核实默认边界而补取的指令证据：静态初始化 `0x42CF18` 与 Clear 上下文 `0x597F14`
  均以 `STR WZR` 把该 ID 字段设为 `0`；Set 上下文 `0x597EF8` 以 `STR W1` 写入注册所得 ID。
  因而本 getter 在未注册/清理后返回 `0`，注册后返回保存的原始 32 位值，不存在隐藏替代值。

## Android 伪代码（5 行）

```text
GetID():                              // 无参数，也不检查 initialized
    id32 = load32(PSBFileClassInfo.id)
    return id32
// 静态初始化与 Clear 后字段默认是 0；Set 后返回其原始 32 位值
// 无条件分支、callee、替代默认值、异常或显式失败边界
```

## 本地精确对照

- class-info 共通类型：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 28–35 行，符号
  `ncbTypedefs`；第 31 行把 `IdentT` 定义为 32 位 `tjs_int32`。
- class-info 模板与本函数的精确源码对照：同文件第 73–114 行，符号
  `ncbClassInfo<T>`；第 83 行 `static inline IdentT GetID() { return _info.id; }` 与 Android
  的单次 32 位字段读取一一对应。
- 同一静态状态的字段、默认值与存储：同文件第 103–114 行，符号
  `ncbClassInfo<T>::InfoT`、`ncbClassInfo<T>::_info`；第 104 行构造时 `id(0)`，第 108 行
  声明 `IdentT id`。Clear 位于第 96–101 行，第 98 行同样将 `id` 设为 `0`；Set 位于第
  88–94 行，第 91 行直接保存传入 `id`。
- typed adaptor 中的源码级消费者：同文件第 156–167 行和第 184–199 行，符号
  `ncbInstanceAdaptor<T>::GetAdaptor`、`SetAdaptorWithNativeInstance`；第 163、195 行把
  `ClassInfoT::GetID()` 传给 `NativeInstanceSupport`。Android O3 可把这些调用内联为静态字段
  读取，因此不要求出现对 `0x597EA8` 的 retained `BL`。
- 显式实例化：同文件第 2160–2172 行，符号 `NCB_REGISTER_CLASS_COMMON` /
  `NCB_REGISTER_CLASS`；第 2161 行显式实例化 `ncbClassInfo<cls>`。
- PSBFile 展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 751-755 行，符号
  `NCB_REGISTER_CLASS(PSBFile)`。

逐项对应：

1. Android 仅取静态状态基址、读取一个 32 位 ID 并返回；本地第 83 行同样只返回
   `_info.id`，没有校验或转换。
2. Android 静态初始化/Clear 的 `0` 与 Set 的原值写入，对应本地第 104、98、91 行；getter
   不另外制造默认值。
3. Android 本 emitted body 无 callee且无 retained caller；本地 getter 是 `static inline`
   template member，消费者可内联，显式 template 实例化仍可产生独立 emitted body。
4. Android 的 AArch64 字段偏移 `+0x10` 只用于确认字段身份；本地以普通 C++ 字段表达同一
   语义，让 wasm32 ABI 自行布局，没有硬凑 ARM64 偏移。

## 六维判定

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 `ncbClassInfo<PSBFile>` 静态状态的独立无参 getter emitted body，只有基址取得、32 位成员读取与返回；本地保留同一 class-info template、`IdentT id` 字段、`static inline GetID()` 以及显式 template 实例化。stripped 二进制不能证明私有成员的原始拼写，`MATCH` 只覆盖可证结构。 |
| 数据流 | `MATCH` | Android 将 `0x1AB5108` 的原始 32 位值直接送入 `W0`，没有变换；本地 `return _info.id` 同样直接返回 32 位 `tjs_int32`。静态初始化/Clear 的 `0` 和 Set 的传入值写入也与本地状态流一致。 |
| 调用链 | `MATCH` | Android 本体无 callee，fresh function xref 无 retained caller；ID 字段的数据 xref 显示 O3 后的 adaptor/NCB 使用点直接读取字段。局部源码以 inline `GetID()` 供相同 typed adaptor 流使用，且没有把 TASK_TREE `[helper]` parent 伪装为 direct call。 |
| 对象生命周期 | `MATCH` | Android 仅非 owning 地读取静态 class-info 当前值，不初始化、清理、分配或调整引用；本地 getter 也只观察 `_info.id`。状态由独立构造/Set/Clear 生命周期管理，本函数不延长或改变其生命周期。 |
| 内部容器实现 | `N/A` | 本入口只访问一个 32 位标量静态字段，不创建、遍历或修改任何容器。 |
| 边界行为 | `MATCH` | Android 不检查 `initialized`，因此静态初始化或 Clear 后如实返回 `0`，Set 后如实返回保存值；没有参数、null 分支、异常或失败码。本地 getter、Info 初值及 Clear/Set 状态流保持同样边界。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 3 指令反汇编、function/global xref 与默认值写点证据可
证明范围内，本地 `ncbClassInfo<PSBFile>::GetID()` 与 `0x597EA8` 的六维行为一致。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 尚待派发的 canonical 直接子节点：无（本节点为 leaf，本 turn 未 spawn child）。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x597EA8` 条目。
- 对 `PSBFile_ncbClassInfo_id @ 0x1AB5108` 的 Android 数据 xref 只是内联/直接字段访问证据，
  不是 TASK_TREE 的额外 canonical parent，也不为任何 xref 创建重复函数 agent。

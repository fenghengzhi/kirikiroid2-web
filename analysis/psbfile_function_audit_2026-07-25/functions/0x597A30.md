# `0x597A30` — `PSBValueDispatch_Construct_primary`

## 审计身份

- canonical 地址：`0x597A30`
- canonical 名称：`PSBValueDispatch_Construct_primary`
- IDA 当前标签：`PSBValueDispatch_Construct`。Android stripped 二进制未保留原始 C++
  符号；本报告沿用 TASK_TREE 的 canonical 接口标签，不从本地标识符反向命名二进制。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 构造对象时安装包含本入口的主 vtable；它不调用
  本入口，因此该边不是 `[direct-call]` 或 `[lifecycle]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_597a30`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅是 NCB 模块名字面量，本入口不访问该
  字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x597A30")`；随后 fresh 调用目标函数完整 `disasm`、`lookup_funcs`、
`xrefs_to`、相关 vtable 原始字节读取，并只把 parent 的完整 `disasm` 作为 vptr 安装上下文。
本地址在本轮开始时没有报告；本文件是本轮 fresh 审计新建结果。

- fresh decompile 精确得到：

  ```c
  __int64 PSBValueDispatch_Construct()
  {
    return 0; /*0x597a34*/
  }
  ```

- `lookup_funcs(0x597A30)` 给出 size=`0x8`；完整 disasm 恰好两条指令：
  `0x597A30 MOV W0, WZR`、`0x597A34 RET`。函数没有栈帧、参数寄存器读取、条件分支、
  内存访问、`BL/BLR`、输出对象写回或异常恢复。写 `W0` 同时清零 `X0`；按接口的
  `tjs_error` 语义，返回值就是成功码 `0`。
- fresh `xrefs_to(0x597A30)` 只返回一条 data xref `0x1A0B4C0`，没有 code xref。
  fresh 读取主 address point `0x1A0B3D8` 起的 32 个 qword 后，
  `0x1A0B4C0 - 0x1A0B3D8 = 29 * 8`，且该 qword 精确为 `0x597A30`；故本入口是
  `PSBValueDispatch` 主 vtable 的 slot 29，而不是 NCB wrapper、NCB callback 或直接
  调用 helper。
- 同轮读取 vtable 尾部 `0x1A0B4B0..0x1A0B4F8`：主表 slot 27/28/29/30/31 依次为
  `@596D80/@596D78/@597A30/@596F38/@597A28`；其后是 secondary-base
  offset-to-top `-8`、空 RTTI 项和 secondary address point `0x1A0B4E8`。该 secondary
  表首 qword 为 TASK_TREE child `@597A38`。这里仅证明两张表中的成员归属；本报告不替
  `@597A38` 审计函数体或下结论。
- parent context `@597AD4:0x597ADC..0x597AF0` 把 address point `0x1A0B3D8` 与
  `0x1A0B4E8` 一并写入新对象。对两只 address point 的 fresh xref 也都包含 parent 的
  对应写入点。这证明 canonical `[vtable-member]` parent 关系；parent 只是安装表，没有
  `BL @597A30`。

## Android 伪代码（4 行）

```text
Construct(self, numparams, params, tjs_obj):
    ignore self, numparams, params, tjs_obj       // 均不读取或解引用
    return 0                                      // 成功；无条件、默认或失败分支
    // 无 callee、状态写入、retain/release、异常或恢复路径
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 依次直接继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`，与 Android parent 安装 primary/secondary 两只
  address point 及 secondary offset-to-top `-8` 的结构一致。
- `cpp/plugins/psbfile/PSBDispatch.h:74-78`：类只声明一个
  `Construct(tjs_int, tTJSVariant **, iTJSDispatch2 *) override`，随后是同一 native
  interface 的 `Invalidate/Destruct`；这恢复 Android 主表尾部三槽与 secondary 表三槽由
  同一组源码级 virtual 方法产生的形状，而不是把 `@597A30` 和 child `@597A38` 写成两个
  独立业务方法。
- `cpp/core/tjs2/tjsInterface.h:326-337`：`iTJSNativeInstance` 的槽序为
  `Construct`、`Invalidate`、`Destruct`，其中 `Construct` 的三个显式参数正是本地 override
  的接口参数。Android O3 因全部未使用而没有在函数体中保留这些参数读取。
- `cpp/plugins/psbfile/main.cpp:470-474`：目标本地符号
  `PSBValueDispatch::Construct(...)`；三个显式形参都省略名字，函数体唯一语句是
  `return TJS_S_OK`，逐指令对应 Android `MOV W0,WZR; RET`。
- `cpp/core/tjs2/tjsErrorDefs.h:35`：`TJS_S_OK` 精确定义为 `0`，与 Android 返回位型
  完全相同。
- `cpp/plugins/psbfile/main.cpp:20-25`：本地 parent 源码级构造函数属于同一
  `PSBValueDispatch` 类；普通 C++ 多继承构造负责安装上述 virtual 方法所在的两只 vptr。
  这与 Android `@597AD4` 的 vptr 写入对应，但构造函数没有调用 `Construct`。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:74` 与 `tjsInterface.h:328-329` 的 virtual
接口；第 2 行对应 `main.cpp:470` 的无名、未使用形参以及 Android 完全无参数读取；第 3 行
对应 `main.cpp:473`、`tjsErrorDefs.h:35` 与 Android `0x597A30`；第 4 行对应本地空单返回体
和 Android `0x597A34 RET` 前不存在其他指令。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一只主 vtable slot 29 的独立两指令入口，并在相邻 secondary-base 表首槽保留另一个 emitted 入口；本地用 `iTJSDispatch2 + iTJSNativeInstance` 的基类顺序和单一 `Construct` override 恢复同一源码级多继承结构。stripped 二进制不保留原始标识符拼写，但当前可证明的函数边界、虚槽与单方法结构均一致。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或三个显式实参，不访问内存，只把零写入返回寄存器；本地全部形参未使用且只返回值为零的 `TJS_S_OK`，无对象或输出参数写回。 |
| 调用链 | `MATCH` | Android 唯一 xref 是主 vtable data item `0x1A0B4C0`，无 direct caller、callee 或 NCB 链；parent 仅安装 vtable。本地同样通过 virtual override 提供入口，函数体不调用任何 helper。TASK_TREE 的 `[vtable-member]` 边准确。 |
| 对象生命周期 | `MATCH` | 本入口虽是 native-instance `Construct` 生命周期回调，但 Android 明确是成功 no-op：不初始化/销毁状态、不 retain/release、不接管参数；本地生命周期回调也完全不改对象并返回成功。对象自身的真实构造属于 parent，不被错误合并进本函数。 |
| 内部容器实现 | `N/A` | 完整两指令函数不创建、访问或修改 STL、KiriKiri、TJS Array 或 packed 容器，也没有任何对象字段访问。 |
| 边界行为 | `MATCH` | Android 对任意 `this/numparams/params/tjs_obj` 位型（包括空指针、负参数数或无效地址）均不解引用且无条件返回 `0`；没有参数数检查、错误码、fallback、callee、异常或恢复路径。本地完全相同。 |

## 总判定

`ALIGNED`

fresh decompile、完整两指令 disasm、唯一 vtable data xref、两张 address point 的原始字节及
本地源码逐行对照共同证明：`0x597A30` 在六个固定维度的所有适用范围内均已对齐。

## 确定 GAP

无。

证据边界：IDA 因参数全部未使用而把签名恢复成无参 `__int64`，且 stripped 二进制不能
恢复原始 C++ 标识符拼写；但 `MOV W0,WZR` 对零返回无 ABI 歧义，主/secondary vtable 结构
也独立证明其 virtual 归属。这些是反编译证据边界，不是已证明偏差。

## TASK_TREE 直接子节点与 cross-reference

阶段 A 未 spawn。尚待由本节点在阶段 B 派发的唯一 canonical 直接子节点为：

- `0x597A38 PSBValueDispatch_Construct_secondary_thunk [vtable-member]`

Cross-reference：

- `@597AD4` 是 canonical parent；它在 `0x597ADC..0x597AF0` 安装两只 vptr，只形成
  `[vtable-member]` 归属，不直接调用本函数。
- `0x1A0B4C0` 是本函数唯一 Android xref，也是主 address point 的 slot 29 数据项；它不是
  另一个函数节点。
- `@597A38` 位于 secondary address point `0x1A0B4E8` 的首槽，是唯一 canonical child。
  本轮只读取其 vtable 项以确认关系，没有替 child 作 fresh 函数审计；不得重复派发或把两只
  emitted 入口合并成同一个 function agent。

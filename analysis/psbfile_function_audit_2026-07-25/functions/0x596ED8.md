# `0x596ED8` — `PSBValueDispatch_CreateNewByNum`

## 审计身份

- canonical 地址：`0x596ED8`
- canonical 名称：`PSBValueDispatch_CreateNewByNum`
- IDA 当前标签：`PSBValueDispatch_CreateNewByNum`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`；这是构造对象所安装 primary vtable 的成员归属，不是
  `0x597AD4` 对本入口的直接调用。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596ed8`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596ED8")`，并补充调用
`disasm(addr="0x596ED8", include_total=true)`、
`xrefs_to(addrs="0x596ED8")`、`lookup_funcs`、`func_profile` 及 primary-vtable 原始字节读取。

- fresh decompile 得到 `return 4294966294LL`。IDA 未恢复此 vtable member 的接口形参，
  并把 W0 的 32-bit 值显示为无符号扩展的 64-bit 常量；按 `tjs_error` 解释，
  `0xFFFFFC16` 严格等于 `-1002`。
- fresh 完整 disasm 覆盖整个 `0x596ED8..0x596EDF`，共两条指令：
  `MOV W0, #0xFFFFFC16`；`RET`。本体无栈帧、无条件分支、无内存访问，也没有
  `BL/BLR`、异常或清理路径。
- fresh `xrefs_to` 只返回 data xref `0x1A0B470`，无静态 code xref。读取 primary vtable
  `@1A0B3D8` 的 32 个 64-bit 项后，偏移
  `0x98 = 19 * sizeof(void*)` 的项确为 `0x596ED8`；相邻 slot 18/20 分别指向
  `0x596EE0/0x596ED0`。因此本入口是 `PSBValueDispatch` primary
  `iTJSDispatch2` vtable slot 19 `CreateNewByNum`，运行时经虚分派进入；不能把没有 code
  xref 误写成“从未调用”。
- `func_profile` 同样确认 1 个 basic block、0 caller、0 callee、0 字符串引用，唯一常量为
  `0xFFFFFC16`。本入口不是 NCB wrapper 或注册 callback，也不调用任何 psbfile helper。

## Android 伪代码（2 行）

```text
CreateNewByNum(flag, num, result, numparams, params, objthis):
    return tjs_error(0xFFFFFC16)  // -1002；无条件，全部参数均不读取，result 不写入
```

该伪代码覆盖本入口的全部条件（没有条件分支）、默认结果和失败边界：任意 flag、索引、
参数个数以及任意指针值（包括 null）都不会在本入口被读取或解引用，输出指针保持调用前值，
函数直接返回 `TJS_E_NOTIMPL`。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:259-266`：`iTJSDispatch2::CreateNewByNum` 的接口形参依次为
  `flag, num, result, numparams, param, objthis`，对应 Android primary vtable slot 19 的 ABI
  归属。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,91-96`：`PSBValueDispatch final` 直接继承
  `iTJSDispatch2`，并以完整六参数签名显式声明 `CreateNewByNum(... ) override`；没有用 NCB
  wrapper、基类默认实现或其他 helper 替代该 vtable member。
- `cpp/plugins/psbfile/main.cpp:520-523`：本地
  `PSBValueDispatch::CreateNewByNum` 的六个形参全部未命名，函数体唯一语句是
  `return TJS_E_NOTIMPL;`，逐指令语义对应 Android 的“写 W0 后立即 RET”。
- `cpp/core/tjs2/tjsErrorDefs.h:23-24`：本地 `TJS_E_NOTIMPL` 定义为 `-1002`，其 32-bit
  补码正是 Android 立即数 `0xFFFFFC16`。

逐行对应：伪代码第 1 行由 Android vtable slot 19 及接口顺序定位；第 2 行对应
`main.cpp:522` 和 Android `0x596ED8..0x596EDC`。双方均没有参数读取、`this` 状态检查、
`result` 写入、callee、引用计数操作或 fallback。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一只独立的 8-byte emitted 入口，并由 primary vtable slot 19 直接指向；本地同样以 `PSBValueDispatch` 的显式 `CreateNewByNum` override 提供独立函数体。`MATCH` 只覆盖二进制可证明的结构，不声称恢复不可观察的参数名或排版。 |
| 数据流 | `MATCH` | Android 只把常量 `0xFFFFFC16` 写入返回寄存器；本地只返回数值相同的 `TJS_E_NOTIMPL`。双方均不读任何形参、不访问 `this`、不写 `result`。 |
| 调用链 | `MATCH` | Android 本体无 direct/indirect callee，经 primary `iTJSDispatch2` vtable slot 19 虚分派；本地同样由接口 override 占据该槽，且没有基类转发、NCB wrapper 或 helper 调用。 |
| 对象生命周期 | `N/A` | 本入口不读取或改变 dispatch/参数对象，不执行 AddRef/Release，不构造、销毁或接管任何对象；相关生命周期由 sibling 入口单独审计。 |
| 内部容器实现 | `N/A` | 本入口不创建、访问或遍历 packed、TJS、STL 或其他容器。 |
| 边界行为 | `MATCH` | Android 与本地对 flag、任意正负索引、null/非 null 输出槽、任意参数个数/数组及 objthis 均无条件返回 `-1002`；所有指针均不解引用，输出槽保持不变，没有异常、fallback 或副作用。 |

## 总判定

`ALIGNED`

Android 当前可证明的 emitted 结构、常量数据流、vtable 调用归属和全部边界与本地一一
一致；本入口没有确定 GAP，也没有影响判定的证据缺口。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。阶段 A 未 spawn 任何 subagent。
- canonical parent cross-reference：`@597AD4` 构造对象并安装包含本槽的 primary vptr；
  该关系严格为 `[vtable-member]`，不是 direct-call。
- fresh IDA 唯一 xref 是 primary-vtable 数据项 `@1A0B470`；没有其他属于 114-node
  manifest 的非 canonical 关系，因此不派发重复 agent。

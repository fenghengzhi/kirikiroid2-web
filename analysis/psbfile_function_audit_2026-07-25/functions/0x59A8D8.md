# `0x59A8D8` — `PSBFile_AutoRegister_Regist_guess`

> `LSDA-CALLSITE-TOPOLOGY`（2026-08-03）：FDE 的 LSDA `0x186E3B0` 恰有 6 项：
> 3 项无 landing、1 项 cleanup-only、2 项 action-1 null-type catch-all；完整四元组由全量门禁固定。

## 审计身份

- canonical 地址：`0x59A8D8`
- canonical 名称：`PSBFile_AutoRegister_Regist_guess`
- IDA 当前标签：`PSBFile_ncbAutoRegister_Regist_guess`；二进制没有保留精确源码符号，
  因此 canonical 名称与 IDA 标签均保留 `_guess`。
- 分组：F（typed NCB wrapper、adaptor 与反注册）
- TASK_TREE parent：`0x42CF28 psbfile_static_init`
- parent 关系：`[registration]`。parent 只构造含本入口 vtable address-point 的
  class-autoreg 对象；后续模块加载才经 vdispatch 进入本函数，不是 `0x42CF28` 的直接调用。
- 审计 agent path：`/root/f_42cf28/f_59a8d8`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `L"PSBFile.dll"` 只作为 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由 `/root/f_42cf28/f_59a8d8` fresh 调用 IDA MCP
`decompile(addr="0x59A8D8")`；随后调用完整
`disasm(addr="0x59A8D8", include_total=true)`、`xrefs_to(0x59A8D8)`、
`lookup_funcs` 和 vtable 原始字节读取复核。此次记录不是复用旧报告。

- fresh decompile 的函数范围为 `0x59A8D8..0x59A967`（IDA size `0x90`，完整
  36 条指令），入口属于 `ncbNativeClassAutoRegister<PSBFile>` 的注册虚槽。
- `xrefs_to(0x59A8D8)` 只有一个数据引用 `0x1A0B578`，没有 retained direct-code
  caller。`get_bytes(0x1A0B550, 64)` 复核该 address-point 的两槽为
  `[0x1A0B578, 0x1A0B580] = [0x59A8D8, 0x59A968]`，分别对应注册与反注册虚槽。
- `0x59A8FC` 从 autoreg 对象 `this+0x18` 读取 class name；
  `0x59A904..0x59A910` 建立 `{className, classObj=0, hasCtor=false}` delegate，
  再建立 `{impl=&delegate, isRegist=true}` registrar。
- `0x59A914` 直接调用 `0x59AA84`（RegistBegin），`0x59A91C` 直接调用
  `0x597F38`（PSBFile member-registration body），`0x59A924` 正常路径直接调用
  `0x59AD84`（RegistEnd）。这些目标只作为本入口调用链证据，不替子节点下行为结论。
- `0x59A950..0x59A960` 是编译器生成的异常清理落点：保存当前异常、再次调用
  `0x59AD84`，随后 `_Unwind_Resume`；若析构清理自身抛出，则
  `0x59A94C/0x59A964` 进入 `__cxa_begin_catch + std::terminate` helper。
- `0x59A928..0x59A948` 只有栈 canary 检查。正常 `RET` 时 X0 恰好仍保留 End
  callee 的寄存器值；旧反编译因此误推为指针返回。2026-08-02 已按 caller/vtable ABI
  纠正为 `void(const PSBFile_ncbAutoRegister_arm64 *self)`，该寄存器残值不构成可消费
  返回协议。

## Android 伪代码（9 行）

```text
Regist(self):
    d = Delegate(name=*(self+0x18), classObj=null, hasCtor=false)
    r = RegistClass(impl=&d, isRegist=true); d.RegistBegin()
    // 若 Begin 抛出：r 尚未完成构造，直接传播，不调用 End
    try: PSBFile_registerMembers(&r)
    catch e: d.RegistEnd(); rethrow e       // cleanup End 若抛出则 terminate
    d.RegistEnd()                           // normal dtor；若抛出则 terminate
    return void                             // 无 null/default/status 分支
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/plugin/ncbind.hpp:2148-2157`，符号
  `ncbNativeClassAutoRegister<T>`；第 2156 行的
  `ncbNativeClassAutoRegister<PSBFile>::Regist() const` 源码形状精确为：构造
  `DelegateT d(_name)`，在内层作用域构造 `RegistT r(d, true)`，调用
  `r.Regist()`，再由作用域析构 `r`。
- `cpp/core/plugin/ncbind.hpp:1664-1679`，符号 `ncbRegistClass<IMPL>`；第 1674 行
  保存 `_impl` 与 `_isRegist` 并在构造器中 `Begin()`，第 1675 行析构器调用
  `End()`，第 1678-1679 行是 Android 栈上 `{impl pointer, true}` 的源码字段。
- `cpp/core/plugin/ncbind.hpp:1705-1712`，符号
  `ncbRegistClass<IMPL>::Begin/End`；第 1709-1710 行在 `_isRegist=true` 时分别
  分派到 delegate 的 `RegistBegin/RegistEnd`，没有替代默认值。
- `cpp/core/plugin/ncbind.hpp:1843-1855`，符号
  `ncbRegistNativeClass<PSBFile>`；第 1853 行 delegate 构造器按
  `{_className=name, _classobj=0, _hasCtor=false}` 初始化，与 Android
  `0x59A8FC..0x59A910` 一一对应。
- `cpp/core/plugin/ncbind.hpp:2160-2172`，符号
  `NCB_REGISTER_CLASS_COMMON/NCB_REGISTER_CLASS`；宏实例化 typed class autoreg，
  并把随后的 PSBFile 注册块定义为 `ncbRegistClass<...>::Regist()` 专门化。
- `cpp/plugins/psbfile/main.cpp:14` 固定模块名 `TJS_W("PSBFile.dll")`；同文件
  `:1094-1098` 的 `NCB_REGISTER_CLASS(PSBFile)` 是本入口所调用的专门化 body，
  顺序为 Factory、`root` Property、`load` Method。本报告只核对 body 作为单一
  direct callee 的归属，其内部成员注册由 `0x597F38` 子节点独立审计。
- `cpp/core/plugin/ncbind.cpp:12-28`，符号 `ncbAutoRegister::LoadModule`；第
  20-23 行遍历已索引模块的 registrar 并经 `i->Regist()` vdispatch 进入本入口，
  说明 `0x42CF28` 的静态挂链与本函数执行之间没有伪造的 direct-call 边。

逐行对应：伪代码第 1-2 行对应 `ncbind.hpp:2156` 的 `_name` 与
`ncbind.hpp:1853` 的 delegate 默认值；第 3-4 行对应 `ncbind.hpp:1674`、
`:1709`；第 5 行对应 `main.cpp:751-755` 的专门化 body；第 6-7 行对应
`ncbind.hpp:1675,1710` 的 RAII 析构以及 Android `0x59A950..0x59A964` 的
unwind/terminate 落点；第 8 行对应 `void Regist() const override`，Android X0
残值只是 ABI/反编译产物。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 明确形成一只含 `name/null/false` 状态的 stack delegate，以及一只含 `delegate* / true` 的 stack registrar，结构为构造 Begin、专门化 body、析构 End；本地 `ncbNativeClassAutoRegister<PSBFile>::Regist`、`ncbRegistNativeClass<PSBFile>` 与 `ncbRegistClass<...>` 保留同一模板分层和作用域。O3 省略局部 vptr/空 tag 不构成源码结构差异。 |
| 数据流 | `MATCH` | Android 从 `this+0x18` 取 class name，写入 delegate，同时把 class object 和 ctor flag 清零；registrar 保存 delegate 地址并把注册方向设为 true，随后同一对象依次流过 Begin、member body、End。本地 `_name → DelegateT d → RegistT r(d,true)` 的数据流、默认值和顺序完全一致。 |
| 调用链 | `MATCH` | Android 由 `0x1A0B578` vslot 进入，直接调用 `@59AA84 → @597F38 → @59AD84`，异常清理再次调用 `@59AD84`；本地 `LoadModule → virtual Regist → RegistT ctor/专门化 Regist/dtor` 保持相同层级。canonical parent `[registration]` 不是 direct caller，三个 canonical children 才是本入口 retained direct calls。 |
| 对象生命周期 | `MATCH` | Android delegate 与 registrar 都在当前栈帧；正常作用域退出执行 End，member body 异常时清理落点仍执行 End 后继续展开，清理 End 再抛则 terminate；本地内层 RAII registrar 的构造/析构与隐式 noexcept 析构提供同一生命周期和失败清理。此入口自身没有堆分配或引用计数。 |
| 内部容器实现 | `N/A` | 本入口不读取或修改 `_top`、模块 map/list、TJS Array 或任何 STL/内部容器；它只消费已经选中的一个 autoreg 对象并编排三个注册调用。容器遍历属于外层 `LoadModule`，不能归入本函数。 |
| 边界行为 | `MATCH` | Android 不检查 `self`、`self+0x18` 的 name、callee 状态码，也没有 fallback/default 分支；Begin 失败在 registrar 未完成构造时直接传播，body 失败走 End 清理，析构 End 失败 terminate。源码模板依赖有效 autoreg 对象且用同一 C++ RAII/异常语义；栈 canary 与 X0 残值仅是目标 ABI 编译产物。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 disasm、唯一 vtable 数据 xref、原始槽字节与
本地模板生成点可证明范围内，`0x59A8D8` 的六维行为与
`ncbNativeClassAutoRegister<PSBFile>::Regist() const` 一致。

## 确定 GAP

无。

证据边界：stripped 二进制没有保留该 template override 的精确源码拼写，因此名称继续用
`_guess`；旧 pointer-return 与局部字段泛化均已在 IDB 中纠正，不能据此恢复未知源码
标识符。这不是生产行为差异。

## TASK_TREE 直接子节点与 cross-reference

- 尚待由本节点在阶段 B 派发的 canonical 直接子节点（本阶段未 spawn）：
  - `0x59AA84 PSBFile_RegistBegin_guess [direct-call]`（调用点 `0x59A914`）
  - `0x597F38 PSBFile_ncb_registerMembers_guess [direct-call]`（调用点 `0x59A91C`）
  - `0x59AD84 PSBFile_RegistEnd_guess [direct-call]`（正常调用点 `0x59A924`，
    异常清理调用点 `0x59A958`）
- `0x42CF28` 仅是本节点的 canonical `[registration]` parent：它把
  `0x1A0B578` 写入 class-autoreg 对象，后续才 vdispatch；不得伪装为 direct call。
- `0x59A968 PSBFile_AutoRegister_Unregist_guess` 位于相邻 vslot
  `0x1A0B580`，是 `0x42CF28` 的 sibling child，不是本节点 child；TASK_TREE 另记录它
  对 `@597F38` 的非 canonical cross-reference，故不得为该关系重复派发 agent。

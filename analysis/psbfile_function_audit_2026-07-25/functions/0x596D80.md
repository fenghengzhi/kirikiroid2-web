# `0x596D80` — `PSBValueDispatch_Reserved2`

## 审计身份

- canonical 地址：`0x596D80`
- canonical 名称：`PSBValueDispatch_Reserved2`
- IDA 当前标签：`PSBValueDispatch_Reserved2`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 构造并安装 primary vptr；本节点是该 vtable 的
  slot 27，不是 parent 的直接 callee。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596d80`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596D80")`；随后 fresh 调用
`disasm(addr="0x596D80", include_total=true)`、`xrefs_to(addrs="0x596D80")`，并读取
primary vtable 原始字节。本地址在本轮开始时没有报告，本文件是本轮 fresh 审计的新建结果。

- fresh decompile 的完整函数体是
  `__int64 PSBValueDispatch_Reserved2() { return 4294966294LL; }`。Hex-Rays 因缺失接口类型
  把返回值显示为 `__int64`；实际指令只写 W0，其低 32 位 `0xFFFFFC16` 按 `tjs_error`
  解释为 `-1002`（`TJS_E_NOTIMPL`）。全部 ABI 实参因未使用而没有出现在伪签名中。
- fresh 完整 disasm 覆盖 `0x596D80..0x596D87`，共两条指令：
  `MOV W0, #0xFFFFFC16`；`RET`。没有栈帧、实参/字段读取、条件分支、内存写入、
  `BL/BLR`、临时对象或异常清理。
- fresh `xrefs_to(0x596D80)` 仅返回一条 data xref `0x1A0B4B0`，没有 code xref。
  primary address point `0x1A0B3D8` 的 fresh 原始 qword 证明：slot 25–28 依次为
  `0x596D90`、`0x596D88`、`0x596D80`、`0x596D78`；且
  `0x1A0B3D8 + 27*8 = 0x1A0B4B0`。因此本入口精确归属于
  `PSBValueDispatch` primary `iTJSDispatch2` vtable slot 27 `Reserved2`。
- 本入口不是 NCB 注册函数、wrapper 或 callback，也不调用其他 Android 函数。
  `@597AD4` 对 `@1A0B3D8` 的 data xref 位于 `0x597ADC`，证明 parent 只安装整张 vtable；
  它没有直接调用本入口，符合 TASK_TREE 的 `[vtable-member]` 关系。

## Android 伪代码（4 行）

```text
Reserved2(this):
    不读取 this 或任何对象字段
    不写内存，不调用其他函数
    return tjs_error(int32(0xFFFFFC16))  // -1002，TJS_E_NOTIMPL
```

本函数没有条件、默认值、成功分支或其他失败路径；一旦控制流进入该入口，任意传入寄存器值
都不影响唯一的固定错误返回。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:307-322`：`iTJSDispatch2` 在
  `NativeInstanceSupport`、`ClassInstanceInfo` 之后依次声明纯虚 `Reserved2()`、
  `Reserved3()`；这与 Android slot 25–28 的 fresh qword 顺序一致。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,111-116`：`PSBValueDispatch final` 以
  `iTJSDispatch2` 为 primary base，并在 `ClassInstanceInfo` 与 `Reserved3` 之间显式声明
  `tjs_error Reserved2() override`；没有 wrapper 或附加状态。
- `cpp/plugins/psbfile/main.cpp:552`：`PSBValueDispatch::Reserved2()` 的完整独立定义只有
  `return TJS_E_NOTIMPL;`，逐条对应 Android 的“写固定返回值后立即返回”。
- `cpp/core/tjs2/tjsTypes.h:35-40,57`：`tjs_error` 是 `std::int32_t`；
  `cpp/core/tjs2/tjsErrorDefs.h:23-24`：`TJS_E_NOTIMPL` 定义为 `-1002`。其 32 位二补码
  正是 Android 写入 W0 的 `0xFFFFFC16`。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 primary `iTJSDispatch2` vtable slot 27 的独立 8-byte leaf；本地同样以 `PSBValueDispatch::Reserved2` 提供独立 out-of-line override，位于相同接口顺序。stripped 二进制不能证明不可观察的原始排版，但当前可证明结构一致。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或任何状态，只把 `0xFFFFFC16` 写入 W0；本地函数体也不读取状态，只返回数值相同的 `TJS_E_NOTIMPL(-1002)`。 |
| 调用链 | `MATCH` | Android 没有 `BL/BLR`，且只有 vtable data xref、没有直接 caller；本地也没有 helper/base/NCB 调用。parent 仅安装 vptr，运行入口归属均为 slot 27 虚分派。 |
| 对象生命周期 | `N/A` | 本入口不增减引用、不构造/析构对象、不改变 valid 状态，也不转移或借用所有权。 |
| 内部容器实现 | `N/A` | 本入口不访问 packed PSB 表、TJS 容器、STL 容器或任何集合状态。 |
| 边界行为 | `MATCH` | 进入入口后无论 `this`/其他传入寄存器取值如何，Android 都不解引用、不写回并无条件返回 `-1002`；本地同样无 guard、fallback、异常或替代错误码。 |

## 总判定

`ALIGNED`

Android 当前可见的 vtable 归属、固定返回数据流、零内部调用、零生命周期/容器行为和无条件
失败边界均与本地实现一致；没有确定架构或行为差异。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 直接子节点：无；本节点是 canonical leaf，阶段 A 未 spawn。
- TASK_TREE 的非 canonical cross-reference 清单没有涉及 `@596D80`。
- `@1A0B4B0 -> @596D80` 是 primary vtable slot 27 的 data reference；相邻
  `@596D90`、`@596D88`、`@596D78` 只是 vtable sibling，不是直接调用或 canonical
  cross-reference，也不由本节点派发 agent。
- `@597AD4` 是唯一 canonical parent；它安装包含本槽的 primary vptr，但不直接调用本函数。

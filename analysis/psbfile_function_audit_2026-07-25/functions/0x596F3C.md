# `0x596F3C` — `PSBValueDispatch_native_Invalidate_secondary_nullsub`

## 节点身份

- canonical 地址：`0x596F3C`
- canonical 名称：`PSBValueDispatch_native_Invalidate_secondary_nullsub`
- IDA 当前名称：`nullsub_259`；这是 IDA 对空函数生成的标签，不作为原始 C++ 符号名证据。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x596F38 PSBValueDispatch_native_Invalidate_primary_nullsub`
- parent 关系：`[vtable-member]`。该边仅把 secondary-vtable duplicate/sibling 归到同一
  native `Invalidate` 槽族，绝不表示 `0x596F38` 直接调用 `0x596F3C`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596f38/f_596f3c`
- 审计日期：2026-07-25
- 唯一权威来源：Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25，本 function subagent fresh 执行了 `decompile(0x596F3C)`，并补充执行
`disasm(0x596F3C)`、`xrefs_to(0x596F3C)`、`lookup_funcs(0x596F3C)`、secondary vtable
原始字节读取及 `xrefs_to(0x1A0B4E8)`。本报告是本轮新建，不是旧报告冒充本轮结果。

fresh decompile 的完整函数体为：

```c
void nullsub_259()
{
  ; /*0x596f3c*/
}
```

补充证据：

- `lookup_funcs` 给出函数大小 `0x4`；完整反汇编只有
  `0x596F3C: RET` 一条指令。函数没有参数读取、内存访问、条件跳转、返回值构造、
  `BL/BLR`、尾调用或异常恢复路径。
- `get_bytes(0x596F3C, 4)` 为 `C0 03 5F D6`，即 AArch64 `RET`；紧邻的 primary
  sibling `0x596F38` 也是同一 4-byte 指令。相同机器体只证明两入口行为相同，不表示
  两者互相调用。
- `xrefs_to(0x596F3C)` 恰好只有一条 data xref：`0x1A0B4F0`，没有 code xref。
- secondary vtable address point 是 `0x1A0B4E8`；
  `0x1A0B4F0 - 0x1A0B4E8 = 0x8 = 1 * 8`，故该 data xref 精确落在 secondary slot 1。
- `get_bytes(0x1A0B4D8, 40)` 显示 address point 前的 offset-to-top 为 `-8`，其后三个
  64 位表项依次为 `0x597A38`、`0x596F3C`、`0x597A2C`，对应 secondary
  `Construct`、本节点 `Invalidate`、secondary `Destruct`。这些是 vtable sibling，
  不是函数调用边。
- `xrefs_to(0x1A0B4E8)` 显示 `0x597AD4` 在 `0x597AE0` 安装该 secondary vtable；
  `0x59673C` 与 `0x597A40` 还在对象重置/释放上下文引用同一 address point。这些引用均不
  是对本入口的 direct call。

## Android vtable / NCB / 调用链归属

本入口是 `PSBValueDispatch` secondary `iTJSNativeInstance` vtable `0x1A0B4E8` 的 slot 1，
对应 `iTJSNativeInstance::Invalidate()` override 的 secondary-base 入口。它是 primary
vtable slot 30 `0x596F38` 的 ABI duplicate/sibling，不是 NCB member wrapper、注册 callback
或业务 helper。正常到达形状是经 native-instance 虚槽间接分派；函数本体立即返回且没有
callee，因此调用链在本入口处终止。

## Android 关键伪代码（5 行）

```text
PSBValueDispatch::native_Invalidate_secondary(self):
    // 不读取或调整 self，不访问 dispatch、raw owner、node、valid 或引用计数
    // 不清理、不析构、不写回，也没有成功/失败返回值
    // 无条件、无默认值选择、无 callee、无局部异常处理
    return
```

## 本地精确实现与逐行对照

本地精确位置：

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 同时继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`，保留生成 primary/secondary vtable 的普通 C++
  多继承结构。
- `cpp/plugins/psbfile/PSBDispatch.h:74-78`：同一派生类按 native-interface 语义声明
  `Construct`、`void Invalidate() override`、`Destruct`；本节点对应的声明精确位于 `:76`。
- `cpp/plugins/psbfile/main.cpp:476-479`：符号 `PSB::PSBValueDispatch::Invalidate()` 的
  out-of-line 空定义；`:739-740` 仅记录 Android primary/secondary 入口地址，不产生行为。
- `cpp/core/tjs2/tjsInterface.h:326-335`：`iTJSNativeInstance` 的虚接口顺序为
  `Construct`、`Invalidate`、`Destruct`，其中 `Invalidate` 精确位于 `:332`，与 Android
  secondary slot 1 一致。

| Android 伪代码步骤 | 本地逐行复刻 |
| --- | --- |
| 行 1，经 secondary native-instance `Invalidate` 虚槽进入 | `PSBDispatch.h:16-17,74-78` 保留双继承和 `Invalidate() override`；`tjsInterface.h:326-335` 保留该 secondary 接口的槽顺序。Android 是否为 secondary entry 另发一份 ARM64 空函数属于 ABI/O3 产物，不应以 padding 或手写 thunk 硬凑。 |
| 行 2，不读取或调整 `self`，不访问任何成员 | `main.cpp:476-479` 的空 body 没有字段、指针、this-adjust 或引用计数访问。 |
| 行 3，不清理、不析构、不写回且无返回值 | 本地返回类型同为 `void`，空 body 不调用 `Release`、析构、owner 清理或状态重置。 |
| 行 4，无条件、默认值、callee 或异常包装 | 本地函数体没有 guard、helper、返回码或异常处理。 |
| 行 5，直接返回 | 空 `void` body 到达函数末尾，语义与 Android 单条 `RET` 一致。 |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 的 secondary vtable slot 1 指向独立的 4-byte 空入口；其前后槽分别是 native `Construct`/`Destruct`。本地用普通 C++ 双继承、正确的 `iTJSNativeInstance` 三槽顺序和单一 out-of-line `Invalidate() override` 表达相同源码结构；secondary duplicate 的具体 ARM64 emitted 形态是 ABI 产物而非需要写入源码的第二个函数。 |
| 数据流 | `MATCH` | Android 只有 `RET`，不读取隐式 `self`、不调整 secondary-base 指针、不访问成员且无输出；本地空 body 同样没有读写、传递或返回数据。 |
| 调用链 | `MATCH` | Android 唯一函数 xref 是 secondary vtable slot 1 的 data 项，表明 vdispatch 间接到达；本体无 callee。`0x596F38` 只是 canonical primary sibling parent，本地通过 `iTJSNativeInstance::Invalidate` override 保留“虚分派进入、立即终止”的形状，没有伪造 direct call。 |
| 对象生命周期 | `MATCH` | 虽然本入口是 native-instance 生命周期回调，Android 明确不释放 raw owner、不改 dispatch refcount/valid、不析构或清空对象；本地 `Invalidate()` 也故意为空，保留相同 no-op 生命周期效果。 |
| 内部容器实现 | `N/A` | 完整单指令入口不创建、遍历、查询或修改 packed、TJS、STL 或自定义容器。 |
| 边界行为 | `MATCH` | Android 对所有正常进入该 emitted body 的调用均无条件立即返回，没有空值检查、fallback、错误码、局部异常或参数相关分支；本地空 `void` override 一致。本机器码事实不扩张为“以非法 `this` 发起 C++ 虚调用是受支持契约”。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整单指令反汇编、唯一 secondary-vtable data xref、slot 精确定位与
本地空 override 可证明范围内，未发现 `0x596F3C` 的六维确定偏差。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 阶段 A 本轮未 spawn。
- TASK_TREE canonical 直接子节点：无。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x596F3C` 条目。
- `@596F38` 是 primary-vtable duplicate/sibling 的 canonical parent，关系为
  `[vtable-member]`；它不是本入口的 caller 或 callee。
- `@597A38`、`@597A2C` 是 secondary vtable 的相邻 siblings；`@597AD4`、`@59673C`、
  `@597A40` 只是 secondary address point 的安装/重置引用上下文。均不为它们创建重复
  agent，也不把这些 vtable/对象关系写成 direct-call 边。

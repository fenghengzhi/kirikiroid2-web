# `0x597A18` — `PSBValueDispatch_FuncCallByNum`

## 节点身份

- 审计日期：`2026-07-25`
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_597a18`
- 分组：`A`
- canonical 地址：`0x597A18`
- canonical 名称：`PSBValueDispatch_FuncCallByNum`
- TASK_TREE parent：`0x597AD4` `PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`
- TASK_TREE 直接子节点：无
- 名称证据边界：stripped Android 二进制没有保留该方法的原始源码拼写；本报告沿用 TASK_TREE canonical 名称。函数归属由 `PSBValueDispatch` 主 vtable 的第 4 个方法槽（vslot 03）及相邻槽布局确定，本地标识符只用于复原对照，不能反向命名二进制。

## Android 归属与本轮 fresh 取证

本轮新会话对 Android kirikiroid2 `libkrkr2.so` 内嵌实现执行了以下 fresh IDA MCP 查询：

1. `decompile(0x597A18)`：fresh 结果为无参恢复签名下的单返回体，唯一语句是 `return 4294966294LL`；其低 32 位为 `0xFFFFFC16`，按 `tjs_error` 解释即 `-1002`。
2. `disasm(0x597A18)`：完整函数仅有 `MOV W0, #0xFFFFFC16`（`0x597A18`）与 `RET`（`0x597A1C`），共 2 条指令；没有参数读取、条件分支、调用、内存访问或输出写回。
3. `xrefs_to(0x597A18)`：唯一引用是主 vtable 数据项 `0x1A0B3F0`；没有代码直接调用引用。
4. `get_bytes(0x1A0B3B8, 96)`：主 vtable address point `0x1A0B3D8` 起依次为 `0x597AC0`、`0x597A40`、`0x597A20`、`0x597A18`、`0x597854`……，故本函数位于 address point `+0x18` 的 vslot 03。
5. contextual `decompile(0x597AD4)`：parent 构造函数在 `0x597AE4..0x597AF0` 把 `off_1A0B3D8` 写入对象的 dispatch vptr。它只安装 vtable，不直接调用本函数，因此 canonical parent 边准确为 `[vtable-member]`。

本节点属于 `PSBValueDispatch` 的 `iTJSDispatch2` 主接口 vtable，不是 NCB wrapper 或注册 callback。运行期调用只能经该虚槽间接进入；函数体自身不接触对象状态。

## Android 伪代码（不超过 10 行）

```cpp
tjs_error PSBValueDispatch_FuncCallByNum(flag, num, result,
                                          numparams, params, objthis) {
    // 所有实参均忽略；不读取对象，也不写 result。
    return TJS_E_NOTIMPL; // -1002；无成功或其他失败分支
}
```

## 本地实现与逐行对照

本地精确位置：

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch` 直接实现 `iTJSDispatch2`，形成本节点所属的主 dispatch vtable。
- `cpp/plugins/psbfile/PSBDispatch.h:22-35`：按 `AddRef`、`Release`、`FuncCall`、`FuncCallByNum` 顺序声明覆写；`FuncCallByNum` 的完整六参数接口位于 `:34-35`。
- `cpp/plugins/psbfile/main.cpp:114-117`：`PSBValueDispatch::FuncCallByNum(...)` 的独立定义；全部形参无名称且函数体只返回 `TJS_E_NOTIMPL`。
- `cpp/core/tjs2/tjsInterface.h:132-139`：`iTJSDispatch2::FuncCallByNum` 的 ABI 参数顺序与虚方法声明。
- `cpp/core/tjs2/tjsTypes.h:39,57`：`tjs_error` 是 32 位有符号 `tjs_int32`。
- `cpp/core/tjs2/tjsErrorDefs.h:23-24`：`TJS_E_NOTIMPL` 定义为 `-1002`。

逐项映射：

| Android 证据 | 本地源码 | 对照 |
| --- | --- | --- |
| vtable address point `0x1A0B3D8` 的第 4 个方法项 `0x1A0B3F0 -> 0x597A18` | `PSBDispatch.h:22-35` 在 `AddRef/Release/FuncCall` 后覆写 `FuncCallByNum` | 主接口虚槽结构与顺序一致 |
| `0x597A18: MOV W0,#0xFFFFFC16` | `main.cpp:116` 返回 `TJS_E_NOTIMPL`；`tjsErrorDefs.h:24` 为 `-1002` | 32 位返回值逐位一致 |
| 无任何实参寄存器读取或内存访问 | `main.cpp:114-115` 的六个形参全部省略名称，函数体不使用它们 | 输入与对象数据流一致 |
| `0x597A1C: RET`，无分支、调用及写回 | `main.cpp:114-117` 为独立单返回方法 | 控制流及副作用一致 |
| `0x597AD4` 仅安装 `off_1A0B3D8` | 本地继承并覆写 `iTJSDispatch2`，由 C++ 虚表安装该方法 | parent 是 vtable 归属，不是 direct-call |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一个独立 emitted 的两指令虚槽函数；本地同样以 `PSBValueDispatch::FuncCallByNum` 独立覆写并用单返回函数体实现。原始拼写不可见，但当前二进制可证明的结构一致。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或任一调用参数，只向 `W0` 写入 `-1002`；本地全部形参均未使用，只返回同值，也不改写 `result`。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 `0x1A0B3F0` 的主 vtable 项，parent `0x597AD4` 只安装该表；本地通过 `iTJSDispatch2` 虚方法覆写进入相同间接调用链。不存在 Android direct-call，TASK_TREE 的 `[vtable-member]` 边准确。 |
| 对象生命周期 | `N/A` | 本函数不分配、析构、AddRef/Release 或接管任何参数；对象 vptr 的安装属于 parent 构造函数，不属于本函数体。 |
| 内部容器实现 | `N/A` | 完整两指令函数没有对象字段、容器或迭代器访问。 |
| 边界行为 | `MATCH` | 对任意 `flag/num/result/numparams/params/objthis`（含空指针、越界编号及任意参数数）均不解引用、不写回且无条件返回 `TJS_E_NOTIMPL(-1002)`；本地完全相同。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

## Cross-reference

- `@597AD4` 通过 `off_1A0B3D8` 安装包含本函数的主 vtable；这是 canonical `[vtable-member]` parent 关系，不是直接调用。
- `0x1A0B3F0` 是本函数唯一 Android xref，属于上述 canonical vtable 关系；TASK_TREE 未记录其他非 canonical cross-reference。
- 直接子节点：无；不得为本节点派发其他 agent。

# `0x59AC04` — `PSBFile_ncbFinalizeEmptyCallback_guess`

## 审计身份

- canonical 地址：`0x59AC04`
- canonical 名称：`PSBFile_ncbFinalizeEmptyCallback_guess`
- IDA 当前标签：`PSBFile_ncbFinalizeEmptyCallback_guess`；Android 二进制没有保留该
  template specialization 的精确生成符号，因此名称继续保留 `_guess`。
- 分组：F（typed NCB wrapper、adaptor 与反注册）
- TASK_TREE parent：`0x59ABD8 PSBFile_instanceAdaptor_CreateEmpty_ctor_guess`
- parent 关系：`[helper]`。这是 canonical spanning-tree 的 NCB scaffolding 归属关系，
  不是 Android retained direct call；`@59ABD8` 的函数体不调用或引用本入口。
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_59aa84/f_59abd8/f_59ac04`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅视为 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由
`/root/f_42cf28/f_59a8d8/f_59aa84/f_59abd8/f_59ac04` fresh 调用 IDA MCP
`decompile(addr="0x59AC04")`；返回的完整函数为：

```c
__int64 PSBFile_ncbFinalizeEmptyCallback_guess()
{
  return 0; /*0x59ac08*/
}
```

随后只读补充 `disasm(addr="0x59AC04", include_total=true)`、
`xref_query(query="0x59AC04", direction="both")`，并仅以 `@59AA84` 的
decompile/disasm 复核 callback 注册上下文。本报告是本轮新建，不是旧报告冒充本轮结果。
按 IDB 质量规则，本轮随后把已由 NCB callback ABI 证明的原型修正为四参数、32-bit
`int` 返回，post-type fresh decompile 显示
`int __fastcall ...(void *result, int numparams, void **param, void *objthis)` 并仍仅
`return 0`；`idb_save` 已成功持久化。2026-08-02 又将四个泛化指针收紧为准确的
`tTJSVariant *result, int numparams, tTJSVariant **param, iTJSDispatch2 *objthis`，函数体
与零返回不变。

- IDA 函数范围为 `[0x59AC04, 0x59AC0C)`，完整反汇编只有两条指令：
  `0x59AC04 MOV W0, WZR`、`0x59AC08 RET`。因此所有输入寄存器均未读取，没有条件跳转、
  call、内存访问、默认替代值、异常路径或显式失败返回；32-bit 返回寄存器被精确置零。
- `xref_query` 只发现一条来自函数外部的引用：`@59AA84` 内 `0x59AB48` 对本入口的
  **data xref**。`@59AA84` 在 `0x59AB48` 把本入口地址装入 `X0`，于 `0x59AB54`
  传给 `TJSCreateNativeClassMethod`，再以 UTF-16 `L"finalize"`、type `1`
  （`nitMethod`）、flags `0` 调用 `ncb_registerMember`。这是 NCB wrapper 的 callback
  绑定和运行期间接调用关系，不是 `@59AA84 -> @59AC04` retained direct call。
- 本入口不在 `ncbInstanceAdaptor<PSBFile>` vtable 中，也不访问 adaptor/native object；
  Android 的归属是 `ncbRegistNativeClass<PSBFile>` 为字面成员 `finalize` 创建的空 native
  method callback。TASK_TREE 的 canonical parent `@59ABD8 [helper]` 仅组织同一 typed NCB
  scaffolding，不能改写上述唯一 Android xref 事实。
- 初次 fresh decompile 把返回类型暂记为 `__int64` 且省略未使用形参，这是 stripped/O3
  下的 prototype 恢复限制；`MOV W0, WZR` 与其被 `TJSCreateNativeClassMethod` 当作
  method callback 绑定共同证明可观察语义是 32-bit `tjs_error` 成功码零。本轮已据此
  修正 IDB 的 ABI 原型；精确源码符号与形参名仍不可由函数体唯一恢复，但不影响本入口行为
  审计。

## Android 伪代码（5 行）

```text
EmptyCallback(result, numparams, param, objthis):
    ignore(result, numparams, param, objthis)
    return TJS_S_OK  // 0
    // 无条件、无默认替代值、无失败返回、无异常或副作用
    // wrapper 的 objthis/result 处理发生在本入口之外
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/plugin/ncbind.hpp:1843-1853,1940-1945`，符号
  `ncbRegistNativeClass<CLASS>::EmptyCallback`：第 1945 行定义四参数 static callback，
  四个参数均不命名、不读取，并直接 `return TJS_S_OK;`。对应 Android 伪代码第 1-4 行与
  完整的 `0x59AC04..0x59AC08`。
- `cpp/core/tjs2/tjsNative.h:60-65`，符号 `tTJSNativeClassMethodCallback`：第 62-65 行
  固定 callback ABI 为 `(tTJSVariant *result, tjs_int numparams,
  tTJSVariant **param, iTJSDispatch2 *objthis) -> tjs_error`，对应伪代码第 1 行及 Android
  `TJSCreateNativeClassMethod` 绑定所恢复的语义签名。
- `cpp/core/tjs2/tjsTypes.h:57` 与 `cpp/core/tjs2/tjsErrorDefs.h:35`：`tjs_error` 是
  `tjs_int32`，`TJS_S_OK` 精确为 `0`；对应 Android `MOV W0, WZR` 与伪代码第 3 行。
- `cpp/core/plugin/ncbind.hpp:1855-1872`，符号
  `ncbRegistNativeClass<CLASS>::RegistBegin()`：第 1869-1872 行以字面量 `finalize`、
  `TJSCreateNativeClassMethod(EmptyCallback)`、`nitMethod` 和省略后的默认 flags `0`
  注册同一 callback；对应 Android context `0x59AB48..0x59AB74`。这是本入口的 data
  binding/间接 consumer，不是其函数体内部步骤。
- `cpp/core/tjs2/tjsNative.cpp:85-105`，符号 `tTJSNativeClassMethod::FuncCall()`：第 101 行
  经保存的 `Process(result, numparams, param, objthis)` 间接调用 callback。其
  `objthis` null 检查及 `result->Clear()` 位于 wrapper 外层，印证伪代码第 5 行，不能误算作
  `0x59AC04` 自身的分支或副作用。
- `cpp/plugins/psbfile/main.cpp:751-755`，符号 `NCB_REGISTER_CLASS(PSBFile)`：实例化
  `PSBFile` 的 typed registration/template 路径，因而本地实际生成并使用上述
  `ncbRegistNativeClass<PSBFile>::EmptyCallback`，没有另写功能等价的 PSBFile 专用实现。

逐行对应：伪代码第 1-2 行对应 `ncbind.hpp:1945` 未命名、未读取的四个形参以及 Android
函数体对输入寄存器的零访问；第 3 行对应同一行的 `return TJS_S_OK`、
`tjsErrorDefs.h:35` 和 Android `0x59AC04 MOV W0,WZR`；第 4 行由完整两指令 disasm
排除任何 hidden branch/call/store；第 5 行对应 `tjsNative.cpp:85-105` 的外层 wrapper
边界，明确不把 consumer 行为归入本 callback。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是被 native-method wrapper 保存的独立空 callback emitted 入口，完整函数仅无条件返回零；本地保留 `ncbRegistNativeClass<CLASS>` 内的独立 private static `EmptyCallback`，并由 `PSBFile` template specialization 实际生成。stripped 二进制未保留精确生成符号/形参名，不构成已证实源码结构差异。 |
| 数据流 | `MATCH` | Android 不读取任何输入或内存，只把 `WZR` 写入 `W0` 返回；本地四参数全部未命名、未读取，唯一输出是 `TJS_S_OK == 0`，无中间状态或默认值。 |
| 调用链 | `MATCH` | Android `@59AA84:0x59AB48` 仅取 callback 地址，经 `TJSCreateNativeClassMethod` 包装后由注册的 `finalize` method 在运行期间接调用；本地 `RegistBegin -> TJSCreateNativeClassMethod(EmptyCallback) -> tTJSNativeClassMethod::FuncCall -> Process(...)` 保留相同 wrapper 层级。本入口自身无 callee；canonical `@59ABD8 [helper]` 和 non-canonical `@59AA84` data binding 均未伪装成 direct call。 |
| 对象生命周期 | `N/A` | 本入口虽接收 `result/param/objthis` 指针，但 Android 完全不解引用、不 AddRef/Release、不分配、不销毁；本地 callback 同样不触碰任何对象。method wrapper 自身的创建、result 清理和销毁属于外层入口。 |
| 内部容器实现 | `N/A` | 本入口没有 STL、TJS Array、哈希表或自定义容器，也没有任何内存访问；外层 native-class member table 不属于此 emitted callback 的内部实现。 |
| 边界行为 | `MATCH` | Android 对任意参数值（包括 null pointers、任意 `numparams`）均无条件返回成功零，无 guard、失败码、异常或副作用；本地未命名参数加 `return TJS_S_OK` 精确保持该边界。wrapper 的 `objthis == null` 失败发生在 callback 尚未被调用之前，不是本函数 GAP。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整两指令 disasm、唯一外部 data xref、NCB 注册上下文和
本地 `PSBFile` template specialization 可证明范围内，`0x59AC04` 与
`ncbRegistNativeClass<PSBFile>::EmptyCallback` 六维一致。

## 确定 GAP

无。

证据边界：Android stripped/O3 二进制没有保留该模板静态成员的精确源码符号、形参名，
而所有形参又未被使用；因此 canonical 名称继续用 `_guess`，不宣称恢复不可观察的原始拼写。
可观察 callback ABI、返回值、控制流和副作用均已有独立注册点与指令证据，不需要据此降低
总判定。

## TASK_TREE 直接子节点与 cross-reference

- 本节点是 leaf；TASK_TREE canonical 直接子节点：无。本阶段未 spawn 任何 agent。
- canonical parent `@59ABD8` 与本节点的边为 `[helper]`，只表示同一 typed NCB
  scaffolding 的 spanning-tree 归属；`@59ABD8` 不调用、不引用本入口。
- non-canonical cross-reference：`@59AA84` 在 `0x59AB48` 取本入口地址，经
  `TJSCreateNativeClassMethod` 包装并以 `finalize` 注册。这是 data binding/运行期间接调用，
  不是 direct-call 边；不得因此改变 canonical parent 或创建重复函数 agent。
- 除上述 `@59AA84` data xref 外，fresh xref 查询没有发现其他函数外部引用；本入口不是
  vtable member，也没有 vtable sibling 或 classification-only 关系需要派发。

# `0x598E44` — `PSBRawNode_IsValid_guess`

## 审计身份

- canonical 地址：`0x598E44`
- canonical 名称：`PSBRawNode_IsValid_guess`
- Android / manifest 名称：manifest 为 `IsValid_guess`；IDA 当前标签为
  `PSBRawNode_IsValid_guess`。Android stripped 二进制没有保留精确 C++ 标识符，故名称继续
  保留 `_guess`。
- 分组：D（raw PSBFile / PSBRawOwner / PSBRawNode）
- TASK_TREE parent：`0x598A3C PSBFile_GetRoot_guess`
- parent 关系：`[classification-only]`。这是 raw-node API 的 canonical 分类归组，不表示
  Android `0x598A3C` 调用本函数。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b14c/f_5980f4/f_598268/f_598a3c/f_598e44`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `"PSBFile.dll"` 仅作为 NCB 模块名字面量，本函数不读取
  它。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 agent path fresh 调用 IDA MCP
`decompile(addr="0x598E44")`，得到：

```c
bool __fastcall PSBRawNode_IsValid_guess(const PSBRawNode *self)
{
  return self->owner && self->node != nullptr; /*0x598e54*/
}
```

随后 fresh 调用 `lookup_funcs(queries=["0x598E44"])`、
`disasm(addr="0x598E44", include_total=true, max_instructions=64)`、
`xrefs_to(addrs="0x598E44")`、`xref_query(query="0x598E44", direction="both")`、
`callees(addrs="0x598E44")` 与 `analyze_function(addr="0x598E44")`。本报告在本轮开始前不
存在，未把旧报告冒充为 fresh 结果。

- `lookup_funcs` 确认这是独立 emitted 入口 `0x598E44..0x598E63`，大小 `0x20`；完整
  disasm 共 8 条指令。
- `0x598E44` 先执行 `LDR X8, [X0]` 读取首槽 owner；`0x598E48` 在 owner 为零时直接跳到
  `0x598E5C` 返回 `W0=0`，因此失败路径不会读取第二槽。
- 仅当 owner 非零时，`0x598E4C` 才读取 `[X0,#8]` 的 node；`CMP` + `CSET W0,NE` 把
  node 非空规范化为布尔 `1`，否则返回 `0`。两条返回路径穷尽全部分支。
- 完整入口没有 store、`BL`、`BLR`、异常路径、引用计数或指针所指内容的解引用；它只
  检查两只指针槽自身是否为零。
- `xrefs_to` 返回空，`analyze_function` 的 callers 为空；`xref_query` 只返回目标函数内部
  `0x598E44 → 0x598E48` 的顺序代码边，没有外部 code/data 入站引用。`callees` 也为空。
- 本函数不是 vtable member，也没有 NCB wrapper/注册归属：Android 没有指向该入口的
  vtable、NCB 或调用点引用。本地 `PSBRawNode` 同样是无基类、无虚函数的 raw handle；
  `NCB_REGISTER_CLASS(PSBFile)` 只注册 Factory、`root` 与 `load`，没有注册 raw-node API。
  因此其调用链归属只是独立 emitted raw-node helper，TASK_TREE parent 必须保持
  `[classification-only]`。

## Android 伪代码（6 行）

```text
IsValid(self):
    owner = self.owner                 // self==null：立即无防护失败
    if owner == null: return false     // 短路；不读取 node
    node = self.node
    if node == null: return false
    return true                        // 不验证 owner/node 所指内容
```

伪代码覆盖全部条件、返回默认值与失败边界：结果只可能是规范化的 `false/true`；空 self
没有保护，空 owner 走短路 false，owner 非空而 node 为空也返回 false，两者非空即返回 true。
本函数没有其他错误码、异常或默认输出。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBRawFile.h:73-118,120-165`，类 `PSBFile/PSBRawNode`：本地保持一个无
  vtable 的两指针 ABI raw handle；首子对象是含 owner 的 PSBFile-compatible holder，
  `node_` 独立随后，且
  `IsValid_guess() const` 是独立声明。
- `cpp/plugins/psbfile/PSBRawFile.cpp:213-217`，目标实现
  `bool PSBRawNode::IsValid_guess() const`：`return GetOwner() != nullptr && node_ != nullptr;`
  与 Android 的完整判断一致。
- 伪代码第 2 行对应 `PSBRawFile.cpp:216` 的左操作数 `GetOwner() != nullptr` 及
  `PSBRawFile.h:136-138`；C++ `&&` 保证先求值左操作数。
- 伪代码第 3 行对应同一 `&&` 的短路语义：owner 为空时直接返回 false，不读取
  `PSBRawFile.h:165` 的 `node_`，与 Android `CBZ @0x598E48` 一致。
- 伪代码第 4-6 行对应 `PSBRawFile.cpp:216` 的右操作数 `node_ != nullptr`：仅在 owner
  非空时读取 node，node 为空返回 false，否则返回 true；没有增加深层 owner/node 校验。
- `cpp/plugins/psbfile/main.cpp:751-757` 的全部本地 NCB 注册面只有 Factory、`root`、
  `load` 与 pre-register callback，没有把 `PSBRawNode::IsValid_guess` 暴露为 wrapper。
- 两种独立本地搜索（完整限定调用形式与地址/名称搜索，覆盖 `cpp/`、`tests/`、`tools/`、
  `platforms/`）都只找到声明和定义，没有调用或成员指针引用；这与 Android 的空 callers/
  xrefs 互相印证，而不是仅依赖一次 negative grep。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是独立的 `0x20` emitted 入口，接收一个两指针对象地址并返回 bool；本地保留独立的 `PSBRawNode::IsValid_guess() const`，没有内联进其他 raw-node API 或混入 NCB/vtable。精确原始名称不可从 stripped 二进制恢复，故保留 `_guess`，但不构成已证实结构差异。 |
| 数据流 | `MATCH` | Android 严格按 `self+0` holder.owner → 条件性 `self+8` node 的顺序读取，并将两者非空的合取规范化为 0/1；本地 `GetOwner() != nullptr && node_ != nullptr` 保留相同输入、读取依赖、短路顺序与输出。 |
| 调用链 | `MATCH` | Android 完整入口无 callee，IDA 的 callers 与外部 code/data xrefs 均为空；本地也只有声明/定义，没有 call 或成员指针引用，且不在 NCB 注册面。parent `@598A3C` 只是 classification-only，不伪装为 direct-call。 |
| 对象生命周期 | `MATCH` | Android 无 store、AddRef、Release、构造、析构或分配，只借用 self 并读取两槽；本地目标也不改变 owner 引用计数和 node 生命周期。非空只表示句柄槽值，函数不会延长或验证它们所指对象的生命。 |
| 内部容器实现 | `N/A` | 本入口不创建、访问、遍历或修改 STL/KiriKiri 容器、packed dictionary、TJS Array 或 NCB member table；只读取两只裸指针字段。 |
| 边界行为 | `MATCH` | Android 对空 self 无保护；owner 为空时返回 false 且不读 node；owner 非空/node 为空返回 false；两指针只要非空便返回 true，即使所指内容无效或悬空。返回由 `MOV W0,WZR`/`CSET W0,NE` 规范化为 0/1。本地 C++ 短路表达式逐项一致。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整 8 指令 disasm、两类 xref 查询、caller/callee 交叉检查和当前
本地源码可证明范围内，`0x598E44` 的六维行为与本地 `PSBRawNode::IsValid_guess()` 对齐。

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能唯一恢复函数的精确原始 C++ 名称、`const` 拼写或成员
函数/等价 helper 的源码 token；canonical 名称因此继续使用 `_guess`。现有指令、xref 与
本地结构没有证明这些不可恢复 token 形成实现差异。

## TASK_TREE 直接子节点与 cross-reference

- canonical 直接子节点：空。本节点阶段 A 未 spawn 任何 agent。
- canonical parent `0x598A3C PSBFile_GetRoot_guess [classification-only]` 不调用本函数；
  Android 对 `0x598E44` 的 caller/xref 查询为空。
- `0x598E64`、`0x598B58`、`0x5992E8`、`0x599438`、`0x599554` 等是 parent 下的其他
  raw-node API 分类 sibling，不是本函数 callee，也不是本节点 child；不得据此创建重复 agent。
- 未发现非 canonical code/data cross-reference；本入口自身的顺序流边不构成树外函数关系。

# `0x5998C0` — `PSBMedia_NormalizePathName_nullsub`

## 节点元数据

- 审计日期：2026-07-25
- 审计 agent path：`/root/f_42cf28/f_59849c/f_5998c0`
- 分组：E（PSBMedia `psb:` storage gateway）
- canonical 地址：`0x5998C0`
- canonical 名称：`PSBMedia_NormalizePathName_nullsub`
- TASK_TREE parent：`0x59849C`（`PSBFile_preRegister_guess_initPsbFile`）
- parent 关系：`[vtable-member]`
- TASK_TREE 直接子节点：无

## Android 归属与本轮 fresh 证据

唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 的内嵌实现；本报告未查看、引用或从外部
`psbfile.dll` 推导任何结论。

本轮新会话已 fresh 调用 IDA MCP `decompile(0x5998C0)`，结果为：

```c
void __fastcall nullsub_263(PSBMedia *self, ttstr_psb_arm64 *name)
{
  ; /*0x5998c0*/
}
```

补充的本轮二进制证据：

- `disasm(0x5998C0)` 仅有 `0x5998C0: RET`；函数大小为 4 字节、总指令数为 1，
  没有分支、读写或调用。
- `xrefs_to(0x5998C0)` 只有一个 data xref：`0x1A0B540`，没有直接 code xref。
- 从 `0x1A0B510` 开始的连续函数指针表中，`0x1A0B538 → 0x5998BC`、
  `0x1A0B540 → 0x5998C0`、`0x1A0B548 → 0x5998C4`；故本入口是该表从 0
  起算的 vslot 06，而不是由 parent 保留的直接调用。
- 仅作归属上下文而非替 parent 审计：`0x59849C` 在 `0x5984F8` 把
  `off_1A0B510` 写入新建对象的 vptr，并在 `0x59851C` 将同一对象交给 storage-media
  注册函数。因此 `0x5998C0` 属于该 process-lifetime PSBMedia 对象的 storage-media
  vtable。
- 本入口不是 NCB callback/wrapper，也不直接调用 NCB 基础设施；其归属是
  `iTVPStorageMedia` 风格的虚接口槽。动态 caller 通过该槽间接到达，所以目标函数没有
  直接 code xref 与此并不冲突。

## Android 伪代码（4 行）

```text
NormalizePathName(self, name):
    // 无条件、无默认值替换、无参数检查
    // 不读取/写入 self 或 name，也不调用其他函数或制造失败
    return                          // name 原样保留
```

## 本地实现与逐行对照

精确位置：

- `cpp/core/base/StorageIntf.h:114-152` — `iTVPStorageMedia` 虚接口；其中
  `:130` 声明 `NormalizePathName(ttstr &name)`。
- `cpp/plugins/psbfile/PSBMedia.h:10` — `PSBMedia : public iTVPStorageMedia`；
  `:29` 声明 `void NormalizePathName(ttstr &name) override`。
- `cpp/plugins/psbfile/PSBMedia.cpp:15-17` — `PSBMedia::NormalizePathName(ttstr &)`
  的独立空定义，且注释记录 `nullsub_263 @ 0x5998C0`。
- `cpp/core/base/StorageIntf.cpp:428-439` — 通用 storage manager 取得
  `iTVPStorageMedia` 后，在 `:439` 经虚接口调用 `NormalizePathName(path)`。

逐行对照：

| Android 伪代码 | 本地复刻 |
| --- | --- |
| 第 1 行：成员入口接收 `self` 与可变 `name` | `PSBMedia.h:10,29` 以虚 override 保留成员函数和 `ttstr &` 可变参数形状；`StorageIntf.h:130` 保留基类虚槽。 |
| 第 2 行：无条件、无默认值或检查 | `PSBMedia.cpp:15-17` 没有条件、检查或替换逻辑。 |
| 第 3 行：不读写参数且无 callee | 本地定义将参数名省略，函数体仅含地址注释；没有任何表达式或调用。 |
| 第 4 行：直接返回并保持 `name` 不变 | 空 `void` 函数自然返回，调用者传入的 `ttstr` 不发生变化。 |

## 六维审计

| 维度 | 状态 | 证据与判断 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一个独立 emitted 虚函数入口，vtable `0x1A0B510` 的 vslot 06 指向它，函数体为空；本地以 `iTVPStorageMedia` override 加独立空定义复刻同一可证明结构。stripped 二进制不能证明原始参数拼写，但这不构成已知偏差。 |
| 数据流 | `MATCH` | Android 单条 `RET` 不读取或写入 `self/name`；本地省略参数名且空体，同样保持传入路径字符串原样不变。 |
| 调用链 | `MATCH` | Android 只有 vtable data xref、没有 callee 或直接 code caller；本地函数占据 storage-media override，并由 manager 经 `iTVPStorageMedia` 虚调用。canonical parent 边正确标为 `[vtable-member]`，未伪装为直接调用。 |
| 对象生命周期 | `N/A` | 本入口不分配、销毁、复制、引用计数或保存对象/字符串；PSBMedia singleton 的构造与注册属于 parent 上下文，不是本函数行为。 |
| 内部容器实现 | `N/A` | Android 入口不访问任何容器、字段或内存；本地空体亦不涉及容器。 |
| 边界行为 | `MATCH` | Android 对所有到达该入口的调用均直接 `RET`：无特殊输入分支、无 fallback、无错误码、无异常调用且不改变 `name`；本地空 override 在已定义的 C++ 调用范围内保持同一边界行为。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

## Cross-reference

- TASK_TREE 没有为 `0x5998C0` 记录额外的非 canonical cross-reference。
- `0x1A0B540 → 0x5998C0` 是本函数唯一二进制 xref，属于 vtable 数据关系；它不是
  对 `0x5998C0` 的直接调用边，也不应创建重复函数 agent。
- 相邻 vslots `0x5998BC` 与 `0x5998C4` 仅用于机械确认本槽位置，未在本报告中替它们
  作函数审计结论。

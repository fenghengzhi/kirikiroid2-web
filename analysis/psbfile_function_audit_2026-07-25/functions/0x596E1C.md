# `0x596E1C` — `PSBValueDispatch_IsInstanceOfByNum`

## 审计身份

- canonical 地址：`0x596E1C`
- canonical 名称：`PSBValueDispatch_IsInstanceOfByNum`
- IDA 当前标签：`PSBValueDispatch_IsInstanceOfByNum`。Android stripped 二进制未保留原始
  C++ 符号拼写；该标签是既有分析标签，本报告不把它当作独立命名证据。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 安装包含本入口的 primary vptr；它不直接调用本
  函数。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596e1c`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅可视作 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596E1C")`；随后 fresh 调用
`disasm(addr="0x596E1C", include_total=true)`、`xrefs_to(addrs="0x596E1C")`、
`lookup_funcs(0x596E1C)`，并读取 primary vtable 相邻五个 qword 及该表自身的 xref。报告
写入前本地址没有旧报告，本文件是本轮 fresh 审计新建结果。

- fresh decompile 的完整函数体是
  `return 4294966294LL /*0x596E20*/`。Hex-Rays 因 stripped 类型和全部未用形参而显示
  `__int64 fn()`；实际指令只写 `W0`，低 32 位 `0xFFFFFC16` 按 `tjs_error` 有符号语义
  即 `-1002`。
- fresh 完整 disasm 覆盖 `0x596E1C..0x596E23`；`lookup_funcs` 给出大小 `0x8`。两条
  指令依次为 `MOV W0, #0xFFFFFC16` 与 `RET`，没有栈帧、形参读取、对象字段访问、条件
  分支、内存写入、`BL/BLR`、临时对象或异常清理。
- fresh `xrefs_to(0x596E1C)` 只返回 data xref `0x1A0B488`，没有 code xref。
  `0x1A0B488 - 0x1A0B3D8 = 0xB0 = 22 * 8`，精确落在 `PSBValueDispatch` primary
  vtable address point 的 slot 22。
- fresh `get_int(u64le)` 读取的 slot 20..24 依次是
  `0x596ED0`、`0x596E24`、`0x596E1C`、`0x596E14`、`0x596E0C`，即
  `Reserved1`、`IsInstanceOf`、本节点 `IsInstanceOfByNum`、`Operation`、
  `OperationByNum` 的连续接口槽布局。
- 对 primary address point `0x1A0B3D8` 的 fresh xref 查询包含
  `PSBValueDispatch_ctor_guess:0x597ADC`，证明 canonical parent 把该表装入构造对象；
  本节点因此是 primary `iTJSDispatch2` vtable member，不是 NCB wrapper、注册 callback、
  helper 或 parent 的 direct callee。

## Android 伪代码（4 行）

```text
IsInstanceOfByNum(this, flag, num, classname, objthis):
    不读取 this、flag、num、classname 或 objthis
    不访问内存、不写输出，也不调用任何函数
    return tjs_error(int32(0xFFFFFC16))  // -1002，TJS_E_NOTIMPL
```

本函数没有条件、默认值或成功分支。任意 flag/索引/指针值在已经进入本函数后均走同一固定
失败返回；虚分派在进入函数前对 vptr 的访问属于 caller，不是本入口的边界行为。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 以
  `iTJSDispatch2` 为 primary base，对应 Android primary vtable 归属。
- `cpp/plugins/psbfile/PSBDispatch.h:98-105`：`Reserved1` 后声明同签名
  `IsInstanceOfByNum(...) override`，其后是 `Operation`；对应 fresh qword 证明的
  slot 20..23 连续顺序。本节点精确声明位于 `:100-101`。
- `cpp/plugins/psbfile/main.cpp:527-530`：
  `PSBValueDispatch::IsInstanceOfByNum` 的所有形参均未命名，函数体唯一语句是
  `return TJS_E_NOTIMPL;`；逐项对应 Android 的“忽略全部实参 → 零副作用/零 callee →
  固定返回”数据流。
- `cpp/core/tjs2/tjsInterface.h:268-286`：base interface 中 `Reserved1`、
  `IsInstanceOf`、`IsInstanceOfByNum` 的虚方法次序及后者的 `flag/num/classname/objthis`
  参数契约，与 Android slot 20..22 的连续表项一致。
- `cpp/core/tjs2/tjsTypes.h:39,57`：`tjs_error` 是 32 位有符号 `tjs_int32`；
  `cpp/core/tjs2/tjsErrorDefs.h:23-24`：`TJS_E_NOTIMPL` 定义为 `-1002`，其 32 位二补码
  正是 Android `MOV W0` 写入的 `0xFFFFFC16`。

逐行对照：伪代码第 1 行对应 `PSBDispatch.h:100-101` 与
`tjsInterface.h:280-286`；第 2-3 行对应 `main.cpp:527-529` 的全部未命名形参和单一 return
语句；第 4 行由 `main.cpp:529`、`tjsErrorDefs.h:24`、`tjsTypes.h:39,57` 与 Android
`0x596E1C` 的返回位型共同证明。本地没有增加 base fallback、参数检查、输出写回或 helper
调用。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留 primary `iTJSDispatch2` vtable slot 22 的独立 8-byte emitted 入口；本地同样保留 `PSBValueDispatch::IsInstanceOfByNum` 的独立 out-of-line override。stripped 二进制不能证明原始标识符和形参名拼写，但当前可证明的发射结构一致。 |
| 数据流 | `MATCH` | Android 只把 `0xFFFFFC16` 写入 32-bit 返回寄存器，不读取任何实参或对象字段；本地所有形参均未命名/未使用，只返回等值 `TJS_E_NOTIMPL`。 |
| 调用链 | `MATCH` | Android 无 `BL/BLR`，且唯一函数 xref 是 slot 22 的 vtable data reference；本地也没有 helper、base-method、NCB wrapper 或其他 callee，通过同一 override 虚分派进入。parent 只安装 vptr，TASK_TREE 的 `[vtable-member]` 边准确。 |
| 对象生命周期 | `N/A` | 本入口不分配、构造、析构、AddRef/Release 或转移所有权，也不触碰 `this`；对象构造与 vptr 安装属于 parent。 |
| 内部容器实现 | `N/A` | 完整两指令函数不访问 packed PSB table、TJS/STL/KiriKiri 容器、迭代器或任何集合状态。 |
| 边界行为 | `MATCH` | 任意 flag、数字索引、classname/objthis 指针均被忽略；进入本入口后没有 null/bounds/valid 检查或副作用，始终返回 `TJS_E_NOTIMPL (-1002)`。本地没有额外 guard、成功路径或其他错误码。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整两指令 disasm、唯一 vtable data xref、连续槽位与本地
独立 override 的逐行对照可证明范围内，六维实现一致。

## 确定 GAP

无。

证据边界：stripped/O3 不能恢复原始函数/形参标识符拼写，且全部未用形参不会出现在函数体
反编译签名中；vtable slot、接口位置和可观察行为已由 fresh 二进制证据闭合，这不是已证明
偏差。

## TASK_TREE 直接子节点与 cross-reference

- 直接子节点：无；本节点是 canonical leaf，阶段 A 未 spawn。
- TASK_TREE 的非 canonical cross-reference 清单没有涉及 `@596E1C`。
- `@1A0B488 -> @596E1C` 是 primary vtable slot 22 的唯一 Android xref；它属于本节点
  canonical `[vtable-member]` 归属，不是 direct call。
- `@597AD4` 是唯一 canonical parent；fresh table xref 证明它安装 primary address point，
  但不直接调用本函数。
- `@596E24` 与 `@596E14` 只是相邻 vtable sibling，不是本节点的 caller、callee 或 child，
  不得据此重复派发 agent。

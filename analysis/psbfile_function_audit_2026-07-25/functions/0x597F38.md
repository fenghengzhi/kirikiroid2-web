# `0x597F38` — `PSBFile_ncb_registerMembers_guess`

> `LSDA-CALLSITE-TOPOLOGY`（2026-08-03）：FDE 的 LSDA `0x186DB4C` 恰有 7 项：
> 4 项无 landing、3 项 cleanup-only、0 项 catch-all；完整四元组由全量门禁固定。

## 审计身份

- canonical 地址：`0x597F38`
- canonical 名称：`PSBFile_ncb_registerMembers_guess`
- IDA 当前标签：`PSBFile_ncb_registerMembers_guess`。二进制没有保留该模板专门化的
  精确源码符号，故保留 `_guess`。
- 分组：C（PSBFile typed NCB 前段 / factory / root）
- TASK_TREE parent：`0x59A8D8 PSBFile_AutoRegister_Regist_guess`
- parent 关系：`[direct-call]`；Android 保留的调用点是 `0x59A91C`。
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_597f38`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `L"PSBFile.dll"` 仅是其他入口使用的 NCB 模块
  名字面量，本函数不读取它。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由
`/root/f_42cf28/f_59a8d8/f_597f38` fresh 调用 IDA MCP
`decompile(addr="0x597F38")`；随后调用完整
`disasm(addr="0x597F38", include_total=true, max_instructions=500)`、
`xrefs_to(0x597F38)`、`xrefs_to(0x59AEEC)` 复核。本报告不是复用旧报告。

- fresh decompile / disasm 覆盖 `0x597F38..0x5980F0`，IDA size 为 `0x1BC`，共
  107 条指令（含异常清理落点）。本入口不是 vtable member，而是
  `ncbRegistClass<ncbRegistNativeClass<PSBFile>>::Regist()` 的 typed NCB 成员声明体；
  精确模板名由 Android emitted shape 与本地生成点交叉对照，二进制本身未保留拼写。
- `0x597F50` 首先读取 registrar 的 `this+8` 注册方向。为 false 时直接返回；为 true
  时 `0x597F58..0x597F60` 从 `this+0` 的 delegate 取得 class name，作为 Factory 项名称。
- `0x597F64..0x597FB0` 分配 Android ARM64 大小 `0x38` 的 factory wrapper，写入
  `nitMethod=1`、`L"Function"`、内部 `ncbIMethodObject` address-point/self 指针、
  `PSBFile_Factory_guess@0x5980F4`；wrapper 主 address-point 是 `off_1A0B5D0`。
- `0x597FB4` 再读注册方向；为 true 时 `0x597FC4` 以 delegate class name 调用
  `PSBFile_ncbRegistNativeClass_RegistItem_guess@0x59AEEC`。`0x597FC8` 再检查方向。
- `0x597FD4..0x598020` 分配 Android ARM64 大小 `0x50` 的 property wrapper，写入
  `nitProperty=2`、`L"Property"`、getter `PSBFile_GetRootDispatch_guess@0x5981F8`，
  并把 setter 的 pointer-to-member 表示及附加字清零；主 address-point 是
  `off_1A0B6F0`。`0x598024` 再检查方向，`0x598030..0x598038` 以字面名
  `L"root"` 调用 `@59AEEC`。
- `0x59803C` 再检查方向；`0x598048..0x598090` 分配 Android ARM64 大小 `0x40`
  的 method wrapper，写入 `nitMethod=1`、`L"Function"`、pointer-to-member
  `PSBFile_Load@0x598268` 及其零附加字；主 address-point 是 `off_1A0B810`。
  `0x598094` 最后检查方向，true 时以字面名 `L"load"` 在 `0x5980B8` 尾调用
  `@59AEEC`。
- `xrefs_to(0x59AEEC)` 精确返回本函数内三个 retained direct call：
  `0x597FC4`、`0x598038`、`0x5980B8`。因此 TASK_TREE 只创建一个 canonical child
  函数 agent，不能按三次调用重复派发。
- `xrefs_to(0x597F38)` 返回两个 direct caller：canonical parent
  `0x59A8D8` 的 `0x59A91C`，以及 `0x59A968` 的 `0x59A9A0`。后者是 TASK_TREE
  明示的非 canonical unregister cross-reference。
- `0x5980D0..0x5980F0` 是 wrapper 构造失败的 new-expression 清理：删除刚分配的
  raw storage 后继续 `_Unwind_Resume`。三个 `RegistItem` 调用若传播异常，本栈帧没有
  对已完成 wrapper 的独立清理对象；后续所有权行为属于 `@59AEEC` 子节点的审计范围。
- `@0x5980F4`、`@0x5981F8`、`@0x598268` 在本函数中只是写入 wrapper 的 callback /
  pointer-to-member，并非本函数的 direct calls。Android ARM64 的 `0x38/0x50/0x40`
  是 ABI 布局证据，不是要求 wasm32 复刻的对象偏移。
- 旧 Hex-Rays 原型把入口误推为指针返回，是尾调用与 X0 残值造成的恢复错误；caller 不
  消费返回值。2026-08-02 已将 IDB 纠正为
  `void(PSBFile_ncbRegistClassState_arm64 *registrar)`，复反编译直接显示
  `registrar->impl/isRegist` 与三次无返回 `RegistItem`。

## Android 伪代码（9 行）

```text
RegisterMembers(r):
    if !r.isRegist: return
    f = new FactoryWrapper(type=Function, callback=@5980F4); if !r.isRegist: return
    RegistItem(r.impl, r.impl.className, f); if !r.isRegist: return
    p = new PropertyWrapper(type=Property, get=@5981F8, set=null); if !r.isRegist: return
    RegistItem(r.impl, L"root", p); if !r.isRegist: return
    m = new MethodWrapper(type=Function, method=@598268, extraWord=0); if !r.isRegist: return
    tail RegistItem(r.impl, L"load", m)
    wrapper 构造抛出则 delete 刚分配 storage 并重抛；callee 抛出则本帧直接传播
```

每次 `new` 后、每次下一项建立前的重复方向检查均已列出；若该 const 方向字节被越界改写
而在 wrapper 建立后变 false，当前 Android 机器码会直接返回且本帧不回收该 wrapper。
有效 C++ 对象生命周期内该改写属于未定义行为，不能要求不同 ABI/优化器保留同一非法状态
机器路径；本地源码需要对齐的是逐项 `Create(..., _isRegist)` / `DoItem(...)` 结构，不能为
该机器路径擅自补源代码 cleanup。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/main.cpp:751-755`，符号
  `ncbRegistClass<ncbRegistNativeClass<PSBFile>>::Regist()` 的宏生成体：严格依次为
  `Factory(PSBFileFactory)`、
  `Property(TJS_W("root"), &PSBFile::GetRootDispatch, 0)`、
  `Method(TJS_W("load"), &PSBFile::Load)`。
- `cpp/core/plugin/ncbind.hpp:2160-2172`，宏
  `NCB_REGISTER_CLASS_COMMON/NCB_REGISTER_CLASS`：第 2164 行把上述块声明为返回
  `void` 的 `ncbRegistClass<ncbRegistNativeClass<PSBFile>>::Regist()` 专门化。
- `cpp/core/plugin/ncbind.hpp:1665-1679`、`:1705-1732`，符号
  `ncbRegistClass<IMPL>`：`_impl` 位于语义首字段，`_isRegist` 紧随其后；
  `GetName()` 从 delegate 取 class name，`DoItem()` 按每次读取的 `_isRegist` 选择
  `RegistItem/UnregistItem`，与 Android `this+0/this+8` 和逐项重检一一对应。
- `cpp/core/plugin/ncbind.hpp:1767-1776`，`ncbRegistClass::Factory`：raw factory
  overload 以 delegate class name 建立
  `ncbNativeClassFactory<PSBFile>`，对应伪代码第 2-3 行与 Android `0x597F64..0x597FC4`。
- `cpp/core/plugin/ncbind.hpp:1714-1732`，`ncbRegistClass::Property/Method`：
  `Property(..., 0)` 把 setter 转为同型 null pointer-to-member；`Method` 建立 typed method
  wrapper，对应伪代码第 4-7 行。没有把 `root` 错做可写属性，也没有把 callback 存储边
  错写成 direct call。
- `cpp/core/plugin/ncbind.hpp:849-908`，符号 `ncbNativeClassMethodBase`：保存
  `nitMethod/nitProperty`、`Function/Property` 名称与内嵌 `iMethod{parent-self}`；对应
  Android 三只 wrapper 的公共字段与 `off_19FE1F8` interface address-point。
- `cpp/core/plugin/ncbind.hpp:1398-1435`、`:1442-1496`、`:1326-1357`：分别是
  `ncbNativeClassFactory`、`ncbNativeClassProperty`、`ncbNativeClassMethod` 的构造和
  `Create`；都使用单一 `new ThisClassT(...)`，且 `create=false` 返回 null，精确对应
  Android 三种 wrapper、null setter、方向短路与 new-expression 异常清理。
- `cpp/core/plugin/ncbind.hpp:1819-1840`、`:1879-1887`：delegate 的 class name 与
  `RegistItem` handoff 位置。这里仅用于证明本函数的参数流向；`RegistItem` 内部行为由
  `0x59AEEC` 唯一 child agent 独立审计。
- callback / member targets 的本地位置分别是
  `cpp/plugins/psbfile/main.cpp:732-749`（`PSBFileFactory`）、
  `cpp/plugins/psbfile/main.cpp:689-701`（`PSBFile::GetRootDispatch`）、
  `cpp/plugins/psbfile/PSBRawFile.cpp:414-467`（`PSBFile::Load`）。这些位置只确认注册
  指针身份，不替对应函数节点下行为结论。

逐行对应：伪代码第 1 行对应 `ncbind.hpp:1350,1432,1486` 的 `create=false` 短路；
第 2-3 行对应 `main.cpp:752`、`ncbind.hpp:1767-1776`；第 4-5 行对应
`main.cpp:753`、`ncbind.hpp:1720-1732,1442-1496`；第 6-7 行对应
`main.cpp:754`、`ncbind.hpp:1714-1718,1326-1357`；第 8 行对应三处 C++ `new`
表达式的构造失败规则和 Android `0x5980D0..0x5980F0`，本地没有额外 guard 或容器。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是一个 typed `Regist()` body，内联建立 Factory、只读 Property、Method 三种 wrapper，并都带公共 `ncbIMethodObject` 子对象；本地保留 `NCB_REGISTER_CLASS` 专门化、`ncbRegistClass`、三种独立 wrapper template 与相同 Factory→root→load 源码顺序。ARM64 对象尺寸/偏移只作字段完整性证据，不被硬编码到 wasm 源码。 |
| 数据流 | `MATCH` | Android 从 registrar 的 delegate 取得第一项名称，将 `@5980F4`、`@5981F8 + null setter`、`@598268 + zero extra word` 分别写入三只 wrapper，并以 class name、`root`、`load` 传给同一 `@59AEEC`；本地三个声明、`GetName`、`Create`、`DoItem` 的输入与默认值逐项相同。 |
| 调用链 | `MATCH` | Android canonical caller 是 `@59A8D8:0x59A91C`，本入口在 `0x597FC4/0x598038/0x5980B8` 三次直调唯一 child `@59AEEC`；本地 `ncbNativeClassAutoRegister::Regist → r.Regist() → DoItem → delegate.RegistItem` 保留同层调用链。三个 callback 目标仅被存储，未伪装成当前函数的 direct call。 |
| 对象生命周期 | `MATCH` | Android 在注册方向为 true 时逐只堆建 wrapper，完成后交给 `RegistItem`；构造失败删除刚分配 storage 并重抛，方向重检短路和 callee 异常均无本帧额外 owner。对应本地是 `Create` 内单一 `new` 后立即 `DoItem`，没有 `shared_ptr`、临时容器或补造 cleanup；后续 ownership 由 child 审计。 |
| 内部容器实现 | `N/A` | 本入口不创建、遍历或修改 STL/KiriKiri 容器、TJS Array、module map/list；它只建立三个独立 wrapper 并逐项 handoff。外层 autoreg 索引与 class member table 属其他函数。 |
| 边界行为 | `MATCH` | Android 对 null `r/impl` 不做保护；正常的 false 注册方向立即无操作返回，并在 emitted Create/DoItem 边界重读方向；`root` setter 明确为 null；分配/构造异常按 C++ new-expression 展开，callee 异常不被本帧吞掉。当前本地保持相同定义内短路、null/default 与异常传播，没有“更安全”补丁；越界改写 const 字节属于未定义行为，不被错误包装成跨 ABI 契约。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 107 指令 disasm、双 caller xref、三处 child call xref
与本地宏/template 生成点可证明范围内，`0x597F38` 的六维行为与当前
`NCB_REGISTER_CLASS(PSBFile)` 成员声明体一致。

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能恢复专门化的精确原始标识符拼写，因此名称保留
`_guess`；旧 X0 伪返回已按 caller 数据流纠正为 `void`。这不是生产行为差异。详见
[../FOLLOWUP_IDB_CLASSINFO_REGISTRATION_ABI_TYPES_2026-08-02.md](../FOLLOWUP_IDB_CLASSINFO_REGISTRATION_ABI_TYPES_2026-08-02.md)。

## TASK_TREE 直接子节点与 cross-reference

- 尚待由本节点在阶段 B 派发的唯一 canonical 直接子节点（本阶段未 spawn）：
  - `0x59AEEC PSBFile_RegistItem_guess [direct-call]`；本入口的三个调用点为
    `0x597FC4`、`0x598038`、`0x5980B8`，但只允许一个 function agent。
- 非 canonical cross-reference：
  `0x59A968 PSBFile_AutoRegister_Unregist_guess` 在 `0x59A9A0` 也直接调用本函数；
  它传入 `isRegist=false` 的 registrar，进入本函数的无操作短路。该节点的 canonical parent
  是 `0x42CF28`，不得由本节点重复派发。
- `@5980F4`、`@5981F8`、`@598268` 是 registration binding targets，不是本入口的
  direct-call children；它们沿 TASK_TREE 各自唯一 canonical parent 审计。

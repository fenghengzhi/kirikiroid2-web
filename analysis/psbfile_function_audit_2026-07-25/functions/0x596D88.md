# `0x596D88` — `PSBValueDispatch_ClassInstanceInfo`

## 审计身份

- canonical 地址：`0x596D88`
- canonical 名称：`PSBValueDispatch_ClassInstanceInfo`
- IDA 当前标签：`PSBValueDispatch_ClassInstanceInfo`。Android stripped 二进制没有保留原始
  C++ 符号；本报告只使用 TASK_TREE 已固定的 primary vtable slot 26 ABI 归属，不把本地
  名称反向当作 Android 行为证据。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 构造对象并安装 primary vptr；本边是 vtable 成员
  归属，不是 `@597AD4` 对本函数的直接调用。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596d88`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅是 NCB 模块名字面量，本入口不读取它。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596D88")`；随后 fresh 调用
`disasm(addr="0x596D88", include_total=true)`、
`xrefs_to(addrs="0x596D88")`，并用相邻三槽 xref 与
`get_bytes(addr="0x1A0B498", size=32)` 核对 vtable 位置。本地址在本轮开始时没有报告；
本文件是本轮 fresh 审计新建结果。

- fresh decompile 恢复为无条件 `return 4294966294LL`；完整 disasm 证明函数恰好两条
  指令：`0x596D88 MOV W0, #0xFFFFFC16`，`0x596D8C RET`。`0xFFFFFC16` 按
  32-bit `tjs_error` 解释即 `-1002`；反编译器显示 `__int64`/正数只是丢失函数原型后对
  AArch64 `W0` 返回的错误提升，不是 64-bit 业务返回契约。
- fresh xref 恰好一项：data xref `0x1A0B4A8`，没有 code xref。相邻槽 fresh xref 为
  `0x1A0B4A0 -> @596D90`、`0x1A0B4B0 -> @596D80`；原始 bytes 也依次编码
  `@596E0C, @596D90, @596D88, @596D80`。相对 primary vtable 基址 `@1A0B3D8`，
  `0x1A0B4A8 - 0x1A0B3D8 = 26 * 8`，故本入口确属 slot 26。
- 本体没有栈帧、条件跳转、`BL/BLR`、load/store 或间接访问；所有传入 ABI 参数均不被
  读取，返回前也不修改对象、输出 Variant 或全局状态。
- 本入口不是 NCB wrapper/callback；运行时只能通过 `iTJSDispatch2` primary vtable 的
  `ClassInstanceInfo` 虚分派到达。IDA 没有静态 code xref 正是该间接调用形态的预期结果，
  不能据此断言没有运行时 caller。

## Android 伪代码（4 行）

```text
ClassInstanceInfo(flag, num, value):
    ignore(flag, num, value)                // 包括 value == null；不解引用、不写回
    return (tjs_error)0xFFFFFC16             // -1002 / TJS_E_NOTIMPL
                                             // 无分支、callee、异常或其他失败边界
```

该伪代码覆盖全部条件（无条件分支）、固定返回默认值、null/任意参数以及无副作用边界。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 直接继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`，使本方法属于 primary dispatch vtable，而不是
  secondary native-instance vtable。
- `cpp/plugins/psbfile/PSBDispatch.h:111-112`：显式声明
  `tjs_error ClassInstanceInfo(tjs_uint32, tjs_uint, tTJSVariant *) override`，与 canonical
  slot 26 的三参数接口归属一致。
- `cpp/core/tjs2/tjsInterface.h:313-318`：`iTJSDispatch2::ClassInstanceInfo` 的接口槽明确接收
  `flag`、`num`、`value`，返回 `tjs_error`；这解释了 Android O3 因参数全未使用而把它们
  从反编译伪原型中抹去的结果。
- `cpp/plugins/psbfile/main.cpp:547-550`：本地
  `PSBValueDispatch::ClassInstanceInfo` 不命名也不读取三个参数，函数体唯一语句是
  `return TJS_E_NOTIMPL;`，逐行对应 Android 的 `MOV W0` 与 `RET`。
- `cpp/core/tjs2/tjsTypes.h:57` 与 `cpp/core/tjs2/tjsErrorDefs.h:21-25`：本地
  `tjs_error` 是 `tjs_int32`，`TJS_E_NOTIMPL` 定义为 `-1002`；其 32-bit bit pattern 正是
  Android 指令中的 `0xFFFFFC16`。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:111-112` 和接口 `tjsInterface.h:313-318`；
第 2 行对应 `main.cpp:547-548` 的匿名参数及函数体没有任何读取；第 3 行对应
`main.cpp:549`、`tjsTypes.h:57`、`tjsErrorDefs.h:24`；第 4 行对应本地函数同样没有其他
语句、callee、状态写入或错误恢复。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 primary vtable slot 26 指向的独立两指令 override；本地在 `PSBValueDispatch` 中显式覆写同一 `iTJSDispatch2` 接口槽，函数体同样只含固定错误返回。`MATCH` 仅表示 stripped 二进制当前可证明的结构，不声称恢复了不可见的原始参数名或格式。 |
| 数据流 | `MATCH` | Android 不读取任何参数/对象/全局，不写 `value`，仅把 `0xFFFFFC16` 放入 `W0`；本地匿名三个参数且唯一数据流是返回 `TJS_E_NOTIMPL`。 |
| 调用链 | `MATCH` | Android 唯一静态引用是 `@1A0B4A8` vtable data xref，无 direct caller、无 callee；本地以 `override` 安装到 primary `iTJSDispatch2` vtable，并且函数体不调用任何 helper。parent 边准确记为 `[vtable-member]`，未伪装为 direct-call。 |
| 对象生命周期 | `N/A` | 本入口不读写 `this`、引用计数、owner、Variant 或任何分配；不会构造、retain、release、销毁对象。本地同样无生命周期动作。 |
| 内部容器实现 | `N/A` | 本入口没有容器、迭代、容量、节点或 packed-table 操作。 |
| 边界行为 | `MATCH` | 任意 `flag`/`num`、任意或 null `value`、任意对象状态都走同一无解引用路径并返回 32-bit `-1002`；没有参数校验、输出清零、异常、fallback 或其他失败码。本地完全相同。 |

## 总判定

`ALIGNED`

Android 当前可见的 vtable 归属、固定返回数据流、间接调用链、无副作用及全部边界与本地
一致；没有确定 GAP，也没有影响六维结论的证据缺口。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- canonical 直接子节点：无。阶段 A 未 spawn。
- `@597AD4` 是 canonical parent；它通过对象 primary vptr 建立本入口的
  `[vtable-member]` 归属，不直接调用 `@596D88`。
- `@596D90`（前一槽）与 `@596D80`（后一槽）仅是同一 primary vtable 的 sibling，分别
  由各自唯一 function subagent 审计；它们不是本节点 child，也不得从本节点重复派发。
- `@1A0B4A8` 是本函数唯一 Android data cross-reference（primary vtable slot 26），不是
  emitted 函数节点，不创建 function agent。

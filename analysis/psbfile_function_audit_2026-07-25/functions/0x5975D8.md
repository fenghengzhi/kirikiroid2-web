# `0x5975D8` — `PSBValueDispatch_GetCountByNum`

## 审计身份

- canonical 地址：`0x5975D8`
- canonical 名称：`PSBValueDispatch_GetCountByNum`
- IDA 当前标签：`PSBValueDispatch_GetCountByNum`。Android stripped 二进制没有保留原始
  C++ 符号拼写；名称沿用 TASK_TREE canonical 标签，函数归属由主 vtable 槽位和相邻项
  独立确认，本地标识符只用于对照，不能反向命名二进制。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 仅把 primary vtable address point
  `0x1A0B3D8` 安装进对象；它不直接调用本入口。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_5975d8`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅可视为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x5975D8")`；随后 fresh 调用目标完整
`disasm(addr="0x5975D8", include_total=true)`、`xrefs_to(addrs="0x5975D8")`、
`lookup_funcs` 与 primary vtable 原始字节读取。另 fresh decompile canonical parent
`@597AD4` 只为核对 vptr 安装上下文，没有替 parent 作第二份函数审计。本地址在本轮开始时
没有报告，本文件是本轮 fresh 审计新建结果。

- fresh decompile 完整结果只有
  `return 4294966294LL`。该值低 32 位是 `0xFFFFFC16`；按 Android 接口返回的
  32-bit `tjs_error` 解释即 `-1002`（`TJS_E_NOTIMPL`）。IDA 因全部实参均未使用而把
  恢复签名显示成无参 `__int64`，不能把这一反编译类型误写成原始 C++ 接口签名。
- fresh disasm 覆盖完整函数 `0x5975D8..0x5975DC`，size=`0x8`、共 2 条指令：
  `MOV W0, #0xFFFFFC16`；`RET`。函数没有读取 `this/result/num/objthis`，没有内存访问、
  输出写回、条件分支、`BL/BLR`、异常或清理路径。
- fresh `xrefs_to(@5975D8)` 恰好返回唯一 data xref `0x1A0B420`，没有 code xref。
  `get_bytes(0x1A0B3D8, 96)` 解析出的连续 qword 依次为
  `@597AC0, @597A40, @597A20, @597A18, @597854, @5976C4, @5976BC, @5976B4,
  @5975E0, @5975D8, @5975D0, @596F50`；因此本入口精确位于 primary address point
  `0x1A0B3D8 + 0x48` 的 vslot 09，前后分别是 `GetCount` 与 `PropSetByVS` 槽。
- contextual parent decompile 显示 `@597AD4:0x597AE4..0x597AF0` 把
  `off_1A0B3D8` 与 secondary vptr 一并写入新 `PSBValueDispatch` 对象。parent 本体没有
  对 `@5975D8` 的调用，故 TASK_TREE 的 `[vtable-member]` 是对象/vtable 归属关系，不是
  `[direct-call]`。
- 本入口属于 `PSBValueDispatch` 的 primary `iTJSDispatch2` vtable，不是 NCB wrapper、
  NCB callback、registration 入口或 secondary `iTJSNativeInstance` 槽。运行期只会经
  对应虚槽间接进入，函数体自身不触碰对象状态。

## Android 伪代码（4 行）

```text
GetCountByNum(this, result, num, objthis):
    ignore this, result, num, objthis
    return TJS_E_NOTIMPL  // int32 -1002；不写 result
                          // 无条件、默认值、其他失败边、callee 或异常路径
```

四行覆盖完整 emitted body：任何分支、fallback、成员状态读取和输出写回均不存在。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 直接实现
  `iTJSDispatch2`，形成 Android primary dispatch vtable 所对应的类结构。
- `cpp/plugins/psbfile/PSBDispatch.h:50-56`：在 `GetCount` 后声明完整
  `GetCountByNum(tjs_int *, tjs_int, iTJSDispatch2 *) override`，随后是
  `PropSetByVS`；这一覆写顺序与 Android vtable 中
  `@5975E0 -> @5975D8 -> @5975D0` 的相邻三槽一致。
- `cpp/plugins/psbfile/main.cpp:328-330`：目标
  `PSBValueDispatch::GetCountByNum(...)` 是独立 out-of-line 定义；三个显式形参均省略
  名称，函数体唯一语句是 `return TJS_E_NOTIMPL;`，逐指令对应 Android 两指令函数。
- `cpp/core/tjs2/tjsInterface.h:173-185`：`iTJSDispatch2::GetCount` 后紧接
  `GetCountByNum`，并给出 `result/num/objthis` 的接口参数顺序；Android O3 body 因不读
  参数而无法自行恢复这些源码 token，但 vslot 位置与本地接口层次相符。
- `cpp/core/tjs2/tjsTypes.h:39,57`：`tjs_error` 是 32-bit 有符号 `tjs_int32`；
  `cpp/core/tjs2/tjsErrorDefs.h:23-24`：`TJS_E_NOTIMPL` 定义为 `-1002`。因此本地返回值
  和 Android `W0=0xFFFFFC16` 逐位相同。
- `cpp/plugins/psbfile/main.cpp:20-25`：canonical parent 对应的本地构造函数通过普通
  C++ 双继承对象构造安装 vtable；和 Android 一样，构造与本虚槽的执行彼此分离。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:53` 和 `tjsInterface.h:181-185` 的虚槽接口；
第 2 行对应 `main.cpp:328` 的全部无名参数及 Android 零参数读取；第 3-4 行对应
`main.cpp:329` 与 Android `MOV W0,#0xFFFFFC16; RET`，双方均无其他控制流或副作用。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留独立 emitted 的 8-byte primary-vtable 槽函数，且它位于 `GetCount` 与 `PropSetByVS` 之间；本地同样以 `PSBValueDispatch::GetCountByNum` 独立覆写并使用单返回函数体。stripped 二进制不能证明原始标识符拼写，但当前可证明的类/vslot/函数边界一致。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或任一显式参数，只把 `0xFFFFFC16` 写入 `W0`，尤其不写 `*result`；本地全部形参未使用且只返回同一 32-bit 错误值。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 primary vtable 数据项 `0x1A0B420`，目标无 caller code xref、无 callee；parent 只安装该表。本地也只能经 `iTJSDispatch2` override 虚分派进入，目标函数不调用任何 helper。 |
| 对象生命周期 | `N/A` | 完整两指令入口不分配、构造、析构、AddRef/Release、retain 或转移参数；vptr 安装属于 parent 构造函数，不属于本函数体。 |
| 内部容器实现 | `N/A` | 本入口没有字段、packed 数据、STL/KiriKiri 容器、TJS Array、迭代器或 NCB member table 访问。 |
| 边界行为 | `MATCH` | 一旦控制流进入本入口，对任意 `result/num/objthis` 值都不解引用、不校验、不写回并无条件返回 `TJS_E_NOTIMPL(-1002)`；没有成功分支、替代错误码、异常或隐藏 cleanup。本地完全相同。通过虚分派取得目标前所需的有效对象/vptr 是 caller 边界，不是本函数额外行为。 |

## 总判定

`ALIGNED`

本轮 fresh target decompile、完整两指令 disasm、唯一 vtable data xref、连续 vtable 原始
字节、parent vptr 安装上下文及本地精确逐行对照共同证明：`0x5975D8` 在六维可适用范围内
与当前实现一致。

## 确定 GAP

无。

证据边界：Android stripped/O3 二进制不能恢复未使用形参的原始 C++ 名称、精确源码拼写，
IDA 因此显示无参 `__int64`；vtable 归属与 `W0` 返回机器码足以证明当前可见结构和行为，
但本报告不把不可观察的拼写宣称为已恢复。该证据限制不是已确定 GAP。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。本节点是 leaf，阶段 A 未 spawn 任何 agent。
- canonical parent `@597AD4` 通过 primary vptr 安装包含本函数的 vtable；这是
  `[vtable-member]` 关系，不是 direct call。
- `0x1A0B420` 是目标唯一 Android xref，属于上述 canonical vtable 关系；TASK_TREE 未为
  本节点列出其他非 canonical cross-reference。
- 相邻 `@5975E0 GetCount` 与 `@5975D0 PropSetByVS` 只是同一 primary vtable 的 sibling
  slots，不调用本入口、也不是本节点的 child；不得因邻接关系重复派发它们。

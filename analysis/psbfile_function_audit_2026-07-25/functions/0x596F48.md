# `0x596F48` — `PSBValueDispatch_DeleteMember`

## 审计身份

- canonical 地址：`0x596F48`
- canonical 名称：`PSBValueDispatch_DeleteMember`
- IDA 当前标签：`PSBValueDispatch_DeleteMember`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 构造对象并安装 primary vptr；它不直接调用
  本入口，不能把该归属边写成 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596f48`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。本入口也不读取二进制中的 `PSBFile.dll` 模块名字面量。

## Android vtable / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596F48")`，并补充完整 `disasm`、`xrefs_to`、相邻 vtable 原始字节与
函数边界查询；没有替任何其他地址作审计结论。

- 首次 fresh decompile 在尚未补全被优化掉的参数类型时精确得到：

  ```c
  __int64 PSBValueDispatch_DeleteMember()
  {
    return 4294966294LL; /*0x596f4c*/
  }
  ```

- 完整 disasm 只有两条指令，函数 size 为 `0x8`：
  `0x596F48 MOV W0, #0xFFFFFC16`；`0x596F4C RET`。写 `W0` 返回的 32-bit
  `0xFFFFFC16` 按 `tjs_error` 解释即 `-1002`；本体没有读取 `X0..X4`，没有内存访问、
  条件、callee、异常路径或任何对象状态写入。
- fresh `xrefs_to(0x596F48)` 只返回 data xref `0x1A0B438`，没有 code xref。
  `get_bytes(0x1A0B430,24)` 的三个连续 qword 依次为
  `@596F50`、`@596F48`、`@596F40`；它们分别是 `EnumMembers`、当前
  `DeleteMember` 与 `DeleteMemberByNum`。当前槽地址相对 primary vtable address-point
  `@1A0B3D8` 为 `0x60 = 12 * 8`，机械确认本入口是 primary vslot 12。
- 依据该已证 vtable 槽补全 Android 函数的五个 ABI 参数及 32-bit 返回类型后，再次
  fresh decompile 得到：

  ```c
  int __fastcall PSBValueDispatch_DeleteMember(
          PSBValueDispatch *self, unsigned int flag,
          const unsigned __int16 *membername, unsigned int *hint,
          iTJSDispatch2 *objthis)
  {
    return -1002; /*0x596f4c*/
  }
  ```

  该类型修正已保存到 IDB；参数名只用于表达槽 ABI，不声称 stripped 二进制保留了原始
  源码拼写。
- 本入口不是 NCB wrapper/callback，也没有直接 caller/callee；运行时只会经
  `iTJSDispatch2` primary vtable 虚分派进入。canonical parent `@597AD4` 只建立含该槽的
  dispatch 对象结构。

## Android 伪代码（3 行）

```text
DeleteMember(self, flag, membername, hint, objthis):
    ignore self, flag, membername, hint, objthis
    return int32(0xFFFFFC16)  // -1002；无条件，无默认/失败/异常分支
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:196-215`：`iTJSDispatch2` 的接口顺序是 vslot 11
  `EnumMembers`、vslot 12 `DeleteMember`、vslot 13 `DeleteMemberByNum`；这与 Android
  `@1A0B430/@1A0B438/@1A0B440` 的三个连续 qword 完全对应。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,65-69`：`PSBValueDispatch` 直接实现
  `iTJSDispatch2`，并显式声明 `DeleteMember(...) override` 与其独立的 ByNum sibling；
  没有继承 `tTJSDispatch` 那个会按 `membername` 改变错误码的默认实现。
- `cpp/plugins/psbfile/main.cpp:441-444`，目标本地符号
  `PSBValueDispatch::DeleteMember`：函数体唯一语句是第 705 行
  `return TJS_E_NOTIMPL;`；所有形参均不命名、不读取，对应 Android 的无输入读取两指令
  函数。
- `cpp/core/tjs2/tjsTypes.h:39,57`：`tjs_error` 是 32-bit 有符号整数；
  `cpp/core/tjs2/tjsErrorDefs.h:23-25`：`TJS_E_NOTIMPL` 精确定义为 `-1002`，其位模式就是
  Android 写入 `W0` 的 `0xFFFFFC16`。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:65-66` 的 override 槽签名；第 2 行对应
`main.cpp:441-443` 中未命名、未使用的全部参数以及 Android 无输入读取；第 3 行对应
`main.cpp:443`、`tjsErrorDefs.h:24` 与 `0x596F48 MOV W0,#0xFFFFFC16`。两边都没有附加
guard、基类转发、callee 或输出参数写入。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一个独立 size=`0x8` 的 primary vslot 12 入口，夹在 EnumMembers 与 DeleteMemberByNum 之间；本地 `PSBValueDispatch` 同样显式提供独立的 `DeleteMember override`，其函数体是一条常量 return，而不是借用有 member-sensitive 行为的基类默认实现。 |
| 数据流 | `MATCH` | Android 不读取五个输入、不访问内存，只把 32-bit `0xFFFFFC16` 写入返回寄存器；本地所有参数未命名且未使用，只返回位模式相同的 `TJS_E_NOTIMPL`。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 primary vtable data slot，没有 code caller/callee；本地入口同样作为 `iTJSDispatch2` override 由虚分派进入且不转调任何 helper。canonical parent 只是构造/槽归属，不是 direct caller。 |
| 对象生命周期 | `N/A` | 本入口不读取 `self`，不构造、销毁、retain/release，也不修改 `valid_`、owner、node 或引用计数；所有生命周期行为均在其他独立入口。 |
| 内部容器实现 | `N/A` | 本入口不访问 packed PSB、TJS Array、STL 或任何自定义容器，仅返回标量错误码。 |
| 边界行为 | `MATCH` | Android 对任意 flag、null/non-null membername、hint、objthis 以及任意 self 位值都走同一函数体并返回 `-1002`；没有有效性检查、成员存在性分支、默认成员特例、输出写入或异常。只要虚分派已到达函数体，本地行为完全相同。 |

## 总判定

`ALIGNED`

fresh decompile、完整两指令 disasm、唯一 vtable data xref、相邻槽原始 qword 以及本地
override/常量定义逐行共同证明：Android 当前可证明的六维行为与本地实现一致。

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能恢复未使用参数的原始变量名或精确源码排版；vtable 槽与
ABI 足以证明参数位置和运行行为，本地也没有依赖这些不可恢复的拼写，因此这不是确定 GAP。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。本节点是 leaf，阶段 A 未 spawn 任何 agent。
- TASK_TREE 的非 canonical cross-reference 列表没有指向或发自 `@596F48` 的函数边。
- `@1A0B438` 是当前入口的 primary vtable data xref；`@597AD4` 是安装该 vptr 的
  canonical `[vtable-member]` parent，二者都不是对本入口的直接 code call。
- `@596F50` 与 `@596F40` 只是相邻 vtable sibling；它们各自有唯一 canonical agent，
  不是本节点的 child、caller 或 callee，不得从本 leaf 重复派发。

# `0x5975D0` — `PSBValueDispatch_PropSetByVS`

## 审计身份

- canonical 地址：`0x5975D0`
- canonical 名称：`PSBValueDispatch_PropSetByVS`
- IDA 当前标签：`PSBValueDispatch_PropSetByVS`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。该边表示构造出的 `PSBValueDispatch` 对象通过 primary
  vtable 暴露本入口，不表示 `@597AD4` 直接调用本函数。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_5975d0`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只作为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x5975D0")`；随后 fresh 调用
`disasm(addr="0x5975D0", include_total=true, max_instructions=32)`、
`xrefs_to(addrs="0x5975D0")`、`lookup_funcs(queries=[...])`，并读取 primary vtable
`@1A0B3D8` 的对应字节。本地址在开始审计时没有报告，本文件是本轮 fresh 结果。

- fresh decompile 恢复的完整函数体为无参形态
  `return 4294966294LL`。这是未恢复正确返回类型时对 W0 的无符号显示；完整 disasm 只有
  `MOV W0, #0xFFFFFC16; RET` 两条指令，故按 32 位 `tjs_error` 解释为 `-1002`。
- `cpp/core/tjs2/tjsErrorDefs.h:24` 把 `-1002` 定义为 `TJS_E_NOTIMPL`；此本地常量只用于
  数值对照，Android 的权威行为直接来自上述 `MOV W0`。
- fresh `xrefs_to` 只返回一条 data xref `@1A0B428`，没有 code xref。fresh vtable bytes
  证明 `@1A0B428` 存放小端函数指针 `0x5975D0`；primary vtable 从 `@1A0B3D8` 开始，
  `0x1A0B428 - 0x1A0B3D8 = 0x50 = 10 * 8`，所以本入口是 0-based slot 10。
- 同一份 vtable bytes 在它之前依次包含 slot 8 `@5975E0` 与 slot 9 `@5975D8`，之后是
  slot 11 `@596F50`，与 `iTJSDispatch2::PropSetByVS` 的接口顺序一致。因此它归属于
  `PSBValueDispatch` primary `iTJSDispatch2` vtable，不是 NCB wrapper、NCB callback、
  secondary-base thunk 或直接调用 helper。
- fresh `lookup_funcs` 给出函数范围 `0x5975D0..0x5975D8`、大小 `0x8`。函数体没有
  `BL/BLR`、内存读写、条件跳转或异常路径；运行调用链只有外部调用者经该 vtable 槽进行
  虚分派，进入后立即返回固定错误码。

## Android 伪代码（3 行）

```text
PropSetByVS(flag, membername, param, objthis):
    ignore flag, membername, param, objthis and object state
    return tjs_error(-1002)                    // TJS_E_NOTIMPL；无条件、无其他失败路径
```

该伪代码覆盖本入口全部条件、默认值与失败边界：函数没有条件分支或默认输出参数，唯一结果
始终是 `-1002`；一旦控制流已到达入口，所有实参与对象内部状态均不被读取。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 直接继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`，对应 Android 本入口所在的 primary dispatch
  vtable 归属。
- `cpp/plugins/psbfile/PSBDispatch.h:55-56`：声明完整 override
  `PSBValueDispatch::PropSetByVS(tjs_uint32, tTJSVariantString *, const tTJSVariant *,
  iTJSDispatch2 *)`，对应 primary vtable slot 10 的接口形状。
- `cpp/core/tjs2/tjsInterface.h:181-194`：接口顺序为 slot 9 `GetCountByNum` 后紧接
  `PropSetByVS`，随后是 slot 11 `EnumMembers`；这与 Android `@1A0B420/@1A0B428/
  @1A0B430` 三个函数指针的排列一致。
- `cpp/plugins/psbfile/main.cpp:332-335`：本地函数忽略四个具名省略的实参并直接
  `return TJS_E_NOTIMPL;`，逐条对应 Android 的“无实参读取、无对象读取、固定返回、立即
  `RET`”。
- `cpp/core/tjs2/tjsErrorDefs.h:23-29`：本地 `TJS_E_NOTIMPL` 精确定义为 `-1002`，其
  32 位位型是 Android 写入 W0 的 `0xFFFFFC16`。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:55-56` 的 override；第 2 行对应
`main.cpp:332-333` 中未命名且从未消费的全部参数；第 3 行对应 `main.cpp:334` 与
`tjsErrorDefs.h:24`。本地没有添加有效性检查、membername 分支、参数转换、输出写入或
fallback。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 primary `iTJSDispatch2` vtable slot 10 的独立 8-byte 常量返回入口；本地以 `PSBValueDispatch` 的独立 `PropSetByVS` override 声明并定义，接口前后槽顺序也与 fresh vtable bytes 一致。优化后未使用参数不出现在反编译签名中，不构成结构偏差。 |
| 数据流 | `MATCH` | Android 只把 `0xFFFFFC16` 写入 W0 后返回；本地只求值并返回数值相同的 `TJS_E_NOTIMPL`。双方均不读取 flag/membername/param/objthis/this，不写输出或对象状态。 |
| 调用链 | `MATCH` | Android 仅有 vtable data xref、没有 code xref，本体没有任何 callee；本地同样作为 virtual override 被分派且函数体不调用 helper。canonical parent 只表示 `[vtable-member]` 归属，未伪装成 direct-call。 |
| 对象生命周期 | `N/A` | 本入口不读取或修改引用计数，不 retain/release，不构造、析构、分配或释放任何对象，也不建立临时 Variant/String 生命周期。 |
| 内部容器实现 | `N/A` | 本入口不访问 packed PSB、STL、TJS 或自定义容器；它甚至不解引用输入参数。 |
| 边界行为 | `MATCH` | Android 与本地均无条件返回 `TJS_E_NOTIMPL`：不因 flag、空 membername、空 param、空 objthis 或 dispatch 内部 valid/owner/node 状态改变结果；函数体自身无解引用、异常、fallback 或额外错误码。这里仅描述已进入入口后的行为，外部虚分派前的对象取址不属于本函数。 |

## 总判定

`ALIGNED`

Android 可见的 vtable 归属、独立入口结构、固定数值数据流、零 callee、零生命周期/容器
副作用及所有可见边界都与本地逐项一致；没有确定 GAP，也没有影响本函数结论的 stripped/O3
证据缺口。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 阶段 A 未 spawn；本节点没有 canonical 直接子节点。
- canonical parent `@597AD4` 仅通过构造对象时安装 primary vptr，使本入口成为
  `[vtable-member]`；本入口 fresh xref 中没有来自该构造器的 code call。
- Android data xref `@1A0B428` 是本入口在 primary vtable 中的唯一引用，不对应另一只
  function agent。
- TASK_TREE 的非 canonical Cross-reference 清单没有涉及 `@5975D0` 的额外 DAG 边；
  因此没有 cross-reference function agent，也不会重复派发。

# `0x597A20` — `PSBValueDispatch_FuncCall`

## 审计身份

- canonical 地址：`0x597A20`
- canonical 名称：`PSBValueDispatch_FuncCall`
- IDA 当前标签：`PSBValueDispatch_FuncCall`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`；这是构造对象所安装 primary vtable 的成员归属，不是
  `0x597AD4` 对本函数的直接调用。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_597a20`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本 function subagent fresh 调用 IDA MCP
`decompile(addr="0x597A20")`，并补充调用
`disasm(addr="0x597A20", include_total=true)`、
`xrefs_to(addrs="0x597A20")`、`lookup_funcs` 与 primary-vtable 原始字节读取。

- fresh decompile 得到 `return 4294966294LL`；IDA 尚未恢复接口签名，故把 W0 中的
  `0xFFFFFC16` 显示成无符号扩展的 64-bit 常量。按 Android 接口的 32-bit
  `tjs_error` 解释，该值严格等于 `-1002`。
- fresh 完整 disasm 覆盖整个 `0x597A20..0x597A27`，共 2 条指令：
  `MOV W0, #0xFFFFFC16`；`RET`。函数无栈帧、无分支、无内存访问、无 `BL/BLR`。
- fresh `xrefs_to` 只返回一个 data xref：`0x1A0B3E8`，无静态 code xref。
  primary vtable 从 `0x1A0B3D8` 起的前五个 64-bit 项依次为
  `0x597AC0, 0x597A40, 0x597A20, 0x597A18, 0x597854`，因此本入口是
  `PSBValueDispatch` primary `iTJSDispatch2` vtable 的 slot 02 `FuncCall`；运行时调用是
  虚分派，不能把“无 code xref”误写成“从未调用”。
- 本入口不是 NCB wrapper 或注册 callback；它是 NCB 返回给 TJS 的 dispatch 对象上的
  `iTJSDispatch2::FuncCall` override。Android 本体不调用任何 psbfile helper。

## Android 伪代码（2 行）

```text
FuncCall(flag, membername, hint, result, numparams, params, objthis):
    return tjs_error(0xFFFFFC16)  // -1002；无条件，全部参数均不读取
```

该伪代码覆盖本入口的全部条件（没有条件分支）、默认结果与失败边界：任何参数组合，包括
所有指针为 null，均不解引用输入并直接返回 `TJS_E_NOTIMPL`。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:116-130`：`iTJSDispatch2` 的前两只虚函数是
  `AddRef/Release`，随后是完整七参数 `FuncCall`；这与 Android vtable 的
  `0x597AC0, 0x597A40, 0x597A20` slot 顺序一致。
- `cpp/plugins/psbfile/PSBDispatch.h:16-32`：`PSBValueDispatch final` 直接继承
  `iTJSDispatch2`，并以相同完整签名显式声明 `FuncCall(... ) override`；没有用 NCB wrapper
  或基类转发替代该 emitted vtable member。
- `cpp/plugins/psbfile/main.cpp:108-112`：本地
  `PSBValueDispatch::FuncCall` 的七个形参均未命名、函数体只执行
  `return TJS_E_NOTIMPL;`，逐指令语义对应 Android 的“写 W0 后立即 RET”。
- `cpp/core/tjs2/tjsErrorDefs.h:21-24`：本地 `TJS_E_NOTIMPL` 定义为 `-1002`，32-bit
  补码即 `0xFFFFFC16`，与 Android 立即数完全相同。

逐行对应：伪代码第 1 行由 Android vtable slot 与本地接口/override 签名共同定位；第 2 行
对应 `main.cpp:111` 和 Android `0x597A20..0x597A24`。双方均没有隐藏的参数读取、对象状态
检查、结果写入、callee 或清理路径。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一只独立的 8-byte emitted 入口，并由 primary vtable slot 02 直接指向；本地同样以 `PSBValueDispatch` 的显式 `FuncCall` override 提供独立函数体。`MATCH` 只覆盖二进制可证的结构，不声称恢复不可观察的参数名或源码排版。 |
| 数据流 | `MATCH` | Android 只把常量 `0xFFFFFC16` 写入返回寄存器；本地只返回数值相同的 `TJS_E_NOTIMPL`。双方均不读参数、不写 `result`、不访问 `this`。 |
| 调用链 | `MATCH` | Android 本体无 direct/indirect callee，并通过 primary `iTJSDispatch2` vtable slot 02 被虚分派；本地同样由接口 override 提供该槽，无基类转发或 helper 调用。 |
| 对象生命周期 | `N/A` | 本入口不读写引用计数、不 retain/release、不构造或销毁对象，也不接管任何参数所有权；相关生命周期由 sibling vtable 入口单独审计。 |
| 内部容器实现 | `N/A` | 本入口不创建、访问或遍历 packed、TJS、STL 或其他容器。 |
| 边界行为 | `MATCH` | Android 与本地对 flag、名称、hint、result、参数数量/数组及 objthis 的所有取值均无条件返回 `-1002`；null/悬空指针也不会在本入口被解引用，且没有异常、fallback 或副作用。 |

## 总判定

`ALIGNED`

Android 当前可证明的 emitted 结构、常量数据流、vtable 调用归属和全部边界与本地一一
一致；本入口没有确定 GAP，也不存在影响总判定的证据缺口。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。阶段 A 未 spawn 任何 subagent。
- canonical parent cross-reference：`@597AD4` 安装包含本槽的 primary vptr；该关系严格为
  `[vtable-member]`，不是 direct-call。
- fresh IDA 唯一 xref 是 vtable 数据项 `@1A0B3E8`；没有其他属于 114-node manifest 的
  非 canonical 关系，因此不派发重复 agent。

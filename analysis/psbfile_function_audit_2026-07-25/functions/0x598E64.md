# `0x598E64` — `PSBRawNode_GetDictionaryKeys_guess`

## 审计身份

- canonical 地址：`0x598E64`
- canonical 名称：`PSBRawNode_GetDictionaryKeys_guess`
- Android / manifest 名称：`GetDictionaryKeys_guess`；IDA 当前标签为
  `PSBRawNode_GetDictionaryKeys_guess`。Android stripped 二进制没有保留精确 C++ 标识符，
  因此名称继续保留 `_guess`。
- 分组：D（raw PSBFile / PSBRawOwner / PSBRawNode）
- TASK_TREE parent：`0x598A3C PSBFile_GetRoot_guess`
- parent 关系：`[classification-only]`。这是 raw-node API 的 canonical 分类归组，绝不
  表示 Android `0x598A3C` 调用本函数。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b14c/f_5980f4/f_598268/f_598a3c/f_598e64`
- 本轮日期：2026-07-25；后续 source-factorization 复核：2026-08-02
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `"PSBFile.dll"` 仅视为 NCB 模块名字面量，本函数不读取
  它。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 agent path fresh 调用 IDA MCP
`decompile(addr="0x598E64")`；随后 fresh 调用
`lookup_funcs(queries=["0x598E64","0x599174","0x59B7E8","0x597B1C"])`、
`disasm(addr="0x598E64", include_total=true, max_instructions=5000)`、
`xrefs_to(addrs=["0x598E64","0x597B1C","0x599174","0x59B7E8"])`、
`find(type="code_ref", targets=["0x598E64","0x597B1C","0x40CD20","0x423250"])` 与
`find(type="data_ref", targets="0x598E64")`。另只为调用点上下文读取
`sub_695DE8` 在 `0x69616C`、`0x696804` 周围的 disasm，以及两个 STL PLT stub；不替
这些其他函数下审计结论。本文件此前不存在，不沿用旧报告。

2026-08-02 主线又 fresh 调用 `decompile(0x598E64)`、完整 193 指令
`disasm(0x598E64, include_total=true)` 和 `xrefs_to(0x598E64)`。同时逐指令复核同谱系
iOS arm64 的 retained calls；Android 仍是唯一行为权威，iOS 只用于在 Android
完整优化残留允许的多个源码 factorization 中选择调用与对象分层。

- `lookup_funcs` 确认目标是独立 emitted 入口 `0x598E64..0x599173`，大小 `0x310`；完整
  disasm 共 193 条指令（含异常清理 landing pads）。Hex-Rays 恢复签名
  `void PSBRawNode_GetDictionaryKeys_guess(const PSBRawNode *self@X0,
  std_vector_string_arm64 *result@X8)`；`X8` 是按值返回
  `std::vector<std::string>` 的 AArch64 间接返回槽。
- `0x598E98..0x598E9C` 在读取 node 之前把结果 vector 的 begin/end/capacityEnd 三槽全部
  置空。`0x598EA0..0x598EC4` 随即无保护读取 `self->node[0]` 并进入完整 classifier
  category-7 gate 的专化 tag residual。
  tags `1..12, 21..32, 35..41, 44..49, 51..53, 55..57, 59..61, 63, 65` 全部直接返回
  已构造空 vector；只有 Dictionary tag `0x21` 进入枚举。其余 tag 调用
  `sub_95440C(L"psb: internal error: unknown internal type detected.\n")`，若异常 helper
  反常返回，则仍从公共返回块返回空 vector。
- 同谱系 iOS arm64 `GetDictionaryKeys@0x1000EDB8C` 在 `0x1000EDBBC` 保留共享
  classifier 调用，并在 `0x1000EDBDC/0x1000EDBF4` 顺序调用 packed-view constructor
  `0x1000EE0C4`。后续只
  读取第一只 keys record 的 count/width/data；第二只 offsets record 构造后完全不消费。
- 因此 Android `0x598F0C..0x598F74` 是第一只 keys view 的 SROA 标量，第二只 view 被
  DCE；它没有反证源码中的共享 classifier 和第二只 dead view。target 本体继续独立约束
  全部 tag、默认值、packed 算术、循环和异常清理。
- Dictionary 分支先在 `0x598EF8..0x598F04` 构造一只 reusable empty
  `std::string`。随后令 `code=node+1`、`l=code[0]-0x0B`；count tag 只在
  `0x0D/0x0E/0x0F/0x10` 时分别读取 8/16/24/32 bit，其他值默认 count=0，但仍会按
  `code[l]` 读取 value tag。`width=code[l]-0x0C`，values base 为 `code+l+1`。
- `0x598F80` 经 PLT `0x40CD20/0x40CD2C` 调用
  `std::vector<std::string>::reserve@0x599174`，容量请求恰为 count；count 为 0 时
  `0x598F84` 直接跳过整个 decode/append 循环。
- 非零 count 循环在 `0x598FA0..0x59900C` 保持 32-bit stride 累加。只有
  `width in 1..5` 才从 `values + u32(index*width)` 读取 u32 并应用 AArch64 W-register
  shift-modulo mask；非法 width 不形成该 value 地址，name index 默认为 0。width=5 特意
  被接受，并因 shift modulo 32 只保留低 8 bit。
- 每轮 `0x598FCC` 都把同一 reusable string、`self->owner` 和 name index 交给
  `PSB_DecodeName_guess@0x597B1C`。若 vector 尚有容量，`0x598FDC..0x598FEC` 对该
  lvalue string 执行 copy-shaped 构造并推进 end；容量满时，`0x598FFC` 经 PLT
  `0x423250/0x42325C` 进入
  `std::vector<std::string>::_M_emplace_back_aux<std::string &>@0x59B7E8`。这不是
  move-emplace，也不是每轮创建新 string。
- `0x599010..0x599068` 在正常出口释放 reusable Android COW string；
  `0x599070..0x599170` 的异常路径先释放该 string（若已构造），再逐个析构已进入结果
  vector 的 strings、释放 vector buffer 并 `_Unwind_Resume`。reserve、decode、fast copy
  或 slow growth 抛出时均走与构造阶段相符的清理层。
- `xrefs_to` 与独立 `find(code_ref)` 均确认本入口只有两个 code caller：
  `sub_695DE8+0x384 @ 0x69616C` 和 `sub_695DE8+0xA1C @ 0x696804`。前者枚举外层 source
  dictionary，后者枚举其 `icon` 子 dictionary。`find(data_ref)` 返回空，因此本函数不是
  vtable member，也不由 NCB 注册；它是 raw `PSBRawNode` 的普通直接调用 API。
- canonical parent `@598A3C` 没有指向本入口的 xref。目标对
  `PSB_DecodeName_guess@0x597B1C` 的 `0x598FCC` direct call 是 non-canonical DAG 边；
  后者的 canonical parent 固定为 `@596F50`，不得重复派发。

## Android 关键伪代码（8 行）

```text
GetDictionaryKeys(self, out@X8): out=empty vector
    if GetTypeCategory(*self.node) != 7: return out // unknown throw；helper-return 也为空
    key=""; packed=self.node+1
    keys=PsbArray(packed)
    offsets=PsbArray(packed+keys.nBytes)             // 构造后不消费
    out.reserve(keys.count)
    for unsigned i<keys.count: DecodeName(key,self.owner,keys[i]); out.emplace_back(key)
    return out; exception: destroy key if live, then result strings/buffer, and unwind
```

伪代码覆盖结果默认值、共享 category gate、unknown-type helper 的反常返回、两只 view、
packed count 默认 0、非法 width 的 index=0、count=0、W32 stride、reusable string、
vector 满/未满的同一源码 append 语义及异常清理边界。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBRawFile.h:73-165`，类 `PSBFile/PSBRawNode`：本地保持
  PSBFile-compatible holder 首子对象加独立 `node_` 的两指针 ABI raw handle；`:146` 的
  `GetType()` 与 Android 一样无保护读取 `node_[0]`，`:152` 声明目标为
  `std::vector<std::string> GetDictionaryKeys() const`。
- `cpp/plugins/psbfile/PSBRawFile.cpp:251-278` 是目标完整实现：`:253` 先构造 empty
  result；`:254-259` 通过共享 `GetTypeCategory_guess` 只接受 category 7，unknown helper
  若反常返回也落入 empty-result return。
- `PSBRawFile.cpp:263-270` 对应 Android `0x598EF8..0x598F74` 和 iOS arm64 retained calls：
  reusable `std::string key` 只在 Dictionary gate 后构造，随后顺序建立 `keys(packed)` 与
  `offsets(packed+keys.nBytes)`；第二只 offsets view 故意不消费。
- `cpp/plugins/psbfile/PSBPackedInternal.h:187-202` 的
  `ReadPackedCount_guess` 与 Android count tag/8、16、24、32-bit/default-0 完全一致；
  `:218-257` 的 `PsbArray_guess` 依次形成 `l/count/width/values base`，包括 count tag 非法时
  仍访问 `code[l]` 的损坏输入边界。
- `PSBPackedInternal.h:243-256` 对应 Android `0x598FA0..0x598FC4`：只接受 width 1..5，
  非法 width 在形成 value 地址前返回 0；index 与 width 均为 u32，乘法保持 W32 回绕；
  mask 显式保留 width=5 的 AArch64 shift-modulo 行为。
- `PSBRawFile.cpp:271-277` 对应伪代码第 6-8 行：以 exact count 调 `reserve`，遍历同一
  `PsbArray_guess`，每轮把 key、owner、index 交给 `detail::DecodeName_guess`，再执行
  `result.emplace_back(key)` 的 lvalue copy。`:263` 的 key 在全循环复用；本地没有改为
  `std::move(key)` 或每轮新建 string。
- `PSBRawFile.cpp:83-106`，共享 callee `detail::DecodeName_guess`：接受调用者提供的
  `std::string &` 与借用 `PSBRawOwner *`，与本目标 `0x598FCC` 的三个实参角色一致。本报告
  只把它作为调用上下文，不替 `@597B1C` 下审计结论。
- `PSBPackedInternal.h:35-99` 的 classifier 保留 Android 完整 category 分区、相同未知类型
  宽字符串与 `-1` continuation；`PSBRawFile.cpp:261-263` 使该 continuation 返回 empty result。
- `cpp/plugins/motionplayer/PlayerResource.cpp:399-400,406`，本地 caller
  `buildKrkrAtlasGroupLike_0x695DE8`：`:400` 枚举 `sourceRoot` keys，`:406` 枚举
  `iconRoot` keys，分别对应 Android `sub_695DE8@0x69616C` 和 `@0x696804`；调用次数、
  外/内层 dictionary 层级与 encounter-order 遍历一致。

逐行对应：伪代码第 1-2 行对应 `PSBRawFile.cpp:257-263` 与
`PSBPackedInternal.h:35-99`；第 3-5 行对应 `PSBRawFile.cpp:264-274` 与
`PSBPackedInternal.h:187-257`；第 6-7 行对应 `PSBRawFile.cpp:275-280`；第 8 行由
`PSBRawFile.cpp:281-282` 的局部对象逆序析构/异常 RAII 自然产生。Android 的 COW string
机器路径与 Web/macOS libc++ 的 string ABI 不同，但本地保持了可证的源码容器类型、单一
key 对象边界和 lvalue-copy token；标准库 ABI 布局差异不是本函数的源码结构 GAP。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是独立 const raw-node API，以隐藏返回槽产出 `std::vector<std::string>`；target 保存 classifier 的完整 category-7 专化 residual，iOS arm64 保留两只顺序 packed-view constructor 与共享 classifier call。本地恢复相同方法边界、gate、局部作用域、`keys + dead offsets` 对象拓扑和返回类型。 |
| 数据流 | `MATCH` | target 从 `node+1` 形成 keys 的 count/width/values，以 count reserve；iOS arm64 支持之后还构造 `packed+keys.nBytes` 的 offsets view 但不消费。每轮以 W32 stride 读取 name index，复用同一 key 经 `DecodeName(owner,index)` 覆盖后 copy-emplace；本地中间量、默认 0、mask、循环上界和输出顺序逐项一致。 |
| 调用链 | `MATCH` | Android 两个 direct caller 均在 `sub_695DE8`，分别位于 `0x69616C/0x696804`；本地 `buildKrkrAtlasGroupLike_0x695DE8` 的 `PlayerResource.cpp:400/406` 一一对应。本函数直接调用 `@597B1C`，并经 PLT 调 reserve 与满容量 emplace slow path；本地保持 `DecodeName + reserve + emplace_back(lvalue)`。无 data xref，故不伪装成 vtable/NCB member；parent 明确保留 classification-only。 |
| 对象生命周期 | `MATCH` | Android result 在 node 解引用前构造；reusable key 只在 Dictionary gate 后构造、贯穿整个循环并逐次复制进 vector。正常出口释放 key，异常出口按构造阶段先 key、再已构造 elements/buffer 清理；owner 仅被借用且本函数不 AddRef/Release。当前本地局部声明顺序与 RAII 产生相同源码生命周期。 |
| 内部容器实现 | `MATCH` | Android 明确使用 `std::vector<std::string>`，先 `reserve(count)`，容量未满走 string lvalue copy，满时进入 `_M_emplace_back_aux<std::string &>@0x59B7E8`；iOS arm64 支持 `keys + dead offsets` 两只四字段 packed view。本地正是该对象拓扑、`std::vector<std::string>`、一次 reserve 与 `emplace_back(key)`；标准库 ABI 差异不改变源码容器选型/copy token。 |
| 边界行为 | `MATCH` | Android 与本地都对 null self/node 无防护；已知非 Dictionary tag 返回空，unknown tag 抛精确消息且 helper 反常返回空；非法 count tag 默认 count=0 但仍按 `code[l]` 读 width；非法 width 返回 name index 0 且不读 value，width=5 被接受，stride W32 回绕；count=0 不触碰 owner/decode。没有添加安全化校验。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整 193 指令 disasm、iOS arm64 retained-call/constructor、
两个真实 target caller、两只 STL PLT 路径以及当前本地源码可证明范围内，`0x598E64` 的共享
classifier、两只 packed view 和其余六维行为均与本地对齐。

## 确定 GAP

已闭合：旧报告把 target 中无 `BL @599554` 误作“源码拥有 raw-tag switch”，并只保留一只
keys view。Android 完整 specialized residual 加 iOS arm64 的 classifier call 与两次
constructor 正证据推翻了该 negative-xref 推断；本地已恢复共享调用和第二只未消费 offsets
view。当前确定 GAP：无。

证据边界：stripped/O3 二进制不能唯一恢复本函数的精确原始标识符、
`PsbArray_guess` 的 type/field/`operator[]`、unaligned read 源码拼写或 compiler 的
exact inline 决策；四字段 record/constructor factorization得到 iOS arm64 支持。
这些不可恢复 token 已通过 `_guess` 和注释保持显式，不构成已证实的六维实现差异。

## TASK_TREE 直接子节点与 cross-reference

- 尚待由本节点在阶段 B 派发的 canonical 直接 children（本阶段未 spawn）：
  - `0x599174 std_vector_string_reserve [stl-instantiation]`
  - `0x59B7E8 std_vector_string_emplace_back_aux [stl-instantiation]`
- child `@599174` 由目标 `0x598F80` 经 reserve PLT `0x40CD20/0x40CD2C` 到达；child
  `@59B7E8` 仅在 `finish==end_of_storage` 时由 `0x598FFC` 经 PLT
  `0x423250/0x42325C` 到达。阶段 A 未派发二者，也未替二者审计其内部实现。
- non-canonical direct-call cross-reference：目标在 `0x598FCC` 直接调用
  `0x597B1C PSB_DecodeName_guess`；后者 canonical parent 固定为
  `0x596F50 PSBValueDispatch_EnumMembers`，不得为此重复创建 agent。
- 树外 direct callers：`sub_695DE8@0x69616C`、`@0x696804`；该函数不在本轮 114-address
  manifest 中，不创建额外 function agent。
- canonical parent `0x598A3C PSBFile_GetRoot_guess [classification-only]` 不调用本函数；
  fresh xref 集没有来自 `@598A3C` 的边，不得把 raw-node sibling 分类关系改写成调用。

## 2026-08-03 LSDA 精确调用点拓扑复扫

`LSDA-CALLSITE-TOPOLOGY`

权威 Android ARM64 FDE `0x598E64..0x599174` 指向 LSDA `0x186DEC4`。原始表恰有
4 项：`[0x598F3C,0x598F48)->0x599070`、
`[0x598F78,0x598F84)->0x599078`、`[0x598FC8,0x599000)->0x59907C`
均为 cleanup-only，最后 `[0x599000,0x599174)` 无 landing；不存在 catch-all 项。三层
landing 分别覆盖 unknown-type 诊断、reserve、名称解码/字符串复制/向量增长阶段，并按当时
已构造对象层数析构后 `_Unwind_Resume`。

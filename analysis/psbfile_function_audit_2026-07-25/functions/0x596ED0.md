# `0x596ED0` — `PSBValueDispatch_Reserved1`

## 节点身份

- 审计日期：`2026-07-25`
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596ed0`
- 分组：`A`（PSBValueDispatch / packed dispatch ABI）
- canonical 地址：`0x596ED0`
- canonical 名称：`PSBValueDispatch_Reserved1`
- TASK_TREE parent：`0x597AD4` `PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`
- TASK_TREE 直接子节点：无
- 名称证据边界：Android 二进制已 stripped，未保留本函数的原始 C++ 符号拼写；本报告沿用 TASK_TREE canonical 名称。归属依据是 primary `iTJSDispatch2` vtable 的 slot 20、相邻槽布局及该表在 parent 构造函数中的安装，本地标识符仅用于复原对照。

## Android 归属与本轮 fresh 取证

本 function-agent 于 `2026-07-25` 对 Android kirikiroid2 `libkrkr2.so` 内嵌实现执行了本轮 fresh IDA MCP 取证：

1. `decompile(0x596ED0)`：函数体仅为 `return 4294966294LL;`。其低 32 位是 `0xFFFFFC16`，按 `tjs_error` 有符号语义解释为 `-1002`。
2. `disasm(0x596ED0)`：完整函数只有 `MOV W0, #0xFFFFFC16`（`0x596ED0`）和 `RET`（`0x596ED4`）两条指令；没有参数读取、分支、调用、内存访问或输出写回。
3. `xrefs_to(0x596ED0)`：唯一引用是 data xref `0x1A0B478`，没有 direct-call xref。`0x1A0B478 - 0x1A0B3D8 = 0xA0 = 20 * 8`，精确落在 primary vtable address point 的 slot 20。
4. `get_bytes(0x1A0B468, 40)`：五个连续 64 位表项依次为 `0x596EE0`、`0x596ED8`、`0x596ED0`、`0x596E24`、`0x596E1C`，即 slot 18 `CreateNew`、slot 19 `CreateNewByNum`、本节点 slot 20、slot 21 `IsInstanceOf`、slot 22 `IsInstanceOfByNum`。
5. contextual `decompile(0x597AD4)`：parent 在 `0x597AE4..0x597AF0` 将 `off_1A0B3D8` 写入对象的 primary dispatch vptr；parent 仅安装该表，并不直接调用本函数。

因此，本节点是 `PSBValueDispatch` primary `iTJSDispatch2` vtable 的 `Reserved1` 槽入口，不属于 NCB wrapper、注册 callback 或 helper 调用链；运行时只会经虚槽间接进入。

## Android 伪代码（不超过 10 行）

```cpp
tjs_error PSBValueDispatch::Reserved1() {
    return TJS_E_NOTIMPL; // -1002；无条件、无其他成功或失败分支
}
```

## 本地实现与逐行对照

本地精确位置：

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 直接实现 `iTJSDispatch2`，形成本节点所属的 primary dispatch vtable。
- `cpp/plugins/psbfile/PSBDispatch.h:91-101`：`CreateNew`、`CreateNewByNum`、`Reserved1`、`IsInstanceOfByNum` 的覆写声明顺序；本节点的独立声明在 `:98`。
- `cpp/plugins/psbfile/main.cpp:525`：`PSBValueDispatch::Reserved1()` 的独立定义，函数体仅 `return TJS_E_NOTIMPL;`。
- `cpp/core/tjs2/tjsInterface.h:259-268`：`iTJSDispatch2::CreateNewByNum` 后紧接纯虚 `Reserved1()`，确认接口槽顺序与无显式参数签名。
- `cpp/core/tjs2/tjsTypes.h:39,57`：`tjs_error` 是 32 位有符号 `tjs_int32`。
- `cpp/core/tjs2/tjsErrorDefs.h:24`：`TJS_E_NOTIMPL` 定义为 `-1002`。

逐项映射：

| Android 证据 | 本地源码 | 对照 |
| --- | --- | --- |
| `0x1A0B478 -> 0x596ED0`，位于 `0x1A0B3D8 + 20 * 8` | `PSBDispatch.h:91-101` 在 `CreateNewByNum` 后显式覆写 `Reserved1` | primary 接口虚槽结构与顺序一致 |
| `0x596ED0: MOV W0,#0xFFFFFC16` | `main.cpp:525` 返回 `TJS_E_NOTIMPL`；`tjsErrorDefs.h:24` 为 `-1002` | 32 位返回位型完全一致 |
| 不读取隐式 `this`，不访问任何内存 | 本地方法体不访问对象或字段 | 数据流及副作用一致 |
| `0x596ED4: RET`，无分支或 callee | 本地为独立单返回方法 | 控制流与失败边界一致 |
| `0x597AD4` 只安装 `off_1A0B3D8` | 本地通过 `iTJSDispatch2::Reserved1` override 进入虚分派 | canonical parent 是 vtable 归属，不是 direct-call |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一个 primary `iTJSDispatch2` vtable slot 20 的独立 8-byte emitted 入口；本地同样以 `PSBValueDispatch::Reserved1` 的独立 override 声明与定义表达该结构。stripped 二进制不能证明原始标识符拼写，但可证明的发射结构一致。 |
| 数据流 | `MATCH` | Android 仅向 `W0` 写入 `0xFFFFFC16`，不读取 `this` 或对象字段；本地只返回等值常量，不读写任何状态。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 primary vtable slot 20，parent 只负责安装该表；本地经 `iTJSDispatch2::Reserved1` override 形成相同虚分派路径，没有额外 helper、fallback 或 direct callee。TASK_TREE 的 `[vtable-member]` 边准确。 |
| 对象生命周期 | `N/A` | 本函数不分配、构造、析构、AddRef/Release 或转移对象；vptr 安装属于 parent 构造函数，不属于本函数体。 |
| 内部容器实现 | `N/A` | 完整两指令入口不访问 packed node、对象字段、容器或迭代器。 |
| 边界行为 | `MATCH` | 无论隐式对象地址为何，本函数均不解引用它，且无条件返回 `TJS_E_NOTIMPL (-1002)`；没有成功分支、参数相关分支、写回或其他失败码。本地行为一致。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

## Cross-reference

- `@597AD4` 安装包含本函数的 primary vtable，是 canonical `[vtable-member]` parent，不是直接 caller。
- `0x1A0B478` 是本函数唯一 Android xref，属于上述 vtable 关系；TASK_TREE 没有为本节点列出其他非 canonical cross-reference。
- `@596ED8` 与 `@596E24` 只是相邻 vtable sibling，不是本节点的 caller、callee 或 child；不得据此重复派发 agent。
- 直接子节点：无。

# `0x5998C4` — `PSBMedia_CheckExistentStorage_guess`

## 节点身份与审计边界

| 项目 | 值 |
| --- | --- |
| canonical 地址 | `0x5998C4` |
| canonical 名称 | `PSBMedia_CheckExistentStorage_guess`（精确原始 C++ 名未保留） |
| MANIFEST 分组 | E |
| TASK_TREE parent | `0x59849C` `PSBFile_preRegister_guess_initPsbFile` |
| canonical 边 | `[vtable-member]`（不是直接调用） |
| 审计 agent path | `/root/f_42cf28/f_59849c/f_5998c4` |
| 本轮日期 | `2026-07-25` |
| 本轮范围 | 仅审计 `0x5998C4`；阶段 A 未派发其他函数，未修改 `cpp/` |

唯一权威来源是 Android kirikiroid2 `libkrkr2.so` 的内嵌实现。本报告未查看、
引用或从任何外部 `psbfile.dll` 推导；二进制中的 `"PSBFile.dll"` 仅是模块名字面量。

## 本轮 fresh IDA 证据

- 本轮新会话 fresh 调用
  `mcp__ida_mcp_pro__decompile(addr="0x5998C4")`，成功取得区间
  `[0x5998C4, 0x59993C)` 的完整函数体；本地址此前没有可直接沿用的本轮报告。
- 补充 `disasm(addr="0x5998C4", include_total=true)`，共 30 条指令：
  `0x5998EC` 直接 `BL 0x599E04`，`0x5998F0` 在返回位为 0 时跳到
  `0x599910` 写入 `false`；只有成功路径才在 `0x599900` 直接
  `BL 0x59A0B4`，随后 `0x599904..0x599908` 将返回指针非空转换为 `bool`。
- `0x5998F4` 把栈上 4 字节 `size` 的地址作为第三参数传给 `0x59A0B4`；
  该局部在调用前没有初始化，且 `0x5998C4` 自身从不读取其值。
- `xrefs_to(0x5998C4)` 唯一返回数据引用 `0x1A0B548`。读取
  `0x1A0B510` 起的 qword 表得到 11 个连续成员地址；第 7 槽
  `0x1A0B510 + 7*8 = 0x1A0B548` 正是 `0x5998C4`。
- 对 canonical parent 仅补充反汇编上下文：`0x5984F8` 把
  `0x1A0B510` 写成新建 `PSBMedia` 的 vptr。因此 parent 与本节点是
  `[vtable-member]` 归属，不存在 `0x59849C` 对 `0x5998C4` 的直接 `BL`。
- `xrefs_to(0x599E04, 0x59A0B4)` 分别确认本节点的直接调用点
  `0x5998EC`、`0x599900`，并确认它们还有其他 caller；这些 caller 只记作
  cross-reference，不替它们做函数审计。
- `0x599914..0x599938` 是编译器 stack-canary 校验；失配时调用
  `__stack_chk_fail`。这是 emitted 边界证据，不代表源码显式写了该调用。

## Android vtable / NCB / 调用链归属

`0x5998C4` 是 Android `PSBMedia` vtable `0x1A0B510` 的 slot 7，属于
`iTVPStorageMedia::CheckExistentStorage` 形状。它不是 NCB typed wrapper，
也没有 PSB-specific 直接代码 caller；运行时由通用 storage-media 接口虚分派进入。
canonical parent `0x59849C` 负责构造并注册采用该 vtable 的进程期 media 对象，
因此 TASK_TREE 使用 `[vtable-member]`，不得把父子关系伪装成直接调用。

本节点体内保留的调用链只有：

```text
PSBMedia vslot 7 @0x5998C4
├─ [direct, always first] @0x599E04 EnsureContainer_guess
└─ [direct, only when first returned true] @0x59A0B4 GetResourceData_guess
```

两个 callee 的内部实现与后代关系分别由它们自己的 function agent 审计；本报告只
判定 `0x5998C4` 对它们的调用顺序、参数流和返回值折叠。

## Android 关键伪代码（6 行）

```text
CheckExistentStorage(self, name):
  uint32 size                                      // 未初始化，仅作为 out 地址
  if !EnsureContainer(self, name): return false    // 不调用 GetResourceData
  data = GetResourceData(self, name, &size)
  if data == null: return false; else return true  // size 不参与结果
  if stack_canary_changed: __stack_chk_fail()      // 编译器 hardening 边
```

没有其他默认值、fallback 或异常吞并路径；任一 helper 抛出的异常都直接向外传播。

## 本地实现与逐行对照

精确位置：

- `cpp/plugins/psbfile/PSBMedia.cpp:126-130` —
  `PSBMedia::CheckExistentStorage(const ttstr &name)` 完整实现；
- `cpp/plugins/psbfile/PSBMedia.h:10,31,39-43` — `PSBMedia` 的继承、
  override 声明以及两个 private helper 声明；
- `cpp/core/base/StorageIntf.h:114-147` — `iTVPStorageMedia` 接口次序，
  `CheckExistentStorage` 位于析构双槽、AddRef、Release、GetName、两个 normalize
  成员之后，与 Android slot 7 形状一致。

| Android 伪代码步骤 | 本地精确复刻 |
| --- | --- |
| 行 1，接口虚方法 | `PSBMedia.h:31` 以同一 `bool` 返回值和 `const ttstr &` 参数 override；`PSBMedia.cpp:126` 定义该成员。 |
| 行 2，未初始化的 32 位 out 局部 | `PSBMedia.cpp:128` 声明 `std::uint32_t size;`，没有为了“安全”补零。 |
| 行 3，先调用 container helper并短路失败 | `PSBMedia.cpp:129` 的 `EnsureContainer(name) && ...` 保留同一求值顺序；左侧为 false 时 C++ 不求值右侧。 |
| 行 4，原样传入 name 与 size 地址 | 同一行调用 `GetResourceData(name, size)`；引用参数在 ABI 上对应 Android 传出的 `&size`。 |
| 行 5，只测试资源指针 | 同一行使用 `!= nullptr`，不读取 `size`，并由 `&&` 折叠成严格布尔结果。 |
| 行 6，stack canary | 这是 Android 编译器 hardening 产物，不是应显式抄入可移植 C++ 的源码 token；本地源码没有改变业务失败边界。 |

## 六维审计

| 维度 | 状态 | Android 证据与本地对照 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是一个 vslot override、一个未初始化 `uint32` 局部和单个短路 `&&` 返回表达式；本地 `PSBMedia.cpp:126-129` 保留相同源码结构，`PSBMedia.h:31` 保留虚方法边界。 |
| 数据流 | `MATCH` | Android 将同一 `name` 先交给 `0x599E04`，成功后再交给 `0x59A0B4`，并传入 `&size`；最终只消费第二个 helper 的指针是否非空。本地参数流、out 局部和未消费的 size 完全一致。 |
| 调用链 | `MATCH` | Android `0x5998EC` → `0x599E04`，成功后 `0x599900` → `0x59A0B4`；本地 `EnsureContainer(name) && GetResourceData(name, size)` 保留两个 helper 边、顺序和短路门控，没有额外 wrapper。 |
| 对象生命周期 | `N/A` | 本 emitted 入口不构造、retain、release 或销毁对象；返回的资源指针仅用于空值比较。`EnsureContainer` 可能改变 media cache，但其所有权行为属于 canonical child `0x599E04` 的独立审计。 |
| 内部容器实现 | `N/A` | 本函数不直接读取或修改任何容器，只调用两个 helper。cache、PSB tree 与资源表的容器选择属于两个 child 的函数体，不能由本节点替它们下结论。 |
| 边界行为 | `MATCH` | `EnsureContainer` 失败时严格短路为 false；成功后资源空指针为 false、非空为 true；size 未初始化但从不被本函数读取；helper 异常不被吞并。Android canary 失配进入 `__stack_chk_fail` 是编译器边界，不构成本地业务源码 GAP。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整反汇编、vtable 数据和直接调用 xref 可证明的范围内，
本地 `PSBMedia::CheckExistentStorage` 与 Android `0x5998C4` 的源码结构、数据流、
调用链和所有业务边界一致；本节点不直接承担对象生命周期或内部容器操作。

## 所有确定 GAP

无。

## Cross-reference 与阶段 B 子节点

- canonical parent：`0x59849C`，边为 `[vtable-member]`；parent 构造采用
  `0x1A0B510` 的对象，不直接调用本函数。
- `0x59993C` 也直接调用 `0x599E04` 与 `0x59A0B4`；按 TASK_TREE 它是
  `0x59849C` 的 sibling，不从本节点重复派发。
- `0x5999F4` 也直接调用 `0x599E04`；按 TASK_TREE 它同样是 sibling，
  不从本节点重复派发。
- 本节点尚待主 agent 在阶段 B 唤醒后派发的**直接 canonical children**为：
  `0x599E04`、`0x59A0B4`；边类型均为 `[direct-call]`。

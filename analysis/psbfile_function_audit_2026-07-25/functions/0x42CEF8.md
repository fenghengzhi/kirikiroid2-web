# `0x42CEF8` — `PSBFile_ncbClassInfo_static_init`

## 身份与归属

- canonical 地址：`0x42CEF8`
- canonical 名称：`PSBFile_ncbClassInfo_static_init`
- 分组：H / 静态初始化入口
- TASK_TREE parent：`ROOT`，关系为 `[classification-only]`；这是调度归组，不是 Android 调用边。
- 审计 agent path：`/root/f_42cef8`
- Android vtable / NCB / 调用链归属：本入口不是 vtable 槽、NCB member wrapper 或注册
  callback，而是 `ncbClassInfo<PSBFile>::_info` 的 compiler-emitted 动态初始化入口。
  `.init_array@0x19EA088` 是其唯一 xref；函数体不调用任何注册函数或 callback。
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看或引用外部
  `psbfile.dll`。

## 本轮 fresh Android 证据

2026-07-25 在本会话由 `/root/f_42cef8` fresh 执行了
`decompile(0x42CEF8)`；随后执行完整 `disasm(0x42CEF8)`（11 条指令）、
`xrefs_to(0x42CEF8)` 和 `get_bytes(0x19EA088, 16)` 复核：

- `0x42CEF8..0x42CF00`：从 guard `0x1AB5118` 读低字节，并以 `TBNZ bit 0`
  跳到 `0x42CF24` 返回；guard 已置位时不写任何 class-info 字段。
- `0x42CF04..0x42CF1C`：首次进入时按
  `initialized/name/id/classObject` 顺序向
  `0x1AB50F8/0x1AB5100/0x1AB5108/0x1AB5110` 写入
  `false/null/0/null`。
- `0x42CF20`：四个字段写完后才把 guard 写成 `1`；`0x42CF24` 无条件返回。
- 完整反汇编没有 `BL`，因此本入口没有 out-of-line callee，也没有隐藏在本函数内的注册
  callback 调用。
- fresh xref 只有数据项 `.init_array@0x19EA088`。该处 16 字节依次解码为
  `0x42CEF8`、`0x42CF28`，证明本入口先于相邻的 psbfile autoreg 静态初始化入口执行；
  这只是初始化顺序，不是两函数间的直接调用。

## Android 伪代码（8 行）

```text
guard = load8(0x1AB5118)
if (guard bit 0) != 0: return
classInfo.initialized = false
classInfo.name = null
classInfo.id = 0
classInfo.classObject = null
store_guard(0x1AB5118, 1)
return  // 无参数、返回值、callee、throw 或其他失败路径
```

## 本地精确对照

- class-info 类型与静态对象定义：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 73–114 行，符号
  `ncbClassInfo<T>::InfoT` 与 `ncbClassInfo<T>::_info`。
- 四字段构造器：同文件第 103–110 行；第 104 行以
  `initialized(false), name(nullptr), id(0), obj(nullptr)` 明确给出与 Android 相同的
  字段顺序和默认值。
- PSBFile 显式实例化/静态注册发射宏：同文件第 2160–2164 行；
  `NCB_REGISTER_CLASS_COMMON` 先显式实例化 `ncbClassInfo<cls>`，再声明 class autoreg
  静态对象。
- PSBFile 宏展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 751-755 行，符号
  `NCB_REGISTER_CLASS(PSBFile)`；模块字面量只在同文件第 14 行定义为
  `TJS_W("PSBFile.dll")`，本入口本身不消费该字面量。

逐项对应：

1. Android 的四次字段清零对应 `InfoT` 第 104 行的四项成员初始化；本地没有增加容器、
   分配或错误处理。
2. Android 的 guard 是编译器对静态模板对象动态初始化的实现；本地第 114 行的静态模板成员
   与第 2161 行的显式实例化保持“一次初始化后复用”的源码生命周期语义。跨 ABI 不要求
   guard 的机器宽度或 store 指令完全相同。
3. Android `.init_array` 中 class-info 初始化先于 autoreg 初始化；本地注册宏也先实例化
   class-info，再于第 2163 行声明 autoreg 对象。静态初始化阶段均不执行注册 callback。

## 六维判定

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 可见的是一只 guarded `ncbClassInfo<PSBFile>` 静态状态构造入口，字段为 bool、name 指针、32 位 id、class-object 指针；本地 `InfoT` 保留同一四字段顺序、显式构造器和模板静态对象。`MATCH` 仅覆盖二进制可证明结构，不宣称不可观察的原始标识符拼写。 |
| 数据流 | `MATCH` | guard 未置位时依次写 `false/null/0/null`，最后置 guard；已置位时无字段写入。本地构造器默认值与一次性静态初始化语义逐项一致。 |
| 调用链 | `MATCH` | Android 唯一入口边来自 `.init_array@0x19EA088`，本体没有 `BL`；本地由模板静态成员和 `NCB_REGISTER_CLASS(PSBFile)` 显式实例化发射，不把初始化阶段误写成注册 callback 调用。 |
| 对象生命周期 | `MATCH` | Android class-info 为进程静态状态，首次初始化后由 guard 阻止重复构造；四字段均无本入口析构动作。本地 `_info` 同为静态存储期，成员均无需拥有资源的析构。 |
| 内部容器实现 | `N/A` | 本入口只初始化四个标量/指针字段，不创建、访问或变更任何容器。 |
| 边界行为 | `MATCH` | 唯一条件边界是 guard bit 0：已置位立即返回，未置位完整清零后置位；无参数/null 检查、返回值、异常或失败 continuation。本地没有额外安全化分支。 |

## 总判定

`ALIGNED`

在本轮 Android fresh decompile、完整指令和 `.init_array` 引用可证明范围内，未发现本地
`ncbClassInfo<PSBFile>::_info` 静态初始化的确定偏差。

## 确定 GAP

无。

## TASK_TREE 子节点与 cross-reference

- 尚待由本节点在阶段 B 派发的唯一 canonical 直接子节点：
  `0x597F24 ncbClassInfo_PSBFile_InfoCtor_guess [lifecycle]`。Android 本入口没有 `BL`，所以
  该 TASK_TREE 生命周期关系不得表述为保留的直接调用。
- TASK_TREE 未列出本节点的非 canonical cross-reference。
- `.init_array` 下一项 `0x42CF28` 仅作为静态执行顺序上下文记录；它是 `ROOT` 的另一顶层
  sibling，不是本节点的 child 或 direct callee。

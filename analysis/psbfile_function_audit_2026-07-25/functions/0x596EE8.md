# `0x596EE8` — `PSBValueDispatch_IsValidByNum`

## 审计身份

- canonical 地址：`0x596EE8`
- canonical 名称：`PSBValueDispatch_IsValidByNum`
- IDA 当前标签：`PSBValueDispatch_IsValidByNum`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。该边表示构造出的 `PSBValueDispatch` primary
  vtable 包含本入口，不表示 `@597AD4` 直接调用本函数。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596ee8`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅视为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596EE8")`；随后 fresh 调用
`disasm(addr="0x596EE8", include_total=true, max_instructions=32)`、
`xrefs_to(addrs="0x596EE8")`，并以 `get_int` 读取 primary vtable 的目标槽及两侧槽。
本轮开始时本地址没有报告，本文件是本轮 fresh 审计新建结果。

- fresh decompile 结果只有一个无条件返回：
  `return 4294966294LL; /*0x596eec*/`。IDA 未恢复虚方法签名，因而把 `W0` 中的
  32-bit 位型显示成无符号 `4294966294`；该位型是 `0xFFFFFC16`，按 `tjs_error`
  解释即 `-1002`。
- 完整 disasm 仅两条指令：`0x596EE8 MOV W0,#0xFFFFFC16`；
  `0x596EEC RET`。函数无条件分支、无内存读写、无 `BL/BLR`、无栈帧，也不读取
  `this/flag/num/objthis`。
- primary vtable 起点是 `@1A0B3D8`；`@1A0B3D8 + 17 * 8 = @1A0B460`。
  fresh `xrefs_to` 只返回这个 data xref，没有 code xref。fresh `get_int(u64le)` 读取
  `@1A0B460 = 0x596EE8`，同时两侧 `@1A0B458 = 0x596EF0`、
  `@1A0B468 = 0x596EE0`，机械确认本入口是 primary vtable 的 slot 17，位于
  `IsValid` 与 `CreateNew` 两槽之间。
- 本入口不是 NCB wrapper、注册 callback 或 helper；它是 `iTJSDispatch2` 数字索引
  `IsValidByNum` 的虚方法 override。没有 Android direct caller，运行时只经 vdispatch
  到达。

## Android 伪代码（4 行）

```text
IsValidByNum(this, flag, num, objthis):
    ignore this, flag, num, objthis
    return tjs_error(0xFFFFFC16)       // -1002 == TJS_E_NOTIMPL
    // 无分支、无默认输出、无内存访问或其他失败路径
```

该伪代码覆盖本入口的全部条件（无）、默认值（无输出写入）及失败边界：任何参数位型都被
忽略，函数始终返回同一个失败码。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:240-246`：`iTJSDispatch2::IsValidByNum` 的接口形状为
  `tjs_error(flag, num, objthis)`；对象的隐式 `this` 加上这三个显式参数构成 Android
  入口所忽略的全部输入。
- `cpp/core/tjs2/tjsTypes.h:57`：`tjs_error` 是 `tjs_int32`；因此 Android
  `MOV W0,#0xFFFFFC16` 的低 32 位语义是 `-1002`，而不是 IDA 缺失签名时显示的正
  `__int64` 值。
- `cpp/core/tjs2/tjsErrorDefs.h:23-29`：`TJS_E_NOTIMPL` 精确定义为 `-1002`。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,80-89`：`PSBValueDispatch` 直接实现
  `iTJSDispatch2`，并显式声明 `IsValidByNum(tjs_uint32,tjs_int,iTJSDispatch2 *)`
  override；它没有借用通用 `tTJSDispatch` 的数字转名字 fallback。
- `cpp/plugins/psbfile/main.cpp:510-512`：独立的
  `PSBValueDispatch::IsValidByNum` 函数体忽略所有参数并直接
  `return TJS_E_NOTIMPL;`，逐指令对应 Android 的常量装载与返回。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:89` 的独立虚方法槽；第 2 行对应
`main.cpp:510` 未命名的三个显式参数以及函数体不访问 `this`；第 3 行对应
`main.cpp:511` 与 `tjsErrorDefs.h:24`；第 4 行由 Android 完整两指令 disasm 和本地空
函数体共同证明。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一只独立 emitted leaf，并由 primary vtable slot 17 指向；本地同样以 `PSBValueDispatch` 的显式 out-of-line virtual override 承载该槽，而不是继承通用 fallback 或合并进其他方法。stripped 二进制不保留参数名不影响已证明的接口结构。 |
| 数据流 | `MATCH` | Android 只把 `0xFFFFFC16` 写入返回寄存器低 32 位；本地只返回数值相同的 `TJS_E_NOTIMPL`。双方都不读取输入、不写对象或输出参数，也没有中间值。 |
| 调用链 | `MATCH` | Android 本体无 callee，唯一 xref 是 vtable data slot，故仅可由 slot 17 vdispatch 进入；本地通过同一 `iTJSDispatch2::IsValidByNum` override 槽分派，函数体也无 callee。canonical parent 只是 `[vtable-member]` 归属，不被伪装为 direct call。 |
| 对象生命周期 | `N/A` | 本入口不分配、retain/release、构造、析构或改变 `valid_`；甚至不读取 `this`。对象建立和销毁属于其他 emitted 节点。 |
| 内部容器实现 | `N/A` | 本入口没有任何内存访问，不涉及 packed array、TJS 容器、STL 容器或自定义 holder。 |
| 边界行为 | `MATCH` | Android 与本地都对任意 `flag/num/objthis` 以及任意未解引用的 `this` 位型无条件返回 `TJS_E_NOTIMPL`；不根据 `num` 范围、对象 validity 或 null `objthis` 改码，不产生输出副作用，也不存在异常/回退路径。 |

## 总判定

`ALIGNED`

Android 当前可见的独立虚槽结构、常量数据流、vdispatch 归属、无生命周期/容器副作用及
全部边界行为均与本地实现一致；没有确定 GAP，也不存在影响本函数结论的证据缺口。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- canonical 直接子节点：无。阶段 A 未 spawn，且本节点无需阶段 B 派发。
- `@597AD4` 是 canonical `[vtable-member]` parent：其构造对象使用包含本槽的 primary
  vtable；fresh xref 证明它不是本入口的 direct caller。
- `@1A0B460` 是本入口唯一 data xref，即 primary vtable slot 17，不是函数节点，不创建
  agent。
- TASK_TREE 的非 canonical Cross-reference 清单没有指向本节点的额外 DAG 边；相邻槽
  `@596EF0/@596EE0` 仅用于机械确认槽位边界，未在本报告替它们下审计结论。

# `0x59B378` — `PSBFile_root_PropSet_guess`

## 审计身份

- canonical 地址：`0x59B378`
- canonical 名称：`PSBFile_root_PropSet_guess`
- IDA 当前标签：`PSBFile_rootProperty_PropSet_guess`。Android stripped 二进制没有
  保留该模板专门化的精确 C++ 符号，故 canonical 名称继续保留 `_guess`。
- 分组：F（typed NCB wrapper、adaptor 与反注册）
- TASK_TREE parent：`0x59B28C PSBFile_root_PropGet_guess`
- parent 关系：`[vtable-member]`。两者只是同一 root Property wrapper 主 vtable 中的
  sibling 槽；`0x59B28C` 不直接调用本入口，不能把该归属边伪装成 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b378`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只视为 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由
`/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b378` fresh 调用
IDA MCP `decompile(addr="0x59B378")`；随后调用
`analyze_function(addr="0x59B378", include_asm=true)` 取得完整 disasm，并补取
`xrefs_to(0x59B378/0x59B48C)`、root Property vtable 原始字节以及
`@597F38` registration disasm 作为上下文。本地址此前没有报告，本报告不是沿用旧结论。

- fresh decompile / disasm 覆盖 `0x59B378..0x59B45F`，IDA size 为 `0xE8`；入口含
  15 个 basic blocks。参数 ABI 为
  `this=x0, flag=x1, membername=x2, hint=x3, param=x4, objthis=x5`。
- root Property 主 vtable address-point 为 `0x1A0B6F0`。本轮读取的原始 qword 在
  `+0x20` 保存 sibling `PropGet@0x59B28C`，在 `+0x30`（地址 `0x1A0B720`）保存
  当前 `PropSet@0x59B378`；`xrefs_to(0x59B378)` 只有这一条 data xref，没有 parent
  `@59B28C` 的 code xref。
- registration 上下文 `@597F38:0x597FD0..0x598038` 分配 `0x50` 字节 root Property
  wrapper，写入 `nitProperty`、字面量 `L"Property"`、getter `@5981F8`、root Property
  vtable，并在 `0x598018/0x598020` 把 setter 的两个 qword 全部清零；随后以二进制
  字面量 `L"root"` 注册该 wrapper。故标准 root wrapper 的 self-call 会在当前入口的
  setter 判空处拒写；后续通用 setter 支路虽保留在 emitted body 中，但对这只正常构造
  的 wrapper 不可达。
- `0x59B39C..0x59B3A4` 在 `membername != null` 时把 base fallback 优化为
  `0xFFFFFC17 = TJS_E_MEMBERNOTFOUND (-1001)`；`flag`、`hint` 在此路径之外也不消费。
- `0x59B3A8..0x59B3B8` 对 wrapper `+0x40/+0x48` 的 ARM64 pointer-to-member 表示
  做 null 判断；为 null 时返回
  `0xFFFFFC11 = TJS_E_ACCESSDENYED (-1007)`。这些偏移只是 ARM64 ABI 证据，不要求
  wasm 源码硬凑对象布局。
- 仅在 setter 非空的通用支路中，null `objthis` 于 `0x59B3BC/0x59B428` 返回
  `TJS_E_NATIVECLASSCRASH (-1008)`，null `param` 于 `0x59B3C0/0x59B438` 返回
  `TJS_E_FAIL (-1)`。
- setter、`objthis`、`param` 均非空时，`0x59B3C4..0x59B3FC` 以
  `TJS_NIS_GETINSTANCE=2` 和 `PSBFile_ncbClassInfo_id` 对 `objthis` vtable `+0xC8`
  执行 `NativeInstanceSupport`。返回负值、out adaptor 为 null、或 adaptor 中 native
  `PSBFile*` 为 null，三种边界都折叠为 `TJS_E_NATIVECLASSCRASH`。
- 成功时 `0x59B400..0x59B418` 复用栈槽组装
  `{result=null, numparams=1, param=&incomingParam}`，以 setter pointer-to-member 和借用
  native instance 直接进入 `PSBFile_rootProperty_Invoke_guess@0x59B48C`。
  `0x59B41C..0x59B424` 将其 bool 结果规范化为
  `true -> TJS_S_OK (0)`、`false -> TJS_E_FAIL (-1)`。
- 本入口不捕获 typed invoke 异常；异常直接传播。末尾 stack-canary failure 是编译器保护
  路径，不是额外业务默认值或 TJS 错误分支。

## Android 伪代码（9 行）

```text
PropSet(self, flag, membername, hint, param, objthis):
    if membername != null: return TJS_E_MEMBERNOTFOUND
    setter = self.setter; if setter == null: return TJS_E_ACCESSDENYED
    if objthis == null: return TJS_E_NATIVECLASSCRASH
    if param == null: return TJS_E_FAIL
    adaptor = null; hr = objthis.NativeInstanceSupport(GETINSTANCE, PSBFileClassID, &adaptor)
    if FAILED(hr) or adaptor == null or adaptor.instance == null: return TJS_E_NATIVECLASSCRASH
    ok = rootTypedInvoke(null, 1, &param, setter, adaptor.instance)  // direct @59B48C
    return ok ? TJS_S_OK : TJS_E_FAIL  // flag/hint ignored；异常直接传播
```

实际 registration 把 `self.setter` 置为 null，因此 root 的正常 self-call 在第 3 行即返回
`TJS_E_ACCESSDENYED`；伪代码仍完整保留 emitted 通用模板支路的全部默认值和失败边界。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/main.cpp:751-755`，`NCB_REGISTER_CLASS(PSBFile)`：第 1096 行
  `Property(TJS_W("root"), &PSBFile::GetRootDispatch, 0)` 明确声明 getter 非空、setter
  为 null 的只读 typed Property，与 Android registration 的 getter/null-setter 输入一致。
- `cpp/core/plugin/ncbind.hpp:1720-1732`，`ncbRegistClass::Property`：第 1727 行把整型
  `0` 转为 `static_cast<GetterT>(0)`，第 1722-1723 行实例化
  `ncbNativeClassProperty<PropertyCommand<...>>`；这保留 Android 中 getter/setter
  pointer-to-member 字段和独立 wrapper vtable，而非把 `root` 简化成专用手写 setter。
- `cpp/core/plugin/ncbind.hpp:1441-1496`，目标符号
  `ncbNativeClassProperty<PropCommandT>::PropSet`：第 1474 行处理非空 membername 的
  base fallback；第 1477、1478、1479 行依次检查 setter、objthis、param；第 1482 行
  精确以 `result=0, numparams=1, param=&param, objthis` 构造
  `doInvoke<SetCommandT>`。字段 `_getter/_setter` 保留在第 1492-1495 行。
- `cpp/core/tjs2/tjsObject.h:132-136`，`tTJSDispatch::PropSet`：当 membername 非空时
  返回 `TJS_E_MEMBERNOTFOUND`，解释 Android 第一条提前返回为何没有保留实际 base call。
- `cpp/core/plugin/ncbind.hpp:1092-1108`，`doInvokeBase/CallInvoke`：构造器只在 result
  非空时 Clear；当前 setter 路径传 null，故不清理任何 Variant。`CallInvoke` 将 typed
  caller 的 bool 结果规范化为 `TJS_S_OK/TJS_E_FAIL`，对应 Android 尾部位运算。
- `cpp/core/plugin/ncbind.hpp:1153-1190`，`doInvoke<SetCommandT>`：先检查参数数目，再取得
  native instance、传播 getter error，最后进入 `CallInvoke`；当前固定
  `numparams=1` 与 Android 栈帧一致。
- `cpp/core/plugin/ncbind.hpp:1031-1056` 的
  `nativeInstanceGetterBase<PSBFile>`，以及 `cpp/core/plugin/ncbind.hpp:156-174` 的
  `ncbInstanceAdaptor<PSBFile>::GetAdaptor/GetNativeInstance`：使用
  `TJS_NIS_GETINSTANCE` 和 `ClassInfoT::GetID()` 查询 adaptor，并把失败/null adaptor/null
  native pointer 统一为 `TJS_E_NATIVECLASSCRASH`；没有在本入口取得 adaptor 或 native
  instance 的所有权。
- `cpp/core/tjs2/tjsErrorDefs.h:23-36` 精确给出本入口涉及的
  `-1001/-1007/-1008/0/-1` 常量。

逐行对应：伪代码第 2 行对应 `ncbind.hpp:1473-1474` 与
`tjsObject.h:132-136`；第 3-5 行对应 `ncbind.hpp:1476-1479`；第 6-7 行对应
`ncbind.hpp:1039-1055,156-174,1180-1185`；第 8-9 行对应
`ncbind.hpp:1092-1108,1186-1190,1481-1482`。`flag/hint` 在本地 override 中只作为
签名与 base fallback 参数存在，self-call 路径同样不消费。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 typed `ncbNativeClassProperty` root 专门化的独立 `PropSet` vslot，保留 base fallback、两 qword setter pointer-to-member、`doInvoke`、native-instance getter 与 typed caller 分层；本地保留相同模板结构、字段和 virtual override。ARM64 字段偏移只作 ABI 证据。 |
| 数据流 | `MATCH` | Android 按 membername → setter → objthis → param → adaptor/native 查询 → `{null,1,&param}` typed invoke 的顺序传递；本地 `PropSet/doInvoke/nativeInstanceGetter/CallInvoke` 使用相同输入、默认值与返回规范化，且 flag/hint 在 self-call 路径均不参与。 |
| 调用链 | `MATCH` | Android `@597F38` 构造并注册 root wrapper，运行期从 `0x1A0B720` vdispatch 进入本入口；canonical parent `@59B28C` 只是同 wrapper sibling，不直接 call 当前函数。当前 emitted body 的 `0x59B418 -> @59B48C` 与本地 `PropSet -> doInvoke<SetCommandT>` 一致，但它是非 canonical cross-reference。 |
| 对象生命周期 | `MATCH` | 实际 null-setter 路径无对象操作；通用支路只借用 objthis、adaptor、native instance 与 incoming Variant 指针，不 AddRef/Release、不接管所有权，且因 result=null 不 Clear Variant。本地具有相同借用关系和无额外 guard/owner 的结构。 |
| 内部容器实现 | `N/A` | 本入口不创建、遍历或修改 STL/KiriKiri 容器、TJS Array、class member table或 autoreg 链；registration 的 class table 更新发生在外层 infrastructure callee，不能计为本函数容器实现。 |
| 边界行为 | `MATCH` | Android 的非空 membername/-1001、null setter/-1007、null objthis/-1008、null param/-1、NativeInstanceSupport 失败或 null adaptor/native/-1008、typed invoke false/-1 与 true/0 全部有本地逐项对应；异常直接传播，未增加“更安全”补丁。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 disasm、vtable qword、data/code xref、registration
上下文与本地模板逐行对照可证明范围内，`0x59B378` 的六维行为与当前
`ncbNativeClassProperty<...>::PropSet` root 专门化一致。

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能恢复该 template 专门化的精确原始标识符拼写，且
Android pointer-to-member 的两个 qword 是 ARM64 ABI 展开；因此 canonical 名称保留
`_guess`，本地不硬凑对象偏移。这些不是已证实的行为差异。

## TASK_TREE 直接子节点与 cross-reference

- 本节点没有 TASK_TREE canonical 直接子节点；阶段 A 未 spawn 任何 agent。
- 非 canonical code cross-reference：当前函数 `0x59B418` 直接调用
  `PSBFile_rootProperty_Invoke_guess@0x59B48C`。`xrefs_to(0x59B48C)` 还返回 sibling
  `@59B28C` 的 `0x59B334`；TASK_TREE 已把 `0x59B48C` 的唯一 canonical parent 选为
  `0x59B28C`，不得由本节点重复派发。
- canonical parent `@59B28C` 与 sibling `@59B460/@59B484` 仅表达同一 property wrapper
  的 vtable / 生命周期归属，不是当前入口的 direct callers，也不得从本节点派发。

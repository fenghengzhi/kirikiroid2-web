# `0x597F08` — `ncbClassInfo_PSBFile_Clear_guess`

## 身份与归属

- canonical 地址：`0x597F08`
- canonical 名称：`ncbClassInfo_PSBFile_Clear_guess`
- IDA 当前名称：`PSBFile_ncbClassInfo_Clear_guess`
- 分组：C / PSBFile typed NCB 前段 / factory / root
- TASK_TREE parent：`0x59A968 PSBFile_AutoRegister_Unregist_guess`，父边类型为
  `[helper]`。该边表示 parent 的反注册清理路径内联了 `Clear()` 的四字段 store shape；
  Android 没有保留 `parent → 0x597F08` 的 direct call，不能把该关系写成
  `[direct-call]`。
- 审计 agent path：`/root/f_42cf28/f_59a968/f_597f08`
- 本轮日期：2026-07-25
- 唯一权威来源：Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或
  从外部 `psbfile.dll` 推导。

Android vtable / NCB / 调用链归属：本入口不是 vtable 槽、TJS member wrapper、注册
callback 或直接反注册入口，而是显式实例化的 `ncbClassInfo<PSBFile>` 静态 class-info
清零 helper。canonical parent `0x59A968` 在 `UnregistEnd` 语义中删除全局 class member
之后内联同一 helper；本报告仅用 parent 作为调用上下文，不替它审计其余逻辑。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cf28/f_59a968/f_597f08` fresh 执行
`decompile(addr="0x597F08")`；随后执行完整
`disasm(addr="0x597F08", include_total=true)`、`xrefs_to(0x597F08)`、
`lookup_funcs(0x597F08)`，并查询四个 class-info 全局字段的 data xref。为验证 TASK_TREE
父边类型，另 fresh `decompile(0x59A968)` 与完整 `disasm(0x59A968)`，但只把它作为
内联调用点上下文。本轮开始时
`functions/0x597F08.md` 不存在，未沿用旧报告。

- IDA 确认函数范围为 `0x597F08..0x597F23`，大小 `0x1C`，共 6 条指令。
- `0x597F08` 只计算 `ncbClassInfo<PSBFile>` 静态状态基址；没有参数读取。
- `0x597F10` 以 64 位零写 `name@0x1AB5100`。
- `0x597F14` 以 32 位零写 `id@0x1AB5108`。
- `0x597F18` 以 64 位零写 `classObject@0x1AB5110`。
- `0x597F1C` 最后以 8 位零写 `initialized@0x1AB50F8`，`0x597F20` 直接返回。
- 完整函数没有条件分支、`BL`、参数检查、分配、引用计数、异常路径、失败返回或同步
  原语；即使状态已清空也仍执行四次 store。
- fresh `xrefs_to(0x597F08)` 为空。parent `0x59A968` 的正常清理路径
  `0x59A9E8..0x59A9F4` 以及异常展开清理路径均直接写同一四个全局字段，没有
  `BL 0x597F08`，所以 canonical 边是源码 helper 的内联归属而非 retained direct call。

## Android 伪代码（7 行）

```text
Clear():
    info.name = null
    info.id = 0
    info.obj = null
    info.initialized = false
    return
// 无条件路径；无 guard、参数、callee、异常、失败返回、引用计数或同步
```

## 本地精确对照

- 共通类型：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 28–35 行，符号
  `ncbTypedefs::{NameT, IdentT, ClassObjectT}`；对应 name 指针、32 位 class id 与
  native-class object 类型。
- class-info 模板与精确函数：同文件第 73–114 行，符号 `ncbClassInfo<T>`；
  `ncbClassInfo<T>::Clear()` 位于第 96–101 行，静态 `InfoT::_info` 的字段声明位于第
  103–111 行。
- 反注册源码调用链：同文件第 1928–1935 行，符号
  `ncbRegistNativeClass<CLASS>::UnregistEnd()`；删除/释放 global 后无条件调用
  `_RemoveClassInfo()`。第 1950–1958 行证明 `_RemoveClassInfo()` 的唯一动作是
  `ClassInfoT::Clear()`。
- 模板显式实例化：同文件第 2160–2172 行，宏 `NCB_REGISTER_CLASS_COMMON` /
  `NCB_REGISTER_CLASS`；第 2161 行显式实例化 `ncbClassInfo<cls>`。
- PSBFile 展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 751-755 行，
  符号 `NCB_REGISTER_CLASS(PSBFile)`。

逐行对应：

1. Android `0x597F10` 清零 `name`，对应本地 `ncbind.hpp:97` 的
   `_info.name = 0`。
2. Android `0x597F14` 清零 32 位 `id`，对应本地 `ncbind.hpp:98` 的
   `_info.id = 0`。
3. Android `0x597F18` 清零 class-object 指针，且没有 `Release`，对应本地
   `ncbind.hpp:99` 的 `_info.obj = 0`；两边都把该槽视为非 owning 状态引用。
4. Android `0x597F1C` 最后清零 initialized byte，对应本地 `ncbind.hpp:100` 的
   `_info.initialized = false`，保持载荷先清、状态位最后提交的顺序。
5. Android 本入口无 guard、callee 和返回状态；本地第 96 行是 `static inline void`
   无参函数，函数体也没有条件、失败分支或“更安全”的额外行为。
6. Android parent 把本体内联，本地源码仍保留
   `UnregistEnd → _RemoveClassInfo → ClassInfoT::Clear` 的模板调用链；O3 内联不构成
   direct-call 证据，也不构成源码结构差异。

## 六维审计

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是无参、无返回状态的静态 class-info Clear helper，依次清空两个指针、一个 32 位 id 和一个 bool；本地 `ncbClassInfo<T>::Clear()` 保留相同四项赋值及顺序。stripped/O3 二进制不能证明模板和成员的原始文本拼写，`MATCH` 仅覆盖可证结构。 |
| 数据流 | `MATCH` | Android 无条件把 `name/id/classObject/initialized` 写成 `null/0/null/false`，没有读取旧值或替代默认值；本地第 97–100 行的数据源、目标和提交顺序完全一致。 |
| 调用链 | `MATCH` | Android 独立 helper 本体无 callee且 out-of-line xref 为空；`0x59A968` 内联同形 Clear。源码层本地由 `UnregistEnd → _RemoveClassInfo → ClassInfoT::Clear` 到达，同样允许编译器内联。TASK_TREE 使用 `[helper]`，没有把内联关系伪装成 direct call。 |
| 对象生命周期 | `MATCH` | Android 只移除四个非 owning class-info 值，不分配、不析构、不对 class-object/name 做 AddRef/Release；本地 Clear 同样只清静态状态，实际 global dispatch 删除/释放由 parent 在调用前负责。重复 Clear 不产生额外生命周期动作。 |
| 内部容器实现 | `N/A` | 本入口只写 class-info 的四个标量/指针字段，不创建、遍历或修改 autoreg 链、map、list 或其他容器。 |
| 边界行为 | `MATCH` | Android 没有 initialized guard：无论尚未初始化、已初始化或已经清空，都执行同序四次 store 后返回；无参数/null 检查、错误码、异常或锁。源码本地保留相同的无条件、幂等值结果与非同步边界。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整反汇编、函数 xref、四字段 data xref 与 parent 内联上下文
可证明范围内，本地 `ncbClassInfo<PSBFile>::Clear()` 与 `0x597F08` 六维一致。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 尚待派发的 canonical 直接子节点：无（本节点为 leaf，本阶段 A 未 spawn child）。
- `@59A968`：canonical parent，关系 `[helper]`；正常和异常清理路径内联 Clear shape，
  不是对 `0x597F08` 的 retained direct call。
- `@42CEF8`：同一四字段静态状态的初始化生命周期入口；只作状态归属上下文，不是本节点
  child 或 direct caller，不重复派发。
- `@597ED0`：同一 class-info 状态的 setter sibling；只作字段语义/data-xref 交叉参照，
  canonical parent 在 TASK_TREE 另有归属，不重复派发。

# `0x596EF0` — `PSBValueDispatch_IsValid`

## 审计身份

- canonical 地址：`0x596EF0`
- canonical 名称：`PSBValueDispatch_IsValid`
- IDA 当前标签：`PSBValueDispatch_IsValid`。Android stripped 二进制未保留原始 C++ 符号；
  本报告只把该名称作为已建立的 vtable 槽语义标签，不反向以本地标识符命名二进制。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 安装包含本入口的 primary vptr；这不是
  `0x597AD4` 对本函数的直接调用。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596ef0`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `"PSBFile.dll"` 仅可视作 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596EF0")`，并补充 fresh
`disasm(addr="0x596EF0", include_total=true, max_instructions=64)`、
`xrefs_to(addrs="0x596EF0")`、`lookup_funcs(queries=["0x596EF0"])` 以及 primary-vtable
原始字节读取。本报告在本轮开始前不存在，是本轮 fresh 审计新建结果。

- fresh decompile 完整结果只有一个条件表达式：读取 `*(uint8_t *)(a1 + 40)`；非零返回
  `1`，零返回 `2`。IDA 因四个接口参数全部未使用而只恢复 `this`，并把返回型显示成
  `__int64`；完整 disasm 明确实际写的是 32-bit `W0`。
- `lookup_funcs` 确认这是独立 emitted 入口 `0x596EF0..0x596F03`，大小 `0x14`；完整
  disasm 恰好 5 条指令：`LDRB W8,[X0,#0x28]`、`CMP W8,#0`、`MOV W8,#1`、
  `CINC W0,W8,EQ`、`RET`。
- 因此函数只有一次单字节 load，没有栈帧、store、`BL/BLR`、异常清理或第二次对象访问。
  `CINC` 在 byte 为零时得到 `2`，非零时保持 `1`；任何非零 byte 都被视为 valid。
- fresh `xrefs_to` 只返回 data xref `0x1A0B458`，没有静态 code xref。fresh 原始字节读取
  证明 primary vtable address point `0x1A0B3D8` 的 `+0x80` 项恰为 `0x596EF0`，即第
  16 槽；其前两项从 `AddRef/Release` 起的完整 Android 槽序列把本入口归属于
  `PSBValueDispatch` 的 primary `iTJSDispatch2::IsValid` vtable member。
- 运行期入口因此是虚分派；不能把“无 code xref”解释成“从未调用”。本入口不是 NCB
  wrapper、注册 callback 或 helper，也不直接调用任何其他 psbfile emitted 函数。

## Android 伪代码（4 行）

```text
IsValid(this, flag, membername, hint, objthis):
    valid = load_u8(this + 0x28)       // this 无 null/地址保护；其余参数完全不读
    if valid != 0: return TJS_S_TRUE   // 1；任意非零 byte 均为 true
    return TJS_S_FALSE                 // 2；零是唯一 false 分支
```

该伪代码覆盖全部条件、默认值与失败边界：本函数没有其他成功码、错误码、fallback 或副作用；
非法 `this` 在唯一 byte load 处按原生 C++ 未定义行为失败，而其余指针即使为 null/悬空也不被
读取。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:217-238`：`Invalidate`、`InvalidateByNum` 后紧接完整四参数
  `iTJSDispatch2::IsValid` 槽，接口约定返回 `TJS_S_TRUE` 或 `TJS_S_FALSE`；这与 Android
  vtable `slot 14/15/16` 的相邻顺序及本入口返回集合一致。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,80-81`：本地 `PSBValueDispatch final` 直接实现
  `iTJSDispatch2`，并以完整四参数签名显式声明 `IsValid(... ) override`，保留独立的 primary
  vtable member，而非改用 NCB wrapper 或基类转发。
- `cpp/plugins/psbfile/PSBDispatch.h:130-132`：对象保存独立 `bool valid_{true}`；这是本地与
  Android 单字节状态的语义对应。ARM64 的 `+0x28` 是二进制 ABI 证据，不在 wasm 源码中
  硬编码或强凑偏移。
- `cpp/plugins/psbfile/main.cpp:486-490`：本地目标定义的四个接口形参均未命名，函数体唯一
  语句是 `return valid_ ? TJS_S_TRUE : TJS_S_FALSE;`，逐项对应 Android 的单 byte load、
  零/非零分支及 `1/2` 返回值。
- `cpp/core/tjs2/tjsTypes.h:35-40,57`：`tjs_error` 是 32-bit `tjs_int32`；
  `cpp/core/tjs2/tjsErrorDefs.h:32-33` 把 `TJS_S_TRUE/TJS_S_FALSE` 分别定义为 `1/2`，与
  Android `W0` 的两种结果逐位一致。

逐行对应：伪代码第 1 行由 Android vtable slot 与本地 interface/override 位置相互对照；
第 2 行对应 Android `0x596EF0` 和本地 `main.cpp:489` 对 `valid_` 的唯一读取；第 3-4 行对应
Android `0x596EF4..0x596EFC` 与本地同一条件表达式。双方都不检查 membername，不读
flag/hint/objthis，也不触发 callee、写回或清理。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一只独立 `0x14` emitted 入口，并由 primary vtable `slot 16` 直接指向；本地同样以 `PSBValueDispatch::IsValid` 的显式 override 提供独立函数体与单一 bool 状态读取。`MATCH` 只覆盖二进制可证结构，不宣称恢复不可观察的参数名、精确原始拼写或 ARM64/wasm ABI 偏移一致。 |
| 数据流 | `MATCH` | Android 只从 `this+0x28` 读取一个 byte，并按零/非零映射为 `2/1`；本地只读取 `valid_` 并用相同常量映射。双方均不读其余参数、不写对象或输出参数。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 `0x1A0B458` 的 primary vtable 数据项，本体没有 callee；本地通过 `iTJSDispatch2` override 提供同一虚分派槽且不转发 helper。canonical parent `@597AD4` 是 vtable-member 归属，不是 direct-call。 |
| 对象生命周期 | `MATCH` | Android 和本地都只借用 `this` 查询逻辑 validity 状态，不分配、析构、AddRef/Release、retain 或改变 invalidation 状态；查询不会延长对象或其 raw owner/node 的生命。 |
| 内部容器实现 | `N/A` | 完整入口只读取一个标量 byte，不创建、访问或遍历 packed、TJS、STL 或其他容器。 |
| 边界行为 | `MATCH` | Android 与本地均把零严格映射为 `TJS_S_FALSE(2)`、任意非零映射为 `TJS_S_TRUE(1)`；不依据 flag/membername/hint/objthis 改变结果，也不解引用这些参数。非法 `this` 无保护；没有错误码、异常处理、fallback 或副作用。 |

## 总判定

`ALIGNED`

Android 当前可证明的 emitted 结构、单字节数据流、vtable 调用归属、生命周期查询语义和完整
边界均与本地实现一一对齐；本入口没有确定 GAP，也没有影响该判定的证据缺口。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。阶段 A 未 spawn 任何 subagent。
- canonical parent cross-reference：`@597AD4` 安装包含本槽的 primary vptr；该关系严格为
  `[vtable-member]`，不是 direct-call。
- fresh IDA 唯一 xref 是 vtable 数据项 `@1A0B458`，属于上述 canonical 关系；没有其他
  属于 114-node manifest 的非 canonical cross-reference，因此不派发重复 agent。

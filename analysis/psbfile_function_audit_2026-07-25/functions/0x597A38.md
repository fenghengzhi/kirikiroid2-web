# `0x597A38` — `PSBValueDispatch_Construct_secondary_thunk`

## 审计身份

- canonical 地址：`0x597A38`
- canonical 名称：`PSBValueDispatch_Construct_secondary_thunk`
- IDA 当前标签：`PSBValueDispatch_Construct_secondary_thunk`。Android stripped 二进制未保留
  原始 C++ 符号；本报告沿用 TASK_TREE 的 canonical 接口标签。完整指令证明该 emitted
  入口实际是 secondary-base duplicate body，而不是含 `this` 调整或 tail-call 的经典
  adjustor thunk。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597A30 PSBValueDispatch_Construct_primary`
- parent 关系：`[vtable-member]`。两地址分别占据同一源码级 virtual `Construct` 的 primary
  与 secondary vtable 槽；`@597A30` 不调用 `@597A38`，本边绝非 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_597a30/f_597a38`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只视为 NCB 模块名字面量，本入口不访问该
  字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x597A38")`；随后 fresh 调用目标函数完整 `disasm`、`lookup_funcs`、
`xrefs_to`、secondary vtable 原始字节/qword读取，并仅把 canonical parent 的完整
`disasm` 作为 duplicate 关系上下文。本地址在本轮开始时没有旧报告；本文件是本轮 fresh
审计新建结果。

- fresh decompile 精确得到：

  ```c
  __int64 PSBValueDispatch_Construct_secondary_thunk()
  {
    return 0; /*0x597a3c*/
  }
  ```

- `lookup_funcs(0x597A38)` 给出 size=`0x8`；完整 disasm 恰好两条指令：
  `0x597A38 MOV W0, WZR`、`0x597A3C RET`。函数没有栈帧、参数寄存器读取、条件分支、
  内存访问、`BL/BLR`、`this` 指针调整、输出对象写回或异常恢复。写 `W0` 同时清零
  `X0`，按 `iTJSNativeInstance::Construct` 的 `tjs_error` 语义即无条件返回成功码 `0`。
- fresh `xrefs_to(0x597A38)` 只返回一条 data xref `0x1A0B4E8`，没有 code xref。
  因此运行时入口只经 vtable 间接分派可达；不存在 Android 保留的 direct caller、callee、
  NCB wrapper 或 NCB callback 链。
- fresh 读取 `0x1A0B4C0..0x1A0B4F8` 的 8 个 qword，依次得到：
  `@597A30`、`@596F38`、`@597A28`、`-8`、`0`、`@597A38`、`@596F3C`、
  `@597A2C`。这精确显示 primary 表尾部三槽、secondary-base offset-to-top `-8`、空 RTTI
  项，以及 secondary address point `0x1A0B4E8` 的
  `Construct/Invalidate/Destruct` 三槽。本地址就是 secondary slot 0。
- canonical parent context 的完整 disasm 是
  `0x597A30 MOV W0,WZR; 0x597A34 RET`，与目标函数体逐指令相同；但两者互不调用。
  因而 TASK_TREE 的 `[vtable-member]` 是 vtable duplicate/sibling 归属，不是函数调用边。
- fresh `xrefs_to(0x1A0B4E8)` 返回 `@59673C:0x5968EC`、`@597A40:0x597A74` 与
  `@597AD4:0x597AE0` 三处 address-point 引用。它们只证明其他生命周期/赋值路径安装或
  恢复 secondary vptr；没有一处是对 `@597A38` 的直接调用，本报告不替这些函数审计。

## Android 伪代码（4 行）

```text
Construct_secondary(this, numparams, params, tjs_obj):
    ignore this, numparams, params, tjs_obj       // 均不读取、校验或解引用
    return 0                                      // 成功；无条件、默认或失败分支
    // 无 this 调整、tail-call、callee、状态写入、retain/release 或异常路径
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 依次直接继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`。普通 C++ 多继承正是 Android primary/secondary
  两张 vtable、secondary offset-to-top 与 duplicate entry 的源码级来源；本地没有把
  secondary entry 错写成第二个业务方法，也没有硬凑 ARM64 ABI 偏移。
- `cpp/plugins/psbfile/PSBDispatch.h:74-78`：类只声明一个
  `Construct(tjs_int, tTJSVariant **, iTJSDispatch2 *) override`，随后是同一 native interface
  的 `Invalidate/Destruct`，与 Android secondary address point 的三槽次序一致。
- `cpp/core/tjs2/tjsInterface.h:326-337`：`iTJSNativeInstance` 的 virtual 槽序精确为
  `Construct`、`Invalidate`、`Destruct`；`Construct` 的三个显式参数与本地 override 一致。
  Android O3 因本体不消费参数而将反编译签名恢复成无参形式，这不改变槽归属。
- `cpp/plugins/psbfile/main.cpp:470-474`：目标源码级符号是唯一的
  `PSBValueDispatch::Construct(...)`；三个显式形参均省略名字，函数体唯一语句是
  `return TJS_S_OK`。同一 override 供 primary 与 secondary vtable 使用，逐指令对应
  Android duplicate body 的 `MOV W0,WZR; RET`。
- `cpp/core/tjs2/tjsErrorDefs.h:35`：`TJS_S_OK` 精确定义为 `0`，与 Android 返回位型完全
  一致。

逐行对应：伪代码第 1 行对应 `tjsInterface.h:328-329`、`PSBDispatch.h:74` 的 virtual
接口；第 2 行对应 `main.cpp:470` 的无名、未使用形参以及 Android 完全无参数读取；第 3 行
对应 `main.cpp:473`、`tjsErrorDefs.h:35` 与 Android `0x597A38`；第 4 行对应本地单返回体
以及 Android 在 `RET` 前不存在任何其他指令。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 在 offset-to-top=`-8` 的 secondary `iTJSNativeInstance` address point 首槽保留独立 duplicate entry，同时 primary 表 slot 29 保留同形入口；本地用 `iTJSDispatch2 + iTJSNativeInstance` 的基类顺序和单一 `Construct` override 恢复这一源码级多继承结构。不同目标 ABI 是否折叠 identical code 不属于需要硬编码的 ARM64 字节布局。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或三个显式参数、不访问内存，只向返回寄存器写零；本地所有形参未使用且只返回值为零的 `TJS_S_OK`，没有对象或输出参数写回。 |
| 调用链 | `MATCH` | Android 目标函数唯一 xref 是 secondary vtable data item `0x1A0B4E8`，函数体没有 direct caller/callee 或 NCB 链；parent 只是在另一张表中的同源 virtual sibling，绝不直接调用本入口。本地同样只通过 virtual override 提供该回调，函数体不调用 helper。 |
| 对象生命周期 | `MATCH` | 本入口是 native-instance `Construct` 生命周期回调，但 Android 明确实现为成功 no-op：不初始化/销毁字段、不 retain/release、不接管参数；本地同一回调也完全不改对象并返回成功。真正的 C++ 对象构造属于 `@597AD4` 及其内联同形路径，不被错误并入本函数。 |
| 内部容器实现 | `N/A` | 完整两指令函数不创建、访问或修改 STL、KiriKiri、TJS Array、packed 表或任何对象字段。 |
| 边界行为 | `MATCH` | Android 对任意 `this/numparams/params/tjs_obj` 位型（包括空指针、负参数数或无效地址）都不解引用且无条件返回 `0`；没有参数数检查、失败码、fallback、callee、异常或恢复路径。本地行为相同。 |

## 总判定

`ALIGNED`

fresh decompile、完整两指令 disasm、唯一 vtable data xref、secondary address point 原始布局与
本地源码逐行对照共同证明：`0x597A38` 在六个固定维度的所有适用范围内均已对齐。

## 确定 GAP

无。

证据边界：IDA 因所有参数均未使用而将签名恢复成无参 `__int64`，且 stripped 二进制不能
恢复原始 C++ 标识符拼写；但 `MOV W0,WZR` 的零返回、独立函数边界及 secondary virtual
归属都无 ABI 歧义。这些是名字/类型恢复上限，不是已证明偏差。

## TASK_TREE 直接子节点与 cross-reference

阶段 A 未 spawn。本节点没有 canonical 直接子节点，`children=无`。

Cross-reference（均不创建重复 agent）：

- `@597A30` 是 canonical parent，也是 primary vtable slot 29 的同源 duplicate；两函数体
  相同但互不调用，关系严格为 `[vtable-member]`。
- `@597AD4`、`@597A40`、`@59673C` 引用 secondary address point `0x1A0B4E8`，只形成
  vptr 安装/恢复上下文，不是本函数的 direct caller，也不是本节点 child。
- `@596F3C` 与 `@597A2C` 是同一 secondary vtable 的其余两个 sibling，各有 TASK_TREE
  canonical parent；它们不是本节点 child，不得由本 agent 派发。
- `0x1A0B4E8` 是 vtable 数据地址，不是函数节点；它只保存指向本入口的 qword。

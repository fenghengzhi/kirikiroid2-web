# `0x596E14` — `PSBValueDispatch_Operation`

## 审计身份

- canonical 地址：`0x596E14`
- canonical 名称：`PSBValueDispatch_Operation`
- IDA 当前标签：`PSBValueDispatch_Operation`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。该边表示 `@597AD4` 构造的对象安装了包含本入口的
  primary vtable，不表示构造器直接调用本函数。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596e14`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只视为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596E14")`，随后 fresh 调用
`disasm(addr="0x596E14", include_total=true, max_instructions=64)`、
`xrefs_to(addrs="0x596E14")`、`func_profile`、`lookup_funcs`，并以 `get_int(u64le)` 读取
primary vtable 的目标槽与两侧槽。本轮开始时本地址没有报告；本文件是本轮 fresh 审计
新建结果。

- fresh decompile 结果只有无条件
  `return 4294966294LL; /*0x596e18*/`。IDA 没有恢复该虚方法的接口原型，因此把 `W0`
  的 32-bit 位型 `0xFFFFFC16` 显示成正的 64-bit 数；按 Android TJS 接口的
  32-bit `tjs_error` 解释，该位型严格等于 `-1002`。
- 完整 disasm 覆盖 `[0x596E14,0x596E1C)`，共两条指令：
  `MOV W0,#0xFFFFFC16`；`RET`。本体没有栈帧、条件分支、内存访问、`BL/BLR`、异常或
  清理路径；`func_profile` 同步确认 1 个 basic block、0 caller、0 callee、0 字符串引用。
- fresh `xrefs_to` 只返回 data xref `@1A0B490`，没有静态 code xref。primary vtable
  address point 为 `@1A0B3D8`，且
  `0x1A0B490 - 0x1A0B3D8 = 0xB8 = 23 * sizeof(void*)`；fresh `get_int` 读取该项为
  `0x596E14`。两侧槽 `21/22/24/25` 分别为
  `0x596E24/0x596E1C/0x596E0C/0x596D90`，机械确认本入口位于
  `IsInstanceOfByNum` 与 `OperationByNum` 之间，是 primary `iTJSDispatch2` vtable
  slot 23 `Operation`。
- 本入口不是 NCB wrapper、注册 callback 或 packed helper；它由
  `PSBValueDispatch` 的 primary vtable 安装并在运行时经 vdispatch 进入。函数本体不调用
  任何 psbfile 或 TJS helper；没有 code xref 不能被解释为“运行时从未调用”。

## Android 伪代码（4 行）

```text
Operation(this, flag, membername, hint, result, param, objthis):
    ignore this, flag, membername, hint, result, param, objthis
    do not write result, hint, object state, or parameter storage
    return tjs_error(0xFFFFFC16)        // -1002 == TJS_E_NOTIMPL；无其他分支/失败路径
```

该伪代码覆盖全部条件（没有条件分支）、默认值（没有输出写入）和失败边界：所有显式参数的
任意位型，包括 null 指针，均不被本入口读取或解引用，固定返回同一错误码。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:280-305`：`iTJSDispatch2` 在
  `IsInstanceOfByNum` 后声明 slot 23 `Operation(flag, membername, hint, result, param,
  objthis)`，随后声明 `OperationByNum`；与 Android vtable 中目标槽及两侧槽的顺序一致。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,103-109`：`PSBValueDispatch final` 直接继承
  `iTJSDispatch2`，并以完整六显式参数签名单独声明 `Operation(... ) override`；没有用
  NCB wrapper、基类默认实现或数字索引入口替代该 vtable member。
- `cpp/plugins/psbfile/main.cpp:532-536`：本地
  `PSBValueDispatch::Operation` 的全部显式参数均未命名，函数体唯一语句为
  `return TJS_E_NOTIMPL;`，逐指令语义对应 Android 的常量装载后立即返回。
- `cpp/core/tjs2/tjsTypes.h:57` 与 `cpp/core/tjs2/tjsErrorDefs.h:23-24`：本地
  `tjs_error` 是 `tjs_int32`，`TJS_E_NOTIMPL` 精确定义为 `-1002`，其 32-bit 补码正是
  Android 的 `0xFFFFFC16`。

逐行对应：伪代码第 1 行由 Android vtable slot 23 和本地接口签名定位；第 2-3 行对应
`main.cpp:532-534` 的未命名参数以及函数体中不存在任何读写；第 4 行对应
`main.cpp:535` 和 Android `0x596E14..0x596E18`。双方均无对象状态检查、输出清空、callee
或 fallback。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一只独立的 8-byte emitted 入口，并由 primary vtable slot 23 直接指向；本地同样以 `PSBValueDispatch` 的显式 `Operation` override 提供独立函数体，而不是继承通用实现或合并到 `OperationByNum`。`MATCH` 只覆盖二进制可证明的接口结构，不声称恢复不可观察的参数名或排版。 |
| 数据流 | `MATCH` | Android 只把常量位型 `0xFFFFFC16` 写入返回寄存器低 32 位；本地只返回数值相同的 `TJS_E_NOTIMPL`。双方均不读形参、不访问 `this`、不写 `result/hint` 或对象状态。 |
| 调用链 | `MATCH` | Android 本体没有 direct/indirect callee，唯一 xref 是 primary vtable data slot，运行入口为 slot 23 vdispatch；本地由同一 `iTJSDispatch2::Operation` override 占据该槽且函数体无 callee。canonical parent 只表示 `[vtable-member]` 归属，不是 direct-call。 |
| 对象生命周期 | `N/A` | 本入口不分配、构造、析构、retain/release 或接管对象，也不读取或修改 dispatch 的 refcount/valid/raw-node 状态；相关生命周期由其他 canonical 节点单独审计。 |
| 内部容器实现 | `N/A` | 本入口没有内存访问，不创建、读取或遍历 packed、TJS、STL 或其他容器。 |
| 边界行为 | `MATCH` | Android 与本地对任意 flag、名称、hint、result、param、objthis 均无条件返回 `-1002`；null 或其他未解引用的指针值不会触发本入口内的读写，输出保持调用前状态，没有异常、fallback、参数校验或副作用。 |

## 总判定

`ALIGNED`

Android 当前可证明的独立虚槽结构、常量数据流、vdispatch 归属、无生命周期/容器副作用及
全部边界行为均与本地实现一致；没有确定 GAP，也不存在影响本函数结论的证据缺口。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。阶段 A 未 spawn 任何 subagent，本节点无需阶段 B 派发。
- canonical parent cross-reference：`@597AD4` 构造对象并安装包含本槽的 primary vptr；
  该关系严格为 `[vtable-member]`，不是 direct-call。
- fresh IDA 唯一 xref 是 primary-vtable 数据项 `@1A0B490`，它不是函数节点，不创建 agent。
- TASK_TREE 的非 canonical Cross-reference 清单没有指向本节点的额外 DAG 边；相邻槽
  `@596E24/@596E1C/@596E0C/@596D90` 仅用于机械确认槽位边界，本报告未替它们下审计
  结论，也不为其派发重复 agent。

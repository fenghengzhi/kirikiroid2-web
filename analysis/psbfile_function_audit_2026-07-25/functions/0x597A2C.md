# `0x597A2C` — `PSBValueDispatch_native_Destruct_secondary_nullsub`

## 审计身份

- canonical 地址：`0x597A2C`
- canonical 名称：`PSBValueDispatch_native_Destruct_secondary_nullsub`
- IDA 当前标签：`nullsub_261`。Android stripped 二进制只保留 nullsub 标签；
  `PSBValueDispatch`、secondary `iTJSNativeInstance::Destruct` 与槽位归属来自本轮唯一
  data xref 和 vtable 原始字节，canonical 名称不冒充二进制保留的 C++ 符号。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597A28 PSBValueDispatch_native_Destruct_primary_nullsub`
- parent 关系：`[vtable-member]`（同一源码 override 的 primary/secondary ABI 表面）。
  两只入口之间没有直接调用，因此该边不是 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_597a28/f_597a2c`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只作为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x597A2C")`；随后 fresh 调用
`disasm(addr="0x597A2C", include_total=true, max_instructions=50)`、
`xrefs_to(addrs="0x597A2C")`，并读取 `0x1A0B4C0..0x1A0B4FF` 的 vtable 原始字节。
写入前本地址没有报告文件，本文件是本轮 fresh 审计的新建结果。

- fresh decompile 完整恢复为 `void nullsub_261() { ; }`。
- fresh disasm 的函数范围只有 `0x597A2C: RET` 一条指令，总指令数为 1；没有栈帧、
  参数或字段读取、条件分支、`BL/BLR/BR`、返回值构造、清理或异常路径。
- fresh `xrefs_to` 恰好返回一个 data xref：`0x1A0B4F8`；没有 direct code xref。
- vtable bytes 从 `0x1A0B4C0` 起按 ARM64 little-endian qword 解码为：
  `@597A30`、`@596F38`、`@597A28`、`offset-to-top=-8`、空 RTTI、`@597A38`、
  `@596F3C`、`@597A2C`。因此 secondary 地址点是 `0x1A0B4E8`，本入口位于其
  slot 2（指针存放于 `0x1A0B4F8`）；它与 primary 尾槽 `@597A28` 是同一
  `iTJSNativeInstance::Destruct` override 的 ABI 配对表面，而不是 NCB wrapper、callback
  或 direct-call helper。
- 本入口不含 callee。静态 xref 不会枚举经 secondary vptr 的运行时虚分派；本报告只据
  vtable 槽确认归属，不把数据引用伪装成直接调用证据。

## Android 伪代码（5 行）

```text
native_Destruct_secondary(secondary_this_ignored):
    // 不读取 this、参数、对象字段或全局状态
    // 不调用 delete、Release、Invalidate 或任何 callee
    // 无条件、默认写值、错误码、清理或异常恢复分支
    return
```

该伪代码覆盖全部条件、默认值与失败边界：本体进入后总是立即返回；即使传入寄存器值不是
有效对象，本函数也不会解引用它。调用点在间接分派前对 vptr 的要求不属于这条一指令函数
内部的检查。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 多继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`，源码结构会形成 Android 所见的 primary 与
  secondary 两个 vtable 表面；本地没有为 secondary duplicate 另造第二个业务方法。
- `cpp/plugins/psbfile/PSBDispatch.h:74-78`：类只声明一个
  `void Destruct() override`，且顺序在 `Construct`、`Invalidate` 之后，对应 Android
  secondary vtable 的 slot 0、1、2。
- `cpp/core/tjs2/tjsInterface.h:326-335`：`iTJSNativeInstance` 的三个虚方法依次为
  `Construct`、`Invalidate`、`Destruct`，独立确认本入口在 secondary slot 2 的接口归属；
  具体 no-op 行为仍只由本轮 Android 一指令函数证明，而非从方法名推导。
- `cpp/plugins/psbfile/main.cpp:481-484`：唯一源码 override
  `PSBValueDispatch::Destruct()` 的函数体为空；注释准确记录 primary `nullsub_260
  @0x597A28` 与本 secondary `nullsub_261 @0x597A2C`。该空体逐行为 Android 的唯一
  `RET`，没有额外状态改变、释放或 fallback。
- `cpp/core/tjs2/tjsObject.cpp:359-364`：通用 TJS 对象析构路径通过
  `iTJSNativeInstance *` 虚分派调用 `Destruct()`，在该多继承对象上对应 secondary
  vtable 视图；这是本地调用面上下文，不作为 Android direct-call xref。
- `cpp/plugins/psbfile/main.cpp:99-105` 与
  `cpp/plugins/psbfile/PSBDispatch.h:118-119`：本地对象实际删除由
  `PSBValueDispatch::Release()` 归零时的 `delete this` 和私有默认析构承担，空
  `Destruct()` 不抢占该生命周期。这与 Android 本入口没有 delete/Release 完全一致。

逐行对应：伪代码首行对应 `PSBDispatch.h:78` 的唯一 override；其余四行对应
`main.cpp:481-484` 的空函数体。secondary emitted duplicate 是普通 C++ 多继承 ABI 的
产物；本地复刻源码结构而不硬编码 Android 的 `-8` offset-to-top 或 ARM64 字节布局。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 的独立 emitted 空入口只被 secondary vtable slot 2 引用，邻域同时呈现 `offset-to-top=-8` 与三槽 secondary surface。本地以 `PSBValueDispatch : iTJSDispatch2, iTJSNativeInstance` 中的单一 `Destruct() override` 表达同一源码结构，由 ABI 生成 secondary 表面，没有把 sibling 关系写成源码 direct call。`MATCH` 仅限 stripped 二进制可证结构，不声称恢复原作者的排版或精确符号名。 |
| 数据流 | `MATCH` | Android 唯一 `RET` 不读取 `this`/参数/字段/全局、不写任何状态且不构造返回值；本地空体同样没有数据流。 |
| 调用链 | `MATCH` | Android 本入口没有 callee，唯一 xref 是 secondary vtable 数据槽而非 code xref；本地方法只提供相同虚函数入口且不调用其他函数。运行时经 secondary vptr 的间接分派不会被静态 xref 枚举，本报告没有将其伪报为直接调用。 |
| 对象生命周期 | `MATCH` | 名称虽为 `Destruct`，Android 本入口明确不 delete、不 Release、不失效 dispatch、不释放 owner；本地 override 同样 no-op，并将真正删除留给 `Release()` 归零路径。 |
| 内部容器实现 | `N/A` | 本入口不访问、构造、遍历或销毁任何 STL、TJS、packed 或自定义容器，连对象字段也不读取。 |
| 边界行为 | `MATCH` | 对所有进入本体的寄存器状态均无条件 `RET`，没有 null guard、默认写值、错误码、异常或 cleanup；本地空函数保持同一入口级边界。 |

## 总判定

`ALIGNED`

本轮 fresh decompile、完整一指令 disasm、唯一 secondary-vtable data xref、原始 vtable
字节与本地多继承类中的空 override 一致；六维没有确定 GAP，也没有影响本入口结论的证据
上限。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

本节点是 leaf。阶段 A 未 spawn，TASK_TREE 中没有待派发的直接子节点（`children=none`）。

Cross-reference：

- canonical parent `@597A28` 是 primary vtable 的配对 `Destruct` nullsub；父子边是
  `[vtable-member]` secondary duplicate / sibling 归属，不表示 `@597A28` 调用
  `@597A2C`。
- `@1A0B4F8` 是本入口唯一 xref 所在的 secondary vtable slot 2 数据地址，不是函数节点，
  不创建 agent。
- `@597AD4` 的对象构造 shape 安装 primary/secondary vptr，是更上层生命周期上下文；它
  不是本节点 canonical parent，也不为其创建重复 agent。
- 本节点没有 TASK_TREE 非 canonical 函数 cross-reference，也没有直接 child。

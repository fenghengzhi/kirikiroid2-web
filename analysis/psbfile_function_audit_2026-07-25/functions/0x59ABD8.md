# `0x59ABD8` — `PSBFile_instanceAdaptor_CreateEmpty_ctor_guess`

## 审计身份

- canonical 地址：`0x59ABD8`
- canonical 名称：`PSBFile_instanceAdaptor_CreateEmpty_ctor_guess`
- IDA 当前标签：`PSBFile_ncbInstanceAdaptor_CreateEmpty_guess`；Android 二进制没有
  保留该模板专门化的精确源码符号，因此名称保留 `_guess`。
- 分组：F（typed NCB wrapper、adaptor 与反注册）
- TASK_TREE parent：`0x59AA84 PSBFile_RegistBegin_guess`
- parent 关系：`[registration]`。Android parent 在 `0x59AAD8..0x59AAE4` 取本入口
  地址并写入 native-class object 的 create-instance callback 槽；这是 data binding，
  不是 retained direct call。
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_59aa84/f_59abd8`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅视为 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由
`/root/f_42cf28/f_59a8d8/f_59aa84/f_59abd8` fresh 调用 IDA MCP
`decompile(addr="0x59ABD8")`。随后只读补充完整
`disasm(addr="0x59ABD8", include_total=true)`、`xrefs_to(0x59ABD8)`、
`xrefs_to(0x1A0B598)`、`get_bytes(0x1A0B598, 64)`、`callees(0x59ABD8)`，并以
parent `0x59AA84` 的 decompile/disasm 仅复核 callback 绑定上下文。本报告为本轮新建，
不是沿用旧报告。

- IDA 函数范围为 `0x59ABD8..0x59AC03`（size `0x2C`，完整 10 条指令）。
  `0x59ABE0..0x59ABE4` 以 `0x18` 调用 throwing `operator new(unsigned long)`；
  `callees` 未列出 import thunk，但完整 disasm 明确保留 `BL ._Znwm`。
- `0x59ABE8..0x59ABF8` 把 allocation 的首槽写为 vtable address-point
  `off_1A0B598`，把 `+8` 的 native `PSBFile *` 清零，并把 `+16` 的 sticky byte
  清零；`0x59AC00` 原样返回 allocation pointer。函数无参数、条件跳转、状态码或
  null fallback。
- `get_bytes(0x1A0B598, 64)` 复核 address-point 的前五个函数槽依次为
  `0x5242A8`、`0x59AC0C`、`0x5242B4`、`0x59AC7C`、`0x59AD08`。其中本 TASK_TREE
  只把 `@59AC0C` 和 `@59AD08` 作为本节点直接 `[vtable-member]` children；
  `@59AC7C` 的 canonical parent 是 `@59AD08 [lifecycle]`。本报告只确认 constructor
  安装了包含这些槽的 vptr，不替任何 vtable member 审计函数体。
- `xrefs_to(0x59ABD8)` 只有 parent `0x59AA84` 内 `0x59AAD8` 的一条 data xref。
  parent 在 `0x59AAE4` 把该地址写到新 native-class object 的 `+0xA8` callback
  槽；运行期由 plugin native class 间接调用。因此 canonical `[registration]` 边没有
  被伪装成 direct-call 边。
- `xrefs_to(0x1A0B598)` 来自本 constructor `0x59ABF0`、complete destructor
  `0x59AC9C` 和 deleting destructor `0x59AD28`，共同确认这是一套
  `ncbInstanceAdaptor<PSBFile>` 生命周期 vtable。其他入口只作归属证据，不在此下结论。
- throwing `operator new` 若失败会在任何字段写入前传播异常；成功后 constructor
  只有不会抛出的固定字段写入，所以本入口没有额外 unwind cleanup。ARM64 尾部 padding
  没有显式初始化；这是对象 ABI/padding，不应被误写成本地需硬凑的源码字段。

## Android 伪代码（7 行）

```text
CreateEmptyAdaptor():
    p = throwing_operator_new(sizeof(ncbInstanceAdaptor<PSBFile>))
    p.vptr = &ncbInstanceAdaptor<PSBFile>::vtable_address_point
    p._instance = null
    p._sticky = false
    return static_cast<iTJSNativeInstance *>(p)
    // 无条件/default/null fallback；allocation 失败直接抛出且不返回对象
```

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/plugin/ncbind.hpp:118-148`，符号 `ncbInstanceAdaptor<T>`：第 120 行保持
  `public tTJSNativeInstance` 继承；第 125 行 constructor 按
  `_instance(0), _sticky(false)` 初始化；第 136-141 行字段顺序为 native pointer 后接
  sticky flag。对应 Android 伪代码第 2-5 行以及 `0x59ABE0..0x59ABF8`。
- `cpp/core/plugin/ncbind.hpp:228-231`，符号
  `ncbInstanceAdaptor<T>::CreateEmptyAdaptor()`：无参数 static callback 以普通 throwing
  `new AdaptorT()` 构造对象，并 `static_cast<iTJSNativeInstance *>` 返回；对应伪代码
  第 1-2、6-7 行和 Android `0x59ABD8..0x59AC00` 的完整入口。
- `cpp/core/tjs2/tjsNative.h:40-54`，符号 `tTJSNativeInstance`：保留
  `iTJSNativeInstance` 虚接口、默认 `Construct`、`Invalidate`、`Destruct` 和 virtual
  destructor；与 Android constructor 安装具有 Construct/Invalidate/Destruct/析构槽的
  adaptor vptr 源码结构一致。
- `cpp/core/plugin/ncbind.hpp:1843-1858`，符号
  `ncbRegistNativeClass<CLASS>::RegistBegin()`：第 1847 行选择同一
  `ncbInstanceAdaptor<NativeClassT>` specialization，第 1857 行把
  `AdaptorT::CreateEmptyAdaptor` 作为 callback 传给 plugin native-class constructor；
  对应 Android parent `0x59AAD8..0x59AAE4` 的唯一 data binding。
- `cpp/core/tjs2/tjsNative.h:245-277`，符号 `tTJSNativeClassForPlugin`：第 247 行定义
  无参数 create-instance callback 类型，第 266-271 行保存 callback，第 274-275 行在
  `CreateNativeInstance()` 中间接调用；说明本入口是 NCB registration callback，不是
  parent 或 vtable child 的直接 callee。
- `cpp/core/tjs2/tjsNative.cpp:323-333`，符号 `tTJSNativeClass::FuncCall()`：第 328 行
  经 virtual `CreateNativeInstance()` 取得本入口新建的 native instance，第 332 行把同一
  pointer 注册到 TJS object。该外层 consumer 只用于确认返回对象的所有权流，不属于本
  emitted 入口的内部步骤。
- `cpp/plugins/psbfile/main.cpp:751-755`，符号 `NCB_REGISTER_CLASS(PSBFile)`：真实
  实例化上述 `PSBFile` typed registration/template 路径；没有另写简化的 PSBFile 专用
  allocation wrapper。

逐行对应：伪代码第 2 行对应 `ncbind.hpp:229-230` 与 Android
`0x59ABE0..0x59ABE4`；第 3 行对应 `ncbind.hpp:120,125` 触发的 derived-object vptr
安装与 `0x59ABE8..0x59ABF4`；第 4-5 行对应 `ncbind.hpp:125,138-141` 与
`0x59ABF4..0x59ABF8`；第 6 行对应 `ncbind.hpp:229-231` 的 base-pointer cast 与
`0x59AC00`；第 7 行对应普通 throwing `new` 且函数内没有 guard/catch 的共同边界。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是无参数 typed static factory，普通 `new` 后内联执行 `ncbInstanceAdaptor<PSBFile>` constructor，再以 base-interface pointer 返回；本地保留同一 template specialization、public inheritance、独立 constructor 与 `CreateEmptyAdaptor` wrapper。ARM64 的 `0x18` 大小/字段偏移是 ABI 结果，不要求 wasm32 硬凑布局。 |
| 数据流 | `MATCH` | Android allocation pointer 同时作为 vptr/instance/sticky 写入基址并原样进入 X0 返回；默认值严格为 `instance=null`、`sticky=false`，没有输入或中间替代对象。本地 `new AdaptorT()`、member-initializer list 和 zero-offset public-base cast 保持同一数据流。 |
| 调用链 | `MATCH` | Android parent 只把 `@59ABD8` 绑定到 native-class callback 槽，运行期再间接调用；本入口自身只调用 allocation import，不 direct-call `@59AC04/@59AC0C/@59AD08`。本地 `RegistBegin → store proc → CreateNativeInstance → proc()` 保留相同间接层级，且没有把 registration/vtable/helper 关系伪装为直接调用。 |
| 对象生命周期 | `MATCH` | Android 在堆上创建一只 vptr 已安装、native pointer 为空、非 sticky 的 fresh adaptor，并把所有权交给 TJS native-instance 注册链；allocation 失败前无对象，成功后的固定初始化不抛。后续 Invalidate/Destruct 属于 vtable children。local `new AdaptorT()`、virtual base lifecycle 与外层 NativeInstanceSupport 注册保持相同所有权阶段。 |
| 内部容器实现 | `N/A` | 本入口没有 TJS Array、STL 或自定义容器；只分配一个固定字段的 adaptor object。allocator 内部状态和外层 native-instance registry 不属于此 emitted 入口。 |
| 边界行为 | `MATCH` | Android 无参数检查、条件、状态返回、nothrow/null fallback 或显式 padding 初始化；throwing allocation 失败直接传播，成功时固定清 null/false 并返回。本地普通 `new` 和 constructor defaults 完全一致；跨 ABI 的对象大小/padding 差异不是源码语义 GAP。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 10 指令 disasm、唯一 callback data xref、adaptor
vtable address-point 原始字节以及本地模板 specialization/consumer 可证明范围内，
`0x59ABD8` 与 `ncbInstanceAdaptor<PSBFile>::CreateEmptyAdaptor()` 六维一致。

## 确定 GAP

无。

证据边界：stripped 二进制未保留该 template specialization 的精确源码拼写，因此
canonical 名称继续使用 `_guess`；constructor 被优化内联进 factory，无法由二进制唯一证明
原始函数是否显式写出 cast 的 token。两项都不构成已证实行为差异。

## TASK_TREE 直接子节点与 cross-reference

- 尚待由本节点在阶段 B 派发的 canonical 直接子节点（本阶段未 spawn）：
  - `0x59AC04 PSBFile_ncbFinalizeEmptyCallback_guess [helper]`
  - `0x59AC0C PSBFile_ncbInstanceAdaptor_Invalidate_guess [vtable-member]`
  - `0x59AD08 PSBFile_ncbInstanceAdaptor_deletingDestructor_guess [vtable-member]`
- `@59AC04` 不在本 constructor 的 vtable 中，本入口也不调用它；它是 TASK_TREE 为同一
  NCB registration scaffolding 选择的 canonical `[helper]` child。Android parent
  `@59AA84` 在 `0x59AB48` 另有取其地址并注册 `finalize` 的 non-canonical data
  cross-reference，不得由 parent 或本节点为该关系重复创建第二个 agent。
- `@59AC0C` 与 `@59AD08` 位于本入口安装的 `off_1A0B598` vtable；这是
  `[vtable-member]` 关系，不是 direct call。`@59AC7C` 也在该 vtable 中，但其 canonical
  parent 已选为 `@59AD08 [lifecycle]`，因此这里只记录 cross-reference，不重复派发。
- `@59AA84` 是唯一 data-xref 来源和 canonical `[registration]` parent；本节点没有其他
  retained direct caller，也没有把 vtable sibling 或分类关系写成直接调用。

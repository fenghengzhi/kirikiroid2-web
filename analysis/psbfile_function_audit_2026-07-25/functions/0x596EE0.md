# `0x596EE0` — `PSBValueDispatch_CreateNew`

## 审计身份

- canonical 地址：`0x596EE0`
- canonical 名称：`PSBValueDispatch_CreateNew`
- IDA 当前标签：`PSBValueDispatch_CreateNew`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`；这是构造对象所安装 primary vtable 的成员归属，
  不是 `0x597AD4` 对本函数的直接调用。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596ee0`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `"PSBFile.dll"` 仅是 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x596EE0")`，并补充调用目标完整 `disasm`、`lookup_funcs`、
`xrefs_to` 以及 primary-vtable 相邻 qword 原始字节读取；相邻槽只用于确认当前入口归属，
不替其他函数作六维审计。

- fresh decompile 得到完整函数体：

  ```c
  __int64 PSBValueDispatch_CreateNew()
  {
    return 4294966294LL; /*0x596ee4*/
  }
  ```

  IDA 因全部接口参数未被读取而省略了形参，并把写入 `W0` 的 32-bit 值显示为
  64-bit 无符号常量；按 `tjs_error` 解释，`4294966294 == 0xFFFFFC16 == -1002`。
- fresh `lookup_funcs` 给出范围起点 `0x596EE0`、size=`0x8`；完整 disasm 总计两条
  指令：`0x596EE0 MOV W0, #0xFFFFFC16`、`0x596EE4 RET`。函数无栈帧、条件分支、
  内存访问、参数读取、结果指针写入、`BL/BLR` 或异常清理边。
- fresh `xrefs_to(0x596EE0)` 只返回 data xref `0x1A0B468`，没有静态 code xref。
  primary vtable 基址为 `0x1A0B3D8`，而 `0x1A0B468 - 0x1A0B3D8 = 0x90 = 18*8`；
  因此本入口是 slot 18。
- fresh 原始字节把 `0x1A0B458..0x1A0B47F` 解成连续五个 qword：
  `0x596EF0, 0x596EE8, 0x596EE0, 0x596ED8, 0x596ED0`，对应 primary
  `iTJSDispatch2` vtable slots 16..20。结合接口顺序，当前 data xref 精确归属于
  `CreateNew`，运行时入口是虚分派；“没有 code xref”不等于“不会被调用”。
- 本入口不是 NCB wrapper 或注册 callback；它是 `PSBValueDispatch` 对
  `iTJSDispatch2::CreateNew` 的 override，本体不调用任何 psbfile helper。

## Android 伪代码（2 行）

```text
CreateNew(flag, membername, hint, result, numparams, params, objthis):
    return tjs_error(0xFFFFFC16)  // -1002；无条件，全部参数均不读取
```

这两行覆盖全部条件、默认值和失败边界：Android 对任何参数组合都不解引用输入、不写
`result`，并直接返回 `TJS_E_NOTIMPL`；函数没有其他分支、fallback、callee 或异常路径。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:248-257`：`iTJSDispatch2::CreateNew` 声明完整七参数
  虚接口；其位置紧随 `IsValidByNum`，对应 Android primary vtable slot 18。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,91-93`：`PSBValueDispatch final` 直接继承
  `iTJSDispatch2`，并以相同签名显式声明 `CreateNew(... ) override`；没有用基类的
  member-sensitive 默认实现或 NCB wrapper 替代该槽。
- `cpp/plugins/psbfile/main.cpp:514-517`：本地
  `PSBValueDispatch::CreateNew` 的七个形参均未命名，函数体唯一语句为
  `return TJS_E_NOTIMPL;`，逐指令语义对应 Android 的“写 `W0` 后立即 `RET`”。
- `cpp/core/tjs2/tjsErrorDefs.h:23-25`：本地 `TJS_E_NOTIMPL` 定义为 `-1002`，其
  32-bit 补码正是 Android 立即数 `0xFFFFFC16`。

逐行对应：伪代码第 1 行由 Android vtable slot、`tjsInterface.h:248-257` 与
`PSBDispatch.h:91-93` 共同定位；第 2 行对应 `main.cpp:517` 和 Android
`0x596EE0..0x596EE4`。双方均没有隐藏的状态检查、参数/结果访问、对象操作、callee 或
清理路径。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一只独立的 8-byte emitted 入口，并由 `PSBValueDispatch` primary vtable slot 18 直接指向；本地同样以 `PSBValueDispatch` 的显式 `CreateNew` override 提供独立函数体，没有退回基类默认实现。`MATCH` 只覆盖二进制可证结构，不宣称恢复不可观察的参数名或排版。 |
| 数据流 | `MATCH` | Android 唯一数据流是把常量 `0xFFFFFC16` 写入返回寄存器；本地唯一语句返回数值相同的 `TJS_E_NOTIMPL`。双方均不读 `this`/形参、不写 `result`、不访问对象字段。 |
| 调用链 | `MATCH` | Android 通过 primary `iTJSDispatch2` vtable slot 18 虚分派到本入口，本体没有 direct/indirect callee；本地同样由接口 override 占据 `CreateNew` 槽并直接返回。canonical parent `@597AD4` 只安装所属 vptr，不直接调用本函数。 |
| 对象生命周期 | `N/A` | 本入口不构造、retain、release、销毁或修改对象，也不接管 `result`/`param` 的所有权；引用计数及析构由 sibling 入口单独负责。 |
| 内部容器实现 | `N/A` | 本入口不创建、访问或遍历 packed、TJS、STL 或其他容器。 |
| 边界行为 | `MATCH` | Android 与本地对 flag、名称、hint、result、参数数量/数组及 objthis 的所有取值均无条件返回 `-1002`；包括 null 指针在内的输入不会被本入口解引用，也没有异常、fallback 或副作用。 |

## 总判定

`ALIGNED`

本轮 fresh target decompile、完整两指令 disasm、唯一 vtable data xref、相邻 qword 槽序列
以及本地接口/override/错误码逐行对照共同证明：Android 当前可证明的 emitted 结构、常量
数据流、vtable 调用归属和全部边界均与本地一致。

## 确定 GAP

无。

证据边界：Android stripped/O3 二进制没有保留参数名等精确源码拼写，IDA 也因参数完全
未使用而省略伪签名；但 vtable 槽、接口顺序和完整机器码已独立闭合函数归属与行为。这不是
已证实偏差。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 直接子节点：无。本节点是 leaf，阶段 A 未 spawn 任何 subagent。
- canonical parent cross-reference：`@597AD4` 构造并安装包含本槽的 primary vptr；该
  关系严格为 `[vtable-member]`，不是 `[direct-call]`。
- fresh IDA 唯一 xref 是 vtable 数据项 `@1A0B468`；相邻
  `@596EF0/@596EE8/@596ED8/@596ED0` 只是 slots 16/17/19/20 的 sibling 入口，均不
  调用本函数，也不应由本节点重复派发。
- TASK_TREE 的非 canonical cross-reference 清单没有指向 `@596EE0` 的额外 DAG 边；
  本节点不创建任何重复 function agent。

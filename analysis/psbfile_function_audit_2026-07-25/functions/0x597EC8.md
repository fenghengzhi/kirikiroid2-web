# `0x597EC8` — `ncbClassInfo_PSBFile_IsSubClass_guess`

## 身份与归属

- canonical 地址：`0x597EC8`
- canonical 名称：`ncbClassInfo_PSBFile_IsSubClass_guess`
- 分组：C / typed NCB class-info helper
- TASK_TREE parent：`0x597F24 ncbClassInfo_PSBFile_InfoCtor_guess`，关系为
  `[helper]`。这是同一 `ncbClassInfo<PSBFile>` emitted surface 内的 canonical 归组边；fresh
  xref 没有 caller，parent 构造器也不调用本入口，因此该关系不是 `[direct-call]`。
- 审计 agent path：`/root/f_42cef8/f_597f24/f_597ec8`
- Android vtable / NCB / 调用链归属：本入口属于 `ncbClassInfo<PSBFile>` typed NCB
  class-info helper surface；它不是 TJS dispatch vtable 槽、native-instance vtable 槽、成员
  wrapper 或注册 callback。IDA 相邻 emitted 入口依次为 GetName、GetID、GetClassObject、
  IsSubClass、Set、Clear、InfoCtor；本报告只审计 IsSubClass，不替任何相邻入口下结论。
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cef8/f_597f24/f_597ec8` fresh 执行了
`decompile(0x597EC8)`；随后执行完整 `disasm(0x597EC8)`、`xrefs_to(0x597EC8)`、
`analyze_function(0x597EC8)` 和相邻函数范围查询复核。此次记录独立于任何旧报告：

- fresh decompile 当前 IDA 标签为 `PSBFile_ncbClassInfo_IsSubClass_guess`，函数范围为
  `0x597EC8..0x597ECF`，反编译体只有 `return 0;`。
- `0x597EC8: MOV W0, WZR`：无条件把返回寄存器低 32 位设为零。
- `0x597ECC: RET`：立即返回；完整函数只有这两条指令。
- `analyze_function` 记录函数大小 8 字节、1 个基本块、圈复杂度 1、零 caller、零 callee、
  零字符串引用和零非平凡常量；`xrefs_to` 同样没有记录到 retained xref。
- 相邻函数查询把 `0x597EC8` 置于 `0x597E98..0x597F24` 的 class-info emitted surface 中；
  这是 NCB 归属上下文，不用于替相邻函数作审计。
- Android 本体没有参数读取、对象/全局状态读取、条件分支、默认值选择、分配、引用计数、
  异常处理或显式失败返回。stripped/O3 二进制不能唯一证明模板标识符的原始拼写，因此
  canonical 名称保留 `_guess`。

## Android 伪代码（4 行）

```text
IsSubClass():
    // 无输入、条件、状态访问或失败路径
    return false
// false 是唯一返回值；没有替代默认值或错误边界
```

## 本地精确对照

- 默认 subclass trait：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 67–69 行，符号
  `ncbSubClassCheck<T>::IsSubClass`，主模板常量为 `false`。
- class-info helper：同文件第 73–85 行，符号 `ncbClassInfo<T>` 与
  `ncbClassInfo<T>::IsSubClass()`；第 85 行无参数地返回
  `ncbSubClassCheck<NativeClassT>::IsSubClass`。
- 显式实例化入口：同文件第 2160–2164 行，宏 `NCB_REGISTER_CLASS_COMMON` 的第 2161 行
  `template struct ncbClassInfo<cls>;`；主类宏路径位于第 2169–2172 行。只有 subclass 宏
  才会在第 2174–2182 行把 trait 专门化为 `true`。
- PSBFile 类型别名与展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 16 行
  `using PSB::PSBFile`，以及第 1094 行 `NCB_REGISTER_CLASS(PSBFile)`。这是主类注册的正面
  证据，因而该实例采用第 69 行的默认 `false` trait，而不是 subclass 的 `true` 特化。

逐项对应：

1. Android 入口没有参数；本地 `IsSubClass()` 同样是无参数 static helper。
2. Android `MOV W0, WZR` 产生唯一零/false 返回值；本地默认 trait 是编译期常量
   `false`，第 85 行直接返回该常量。
3. Android 在返回前不读 class-info 四字段或任何全局地址；本地表达式只读取编译期枚举
   常量，不消费 `_info` 状态。
4. Android 没有 helper call、对象生命周期动作、容器或失败处理；本地单表达式 helper
   同样不引入这些步骤。

## 六维判定

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是无参数、单常量返回的 class-info emitted helper；本地 `ncbClassInfo<T>::IsSubClass()` 保留相同静态 helper 形态，且 PSBFile 的主类注册正向选择默认 false trait。stripped 二进制不能证明模板标识符的原始拼写，`MATCH` 只覆盖可证结构。 |
| 数据流 | `MATCH` | Android 只执行 `W0 := 0` 后返回，不读取输入或状态；本地直接返回编译期 `false`，没有中间状态或替代值。 |
| 调用链 | `MATCH` | Android 本体零 callee，fresh xref/analyze 也没有 retained caller；本地 helper 体不调用任何函数。canonical parent 是 `[helper]` 归组而非 direct call，两侧均未虚构调用边。 |
| 对象生命周期 | `N/A` | 本入口没有 `this`、对象/静态状态访问、分配、引用计数、构造或析构动作；本地 helper 也只返回编译期 trait。 |
| 内部容器实现 | `N/A` | 本入口不创建、访问或修改任何容器；本地对应单常量表达式同样不涉及容器。 |
| 边界行为 | `MATCH` | Android 没有输入或条件边界：所有调用都立即返回 false，且不存在错误码、异常或 fallback；本地行为完全相同。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整两指令反汇编、xref/函数分析摘要与本地精确源码可证明
范围内，本地 `ncbClassInfo<PSBFile>::IsSubClass()` 与 `0x597EC8` 的六维行为一致。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 本节点没有 TASK_TREE canonical 直接子节点；阶段 A 未 spawn child。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x597EC8` 条目。
- `0x597F24` 只是本节点的 canonical `[helper]` parent，不是 retained caller；不得把该关系
  伪装成 direct call，也不为 parent 或相邻 class-info helper 创建重复 agent。

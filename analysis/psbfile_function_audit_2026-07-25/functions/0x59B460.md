# `0x59B460` — `PSBFile_rootProperty_deletingDestructor_guess`

## 审计身份

- canonical 地址：`0x59B460`
- canonical 名称：`PSBFile_rootProperty_deletingDestructor_guess`
- IDA 当前标签：`PSBFile_rootProperty_deletingDestructor_guess`。Android stripped
  二进制没有保留该模板专门化的精确 C++ 符号，故名称保留 `_guess`。
- 分组：F（typed NCB wrapper、adaptor 与反注册）
- TASK_TREE parent：`0x59B28C PSBFile_root_PropGet_guess`
- parent 关系：`[vtable-member]`。两者是同一 `root` Property wrapper 的 vtable
  sibling/lifecycle 归属；`0x59B28C` 不直接调用本入口，不能把该边解释为
  `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b460`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 仅可视为 NCB 模块名字面量，本入口
  不访问该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由
`/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b460` fresh 调用
IDA MCP `decompile(addr="0x59B460")`；随后调用
`disasm(addr="0x59B460", include_total=true, max_instructions=64)`、目标与 vtable
xref、`0x1A0B6F0..0x1A0B7F8` vtable 原始字节读取，并仅把
`@597F38/@9F6D74/@9FBBA4` 作为构造、Release 和基类析构上下文。本地址此前没有报告，
本报告不是沿用旧结论。

- fresh decompile 精确得到：
  `sub_9FBBA4(a1); operator delete(a1);`。完整 disasm 覆盖
  `0x59B460..0x59B480`，IDA size 为 `0x24`，共 9 条指令；`X19` 在第一调用前保存
  原始 `this`，随后同一个指针被送入 `operator delete(void*)`。
- `xrefs_to(0x59B460)` 只返回一个 data xref：root Property 主 vtable 的
  `0x1A0B7E8`；没有任何 direct code xref。该 address-point 为 `0x1A0B6F0`，同一
  qword 表内还包含 `PropGet @0x1A0B710 -> 0x59B28C`、
  `PropSet @0x1A0B720 -> 0x59B378`、complete destructor
  `@0x1A0B7E0 -> 0x9FBBA4`、deleting destructor
  `@0x1A0B7E8 -> 0x59B460` 和 `GetFlags @0x1A0B7F8 -> 0x59B484`。这证明本入口是
  同一 property wrapper 的删除析构槽，而不是 getter-invoker 的另一入口。
- 构造上下文 `@597F38:0x597FD0..0x598020` 分配 `0x50` 字节 wrapper，写入
  `nitProperty`、`L"Property"`、getter `@5981F8`、null setter 与 vptr
  `0x1A0B6F0`；随后 `0x598030..0x598038` 以字面名 `L"root"` 注册该 item。
  `xrefs_to(0x1A0B6F0)` 唯一 code/data 使用点是 `@597F38:0x598014` 的 vptr
  构造。此段仅作为 wrapper 类型归属证据，不替 `@597F38` 下审计结论。
- generic `tTJSDispatch` Release 上下文 `@9F6D74` 在 refcount 为 1、完成
  `BeforeDestruction` gate 且 refcount 仍为 1 时，通过 `vptr+0xF8` 间接调用删除析构；
  对本 vtable，`0x1A0B6F0+0xF8 == 0x1A0B7E8`，正好进入当前函数。由此可证正常
  生命周期入口是 vdispatch，不存在 parent `@59B28C` 的 direct call。
- 被调用的 complete/base destructor context `@9FBBA4` 先读取
  `BeforeDestructionCalled`，把 vptr 恢复为基类 table；若 flag 尚未置位则置 1。
  基类 `BeforeDestruction` 为空，故 O3 后没有额外 call。该 helper 是通用 TJS 基类
  析构，不在 114 个 canonical psbfile 地址中；本报告只用它解释当前函数的 direct
  callee，不为它建立函数审计或 agent。

## Android 伪代码（6 行）

```text
rootPropertyDeletingDestructor(self):
    called = self.BeforeDestructionCalled
    self.vptr = tTJSDispatch_base_vtable       // complete/base dtor @9FBBA4
    if called == false: self.BeforeDestructionCalled = true  // base hook 为空
    operator_delete(self)                      // 始终使用原始同一指针
    return void  // 无默认返回值/错误码/null guard；非法 self 不受支持
```

该伪代码覆盖目标入口唯一的顺序、其 direct complete/base destructor 中的唯一条件分支、
无返回默认值、无错误码、无 null 检查，以及删除必须在正常 base destructor 返回后发生的
边界。目标本体没有其他业务分支；`operator delete` 是 tail branch。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/main.cpp:751-755`，`NCB_REGISTER_CLASS(PSBFile)`：第 1096 行
  `Property(TJS_W("root"), &PSBFile::GetRootDispatch, 0)` 声明与 Android 构造点一致的
  typed 只读 Property wrapper；getter 非空、setter 为 null。
- `cpp/core/plugin/ncbind.hpp:1720-1732`，`ncbRegistClass::Property`：第 1723 行把上述
  声明实例化为 `ncbNativeClassProperty<PropertyCommand<PSBFile,...>>::Create(...)`；
  `Create` 分配的 dispatch 对象正是本删除析构所管理的动态类型。
- `cpp/core/plugin/ncbind.hpp:1441-1450,1485-1495`，
  `ncbNativeClassProperty<PropCommandT>`：类直接继承
  `ncbNativeClassMethodBase`，只增加两个 typed pointer-to-member `_getter/_setter`，
  且没有声明自定义 destructor。二者均为平凡字段，因此隐式 derived destructor 不需
  释放额外资源；Android `@59B460` 同样只调用基类 complete destructor 后释放对象。
- `cpp/core/plugin/ncbind.hpp:849-868`，`ncbNativeClassMethodBase`：继续直接继承
  `tTJSDispatch`，保存 property type/name 和嵌入的 `_imethod`。本目标没有独立清理这些
  非拥有/平凡字段，符合 Android 没有额外 derived-member destructor call 的 9 指令形态。
- `cpp/core/tjs2/tjsObject.h:85-98`，`tTJSDispatch`：第 86 行定义空的虚
  `BeforeDestruction()`，第 88 行保存 `BeforeDestructionCalled`，第 97 行声明 virtual
  destructor。这一 virtual destructor 使编译器为实际
  `ncbNativeClassProperty<...>` 动态类型生成 deleting-destructor vtable entry。
- `cpp/core/tjs2/tjsObject.cpp:94-99`，`tTJSDispatch::~tTJSDispatch`：若 flag 尚未置位，
  先置位再调用空的 `BeforeDestruction()`；Android `@9FBBA4` 在恢复基类 vptr 后执行相同
  gate，空 hook 被优化掉。
- `cpp/core/tjs2/tjsObject.cpp:110-129`，`tTJSDispatch::Release`：在 refcount 为 1 时先
  执行同一 destruction gate，再由第 125 行 `delete this` 触发动态类型的 virtual
  deleting destructor，返回 0；对应 Android `@9F6D74` 的 `vptr+0xF8` 间接调用和当前
  `@59B460 -> @9FBBA4 -> operator delete` 链。

逐行对应：伪代码第 1 行对应 local virtual deleting destructor 的编译器生成入口；第
2-4 行对应 `tjsObject.cpp:94-99` 与 Android `@9FBBA4`；第 5 行对应 C++ `delete this`
生成的 derived deleting-destructor 尾部 `operator delete`；第 6 行对应 virtual
destructor 的 `void` 语义。源文件不手写 ARM64 deleting-destructor ABI 名称或 vtable
偏移，这是同一 C++ 源结构在不同 ABI 下由编译器生成的正确形态。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 root typed `ncbNativeClassProperty` 动态类型的独立 deleting-destructor vslot：derived 层没有待析构资源，只进入 `tTJSDispatch` complete/base destructor，再进入 deallocation。当前本地保持相同 `ncbNativeClassProperty -> ncbNativeClassMethodBase -> tTJSDispatch` 继承层级、平凡 getter/setter 字段与 virtual base destructor；删除析构由编译器生成，没有用手写 wrapper 或字节布局替代。 |
| 数据流 | `MATCH` | Android 把输入 `this` 保存在 `X19`，先把同一指针传给 `@9FBBA4`，再恢复同一指针传给 `operator delete`；helper 只流动 vptr 与 destruction flag。local virtual destructor/`delete this` 保持相同指针、flag gate 和 base-before-deallocate 顺序，没有复制、转换或清空 PSBFile getter/setter 数据。 |
| 调用链 | `MATCH` | Android `@597F38` 构造并注册 wrapper；最终 Release-to-zero 经 generic `@9F6D74` 的 `vptr+0xF8` vdispatch 进入当前槽，再 direct-call base complete destructor 并 tail-call delete。local `Property -> ncbNativeClassProperty`、`tTJSDispatch::Release -> delete this -> implicit derived dtor -> base dtor -> operator delete` 对应一致。canonical parent `@59B28C` 仅是同表 PropGet sibling，不是 caller。 |
| 对象生命周期 | `MATCH` | Android 仅在 wrapper 的 final virtual deletion 路径调用本入口；base gate 保证 `BeforeDestruction` 至多一次，随后释放完整 wrapper allocation。local `Release` 在 refcount==1 时先保证 destruction gate、再次确认 refcount 后 `delete this`，derived 平凡成员无需额外清理，base destructor 重查 gate 后释放，生命周期顺序一致。 |
| 内部容器实现 | `N/A` | 本入口不创建、遍历、修改或销毁 STL/KiriKiri 容器、TJS Array、注册表或 autoreg 链；它只析构一个 wrapper 对象并释放其 allocation。 |
| 边界行为 | `MATCH` | Android 本体无条件调用 base complete destructor，正常返回后无条件 tail-call `operator delete`；没有 null guard、返回值、默认值或错误码，非法/非对象 `self` 不受支持。base flag 已置位时不重复 hook，未置位时置 1；local virtual destructor 与 Release gate 完整复刻这些边界，未增加“更安全”的空指针分支。 |

## 总判定

`ALIGNED`

本轮 fresh Android decompile、完整 9 指令 disasm、唯一 vtable data xref、root Property
vtable 原始 qword、wrapper 构造点以及通用 Release/base-destructor context，与当前本地
模板继承和 virtual destruction 路径逐项一致。

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能恢复该模板专门化的精确原始标识符拼写；Itanium/ARM64
ABI 生成的 complete/deleting destructor 双入口、对象大小和 vtable 字节偏移也不是应在
wasm32 源码中硬凑的 token。因此 canonical 名称保留 `_guess`，但这不构成已证实的六维
差异。

## TASK_TREE 直接子节点与 cross-reference

- TASK_TREE 中本节点没有 canonical 直接子节点；阶段 A 未 spawn 任何 agent。
- canonical parent `@59B28C` 是同一 root Property wrapper 的 `PropGet` sibling；
  `@59B378` 是同表 `PropSet` sibling，`@59B484` 是同表 `GetFlags` sibling。它们与当前
  节点的关系均不是 direct call，不得从本节点重复派发。
- `@597F38` 是 wrapper 构造/registration context，`@59AEEC` 是外层注册链；
  `@9F6D74` 是通用 TJS Release context，`@9FBBA4` 是基类 complete destructor，
  `operator delete` 是外部运行库 callee。后三者不属于 114 个 canonical psbfile 地址，
  只能记录为 context/cross-reference，不能创建额外函数 agent。

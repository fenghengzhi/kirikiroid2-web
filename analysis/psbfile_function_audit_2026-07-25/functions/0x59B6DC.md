# `0x59B6DC` `PSBFile_loadMethod_deletingDestructor_guess` 六维复原审计

## 审计元数据

- 审计日期：`2026-07-25`
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b570/f_59b6dc`
- canonical 地址：`0x59B6DC`
- canonical 名称：`PSBFile_loadMethod_deletingDestructor_guess`
- 分组：F（typed NCB 自动注册与 wrapper 尾链）
- TASK_TREE canonical parent：`0x59B570 PSBFile_load_FuncCall_guess`
- canonical parent 边：`[vtable-member]`
- 直接子节点：无
- 旧报告状态：本轮开始时文件不存在；本文由本节点在新会话中 fresh 反编译后创建。

`[vtable-member]` 仅表示同一个 typed `load` method wrapper 的 vtable 归属。parent
`0x59B570` **不直接调用**本函数；`0x59B570`、本函数及 `0x59B700` 是同一 wrapper
的 sibling vslots。

## Android 归属与本轮 fresh 证据

唯一权威来源是 Android kirikiroid2 `libkrkr2.so` 内嵌实现。本轮未查看、引用或从
外部 `psbfile.dll` 推导。

### Fresh decompile 记录

本轮对 canonical 地址 fresh 调用 IDA MCP `decompile(0x59B6DC)`，结果为：

```c
// Typed PSBFile load Method deleting destructor; method vtable slot @0x1A0B908.
void __fastcall PSBFile_loadMethod_deletingDestructor_guess(
    PSBFile_loadMethodWrapper_arm64 *self)
{
  sub_9FBBA4((__int64)self); /* 0x59B6EC */
  operator delete(self);     /* 0x59B6FC */
}
```

补充证据：

- `disasm(0x59B6DC)` 返回完整的 9 条指令：`0x59B6E8` 把原始 `this` 保存到
  `X19`，`0x59B6EC` 调用 `sub_9FBBA4`，随后在 `0x59B6F4` 把同一指针放回
  `X0`，`0x59B6FC` 以无条件尾跳转进入 `operator delete(void *)`。
- `xref_query(0x59B6DC)` 只有一条 data xref：vtable 项 `0x1A0B908 -> 0x59B6DC`。
- 同表 sibling 的 data xref 分别是 `0x1A0B820 -> 0x59B570`（`FuncCall`）与
  `0x1A0B918 -> 0x59B700`（`GetFlags`）。这证明 sibling/vslot 归属，不证明、
  也不存在三者之间的直接调用。
- 注册上下文 `0x597F38` 的反汇编在 `0x598044` 分配 `0x40` 字节 wrapper，
  `0x598054..0x59808C` 取 `qword_1A0B800 + 0x10`（vtable address point
  `0x1A0B810`）写入对象，`0x598090` 保存 `PSBFile_Load` method pointer，随后以
  字面量 `load` 交给注册链。这把 `0x1A0B908` 的析构槽归到 typed `load` wrapper。
- 仅为解释本函数直接 callee，本轮 fresh 反编译 `sub_9FBBA4@0x9FBBA4`：它把
  vptr 恢复为基类表，读取 `this+8` 的 `BeforeDestructionCalled`，仅在原值为
  false 时置为 true，然后返回。本报告不替该上下文函数作独立六维审计。

## Android 伪代码（覆盖全部边界，4 行）

```text
deleting_destructor(self):
    sub_9FBBA4(self)             // 同一 self；本函数内无条件分支
    operator_delete_void(self)   // 前一步正常返回后无条件执行，且为 tail call
    return void                  // 无默认值、null 检查、替代失败分支或本地 catch
```

## 本地实现与逐行对照

本地对应的是模板生成的
`ncbNativeClassMethod<InvokeCommand<PSBFile, ..., ivtNormal>>` wrapper 的隐式虚析构，
不是 `PSBFile` native holder 自身的析构。

精确位置：

- `cpp/plugins/psbfile/main.cpp:751-755`，`NCB_REGISTER_CLASS(PSBFile)` 中
  `Method(TJS_W("load"), &PSBFile::Load)` 建立 typed `load` wrapper。
- `cpp/plugins/psbfile/PSBRawFile.h:106`，被绑定的 native method 是
  `PSBFile::Load(tTJSVariant)`。
- `cpp/core/plugin/ncbind.hpp:1715-1718`，`ncbRegistClass::Method` 选择
  `ncbNativeClassMethod<InvokeCommand<...>>::Create`。
- `cpp/core/plugin/ncbind.hpp:1326-1353`，`ncbNativeClassMethod` 继承
  `ncbNativeClassMethodBase`、通过普通 `new ThisClassT` 创建，并只增加平凡的
  method pointer 成员 `_method`；类未引入自定义析构。
- `cpp/core/plugin/ncbind.hpp:849-868`，wrapper 基类继承 `tTJSDispatch`。
- `cpp/core/tjs2/tjsObject.h:85-97`，`tTJSDispatch` 声明虚析构且包含
  `BeforeDestructionCalled`。
- `cpp/core/tjs2/tjsObject.cpp:94-99`，基类析构仅在 flag 尚未置位时置位并调用空的
  `BeforeDestruction()`；对于该 wrapper，优化后的 Android 形状就是
  `sub_9FBBA4` 所见的 vptr 恢复与条件置位。

逐行对应：

| Android | 本地源码复刻 |
| --- | --- |
| `0x59B6E8` 保存 `this` | C++ deleting-destructor ABI 为后续析构和 deallocation 保留同一对象指针；不是额外业务数据流。 |
| `0x59B6EC sub_9FBBA4(this)` | `ncbNativeClassMethod` 的隐式析构沿 `ncbNativeClassMethodBase -> tTJSDispatch` 执行；`tTJSDispatch::~tTJSDispatch` 位于 `tjsObject.cpp:94-99`。平凡 `_method` 无额外销毁步骤。 |
| `0x59B6F4` 恢复原 `this` | 本地隐式 deleting destructor 同样把原始 allocation pointer 交给 deallocator。 |
| `0x59B6FC operator delete(void*)` | `Create` 在 `ncbind.hpp:1350` 用普通 `new ThisClassT`；虚 deleting destructor 正常结束后以匹配的普通 `operator delete` 回收 wrapper storage。 |

## 六维审计

| 维度 | 状态 | 证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 typed `load` method wrapper 的 compiler-emitted deleting destructor；本地保持同一 `ncbNativeClassMethod -> ncbNativeClassMethodBase -> tTJSDispatch` 继承结构、隐式虚析构与普通 `new` 创建。stripped 二进制不能证明原始析构 token 的拼写，但 README 的 `MATCH` 仅覆盖可证明结构。 |
| 数据流 | `MATCH` | Android 将入口 `this` 原样传给非 deleting 析构，再把同一指针原样传给 deallocator；本地生成析构没有额外 owning member，也遵循相同指针流。 |
| 调用链 | `MATCH` | Android 链为 deleting-destructor vdispatch `0x1A0B908 -> 0x59B6DC -> 0x9FBBA4 -> operator delete`；本地虚析构链由 `tTJSDispatch` 的虚析构和派生模板隐式析构形成。canonical parent 仅为 `[vtable-member]`，不是 direct call。 |
| 对象生命周期 | `MATCH` | Android 先完成基类析构语义，再释放 wrapper allocation；本地先执行派生/基类析构，再由 deleting destructor 释放 `new ThisClassT` 的 storage。`_method` 为平凡 method pointer，不产生额外 release。 |
| 内部容器实现 | `N/A` | 本入口只负责 wrapper 析构与 deallocation，不读取、构造或销毁任何容器。 |
| 边界行为 | `MATCH` | Android 无 null 检查、无参数/状态分支、无默认返回值、无本地 catch；在 `sub_9FBBA4` 正常返回后无条件使用 unsized `operator delete(void*)`。本地默认虚析构具有相同源码级边界；平台 ABI 的具体 vtable 偏移不属于需硬凑的源码布局。 |

## 总判定

`ALIGNED`

在本轮 Android 证据可证明的范围内，本地 typed `load` method wrapper 的析构结构、
指针数据流、虚调用归属、对象销毁顺序和边界行为均已对齐。

## 确定 GAP

无。

## Cross-reference（非 canonical，不派发）

- `0x59B570`：同一 wrapper 的 `FuncCall` sibling vslot（vtable 项 `0x1A0B820`）；
  它是本节点 TASK_TREE parent，但不直接调用本函数。
- `0x59B700`：同一 wrapper 的 `GetFlags` sibling vslot（vtable 项 `0x1A0B918`）；
  不直接调用本函数。
- `0x597F38` / `0x59AEEC`：wrapper 构造与 NCB 注册上下文；不是本函数的直接 caller，
  且不改变本节点无 children 的 TASK_TREE 事实。
- `0x9FBBA4`：本函数唯一业务外直接 callee（基类/非 deleting 析构上下文），不属于
  psbfile 114-address MANIFEST，故不创建 function agent。

TASK_TREE 尚待派发的直接子节点列表：空。本节点未调用 `spawn_agent`。

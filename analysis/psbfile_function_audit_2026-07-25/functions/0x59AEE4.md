# `0x59AEE4` — `PSBFile_ncbDummyConstructorNotImpl_guess`

## 节点身份

- 审计日期：`2026-07-25`
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_59ad84/f_59aee4`
- 分组：`F`
- canonical 地址：`0x59AEE4`
- canonical 名称：`PSBFile_ncbDummyConstructorNotImpl_guess`
- TASK_TREE parent：`0x59AD84` `PSBFile_RegistEnd_guess`
- parent 关系：`[registration]`
- TASK_TREE 直接子节点：无
- 名称证据边界：Android 二进制未保留该 callback 的精确源码标识符；canonical 名称因此保留 `_guess`。本地 `NotImplCallback` 只作源码对照，不反向命名 Android 函数。

## Android 归属与本轮 fresh 取证

本轮新会话对 Android kirikiroid2 `libkrkr2.so` 内嵌实现执行了以下 fresh IDA MCP 查询：

1. `decompile(0x59AEE4)`：函数体仅无条件返回 `4294966294`，即低 32 位 `0xFFFFFC16 == -1002`。
2. `disasm(0x59AEE4)`：完整函数只有 `MOV W0, #0xFFFFFC16`（`0x59AEE4`）和 `RET`（`0x59AEE8`），共两条指令；没有参数读取、分支、写回或调用。
3. `xrefs_to(0x59AEE4)`：仅有一处 xref，位于 `0x59ADB4`，类型为 `data`，归属函数 `0x59AD84`；无其他 xref。
4. 为确认边类型，fresh contextual `decompile(0x59AD84)`：`0x59ADA8` 在 `!_hasCtor` 时取本 callback 地址，`0x59ADBC` 交给 `TJSCreateNativeClassMethod`，随后于 `0x59ADD8` 注册包装对象。该路径没有直接调用 `0x59AEE4`。

因此本节点属于 `ncbRegistNativeClass<PSBFile>` 的缺省/错误构造 callback。它不是 vtable member；其地址被 NCB 注册路径包装成 native-class method，只有包装对象日后被调用时才会间接进入本 callback。`0x59AD84 -> 0x59AEE4` 必须记为 `[registration]`，不能记为 `[direct-call]`。

## Android 伪代码（不超过 10 行）

```cpp
tjs_error DummyConstructorCallback(result, numparams, params, objthis) {
    // 所有实参均忽略；无条件、无默认值计算、无输出写回。
    return TJS_E_NOTIMPL; // -1002；唯一失败边界
}
```

## 本地实现与逐行对照

本地精确位置：

- `cpp/core/plugin/ncbind.hpp:1843-1848`：`ncbRegistNativeClass<CLASS>` 模板及其 adaptor 归属。
- `cpp/core/plugin/ncbind.hpp:1947-1948`：`ncbRegistNativeClass<CLASS>::NotImplCallback(...)`，四个 callback 参数均未使用并直接返回 `TJS_E_NOTIMPL`。
- `cpp/core/plugin/ncbind.hpp:1951-1954`：`_AddDummyConstructor()` 在 `!_hasCtor` 时经 `TJSCreateNativeClassMethod(NotImplCallback)` 注册该 callback。
- `cpp/core/plugin/ncbind.hpp:1890-1892`：`RegistEnd()` 调用 `_AddDummyConstructor()`。
- `cpp/core/tjs2/tjsNative.h:62-65`：`tTJSNativeClassMethodCallback` 的四参数签名。
- `cpp/core/tjs2/tjsErrorDefs.h:23-24`：`TJS_E_NOTIMPL` 明确定义为 `-1002`。
- `cpp/plugins/psbfile/main.cpp:751-755`：`NCB_REGISTER_CLASS(PSBFile)` 使上述模板路径针对 `PSBFile` 实例化。

逐行映射：

| Android 证据 | 本地源码 | 对照 |
| --- | --- | --- |
| `0x59AEE4: MOV W0,#0xFFFFFC16` | `ncbind.hpp:1948` 返回 `TJS_E_NOTIMPL`；`tjsErrorDefs.h:24` 为 `-1002` | 返回值逐位一致 |
| 函数没有读取任何参数 | `ncbind.hpp:1948` 的四个参数均无名称、无使用 | 数据流一致 |
| `0x59AEE8: RET`，无其他块、调用或写回 | `ncbind.hpp:1948` 单表达式函数体 | 控制流与副作用一致 |
| `0x59ADB4` 仅取地址，`0x59ADBC` 创建 wrapper | `ncbind.hpp:1951-1954` 条件取 `NotImplCallback` 并传给 `TJSCreateNativeClassMethod` | 注册调用链一致；不是 direct-call |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是独立 emitted 的单返回 callback；本地为 `ncbRegistNativeClass<CLASS>` 内同形静态 `NotImplCallback`，并由 `PSBFile` 注册宏实例化。精确原始标识符不可见，但不影响当前可证明结构的一致性。 |
| 数据流 | `MATCH` | Android 不读取 callback 的四个 ABI 实参，只把 `-1002` 写入返回寄存器；本地同样忽略全部参数并返回数值相同的 `TJS_E_NOTIMPL`。 |
| 调用链 | `MATCH` | 唯一 Android xref 是 `0x59ADB4` 的地址获取；`0x59ADBC` 包装、`0x59ADD8` 注册。对应本地 `RegistEnd -> _AddDummyConstructor -> TJSCreateNativeClassMethod(NotImplCallback) -> TJSNativeClassRegisterNCM`，parent 边正确为 `[registration]`。 |
| 对象生命周期 | `N/A` | callback 本体不分配、不析构、不 AddRef/Release，也不接管传入指针；wrapper 的创建与注册生命周期属于调用上下文，不是本函数体的生命周期行为。 |
| 内部容器实现 | `N/A` | 两指令函数不访问任何容器或对象字段。 |
| 边界行为 | `MATCH` | 对任意 `result/numparams/params/objthis`（包括空指针和任意参数数）均不解引用、不写回并无条件返回 `TJS_E_NOTIMPL(-1002)`；无成功分支、默认值或异常路径。本地完全相同。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

## Cross-reference

- 唯一 Android 关系为 `@59AD84` 内 `0x59ADB4` 对本 callback 的 `data` xref，并经 native-class method wrapper 注册；它就是 canonical `[registration]` parent 边，不是直接调用。
- TASK_TREE 未列出本节点的非 canonical cross-reference，本轮全量 `xrefs_to` 也未发现其他引用；不得为本节点重复派发 agent。
- 直接子节点：无。

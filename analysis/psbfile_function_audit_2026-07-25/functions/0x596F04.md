# `0x596F04` — `PSBValueDispatch_InvalidateByNum`

## 节点身份

- 审计日期：`2026-07-25`
- 审计 agent path：`/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596f04`
- 分组：`A`
- canonical 地址：`0x596F04`
- canonical 名称：`PSBValueDispatch_InvalidateByNum`
- TASK_TREE parent：`0x597AD4` `PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`
- TASK_TREE 直接子节点：无
- 名称证据边界：stripped Android 二进制没有保留该方法的原始源码拼写；本报告沿用 TASK_TREE canonical 名称。归属由 `PSBValueDispatch` 主 vtable 的 vslot 15、目标函数的唯一数据 xref 及 parent 安装该 vtable 的构造形状共同确定；本地标识符只作复原对照，不能反向命名二进制。

## Android 归属与本轮 fresh 取证

本轮新会话对 Android kirikiroid2 `libkrkr2.so` 内嵌实现执行了以下 fresh IDA MCP 查询：

1. `decompile(0x596F04)`：fresh 结果是无参恢复签名下的单返回体，唯一语句为 `return 4294966294LL`；其低 32 位是 `0xFFFFFC16`，按 `tjs_error` 解释为 `-1002`。
2. `disasm(0x596F04)`：完整函数仅有 `MOV W0, #0xFFFFFC16`（`0x596F04`）和 `RET`（`0x596F08`），共 2 条指令；没有参数读取、条件分支、调用、内存访问或输出写回。
3. `xrefs_to(0x596F04)`：唯一引用是数据项 `0x1A0B450`；没有代码直接调用引用。
4. `get_bytes(0x1A0B3D8, 256)`：主 vtable address point 自 `0x1A0B3D8` 起按 8 字节函数指针排列；`0x1A0B450 = 0x1A0B3D8 + 15 * 8` 的值为 `0x596F04`，故本函数是 zero-based vslot 15。
5. contextual `decompile(0x597AD4)`：parent 构造形状在 `0x597AE4..0x597AF0` 将 `off_1A0B3D8` 写入对象的 dispatch vptr；它安装虚表但不直接调用本函数，因此 TASK_TREE 的 `[vtable-member]` 边准确。

本节点归属于 `PSBValueDispatch` 的 `iTJSDispatch2` 主接口 vtable，不是 NCB wrapper、注册 callback 或 secondary `iTJSNativeInstance` 槽。运行期只能经 vslot 15 间接分派到该入口；函数体自身不接触对象状态。

## Android 伪代码（不超过 10 行）

```cpp
tjs_error PSBValueDispatch_InvalidateByNum(flag, num, objthis) {
    // 所有实参均忽略；不读取 this，也没有输出参数。
    return TJS_E_NOTIMPL; // -1002；无条件、成功或其他失败分支
}
```

## 本地实现与逐行对照

本地精确位置：

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch` 直接实现 `iTJSDispatch2`，形成该节点所属的主 dispatch vtable。
- `cpp/plugins/psbfile/PSBDispatch.h:83-89`：在 `Invalidate` 后、`IsValidByNum` 前声明独立 `InvalidateByNum` 覆写，虚方法次序对应 Android vslot 15。
- `cpp/plugins/psbfile/main.cpp:505-508`：`PSBValueDispatch::InvalidateByNum(...)` 的独立定义；三个形参均无名称，函数体仅返回 `TJS_E_NOTIMPL`。
- `cpp/core/tjs2/tjsInterface.h:217-229`：`iTJSDispatch2` 中 `Invalidate`、`InvalidateByNum` 的接口次序和 `flag/num/objthis` ABI。
- `cpp/core/tjs2/tjsTypes.h:57`：`tjs_error` 是 32 位有符号 `tjs_int32`。
- `cpp/core/tjs2/tjsErrorDefs.h:23-24`：`TJS_E_NOTIMPL` 定义为 `-1002`。

逐项映射：

| Android 证据 | 本地源码 | 对照 |
| --- | --- | --- |
| `0x1A0B450 -> 0x596F04`，即主 vtable address point 的 vslot 15 | `PSBDispatch.h:83-89` 在相同接口次序独立覆写 `InvalidateByNum` | 主接口虚槽结构与次序一致 |
| `0x596F04: MOV W0,#0xFFFFFC16` | `main.cpp:507` 返回 `TJS_E_NOTIMPL`；`tjsErrorDefs.h:24` 为 `-1002` | 32 位返回值逐位一致 |
| 无实参寄存器读取、对象字段访问或内存写入 | `main.cpp:505-506` 的全部形参均省略名称且未使用 | 输入、对象状态与副作用数据流一致 |
| `0x596F08: RET`，无分支或调用 | `main.cpp:505-508` 是独立单返回方法 | 控制流及失败边界一致 |
| `0x597AD4` 只安装 `off_1A0B3D8` | 本地继承并覆写 `iTJSDispatch2`，由 C++ 虚表承载该方法 | parent 是 vtable 归属，不是 direct-call |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一个独立 emitted 的两指令 vslot 入口；本地同样以 `PSBValueDispatch::InvalidateByNum` 独立覆写并用单返回函数体实现。原始参数名与源码拼写不可见，但二进制可证明的结构一致。 |
| 数据流 | `MATCH` | Android 不读取 `this`、`flag`、`num` 或 `objthis`，仅向 `W0` 写入 `0xFFFFFC16`；本地全部形参未使用，只产生相同的 32 位返回值。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 `0x1A0B450` 的主 vtable 项，parent `0x597AD4` 仅安装该表；本地通过 `iTJSDispatch2` 虚方法覆写进入相同的间接分派链。不存在 Android direct-call，`[vtable-member]` 关系准确。 |
| 对象生命周期 | `N/A` | 完整函数不分配、析构、AddRef/Release、失效对象或接管任何参数；vptr 安装属于 parent 构造函数，不属于本函数体。 |
| 内部容器实现 | `N/A` | 完整两指令函数没有对象字段、容器、分配器或迭代器访问。 |
| 边界行为 | `MATCH` | 对任意 `flag`、任意数值（含负值和越界值）的 `num` 及任意/空 `objthis` 均不解引用、无副作用，并无条件返回 `TJS_E_NOTIMPL(-1002)`；本地完全相同。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

## Cross-reference

- `@597AD4` 通过 `off_1A0B3D8` 安装包含本函数的主 vtable；这是 canonical `[vtable-member]` parent 关系，不是直接调用。
- `0x1A0B450` 是本函数唯一 Android xref，属于上述 canonical vtable 关系；TASK_TREE 没有记录本节点的其他非 canonical cross-reference。
- 直接子节点：无；不得为本节点派发其他 agent。

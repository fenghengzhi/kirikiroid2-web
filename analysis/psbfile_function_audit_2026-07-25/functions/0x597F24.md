# `0x597F24` — `ncbClassInfo_PSBFile_InfoCtor_guess`

## 身份与归属

- canonical 地址：`0x597F24`
- canonical 名称：`ncbClassInfo_PSBFile_InfoCtor_guess`
- 分组：C / PSBFile typed NCB 前段 / factory / root
- TASK_TREE parent：`0x42CEF8 PSBFile_ncbClassInfo_static_init`，关系为
  `[lifecycle]`。这表示同一 class-info 静态状态的构造生命周期归属；Android parent 内联了
  同形的四字段初始化且没有 `BL 0x597F24`，因此本关系不是 `[direct-call]`。
- 审计 agent path：`/root/f_42cef8/f_597f24`
- Android vtable / NCB / 调用链归属：本入口不是 vtable 槽、TJS member wrapper 或注册
  callback，而是 `ncbClassInfo<PSBFile>` 私有 class-info 状态的 template-emitted 构造器。
  它只建立后续 typed NCB getter / setter / clear 所消费的初始状态；本报告不替那些独立入口
  下审计结论。
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cef8/f_597f24` fresh 执行了
`decompile(0x597F24)`；随后执行完整 `disasm(0x597F24)`（5 条指令）、
`xrefs_to(0x597F24)` 与 `lookup_funcs(0x597F24)` 复核。此次记录独立于任何旧报告：

- fresh decompile 当前 IDA 标签为 `PSBFile_ncbClassInfo_InfoCtor_guess`，函数范围为
  `0x597F24..0x597F37`；TASK_TREE canonical 名称采用
  `ncbClassInfo_PSBFile_InfoCtor_guess`。
- `0x597F24: STRB WZR, [X0]`：把首字段写为 `false`。
- `0x597F28: STR XZR, [X0,#8]`：把第二字段指针写为 null。
- `0x597F2C: STR WZR, [X0,#0x10]`：把第三字段 32 位值写为 `0`。
- `0x597F30: STR XZR, [X0,#0x18]`：把第四字段指针写为 null。
- `0x597F34: RET`：没有改写 `X0`；该值只是 incoming self 的寄存器残留，不是 C++
  构造器的 source-level 返回值。2026-08-02 已把 IDB 旧 pointer-return 原型纠正为
  `void(PSBFile_ncbClassInfoInfo_arm64 *self)`。
- 完整函数没有条件分支、`BL`、分配、引用计数、异常路径或显式失败返回。
- fresh `xrefs_to` 没有记录到该 out-of-line emitted body 的代码或数据 xref。作为调用点
  上下文补查的 parent `0x42CEF8` 完整反汇编直接内联相同四次写入，也没有调用本入口；
  这支持 TASK_TREE 的 `[lifecycle]` 边而非直接调用边。stripped/O3 为何仍保留此独立实例不从
  negative xref 反推。

## Android 伪代码（7 行）

```text
InfoCtor(self):                         // self 必须指向可写 Info 存储；无 null guard
    self.initialized = false
    self.name = null
    self.id = 0
    self.obj = null
    return void
// 无条件分支、替代默认值、callee、显式错误码或异常处理
```

## 本地精确对照

- class-info 共通 typedef：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 28–35 行，符号
  `ncbTypedefs::{NameT, IdentT, ClassObjectT}`；其中 `NameT` 是指针、`IdentT` 是
  `tjs_int32`、`ClassObjectT` 是 native-class 类型。
- class-info 模板与静态对象：同文件第 73–114 行，符号
  `ncbClassInfo<T>`、`ncbClassInfo<T>::InfoT`、`ncbClassInfo<T>::_info`。
- 本函数的精确源码对照：同文件第 103–110 行；第 104 行构造器
  `info() : initialized(false), name(nullptr), id(0), obj(nullptr) {}`，第 106–109 行按
  Android 相同顺序声明四个字段。
- 显式实例化与 autoreg 声明顺序：同文件第 2160–2164 行，符号
  `NCB_REGISTER_CLASS_COMMON`；第 2161 行实例化 `ncbClassInfo<cls>`，第 2163 行才声明
  autoreg 静态对象。
- PSBFile 展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 751-755 行，符号
  `NCB_REGISTER_CLASS(PSBFile)`。

逐项对应：

1. Android 四次 store 与本地第 104 行的四项 initializer 一一对应，字段顺序也与第
   106–109 行一致。
2. Android 的 64 位指针槽和 32 位 id 是 AArch64 ABI 布局；本地用源代码类型表达相同
   语义，让 wasm32 / 其他目标 ABI 自行计算偏移，没有硬凑 Android 字节布局。
3. Android 本入口不负责 static guard；guard 与实际静态初始化在 canonical parent
   `0x42CEF8`。本地同样把无 guard 的 `InfoT` 构造器与 `_info` 的静态存储生命周期分开表达。
4. Android 构造器体没有 helper/callback；本地 initializer list 也不引入容器、分配、
   引用计数或额外失败处理。

## 六维判定

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是一只四字段 class-info 状态构造器：bool、name 指针、32 位 id、class-object 指针；本地 `InfoT` 保留相同字段顺序、类型语义和显式构造器。stripped 二进制不能证明私有类型与成员的原始拼写，`MATCH` 只覆盖可证结构。 |
| 数据流 | `MATCH` | Android 无条件按顺序写 `false/null/0/null`；尾部未改写 X0 只是 self 残留，不是源码返回。2026-08-02 复反编译已显示 `void` 构造完成；本地 initializer list 给出完全相同的四个默认值和顺序。 |
| 调用链 | `MATCH` | Android 本体无 callee，fresh xref 未记录 retained caller；parent `0x42CEF8` 内联同形初始化而不直接调用。除编译器生成的构造语义外，本地 `InfoT` 构造器同样没有显式 helper 或注册 callback 调用，且没有把 parent 生命周期关系伪装成 direct call。 |
| 对象生命周期 | `MATCH` | Android 就地初始化调用者提供的状态存储，不分配、不持有引用、不执行析构动作；本地 `_info` 是静态存储期对象，`InfoT` 构造器仅建立四个非 owning 初值。static guard 位于本函数之外，与 Android 分层一致。 |
| 内部容器实现 | `N/A` | 本入口只写一个 bool、一个 32 位整数和两个指针，不创建、访问或修改任何容器。 |
| 边界行为 | `MATCH` | Android 没有参数检查或替代路径：四字段始终全部写入；无效/null `self` 会在首条 store 处失败，而不是返回错误。源码构造器依赖有效对象存储的同一 C++ 前置条件，未增加安全化分支。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 5 指令反汇编、xref 与 parent 内联调用点证据可证明
范围内，本地 `ncbClassInfo<PSBFile>::InfoT::info()` 与 `0x597F24` 的六维行为一致。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 尚待由本节点在阶段 B 派发的 canonical 直接子节点（本阶段未 spawn）：
  - `0x597E98 ncbClassInfo_PSBFile_GetName_guess [helper]`
  - `0x597EA8 ncbClassInfo_PSBFile_GetID_guess [helper]`
  - `0x597EB8 ncbClassInfo_PSBFile_GetClassObject_guess [helper]`
  - `0x597EC8 ncbClassInfo_PSBFile_IsSubClass_guess [helper]`
  - `0x597ED0 ncbClassInfo_PSBFile_Set_guess [helper]`
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x597F24` 条目。
- `0x42CEF8` 仅是本节点的 canonical `[lifecycle]` parent；其内联同形初始化不是对
  `0x597F24` 的 retained direct call，也不为 parent 创建重复函数 agent。

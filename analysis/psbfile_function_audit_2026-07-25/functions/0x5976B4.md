# `0x5976B4` — `PSBValueDispatch_PropSetByNum`

## 审计身份

- canonical 地址：`0x5976B4`
- canonical 名称：`PSBValueDispatch_PropSetByNum`
- IDA 当前标签：`PSBValueDispatch_PropSetByNum`
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 构造并安装 primary vptr；本节点是该 vtable 的
  slot 07，不是 parent 的直接 callee。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_5976b4`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x5976B4")`；随后 fresh 调用
`disasm(addr="0x5976B4", include_total=true)`、`xrefs_to(addrs="0x5976B4")`、
`lookup_funcs(0x5976B4)`，并读取 primary vtable 原始字节。本地址在本轮开始时没有报告，
本文件是本轮 fresh 审计新建结果。

- fresh decompile 恢复的完整函数体为
  `return 4294966294LL /* 0x5976B8 */`。Hex-Rays 因缺失接口签名把返回类型显示为
  `__int64`；ARM64 实际写的是 W0，低 32 位 `0xFFFFFC16` 按 `tjs_error` 解释为
  `-1002`（`TJS_E_NOTIMPL`）。
- fresh 完整 disasm 覆盖 `0x5976B4..0x5976BB`，函数大小 `0x8`、共两条指令：
  `MOV W0, #0xFFFFFC16`；`RET`。没有栈帧、参数读取、字段访问、条件分支、输出写入、
  `BL/BLR`、临时对象或异常清理。
- fresh `xrefs_to(0x5976B4)` 只返回 data xref `0x1A0B410`，没有 code xref。
  primary address point `0x1A0B3D8` 的 fresh 原始 qword 序列中，零基第 7 项恰为
  `0x00000000005976B4`（`0x1A0B3D8 + 7*8 = 0x1A0B410`）；相邻 slot 06/08 分别为
  `0x5976BC`/`0x5975E0`。因此本入口确属 `PSBValueDispatch` primary
  `iTJSDispatch2` vtable slot 07；该二进制中已观测到的归属方式是
  `PropSetByNum` 虚分派，没有保留直接调用点。
- 本入口不是 NCB 注册函数、wrapper 或 callback，也不调用任何其他 Android 函数。
  canonical parent `@597AD4` 仅通过安装 `@1A0B3D8` 把本节点纳入对象虚接口；这正是
  TASK_TREE 的 `[vtable-member]` 关系，不应伪装成直接调用。

## Android 伪代码（4 行）

```text
PropSetByNum(this, flag, num, param, objthis):
    不读取 this、flag、num、param 或 objthis
    不写任何对象、参数或输出位置，也不调用其他函数
    return tjs_error(int32(0xFFFFFC16))  // -1002，TJS_E_NOTIMPL
```

本函数没有条件分支或可选默认值；全部输入（包括 null/无效指针与任意索引/flag）在成功进入
该入口后都走同一无条件失败边界。

## 本地精确文件、符号、行号与逐行对照

- `cpp/core/tjs2/tjsInterface.h:166-171`：`iTJSDispatch2::PropSetByNum` 是
  `PropSet` 之后、`GetCount` 之前的虚接口，参数依次为 `flag`、数字索引、只读 Variant
  参数与 `objthis`；对应 Android primary vtable 的 slot 07 归属。
- `cpp/plugins/psbfile/PSBDispatch.h:16-17,47-48`：`PSBValueDispatch` 以
  `iTJSDispatch2` 为 primary base，并声明同签名 `PropSetByNum(...) override`；没有为该
  入口增加额外状态或 wrapper。
- `cpp/plugins/psbfile/main.cpp:280-283`：
  `PSBValueDispatch::PropSetByNum` 的全部形参均不命名，函数体唯一语句是
  `return TJS_E_NOTIMPL;`。这逐项复刻 Android 的“忽略全部实参 → 不调用/不写入 →
  返回固定错误码”三步。
- `cpp/core/tjs2/tjsErrorDefs.h:23-30`：本地错误码定义明确
  `TJS_E_NOTIMPL == -1002`，其 32 位二补码正是 Android 指令中的 `0xFFFFFC16`。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 primary `iTJSDispatch2` vtable slot 07 的独立两指令 leaf；本地是 `PSBValueDispatch` 的同槽 out-of-line override，函数体同样只有固定错误返回。未用本地名称反推行为；槽位、地址和函数体均由 fresh Android 数据直接证明。 |
| 数据流 | `MATCH` | Android 不读取任何 ABI 实参或对象字段，只把低 32 位常量 `0xFFFFFC16` 写入返回寄存器；本地所有形参均未命名/未使用，只产生 `TJS_E_NOTIMPL(-1002)` 返回值。 |
| 调用链 | `MATCH` | Android 无 `BL/BLR` 且只有 vtable data xref，没有直接 caller；本地也无 helper、base-method、NCB wrapper 或其他调用。parent 仅安装 vptr，双方均通过 slot 07 虚分派归属。 |
| 对象生命周期 | `N/A` | 本入口不读取 `this`、不增减引用、不构造/析构对象，也不转移任何所有权；生命周期仅由其他独立函数负责。 |
| 内部容器实现 | `N/A` | 本入口不访问 packed PSB table、TJS 容器、STL 容器或任何集合状态。 |
| 边界行为 | `MATCH` | 所有 flag、索引、Variant/objthis 指针值均被忽略；没有 null/bounds/valid 检查和输出副作用，进入入口后始终返回 `-1002`。本地同样无分支、guard 或参数解引用。 |

## 总判定

`ALIGNED`

Android 当前可见的 vtable 归属、固定数据流、零调用/零生命周期/零容器行为及无条件失败边界
均与本地实现一致；没有确定架构或行为差异。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 直接子节点：无；本节点是 canonical leaf，阶段 A 未 spawn。
- TASK_TREE 的非 canonical cross-reference 清单没有涉及 `@5976B4`。
- `@1A0B410 -> @5976B4` 是 primary vtable slot 07 的 data reference；相邻
  `@5976BC`、`@5975E0` 只是 vtable sibling，不是调用关系，也不由本节点派发 agent。
- `@597AD4` 是唯一 canonical parent；它安装包含本槽的 vptr，但不直接调用本函数。

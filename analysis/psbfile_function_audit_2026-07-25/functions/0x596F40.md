# `0x596F40` — `PSBValueDispatch_DeleteMemberByNum`

## 节点身份

- 分组：A（`PSBValueDispatch / packed dispatch ABI`）
- canonical 地址：`0x596F40`
- canonical 名称：`PSBValueDispatch_DeleteMemberByNum`
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 边：`[vtable-member]`
- function-agent：`/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_596f40`
- 直接子节点：无

## Android 归属与 fresh 证据

本节点属于 `PSBValueDispatch` 的 primary `iTJSDispatch2` vtable `0x1A0B3D8`，是 slot 13（`DeleteMemberByNum`）。2026-07-25 本 function-agent 对 canonical 地址执行了 fresh IDA MCP `decompile(0x596F40)`，结果为：

```c
__int64 PSBValueDispatch_DeleteMemberByNum()
{
  return 4294966294LL; /*0x596f44*/
}
```

补充证据：

- fresh `disasm(0x596F40)` 只有 `MOV W0, #0xFFFFFC16` 与 `RET` 两条指令，函数范围为 `0x596F40..0x596F47`。
- fresh `xrefs_to(0x596F40)` 只有一个 data xref：`0x1A0B440`；该地址等于 primary vtable 基址 `0x1A0B3D8 + 13 * 8`。
- fresh `get_bytes(0x1A0B440, 8)` 为 `40 6f 59 00 00 00 00 00`，即指向 `0x596F40`。
- `0xFFFFFC16` 按 `tjs_error` 的 32 位有符号语义为 `-1002`。初审时 IDA 因 stripped 原型
  把返回类型显示为 `__int64`；2026-08-02 已按 vtable slot 13 ABI 补成
  `int(self,flag,num,objthis)`，复反编译直接显示 `return -1002`。四个 ABI 参数均未被读取。

## Android 伪代码（4 行）

```text
DeleteMemberByNum(flag, num, objthis):
    ignore flag, num, objthis
    return tjs_error(-1002)  // TJS_E_NOTIMPL
// 无条件分支、无默认值选择、无其他失败路径或副作用
```

## 本地实现对照

| 本地位置 | 符号/证据 | 与 Android 的逐项对照 |
| --- | --- | --- |
| `cpp/plugins/psbfile/PSBDispatch.h:16-17` | `PSBValueDispatch final : public iTJSDispatch2, public iTJSNativeInstance` | 保留 primary dispatch 与 secondary native-instance 的源码结构；本节点归属前者。 |
| `cpp/plugins/psbfile/PSBDispatch.h:68-69` | `PSBValueDispatch::DeleteMemberByNum` override 声明 | 参数为 `tjs_uint32, tjs_int, iTJSDispatch2 *`，对应 slot 13 的接口形态。 |
| `cpp/core/tjs2/tjsInterface.h:211-215` | `iTJSDispatch2::DeleteMemberByNum` | 基类虚函数顺序及参数语义确认该 override 的 vtable 归属。 |
| `cpp/plugins/psbfile/main.cpp:446-448` | `PSBValueDispatch::DeleteMemberByNum` | 不读取任何参数，直接 `return TJS_E_NOTIMPL;`，逐条复刻 Android 两指令入口。 |
| `cpp/core/tjs2/tjsErrorDefs.h:23-25` | `#define TJS_E_NOTIMPL (-1002)` | 与 Android `W0=0xFFFFFC16` 的 32 位有符号值完全相同。 |

## 六维审计

| 维度 | 状态 | 证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是 primary `iTJSDispatch2` vtable slot 13 的独立 8-byte stub；本地以 `PSBValueDispatch` 的显式 `override` 声明并提供独立定义。stripped 二进制不能证明参数名等不可观察拼写，但不影响已发射结构的对齐结论。 |
| 数据流 | `MATCH` | Android 不读入参，只把 `0xFFFFFC16` 写入返回寄存器；本地同样不命名/不读取三个参数，只返回等值常量 `TJS_E_NOTIMPL`。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 primary vtable slot 13；本地通过 `iTJSDispatch2::DeleteMemberByNum` override 进入同一虚分派语义，没有额外 helper、直接 callee 或 fallback。 |
| 对象生命周期 | `N/A` | 本入口不访问 `this`/`objthis`，不修改引用计数，不构造、销毁或转移任何对象。 |
| 内部容器实现 | `N/A` | 本入口不访问 packed node 或任何容器。 |
| 边界行为 | `MATCH` | 对全部 `flag`、正负/越界 `num`、以及任意（包括空）`objthis` 都走同一无条件返回路径，结果始终为 `TJS_E_NOTIMPL (-1002)`；本地行为一致。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

## Cross-reference

- TASK_TREE 未为本节点列出非 canonical cross-reference。
- `0x596F48 DeleteMember` 是相邻 vtable sibling，不是本节点的 caller/callee，也不是 canonical child；未为其创建重复 agent。

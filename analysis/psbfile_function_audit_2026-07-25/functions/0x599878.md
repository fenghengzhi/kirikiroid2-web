# `0x599878` — `PSBMedia_AddRef_guess`

## 节点身份与审计边界

| 项目 | 值 |
| --- | --- |
| canonical 地址 | `0x599878` |
| canonical 名称 | `PSBMedia_AddRef_guess`（精确原始 C++ 标识符未保留） |
| MANIFEST 分组 | E |
| TASK_TREE parent | `0x59849C` `PSBFile_preRegister_guess_initPsbFile` |
| canonical 边 | `[vtable-member]`（不是直接调用） |
| 审计 agent path | `/root/f_42cf28/f_59849c/f_599878` |
| 本轮日期 | `2026-07-25` |
| 本轮范围 | 仅审计 `0x599878`；未审计或派发其他函数，未修改 `cpp/` |

唯一权威来源是 Android kirikiroid2 `libkrkr2.so` 的内嵌实现。本报告没有查看、
引用或从外部 `psbfile.dll` 推导任何结论；二进制中的 `"PSBFile.dll"` 仅是模块名
字面量。

## 本轮 fresh IDA 证据

- 本轮新会话 fresh 调用
  `mcp__ida_mcp_pro__decompile(addr="0x599878")`，得到完整函数体：
  `void PSBMedia_AddRef_guess(PSBMedia *self) { ++self->refcount; }`。这不是沿用旧
  报告；本轮开始时该地址的报告文件不存在。
- 补充调用 `disasm(addr="0x599878", include_total=true)`：函数只有 4 条指令，
  `0x599878 LDR W8,[X0,#8]`、`0x59987C ADD W8,W8,#1`、
  `0x599880 STR W8,[X0,#8]`、`0x599884 RET`。它是普通 32 位读改写，未使用
  `LDXR/LDAXR/STXR/STLXR` 或任何锁，因此可确定为 non-atomic increment。
- `xrefs_to(0x599878)` 只有一个 data xref：`0x1A0B520`。读取
  `0x1A0B510` 起始的 11 个 qword 后，第 3 项正是 `0x599878`，所以本函数是
  `PSBMedia` vtable slot 02；相邻项是 slot 01 `0x599830` 与 slot 03
  `0x599888`。
- 为核实真实间接调用链，仅把通用 storage manager 作为上下文 fresh 反编译：
  `sub_8EA2C8@0x8EA2C8` 转入 `sub_8E881C@0x8E881C`，后者在
  `0x8E8890` 构造 media record；其构造 helper `sub_8E8DA8@0x8E8DA8` 在
  `0x8E8DFC` 执行 `(*vptr[2])(media)`，即通过 `+16` 槽间接调用本函数。
  这些通用引擎函数只用于调用点归属，不在本报告中接受第二份函数审计结论。

## Android vtable / NCB / 调用链归属

本函数属于 `PSBMedia` vtable `0x1A0B510` 的 slot 02，对应
`iTVPStorageMedia::AddRef` 形状；它不是 NCB wrapper、NCB 注册函数或直接 callback。
canonical parent `0x59849C` 负责构造并注册带该 vtable 的 `PSBMedia` 对象，但没有
直接调用 `0x599878`，所以 TASK_TREE 的 `[vtable-member]` 关系不能写成 direct-call。

运行时的已证调用点位于通用 media-record 构造路径：
`0x59849C` 注册 gateway → `0x8EA2C8` → `0x8E881C` →
`sub_8E8DA8@0x8E8DA8` → `vptr[2]` → `0x599878`。本函数本身不调用任何
callee。

## Android 关键伪代码（5 行）

```text
PSBMedia::AddRef(this):
  old = load32(this.refcount)
  next = old + 1                       // 普通 32 位运算，不原子、不饱和
  store32(this.refcount, next)
  return void                          // 无条件、无默认值、无失败分支
```

函数没有 null 检查、并发保护、上限检查、异常处理或备用返回值；有效对象是调用方
前置条件。

## 本地实现与逐行对照

精确位置：

- `cpp/plugins/psbfile/PSBMedia.h:10-16` — `PSBMedia` 继承、构造及
  `void AddRef() override { _ref++; }`；
- `cpp/plugins/psbfile/PSBMedia.h:18-23,45` — 配对的 `Release()` 与普通
  `int _ref` 字段；
- `cpp/core/base/StorageIntf.h:114-119` — `iTVPStorageMedia` 的虚析构、
  `AddRef`、`Release` 接口顺序；
- `cpp/core/base/StorageIntf.cpp:196-209,281-293` — `tMediaRecord` 持有
  media，`tTVPStorageMediaManager::Register()` 构造该 record；
- `cpp/core/tjs2/tjsUtils.h:151-167` — `tTJSRefHolder<T>` 构造时通过
  `Object->AddRef()` 取得持有引用。

| Android 伪代码步骤 | 本地精确复刻 |
| --- | --- |
| 行 1，读取对象内 32 位 refcount | `PSBMedia.h:45` 使用普通 `int _ref`；`PSBMedia.h:16` 的后缀自增读取该字段。Android 的 `+8` 是 ARM64 对象 ABI 偏移，不应硬编码进 wasm 源码。 |
| 行 2，加一且无原子/饱和操作 | `PSBMedia.h:16` 直接 `_ref++`，没有 atomic、mutex、CAS 或上限分支。后缀表达式的旧值未被消费。 |
| 行 3，写回同一字段 | 同一个 `_ref++` 表达式把结果写回 `_ref`，没有旁路字段或输出参数。 |
| 行 4，返回 `void` | 本地 override 与 `StorageIntf.h:117` 的基接口都返回 `void`；没有状态码或默认返回值。 |
| 间接调用归属 | `StorageIntf.cpp:290` 构造 `tMediaRecord`，其 `MediaIntf(media)` 在 `tjsUtils.h:157-160` 虚调用 `AddRef()`，对应 Android `0x8E8DFC` 的 `vptr[2]`。 |

## 六维审计

| 维度 | 状态 | Android 证据与本地对照 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 独立 emitted 入口是一个 `void(PSBMedia*)` vtable member，函数体只有 refcount 自增；本地 `PSBMedia.h:16` 保留同样的 virtual override 与单表达式函数体。stripped 二进制无法证明原始成员拼写，但在现有证据覆盖范围内没有结构差异。 |
| 数据流 | `MATCH` | Android 只执行 `this+8` 的 32 位 load → `+1` → 同址 store；本地只对 `_ref` 做一次 `++`，不产生返回值、输出参数或其他成员写入。 |
| 调用链 | `MATCH` | Android vtable `0x1A0B510` 的 slot 02 指向本函数，注册链 record 构造器在 `0x8E8DFC` 经 `vptr+16` 间接调用；本地 `iTVPStorageMedia` 接口、manager record 与 `tTJSRefHolder` 构造路径以相同虚调用取得引用。本函数无直接 callee。 |
| 对象生命周期 | `MATCH` | Android 本函数只增加 intrusive 引用计数，不分配、不销毁、不释放其他成员；本地 `AddRef()` 同样只增加 `_ref`，删除边界留给配对的 `Release()`，本节点没有额外 ownership wrapper。 |
| 内部容器实现 | `N/A` | 本 emitted 入口不创建、查询、遍历或修改任何容器。注册 manager 的 hash table 属于上下文 callee，不属于 `0x599878` 的函数体。 |
| 边界行为 | `MATCH` | Android 无 null/上限/失败检查且明确非原子；本地普通 `int` 自增同样无这些保护。两者都要求有效对象并且不承诺并发安全或饱和语义；计数器极值处的 C++ signedness/源码拼写不能由 ARM64 `W` 指令唯一恢复，但没有证据形成确定 GAP。 |

## 总判定

`ALIGNED`

本轮 fresh decompile、完整 4 指令反汇编、vtable 数据与注册链间接调用点共同证明：
本地 `PSBMedia::AddRef()` 在可观察的六维范围内复刻了 Android
`0x599878` 的 ordinary non-atomic refcount increment。canonical 名称中的
`_guess` 只表示原始 C++ 标识符未被二进制保留，不构成复原 GAP。

## 所有确定 GAP

无。

## Cross-reference 与阶段 B 子节点

- canonical parent 是 `0x59849C`，关系为 `[vtable-member]`；parent 构造/注册
  采用此 vtable 的对象，不直接调用本函数。
- `0x1A0B520` 是指向本函数的唯一 data xref；`0x8E8DFC` 是通过该 slot 的间接
  runtime call site。`0x8EA2C8`、`0x8E881C`、`0x8E8DA8` 均为 MANIFEST 外
  的通用引擎上下文，不为它们创建 function agent。
- vtable siblings `0x599830`、`0x599888` 等不是本函数的直接调用关系，也不是本
  节点的 canonical child。
- TASK_TREE 未列出本节点的直接 canonical child；尚待派发列表：**无**。

# `0x5976BC` — `PSBValueDispatch_PropSet`

## 节点身份

- 审计日期：`2026-07-25`
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_5976bc`
- 分组：`A`（PSBValueDispatch / packed dispatch ABI）
- canonical 地址：`0x5976BC`
- canonical 名称：`PSBValueDispatch_PropSet`
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 表示构造对象所安装主 vtable 的成员归属，
  不表示 `0x597AD4` 直接调用本入口。
- TASK_TREE 直接子节点：无。
- 名称证据边界：Android stripped 二进制没有保留可独立证明原始 C++ 拼写的符号；
  本报告沿用 MANIFEST/TASK_TREE canonical 标签。归属由主 vtable 槽位和接口签名证明，
  本地名称只用于实现对照，不能反向命名二进制。
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25，本新会话由上述 function agent 对唯一目标地址执行了以下 fresh IDA MCP
取证：

1. `decompile(addr="0x5976BC")`：fresh 结果为
   `__int64 PSBValueDispatch_PropSet() { return 4294966294LL; }`；返回值低 32 位是
   `0xFFFFFC16`，按 `tjs_error` 解释为 `-1002`。
2. `disasm(addr="0x5976BC", include_total=true)`：完整函数只有
   `0x5976BC: MOV W0, #0xFFFFFC16` 与 `0x5976C0: RET`，共 2 条指令；没有参数读取、
   条件分支、调用、内存访问或输出写回。
3. `lookup_funcs(0x5976BC/0x5976B4/0x5976C4)`：本函数边界为
   `0x5976BC..0x5976C3`、size `0x8`；相邻的 `PropSetByNum@0x5976B4` 与
   `PropGetByNum@0x5976C4` 是各自独立入口，未被错误并入本函数。
4. `xrefs_to(0x5976BC)`：唯一引用是 data xref `0x1A0B408`，没有 code xref。
5. `get_bytes(0x1A0B3D8, 80)`：主 vtable address point 从 `0x1A0B3D8` 起依次是
   `@597AC0, @597A40, @597A20, @597A18, @597854, @5976C4, @5976BC,
   @5976B4, @5975E0, @5975D8`；因此 `0x1A0B408 = address point + 0x30`
   精确保存本入口，它是 `PSBValueDispatch` 的 primary `iTJSDispatch2` vslot 06。

本节点不是 NCB wrapper、注册 callback 或 helper，也没有 Android direct caller；运行期
经 primary vtable slot 06 间接进入。TASK_TREE 把它挂在构造节点 `@597AD4` 下的
`[vtable-member]` 关系准确，没有把 vtable sibling/归属伪装成直接调用。

## Android 伪代码（4 行）

```cpp
tjs_error PSBValueDispatch::PropSet(flag, membername, hint, param, objthis) {
    ignore(flag, membername, hint, param, objthis);
    return TJS_E_NOTIMPL; // -1002；无条件、默认值或其他失败分支
}
```

完整二指令体证明：任意输入都走同一返回边界；函数不验证 `this/membername/param/objthis`，
不解引用空指针，不修改 `hint/param`，也没有异常捕获或 callee 失败路径。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 直接实现
  `iTJSDispatch2`，形成本节点所属的 primary dispatch vtable。
- `cpp/plugins/psbfile/PSBDispatch.h:37-48`：在 `PropGet/PropGetByNum` 后、
  `PropSetByNum` 前以完整五参数签名独立覆写 `PropSet`；目标声明精确位于 `:44-45`。
- `cpp/plugins/psbfile/main.cpp:275-278`：独立定义
  `PSBValueDispatch::PropSet(...)`；所有形参省略名称，函数体只返回
  `TJS_E_NOTIMPL`。
- `cpp/core/tjs2/tjsInterface.h:141-171`：`iTJSDispatch2` 依次声明
  `PropGet`、`PropGetByNum`、`PropSet`、`PropSetByNum`；`PropSet` 的 ABI 参数位于
  `:157-164`，与 Android vslot 04..07 的顺序一致。
- `cpp/core/tjs2/tjsTypes.h:35-42,57`：`tjs_error` 是 32 位有符号
  `tjs_int32`；因此 Android 写入 `W0` 的 `0xFFFFFC16` 正是 `-1002`。
- `cpp/core/tjs2/tjsErrorDefs.h:23-29`：`TJS_E_NOTIMPL` 精确定义为 `-1002`。
- 对照边界：`cpp/core/tjs2/tjsObject.h:132-136` 的基类 `tTJSDispatch::PropSet`
  会按 `membername` 在 `TJS_E_MEMBERNOTFOUND/TJS_E_NOTIMPL` 间选择；本地
  `PSBValueDispatch` 没有复用该 member-sensitive 默认实现，而是像 Android 一样保留
  独立、无条件 `TJS_E_NOTIMPL` override。

逐项映射：

| Android 证据 | 本地源码 | 对照 |
| --- | --- | --- |
| vtable `0x1A0B408 -> 0x5976BC`，位于 `PropGetByNum` 与 `PropSetByNum` 之间 | `PSBDispatch.h:37-48` 的同序独立 `PropSet` override | 虚槽结构一致 |
| `MOV W0,#0xFFFFFC16` | `main.cpp:277` 返回 `TJS_E_NOTIMPL`；`tjsErrorDefs.h:24` 为 `-1002` | 32 位返回值逐位一致 |
| 没有参数寄存器读取、内存访问或输出写回 | `main.cpp:275-276` 所有形参省略名称且函数体不使用它们 | 输入与对象数据流一致 |
| 紧随其后的 `RET`；无 branch、`BL/BLR` | `main.cpp:275-278` 为单返回独立方法 | 控制流和副作用一致 |

## 六维审计

| 维度 | 状态 | Android 与本地证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留独立 emitted 的两指令 primary vslot 06；本地同样以 `PSBValueDispatch::PropSet` 独立覆写，而非继承 member-sensitive base 默认实现。原始拼写不可见，但当前二进制可证明的结构一致。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或任一参数，只向 `W0` 写 `-1002`；本地所有形参均未使用，只返回同值，且不改写 `hint/param`。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 primary vtable 数据项 `0x1A0B408`，没有 direct-call；本地通过 `iTJSDispatch2::PropSet` override 经虚分派进入。canonical parent 的 `[vtable-member]` 关系准确。 |
| 对象生命周期 | `N/A` | 完整函数不分配、析构、AddRef/Release、读取对象字段或接管任何参数；vptr 安装属于 parent 构造节点而非本函数体。 |
| 内部容器实现 | `N/A` | 完整二指令函数不创建、遍历或修改任何 STL/KiriKiri/TJS 容器。 |
| 边界行为 | `MATCH` | 对任意 flag、任意 membername、空或非空 `this/param/objthis/hint` 均不解引用、不写回，并无条件返回 `TJS_E_NOTIMPL (-1002)`；本地完全相同。 |

## 总判定

`ALIGNED`

## 确定 GAP

无。

证据边界：stripped/O3 二进制不能恢复原始源码的参数名或精确方法拼写；IDA 将无类型的
入口暂时显示成无参 `__int64`，但 vtable 位置、32 位 `W0` 返回和本地接口 ABI 足以证明
本轮六维结论。该类型恢复限制不是已证实的行为差异。

## Cross-reference 与阶段 A 派发状态

- `0x1A0B408` 是本函数唯一 Android xref，属于 canonical primary-vtable 归属关系，
  不是额外函数节点或 direct caller。
- TASK_TREE 未记录本节点的非 canonical cross-reference。
- 本节点没有 TASK_TREE canonical 直接子节点；阶段 A 未 spawn 任何 agent。

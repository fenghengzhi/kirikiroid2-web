# `0x598B3C` — `PSBRawOwner_dtor_guess`

## 审计身份

- canonical 地址：`0x598B3C`
- canonical 名称：`PSBRawOwner_dtor_guess`
- 分组：D（raw PSBFile / PSBRawOwner / PSBRawNode）
- TASK_TREE parent：`0x598AAC PSBRawOwner_ctor_guess`
- parent 关系：`[lifecycle]`。两者是同一 raw-owner 类型的构造/析构生命周期
  counterpart；`0x598AAC` 不调用本入口，本轮 `xrefs_to(0x598B3C)` 也没有来自
  `0x598AAC` 的引用，因此该边不是 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b14c/f_5980f4/f_598268/f_598538/f_598708/f_598aac/f_598b3c`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。本入口不涉及二进制中的 NCB 模块名字面量 `"PSBFile.dll"`。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 agent path fresh 调用 IDA MCP
`decompile(addr="0x598B3C")`；随后 fresh 调用
`disasm(addr="0x598B3C", include_total=true, max_instructions=500)`，完整取得函数范围
`0x598B3C..0x598B57` 的 7 条指令，并调用
`xrefs_to(addrs="0x598B3C", limit=1000)` 与
`analyze_function(addr="0x598B3C")` 复核引用、prototype 和函数尺寸。报告在本轮新建。
另 fresh decompile/disasm `sub_A0DE90@0xA0DE90` 与异常 helper
`sub_520FAC@0x520FAC`，并 disasm 两个短调用点，仅作为本函数 callee/caller 上下文，
不替这些地址下逐函数审计结论。

- fresh decompile 恢复签名
  `void PSBRawOwner_dtor_guess(PSBRawOwner *self)`，size 为 `0x1C`。它不是 vtable
  member、NCB wrapper、注册 callback 或 storage gateway，而是 raw allocation owner 的普通
  emitted 析构入口；stripped 二进制未保留可证明的精确源码名，故 canonical 名称保留
  `_guess`。
- 正常路径只有 `0x598B44 LDR X0,[X0,#0x58]` 与
  `0x598B48 BL sub_A0DE90`：从 owner 取出 owned aligned data pointer，交给对齐释放 helper，
  随后 `0x598B4C..0x598B50` 恢复栈帧并返回。它不读取 refcount/header/size，不清任何字段，
  也不释放 `self`。
- callee 上下文 `sub_A0DE90@0xA0DE90` 先从 aligned pointer 前一个指针槽读取原始
  allocation base；该 base 非空时尾调用 `operator delete[]`，为空时返回。这证明本入口使用
  KiriKiri 对齐分配器的配对释放路径，而不是对 aligned pointer 本身直接 `delete[]`。
- `0x598B54 BL sub_520FAC` 是正常 `RET` 后的析构异常 landing；该 helper 执行
  `__cxa_begin_catch` 后 `std::terminate()`。正常控制流不会从 `0x598B50` 落入它。
- fresh xref 共有 147 个 code references、20 个唯一 caller function，全部位于 canonical
  psbfile emitted 簇之外；canonical 范围 `0x59641C..0x59B7E8` 内为 0。短样本
  `sub_695CBC@0x695CBC` 的 `0x695CCC..0x695D00` 与
  `Motion_LoadedResourceRecordAndKey_destroy_guess@0x6DB3E8` 的
  `0x6DB408..0x6DB444` 都显示相同上下文：非空 owner refcount 减到 0 后先直接调用本析构，
  再由 caller 调 `operator delete(self)`。这些仅证明本入口的生命周期位置，不审计 caller。
- 因而 TASK_TREE 的 parent `@598AAC` 只表示 constructor/destructor 的 canonical 生命周期
  归组。Android 当前 emitted 调用关系中不存在 `@598AAC -> @598B3C`，也不存在反向调用，
  不得把该边伪装成 direct-call。

## Android 伪代码（7 行）

```text
OwnerDtor(self):
    aligned = self.data                         // 无条件读取；不设默认值、不清字段
    base = *(void **)(aligned - sizeof(void *)) // TJSAlignedDealloc 内部 metadata load
    if base != null: operator delete[](base)
    return                                      // 不递减 refcount、不 delete self
on exception escaping destructor: std::terminate()
invalid self/aligned/metadata address: raw fault; no status, catch, rollback, or fallback
```

第 3、4 行展开的是本入口唯一 callee `sub_A0DE90` 的可观察分支，不表示本函数另有容器或
独立源码 helper；第 6 行对应函数末尾的 EH landing，而非正常顺序执行。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBRawFile.h:31-69`，符号 `PSBRawOwner`：普通非 vtable owner
  类型保存 intrusive `refCount_`、header view、owned `data_` 与 `size_`；`:55-56` 将
  constructor/destructor 都声明为 private lifecycle member，`:61` 的 `data_` 是本析构唯一
  消费的拥有指针。host ABI 自行布局这些字段，不硬凑 Android `+0x58`。
- `cpp/plugins/psbfile/PSBRawFile.cpp:141-145`，符号
  `PSBRawOwner::~PSBRawOwner()`：函数体只有 `TJSAlignedDealloc(data_)`，逐行对应伪代码
  第 1-5 行；没有 refcount 操作、字段清零、header 析构、self deallocation、条件 gate 或
  fallback。
- `cpp/core/tjs2/tjsUtils.h:665-670` 声明配对的 `TJSAlignedAlloc` / `TJSAlignedDealloc`；
  `cpp/core/tjs2/tjsUtils.cpp:258-268` 在 aligned pointer 前一个槽保存原始 allocation base，
  `:272-274` 从同一槽取回 base 并执行 `delete[]`。这与 Android
  `0xA0DE90..0xA0DE9C` 的 metadata load、base-null gate 和 `operator delete[]` 完整对应。
- `cpp/plugins/psbfile/PSBRawFile.h:35-42`，符号 `PSBRawOwner::Release()`，仅作生命周期
  caller 上下文：refcount 先减一，减到 0 才 `delete this`。C++ delete-expression 将
  “调用本析构”和“释放 self storage”分开，匹配 Android 短调用点中先
  `BL 0x598B3C`、后 `operator delete(self)` 的次序；这些动作不被错误塞进析构本体。
- `cpp/plugins/psbfile/PSBRawFile.cpp:440-457` 与 `:796-824` 的成功 raw/MDF 路径使用
  `TJSAlignedAlloc(...,4)` 建立本析构所消费的 allocator-family 契约；`:833-844` 将被接纳的
  pointer 装入 `PSBRawOwner`。这些是 owned data 的来源上下文，不替 Load/Adopt 下审计结论。
- 本地 user-provided destructor 对仅含标量/pointer 成员的类具有隐式 non-throwing 析构
  边界；若函数体调用异常逸出则进入 `std::terminate`，对应 Android `0x598B54` 的 landing。

逐行对应：伪代码第 1-2 行对应 `PSBRawFile.cpp:141-144` 取 `data_` 并调用 helper；第 3-4
行对应 `tjsUtils.cpp:272-274`；第 5 行由 `PSBRawFile.cpp:141-145` 没有其他语句及 target
完整 disasm 共同证明；第 6 行对应 Android `0x598B54` 与 C++ 析构异常规则；第 7 行由
Android 无条件 `LDR self+0x58`、callee 无条件读取 `aligned-8`，以及本地相同 raw pointer
表达式共同证明。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是独立 emitted 普通析构入口，正常函数体只有“取 owned data → 调对齐释放 helper”；本地保留独立 `PSBRawOwner::~PSBRawOwner()`，函数体同样只有一个配对释放调用。析构原始拼写不可由 stripped 二进制唯一恢复，canonical `_guess` 已限定该证据边界，不构成确定 GAP。 |
| 数据流 | `MATCH` | 两端都只把 owner 中保存的 aligned pointer 送入配对 deallocator；deallocator 都从 pointer 前一个槽恢复 raw allocation base，再对该 base 执行 `delete[]`。refcount、header、size 和其他字段不进入本体数据流，data 也不被覆写。 |
| 调用链 | `MATCH` | Android 正常路径直接 `BL sub_A0DE90`，异常 landing 进入 terminate helper；本地析构直接调用 `TJSAlignedDealloc`，由 C++ non-throwing 析构边界承担相同异常终止语义。parent `@598AAC` 只是生命周期 counterpart，不存在 direct-call；本地也没有让 constructor 显式调用 destructor。 |
| 对象生命周期 | `MATCH` | Android 本入口只结束 owned raw allocation 的生命周期，不自行减 refcount或释放 owner storage；caller 在 refcount-to-zero 后调用析构，再单独 delete self。`PSBRawOwner::Release()` 与 private destructor 保留相同分工和次序。 |
| 内部容器实现 | `N/A` | 本入口没有 STL、TJS Array/Dictionary、KiriKiri container 或多元素结构；aligned-allocation 前置 metadata 是单个 allocator bookkeeping pointer，不是容器。 |
| 边界行为 | `MATCH` | 两端都不检查 self/data 是否有效，也不把 null aligned pointer 当作普通 `delete[] nullptr`：会先访问 pointer 前一槽；仅该槽中恢复出的 base 为 null 时跳过 delete。无状态码、清零、catch、回滚或 fallback；析构调用异常逸出时终止。 |

## 总判定

`ALIGNED`

在本轮 fresh target decompile、完整 7 指令 disasm、147 个 xref/20 个唯一 caller 的集合核验、
allocator helper 与异常 landing 上下文复核，以及本地精确逐行对照可证明范围内，
`PSBRawOwner::~PSBRawOwner()` 与 `0x598B3C` 六维对齐。

## 确定 GAP

生产代码六维 GAP：无。

证据边界：stripped/O3 二进制不能唯一恢复析构器及 owner 字段的原始源码拼写，也不能用
ARM64 的 `data@+0x58` 证明跨 ABI byte layout；canonical `_guess` 与普通 C++ 字段布局已经
明确限定这些不可恢复项。它们不是已证明的本地差异。

## TASK_TREE 直接子节点与 cross-reference

- 本节点在 TASK_TREE 中没有直接 child；阶段 A 未 spawn，待派发列表为空。
- canonical parent `@598AAC` 的关系严格为 `[lifecycle]`：它是 constructor counterpart，
  不是 target caller；fresh xref 集合也没有该地址。
- TASK_TREE 的非 canonical Cross-reference 清单没有为本节点指定额外 DAG 边。
- fresh xref 的 147 个 code references 来自 canonical psbfile 簇外的 20 个 caller；它们说明
  同一 raw-owner 析构入口被内嵌 motion/resource 路径复用，但都不是本节点的 TASK_TREE
  child，也不得据此创建重复 function agent。本报告只审计 `0x598B3C` 本体。

## 2026-08-03 LSDA 精确调用点拓扑复扫

`LSDA-CALLSITE-TOPOLOGY`

权威 Android ARM64 FDE `0x598B3C..0x598B58` 指向 LSDA `0x186DE10`。原始表只有
`[0x598B48,0x598B4C)->0x598B54` 一项，action 1 经 null type-table entry 解析为
catch-all，landing 调 `sub_520FAC` 终止。因而正常函数体唯一的 aligned deallocation
确实受析构 non-throw contract 保护，而不是 cleanup 后继续 unwind。

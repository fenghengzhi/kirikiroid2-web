# `0x599888` — `PSBMedia_Release_guess`

## 节点身份与审计边界

| 项目 | 值 |
| --- | --- |
| canonical 地址 | `0x599888` |
| canonical 名称 | `PSBMedia_Release_guess`（stripped 二进制未保留精确 C++ 标识符） |
| MANIFEST 分组 | E |
| TASK_TREE parent | `0x59849C` `PSBFile_preRegister_guess_initPsbFile` |
| canonical 边 | `[vtable-member]`（逻辑上的 vtable 归属，不是 parent 对本函数的直接调用） |
| 审计 agent path | `/root/f_42cf28/f_59849c/f_599888` |
| 本轮日期 | `2026-07-25` |
| TASK_TREE 直接子节点 | 无 |
| 本轮范围 | 仅审计 `0x599888`；未审计或派发其他函数，未修改 `cpp/` |

唯一权威来源是 Android kirikiroid2 `libkrkr2.so` 的内嵌实现。本报告未查看、
引用或从外部 `psbfile.dll` 推导任何结论；二进制里的 `"PSBFile.dll"` 仅是 NCB
模块名字面量。

## 本轮 fresh IDA 证据

- 本轮新会话 fresh 调用
  `mcp__ida_mcp_pro__decompile(addr="0x599888")`，成功得到完整函数体；不是沿用
  旧报告。Hex-Rays 输出为：读取 `self->refcount`，值等于 `1` 时经
  `self->vptr[1]` 尾调，否则执行 `--self->refcount`。
- 补充调用 `mcp__ida_mcp_pro__disasm(addr="0x599888", include_total=true)`；函数
  范围为 `0x599888..0x5998A4`，共 8 条指令：`LDR W8,[X0,#8]`、
  `SUBS W8,W8,#1`、`B.NE 0x5998A0`。减到零时，`0x599894..0x59989C`
  读取 vptr、加载 vtable `+8` 的函数指针并以 `BR X1` 尾调；非零时，
  `0x5998A0` 才把减一后的 `W8` 写回 `self+8`，随后返回。
- `xrefs_to(0x599888)` 给出唯一数据引用 `0x1A0B528`。读取
  `0x1A0B510` 起始的原始 qword 表得到前五项
  `0x5997F0, 0x599830, 0x599878, 0x599888, 0x5998A8`；因此
  `0x599888` 确为该 11 项 vtable 的 slot 3，终止引用路径使用的 `vptr+8`
  则解析为 slot 1 `0x599830`。
- 目标函数没有直接 `BL` callee、原子指令、空指针检查、下溢钳制或异常兜底。
  `BR` 的目标取自运行时 vtable，不能伪装成静态直接调用。

## Android vtable / NCB / 调用链归属

`0x599888` 是 `PSBMedia` vtable `0x1A0B510` 的 slot 3，对应
`iTVPStorageMedia::Release` 形状。它本身不是 NCB 注册函数，也不读写 NCB 对象；
TASK_TREE 把创建并注册该 vtable 对象的 PreRegist callback `0x59849C` 选作
canonical parent，关系是 `[vtable-member]`，并不表示 `0x59849C` 直接调用
`0x599888`。

运行时只有终止引用路径产生调用链：当减一结果为零时，本函数经 vdispatch 尾入
同一 vtable 的 slot 1 `0x599830`；非终止路径只回写 refcount 后返回。`0x599830`
是 TASK_TREE 中与本节点同属 `0x59849C` 的 sibling，并非本节点的 canonical child；
其析构内部行为由自己的 function agent 独立审计。

## Android 关键伪代码（7 行）

```text
PSBMedia_Release(self):
  next = self.refcount - 1                    // 32-bit plain load/subtract
  if next == 0:
    tailcall self.vptr[1](self)               // 不把 0 写回 refcount
  else:
    self.refcount = next
  return                                      // 无 null、<=0、原子或失败兜底
```

该伪代码覆盖全部条件与默认路径：仅旧值为 `1` 时进入终止引用路径；旧值为其他
任何 32 位值时均执行普通减一并回写。无参数校验，`self` 无效会在首次读取时失败。

## 本地实现与逐行对照

精确位置：

- `cpp/plugins/psbfile/PSBMedia.h:10` — `PSBMedia : public iTVPStorageMedia`；
- `cpp/plugins/psbfile/PSBMedia.h:12` — 构造时把 `_ref` 设为 `1`；
- `cpp/plugins/psbfile/PSBMedia.h:18-23` — 唯一的内联
  `PSBMedia::Release()` 实现；
- `cpp/plugins/psbfile/PSBMedia.h:45` — 非原子 `int _ref` 字段；
- `cpp/core/base/StorageIntf.h:114-119` — `iTVPStorageMedia` 的虚接口及
  `Release()` 槽；
- `cpp/plugins/psbfile/PSBMediaRegistry.cpp:10` — process-lifetime
  `PSBMedia` 对象的创建点（仅作生命周期上下文，不替 `0x59849C` 下结论）。

| Android 伪代码步骤 | 本地精确复刻 |
| --- | --- |
| 行 1，作为 storage-media 的 Release 虚入口 | `StorageIntf.h:119` 声明纯虚 `Release()`；`PSBMedia.h:18` 以 `override` 实现，对应 Android vtable slot 3。 |
| 行 2，普通 32 位 refcount 读取/减一 | `PSBMedia.h:45` 使用非原子 `int _ref`；`PSBMedia.h:19-22` 只对该字段比较或递减，没有 atomic/ref-holder 包装。 |
| 行 3–4，旧值为 1 时不回写 0而终止对象 | `PSBMedia.h:19-20` 在 `_ref == 1` 时直接 `delete this`；源码没有先执行 `_ref--`，与 Android 的终止分支不写 `self+8` 一致。虚析构使该表达式进入 deleting-destructor 槽。 |
| 行 5–6，其他值回写减一结果 | `PSBMedia.h:21-22` 的唯一 `else` 执行 `_ref--`；没有其他调用或状态写入。 |
| 行 7，无额外兜底 | 本地 `PSBMedia.h:18-23` 同样没有 null 检查、`<= 0` 钳制、锁、异常处理或返回状态。 |

## 六维审计

| 维度 | 状态 | Android 证据与本地对照 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 被恢复为一个虚 `Release` 入口，主体仅含“终止引用则删除，否则减一”的二分支；本地 `PSBMedia.h:18-23` 保留相同方法边界与分支形状。ARM64 先做 `SUBS` 再按零标志分支，是该源码条件的编译结果，不是额外源码步骤。 |
| 数据流 | `MATCH` | Android 只读取 `self+8` 的 32 位 refcount；终止路径不回写，非终止路径把同一值减一后写回同一字段。本地只读写 `_ref`，且 `delete this` 前不先写 0。 |
| 调用链 | `MATCH` | Android 此入口位于 vtable slot 3；非终止路径无 callee，终止路径经 vdispatch 尾入 slot 1。相应本地由 `Release() override` 占据接口槽，`delete this` 触发虚 deleting destructor；没有引入额外 helper。 |
| 对象生命周期 | `MATCH` | Android 仅在旧 refcount 精确等于 1 时启动删除；其他值保持对象存活并减一。`PSBMedia.h:12,18-23,45` 同样以初值 1 的非原子字段和 `delete this` 表达该生命周期。 |
| 内部容器实现 | `N/A` | 本 emitted 入口不创建、访问、遍历或销毁任何容器；只处理对象自身的整数 refcount，并可能转交析构槽。析构函数内部成员清理不属于本节点。 |
| 边界行为 | `MATCH` | Android 对旧值 `1` 与所有其他值严格分流；没有 `<=1` 合并、0/负值保护、null 检查、同步或错误返回。本地条件同为 `_ref == 1`，默认路径同为普通 `_ref--`。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整 8 指令 disasm、vtable 数据引用与当前本地精确源码可证明
范围内，未发现 `0x599888` 与 `PSBMedia::Release()` 的六维确定偏差。canonical 名称
保留 `_guess` 是因为二进制未保存精确 C++ 标识符，不构成实现 GAP。

## 所有确定 GAP

无。

## Cross-reference 与阶段 B 子节点

- canonical parent：`0x59849C`，边类型 `[vtable-member]`；这是 vtable/注册归属，
  不是直接调用。
- 终止引用路径对 `0x599830` 存在 `[vdispatch]` cross-reference；它是 sibling，
  canonical parent 仍为 `0x59849C`，不得由本节点重复派发。
- `0x5997F0` 是 `0x599830` 的 canonical lifecycle child，不是本节点子节点；本报告
  不对它作审计结论。
- 本节点在 TASK_TREE 中尚待派发的直接 canonical 子节点：**无**。

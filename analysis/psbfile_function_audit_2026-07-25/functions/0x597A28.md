# `0x597A28` — `PSBValueDispatch_native_Destruct_primary_nullsub`

## 审计身份

- canonical 地址：`0x597A28`
- canonical 名称：`PSBValueDispatch_native_Destruct_primary_nullsub`
- IDA 当前标签：`nullsub_260`。Android stripped 二进制只保留了 nullsub 标签；
  `PSBValueDispatch`、primary/native `Destruct` 与 vtable 槽归属来自该入口唯一数据 xref、
  vtable 地址点和相邻槽位结构，canonical 名称没有冒充二进制保留的 C++ 符号。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[vtable-member]`。parent 构造对象时安装包含本入口的 primary vptr；它不
  直接调用本入口，因此该边不是 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_597a28`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只视为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x597A28")`；随后 fresh 调用
`disasm(addr="0x597A28", include_total=true, max_instructions=32)`、
`xrefs_to(addrs="0x597A28")`，并读取 `0x1A0B4B8..0x1A0B4FF` 的 vtable 尾部字节。
本地址在写入前没有报告文件，本文件是本轮 fresh 审计的新建结果。

- fresh decompile 恢复为 `void nullsub_260() { ; }`；完整 disasm 证明函数范围只有
  `0x597A28: RET` 一条指令，总指令数为 1。入口没有栈帧、字段读写、条件分支、
  `BL/BLR/BR`、返回值构造或异常路径。
- fresh `xrefs_to` 恰好返回一个 data xref：`0x1A0B4D0`。该地址等于 primary vtable
  地址点 `0x1A0B3D8 + 31 * 8`，故本入口是 primary vtable 的 slot 31，而不是直接调用
  helper、NCB wrapper 或 NCB callback。
- fresh vtable bytes 将尾部四个 qword 解码为
  `slot28=@596D78`、`slot29=@597A30`、`slot30=@596F38`、`slot31=@597A28`；随后是
  secondary-base 的 `offset-to-top=-8`、空 RTTI qword，以及 secondary 地址点
  `@1A0B4E8` 的 `@597A38/@596F3C/@597A2C` 三个槽。这同时证明本入口与
  `@597A2C` 是同一 `iTJSNativeInstance::Destruct` override 的 primary/secondary ABI
  表面，而不是两者之间存在调用边。
- 对配对地址 `@597A2C` 仅做 xref 上下文查询：其唯一 data xref 为
  `0x1A0B4F8`（secondary 地址点的 slot 2）。本报告不反编译、不审计该 child，也不替它
  下结论。

## Android 伪代码（4 行）

```text
native_Destruct_primary(this_ignored):
    // 不读取 this，不写字段，不调用 delete/Release/Invalidate 或任何 callee
    // 无条件分支、默认值、错误码或异常恢复
    return
```

该伪代码覆盖本入口的全部行为与失败边界：进入函数后总是立即返回；即使传入的寄存器值
不是有效对象，本体也不会解引用它。通过虚表到达入口之前所需的有效 vptr 属调用点边界，
不是该一指令函数内部的检查。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-17`：`PSBValueDispatch final` 直接多继承
  `iTJSDispatch2` 与 `iTJSNativeInstance`，对应 Android primary/secondary 两个 vtable
  地址点；本地没有把 ABI 生成的两只入口误写成两个源码对象或两个独立业务方法。
- `cpp/plugins/psbfile/PSBDispatch.h:74-78`：类只声明一个
  `void Destruct() override`，位于 `Construct`、`Invalidate` 之后；这与 Android primary
  尾部 slot 29/30/31 和 secondary slot 0/1/2 的顺序逐项一致。
- `cpp/plugins/psbfile/main.cpp:481-484`：
  `PSBValueDispatch::Destruct()` 的函数体为空；注释准确记录 primary `nullsub_260
  @0x597A28` 与 secondary `nullsub_261 @0x597A2C`。该空体逐指令对应 Android 唯一的
  `RET`，没有额外状态变化、释放或 fallback。
- `cpp/core/tjs2/tjsInterface.h:326-336`：`iTJSNativeInstance` 将 `Destruct` 定义为
  `virtual void Destruct() = 0`，且接口注释说明它发生在销毁前。这解释了 Android vtable
  归属；具体 PSB override 的一指令 no-op 仍完全由本轮 Android 证据确认，而非从接口名
  推断行为。
- `cpp/core/tjs2/tjsObject.cpp:359-364`：通用 TJS 对象析构路径通过
  `iTJSNativeInstance *` 虚分派调用 `Destruct()`；在多继承对象上该视图对应 secondary
  vtable。它是本地调用面上下文，不是 `@597A28` 的 Android direct-call 证据。
- `cpp/plugins/psbfile/main.cpp:99-105` 与 `PSBDispatch.h:118-119`：实际对象删除由
  `PSBValueDispatch::Release()` 归零时的 `delete this` 和私有默认析构承担；本地
  `Destruct()` 不抢占这条生命周期，与 Android 本入口不 delete、不 Release 完全一致。

逐行对应：伪代码第 1 行对应 `PSBDispatch.h:78` 的唯一 override；第 2-4 行对应
`main.cpp:481-484` 的空函数体。双 vtable 中的 primary/secondary emitted 入口差异由普通
C++ 多继承 ABI 生成，本地源码保持单一 override，没有用硬编码 ARM64 vtable 偏移复刻
编译产物。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 有一只独立 emitted 空 override，位于 primary vtable slot 31；相邻 vtable bytes 还证明同一三方法 surface 在 secondary 表复现。本地以双继承类中的单一 `void Destruct() override` 表达该源码结构，由 ABI 产生两个 vtable 表面，没有把 secondary sibling 伪装成第二个源码方法。`MATCH` 仅指当前二进制可证结构，不声称恢复了原作者空函数的排版或精确符号名。 |
| 数据流 | `MATCH` | Android 唯一 `RET` 不读取 `this`/参数、不写对象或全局、不生成返回值；本地空体同样没有任何数据流。 |
| 调用链 | `MATCH` | Android 本入口无 callee，且唯一 xref 是 primary vtable 数据槽、没有 direct code xref。canonical parent 只通过 vptr 安装建立 `[vtable-member]` 关系；本地方法也只提供虚函数入口而不调用其他函数。通用 TJS 的 secondary-view 虚分派属于配对 child 的调用面，不被虚构为对本 primary 地址的直接调用。 |
| 对象生命周期 | `MATCH` | 尽管接口语义名为 `Destruct`，Android 本入口明确不删除对象、不递减 refcount、不释放 owner、不失效 dispatch；本地 override 也是 no-op，实际删除留给 `Release()` 归零路径。两边均避免了误用通用“Destruct 必须 delete”默认语义。 |
| 内部容器实现 | `N/A` | 本入口没有访问、构造、遍历或销毁任何 STL、TJS、packed 或自定义容器；它连对象字段都不读取。 |
| 边界行为 | `MATCH` | 进入入口后无条件立即返回，无 null guard、错误码、默认写值、异常或 cleanup；由于 `this` 完全未被解引用，任意寄存器值在函数体内都走相同 `RET`。本地空体保持同一入口级边界。 |

## 总判定

`ALIGNED`

本轮 fresh decompile、完整一指令 disasm、唯一 vtable data xref、vtable 尾部原始字节与本地
空 override 一致；六维没有确定 GAP，也没有影响本入口结论的证据上限。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

阶段 A 未 spawn。尚待由本节点在阶段 B 派发的唯一 canonical 直接子节点为：

- `0x597A2C PSBValueDispatch_native_Destruct_secondary_nullsub [vtable-member]`

Cross-reference：

- canonical parent `@597AD4` 通过安装 primary/secondary vptr 建立本节点归属；
  `[vtable-member]` 不表示 parent 直接调用本入口。
- `@1A0B4D0` 是本入口唯一 xref 所在的 primary vtable slot 31；它是数据地址，不是函数
  agent。
- `@597A2C` 位于 secondary vtable `@1A0B4E8` 的 slot 2（指针存放于
  `@1A0B4F8`），是 TASK_TREE 唯一 child。两只 nullsub 之间没有 call edge，且本报告只将
  secondary 地址作为 ABI 配对上下文，不替 child 完成审计。

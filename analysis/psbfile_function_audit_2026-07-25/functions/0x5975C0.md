# `0x5975C0` — `PSBValueDispatch_decodeName_guess`

## 审计身份

- canonical 地址：`0x5975C0`
- canonical 名称：`PSBValueDispatch_decodeName_guess`
- IDA 当前标签：`PSBValueDispatch_decodeName_guess`。Android stripped 二进制没有保留精确
  C++ 符号，故 canonical 名称保留 `_guess`，不把本地标识符反向当作命名证据。
- 分组：A（PSBValueDispatch / packed dispatch ABI）
- TASK_TREE parent：`0x597AD4 PSBValueDispatch_ctor_guess`
- parent 关系：`[helper]`。这是私有 helper 对构造所得 dispatch 对象的逻辑归属；fresh
  `xrefs_to` 为零，`@597AD4` 不直接调用本入口，故没有伪装成 `[direct-call]`。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b28c/f_59b48c/f_5981f8/f_597ad4/f_5975c0`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `PSBFile.dll` 只视为 NCB 模块名字面量，本入口不读取
  该字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x5975C0")`，随后 fresh 调用
`disasm(addr="0x5975C0", include_total=true)` 与
`xrefs_to(addrs="0x5975C0")`。本地址在本轮审计前没有报告，本文件是 fresh 证据后的新建
结果。

- fresh decompile 恢复签名
  `void (PSBValueDispatch *self, std_string_cow_arm64 *name, unsigned int nameIndex)`；函数体
  只有 `PSB_DecodeName_guess(name, self->owner, nameIndex)`，没有条件分支、默认值写入或返回
  值。
- fresh 完整 disasm 覆盖 `0x5975C0..0x5975CC`，共 4 条指令：`LDR X8,[X0,#0x18]` 从
  dispatch 内含 raw-node holder 读取 owner；`MOV X0,X1` 把输出 string 移入第一实参；
  `MOV X1,X8` 把 owner 移入第二实参；`B @597B1C` 保留 `W2=nameIndex` 并直接尾转移。
- 末条是直接 `B`，不是 `BL/BLR`：本入口以 tail-call wrapper 形式进入
  `PSB_DecodeName_guess@0x597B1C`，没有 return-site、post-call cleanup 或 fallback。它仍是
  Android 保留的直接调用关系，但不应描述为普通带返回地址的 `BL`。
- fresh `xrefs_to` 返回 code/data xref 均为空；本入口不是 primary/secondary vtable 槽，
  不是 NCB wrapper、注册 callback 或模块生命周期入口。它归属于
  `PSBValueDispatch` 的私有 packed-name helper surface。

## Android 伪代码（7 行）

```text
decodeName(self, name, nameIndex):
    owner = self.value.owner                 // self 不校验；非法地址直接 fault/UB
    // name、owner、nameIndex 均无 null/range/有效性检查，也无默认输出
    tailcall PSB_DecodeName(name, owner, nameIndex)
    // 本入口没有本地失败分支、返回值、cleanup 或 fallback
    // 解码成功、空名称、异常和越界/无效输入行为全部原样继承 callee
    // 不返回到 wrapper 的独立 post-call 路径
```

伪代码覆盖本入口的全部数据搬运、唯一控制流边、无默认值以及所有本地失败边界；
`@597B1C` 的 packed-name 解码算法属于其唯一 canonical function agent，本报告不替它下审计
结论。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/PSBDispatch.h:16-20,130-132`：本地 `PSBValueDispatch final` 对象的
  `PSBRawNode value_` 字段位于 dispatch 自身，构造入口接收 PSBFile-compatible holder 与
  node；这给出
  Android `self.value.owner` 读取所依附的本地对象边界，而不硬编码 ARM64 `+0x18` ABI
  偏移。
- `cpp/plugins/psbfile/PSBDispatch.h:118-123`：`decodeName_guess(std::string &,
  uint32_t) const` 是独立 private member 声明，精确对应 Android 的独立 emitted、只读 self
  wrapper；`_guess` 明确保留 stripped 二进制无法恢复原名的证据上限。
- `cpp/plugins/psbfile/main.cpp:27-33`：本地 out-of-line 定义只执行
  `detail::DecodeName_guess(name, value_.GetOwner(), nameIndex)`；`main.cpp:27-28` 对应伪代码
  第 1 行的三参数 member 边界，`main.cpp:32` 依次承接伪代码第 2-4 行的 owner 读取与直接
  helper 调用，函数随后无额外语句，对应第 5-7 行。
- `cpp/plugins/psbfile/PSBRawFile.h:111`：`PSBRawNode::GetOwner() const` 直接返回
  `owner_`，对应 Android `LDR [self,#0x18]` 的单次借用 owner 读取；没有 AddRef、Release、
  null gate 或转换。
- `cpp/plugins/psbfile/PSBPackedInternal.h:264-265`：callee 声明参数顺序为
  `(std::string &name, const PSBRawOwner *owner, uint32_t nameIndex)`，逐寄存器对应 Android
  尾跳前的 `X0=name`、`X1=owner`、保留 `W2=nameIndex`。其实现
  `cpp/plugins/psbfile/PSBRawFile.cpp:83-106` 仅作为调用目标定位；算法结论留给
  `@597B1C` 的 canonical agent。

逐行对应：伪代码第 1-2 行对应 `PSBDispatch.h:122-123`；第 2 行的 owner 载入对应
`main.cpp:32` 与 `PSBRawFile.h:111`；第 3-4 行对应 `main.cpp:32` 和
`PSBPackedInternal.h:264-265`；第 5-7 行对应 `main.cpp:33` 前没有任何 post-call 语句，且
Android 以 `B` 而非 `BL` 完成本体。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是独立 emitted 的三参数 const-like member wrapper：读取 dispatch 内含 raw node 的 owner 后直接进入共享 helper；本地保留独立 private out-of-line member、相同参数 surface 和一语句函数体。stripped 二进制不能证明精确原名，本地以 `_guess` 明示该上限；`MATCH` 只覆盖当前可证结构。 |
| 数据流 | `MATCH` | Android 单次读取 `self+0x18` owner，将输入 `name`、该 owner、原样 `nameIndex` 分别置于 X0/X1/W2；本地 `value_.GetOwner()` 后按相同顺序把三者交给 `detail::DecodeName_guess`，没有副本、默认输出或附加转换。 |
| 调用链 | `MATCH` | Android 以直接 tail branch `B @597B1C` 结束；本地直接调用同一个 out-of-line `detail::DecodeName_guess` boundary，调用后无业务步骤。fresh xref 为零，TASK_TREE parent 仅标 `[helper]`，且 callee 只记 cross-reference，不重复派发。 |
| 对象生命周期 | `MATCH` | Android 只借用 self 内的 owner 指针，不 AddRef/Release、不构造临时 owner holder，也不清理 output string；本地 inline `GetOwner()` 同样只借出指针，所有生命周期和异常展开均由 caller/callee 已有对象承担。 |
| 内部容器实现 | `N/A` | 本 wrapper 自身不创建、遍历或修改 packed array/STL/TJS 容器；它只转发已有 `std::string` 与 owner。`@597B1C` 内部 name trie/vector/string 操作属于另一 canonical 函数的审计范围。 |
| 边界行为 | `MATCH` | Android 与本地都不检查 self、name、owner 或 nameIndex，不写失败默认值，不捕获异常，也没有返回后的 fallback；无效 self 在 owner 读取处失败，其余空指针、越界、空名称与异常行为原样转交 `@597B1C`。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整 4 指令反汇编、零 xref 结果及本地精确 source boundary 可证明
范围内，`0x5975C0` 与本地 `PSBValueDispatch::decodeName_guess` 的结构、数据流、直接尾调用、
借用生命周期和边界均一致；没有确定 GAP。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 本节点没有 canonical 直接子节点；尚待派发列表为空，本阶段 A 未 spawn child。
- 非 canonical cross-reference：本函数直接尾调用 `@597B1C PSB_DecodeName_guess`；后者的
  canonical parent 是 `@596F50 PSBValueDispatch_EnumMembers`，不得由本节点重复派发。
- `@597AD4` 是本节点 canonical `[helper]` parent，但 fresh xref 为空，parent 只提供逻辑
  归属而非 Android retained direct call。

# `0x5998BC` — `PSBMedia_NormalizeDomainName_nullsub`

## 身份与归属

- canonical 地址：`0x5998BC`
- canonical 名称：`PSBMedia_NormalizeDomainName_nullsub`
- 分组：E / `PSBMedia` singleton / `psb:` storage gateway
- TASK_TREE parent：`0x59849C PSBFile_preRegister_guess_initPsbFile`
- canonical parent 关系：`[vtable-member]`。该边表示 parent 所建立并注册的
  `PSBMedia` 对象采用包含本入口的 vtable；它不是 `0x59849C -> 0x5998BC` 的直接调用。
- 审计 agent path：`/root/f_42cf28/f_59849c/f_5998bc`
- 审计日期：2026-07-25
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cf28/f_59849c/f_5998bc` fresh 执行了
`decompile(0x5998BC)`；随后执行完整 `disasm(0x5998BC)`、`xrefs_to(0x5998BC)`、
`lookup_funcs(0x5998BC)`，并读取 `0x1A0B500..0x1A0B57F` 的 vtable 原始数据。本报告在
本轮开始时不存在，不是沿用旧报告。

fresh decompile 的完整结果为：

```c
void __fastcall nullsub_262(PSBMedia *self, ttstr_psb_arm64 *name)
{
  ; /*0x5998bc*/
}
```

补充证据：

- `lookup_funcs` 给出函数范围 `0x5998BC..0x5998BF`，大小 `0x4`，IDA 名称
  `nullsub_262`。
- 完整反汇编只有 `0x5998BC: RET` 一条指令；没有参数读取、内存访问、写回、条件跳转、
  `BL`/尾调用、返回值构造或异常路径。
- `xrefs_to(0x5998BC)` 只返回一条 data xref：`0x1A0B538`；没有 code xref，因此不能把
  任一 retained Android 函数写成对本入口的直接 caller。
- vtable 地址点为 `0x1A0B510`；原始 64 位指针序列显示
  `0x1A0B530 -> 0x5998A8`、`0x1A0B538 -> 0x5998BC`、
  `0x1A0B540 -> 0x5998C0`。所以本入口确为地址点 `+0x28` 的 vslot 05；相邻项只用于
  slot 定位，不代表函数间调用。

## Android vtable / NCB / 调用链归属

本入口是 `PSBMedia` 的 `iTVPStorageMedia::NormalizeDomainName(ttstr &)` override，位于
`PSBMedia` vtable vslot 05。它不是 NCB member wrapper、注册 callback 或直接调用 helper；
storage 规范化链通过 `iTVPStorageMedia` 虚调用间接到达该槽。函数本体立即返回，调用链在
本入口处终止。TASK_TREE 将它挂在 `0x59849C` 下是 vtable/object-registration 归属，不是
retained direct-call 边。

## Android 关键伪代码（4 行）

```text
NormalizeDomainName(self, name):
    // 不读取 self 或 name，也不修改 name
    // 无条件、默认值、callee、失败返回或异常处理
    return
```

## 本地精确实现与逐行对照

精确位置：

- [cpp/plugins/psbfile/PSBMedia.cpp](../../../cpp/plugins/psbfile/PSBMedia.cpp) 第 10–13 行，
  符号 `PSB::PSBMedia::NormalizeDomainName(ttstr &)`；
- [cpp/plugins/psbfile/PSBMedia.h](../../../cpp/plugins/psbfile/PSBMedia.h) 第 10、25–31 行，
  `PSBMedia : public iTVPStorageMedia` 及该 override 的声明/相邻虚方法顺序；
- [cpp/core/base/StorageIntf.h](../../../cpp/core/base/StorageIntf.h) 第 114–130 行，
  `iTVPStorageMedia` 的 destructor、`AddRef`、`Release`、`GetName`、
  `NormalizeDomainName`、`NormalizePathName` 虚接口顺序；
- [cpp/core/base/StorageIntf.cpp](../../../cpp/core/base/StorageIntf.cpp) 第 428–439 行，
  storage-name 解析链对 `NormalizeDomainName(domain)` 与随后 path normalize 的虚调用；
- [cpp/plugins/psbfile/PSBMediaRegistry.cpp](../../../cpp/plugins/psbfile/PSBMediaRegistry.cpp)
  第 6–11 行，canonical parent 对象的创建与 storage-media 注册上下文。

| Android 伪代码步骤 | 本地逐行复刻 |
| --- | --- |
| 行 1，签名接收 `self` 与可变 `name` 引用 | `PSBMedia.h:10,27` 在同一派生类上声明 `void NormalizeDomainName(ttstr &name) override`；`PSBMedia.cpp:11` 给出对应 out-of-line 定义。 |
| 行 2，不读取两个参数且不写回 | `PSBMedia.cpp:11` 省略形参名，函数体没有任何表达式；第 12 行仅是地址注释，不产生运行时操作。 |
| 行 3，无分支、默认值、callee 或失败处理 | `PSBMedia.cpp:11-13` 是空 body，没有 guard、字符串变换、helper、返回值或异常包装。 |
| 行 4，直接返回 | 空 `void` body 落到函数末尾；其目标代码可形成与 Android 单条 `RET` 相同的语义。 |

## 六维审计

| 维度 | 状态 | Android 证据与本地对照 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留一个独立的两参数 `void` vtable 入口，body 为空；本地保留 `PSBMedia` 派生类中的独立 out-of-line `override` 与空 body。stripped 二进制不能证明原始标识符拼写，`MATCH` 仅覆盖可证的 override/空函数结构。 |
| 数据流 | `MATCH` | Android 单条 `RET` 不读取 `self/name`、不修改 `name`、不产生返回数据；本地省略形参名的空 body 同样没有读写或转换。 |
| 调用链 | `MATCH` | Android 唯一 xref 是 vtable data 项，表明通过 storage-media vdispatch 间接进入；函数本体无 callee。本文本地接口声明、虚调用点和空 body 保留同一“间接进入、立即终止”形状；未把 canonical parent 或 vtable sibling 伪装成直接调用。 |
| 对象生命周期 | `N/A` | 本入口不分配、析构、改变引用计数、保存指针或转移所有权；`self` 和 `name` 均未被访问。`PSBMedia` 的创建/注册属于 parent 与其他函数的审计范围。 |
| 内部容器实现 | `N/A` | 本入口不创建、遍历、查询或修改任何容器。 |
| 边界行为 | `MATCH` | Android 对任意已进入该 emitted body 的参数位型都只执行 `RET`：不规范化空/非空 domain，不提供 fallback、错误码或局部异常路径；本地空 override 同样保持输入不变。此机器码事实不被扩张为“允许以空 `this` 作 C++ 调用”的源码契约。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、完整单指令反汇编、唯一 vtable data xref 与本地精确空 override 可证明
范围内，未发现 `0x5998BC` 的六维确定偏差。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 本节点没有 canonical 直接子节点；尚待派发列表为“无”，本 turn 未 spawn child。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x5998BC` 条目。
- `0x5998A8`、`0x5998C0` 只是用于确认 vslot 位置的相邻 vtable members；它们不是本入口的
  direct caller/callee，也不为它们创建重复 agent。

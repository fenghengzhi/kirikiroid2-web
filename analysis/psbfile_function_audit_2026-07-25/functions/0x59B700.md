# `0x59B700` — `PSBFile_load_GetFlags_guess`

## 审计身份

- canonical 地址：`0x59B700`
- canonical 名称：`PSBFile_load_GetFlags_guess`
- IDA 当前标签：`PSBFile_loadMethod_GetFlags_guess`。Android stripped 二进制没有保留该
  ncbind 模板实例的精确源码符号，因此 canonical 名称继续保留 `_guess`。
- 分组：F（typed NCB 自动注册与 wrapper 尾链）
- TASK_TREE parent：`0x59B570 PSBFile_load_FuncCall_guess`
- parent 关系：`[vtable-member]`。这是 canonical spanning tree 为同一 load Method
  wrapper 选择的归属边；`0x59B700` 与 parent `0x59B570` 实际是该 wrapper 的 sibling
  vslots，parent 不直接调用本函数。
- 审计 agent path：
  `/root/f_42cf28/f_59a8d8/f_597f38/f_59aeec/f_59b570/f_59b700`
- 本轮日期：2026-07-25
- 唯一权威：Android kirikiroid2 `libkrkr2.so` 内嵌实现；未查看、引用或从外部
  `psbfile.dll` 推导。二进制中的 `"PSBFile.dll"` 仅是 NCB 模块名字面量。

## Android vtable / NCB / 调用链归属与 fresh 证据

2026-07-25 本新会话由上述唯一 function subagent fresh 调用 IDA MCP
`decompile(addr="0x59B700")`；随后补充目标 `disasm`、目标及同 wrapper sibling 的
`xrefs_to`、wrapper vtable 原始字节，并仅作为归属上下文 fresh 读取
`@597F38/@59AEEC`。后两个上下文函数不在本报告中接受六维审计。

- fresh decompile 得到完整函数体：

  ```c
  unsigned int __fastcall PSBFile_loadMethod_GetFlags_guess(
      PSBFile_loadMethodWrapper_arm64 *self)
  {
    return 0; /*0x59b704*/
  }
  ```

- fresh disasm 确认函数范围 `0x59B700..0x59B704`、size `0x8`，总计仅两条指令：
  `MOV W0, WZR`；`RET`。没有参数读取、内存访问、条件分支、直接/间接调用或异常边。
  旧 IDA 泛化类型因 `this` 完全未使用而把它从伪签名省略；2026-08-02 已按 vtable
  interface ABI 纠正为 `unsigned int(self)`。vslot 语义仍是无条件返回
  `tjs_uint32/FlagsT 0` 的 const getter。
- `xrefs_to(0x59B700)` 只有 vtable data xref `0x1A0B918`，没有 code xref。
  `xrefs_to(0x59B570)` 只有 `0x1A0B820`，`xrefs_to(0x59B6DC)` 只有
  `0x1A0B908`。原始字节进一步确认同一连续 vtable：address-point
  `0x1A0B810` 的 `+0x10` 存放 `0x59B570`；`0x1A0B908` 存放
  `0x59B6DC`；`0x1A0B918` 存放 `0x59B700`。
- 注册上下文 `@597F38:0x59804C..0x5980B8` 分配 `0x40` 字节的 typed Method
  wrapper，写入主 vptr `off_1A0B810`、字面量 `L"Function"`、成员指针对
  `{PSBFile_Load@0x598268, 0}`，再以字面量 `L"load"` 将 method-interface
  subobject 交给 `@59AEEC`。这把本 vslot 的归属限定为 `PSBFile.load` wrapper。
- 仅作调用链上下文的 `@59AEEC:0x59B03C..0x59B0A8` 在 item 非空时依次通过
  item-interface vtable 取得 dispatch、type、flags，调用 `ncb_registerMember`，最后
  Release item；其中 flags 是 `(*(*a3 + 8))(a3)` 的虚调用。
- `off_19FE1F8` 的 item-interface `+8` 槽指向通用 shim `@534DAC`；fresh
  decompile/disasm 明确为 `LDR X0,[X0,#8]` 取回 wrapper `_this`，再从 wrapper vptr
  `+0x108` 取函数地址并 `BR`。对 `off_1A0B810` 而言，`+0x108` 正是
  `0x1A0B918 -> @59B700`。因此到本 getter 的运行边是两级 vdispatch，目标没有
  retained code xref；它不是 `@59B570` 或 `@59B6DC` 的 direct callee。

## Android 伪代码（2 行）

```text
GetFlags(this):                 // this 不读取；无条件、默认值或失败分支
    return 0                    // 唯一返回边；不调用、不抛出、不改变状态
```

这两行覆盖完整函数：Android 中没有其他条件、默认值、失败码或异常清理边界。

## 本地精确文件、符号、行号与逐行对照

- `cpp/plugins/psbfile/main.cpp:751-755`，`NCB_REGISTER_CLASS(PSBFile)`：第 1097
  行 `Method(TJS_W("load"), &PSBFile::Load)` 选择 typed instance Method wrapper，
  对应 Android `@597F38` 的 `L"load"`、`off_1A0B810` 与成员指针对构造。
- `cpp/plugins/psbfile/PSBRawFile.h:73-107`，`PSBFile`：第 106 行声明
  `bool Load(tTJSVariant value)`，证明注册目标是非 static 的 `PSBFile` 成员方法；该
  native target 的函数体不属于本 getter，本文不代审计。
- `cpp/core/plugin/ncbind.hpp:1211-1227`，
  `InvokeType::InvokeCommand<REFCLASS, METHOD, ivtNormal>`：第 1224-1226 行把
  `Flags` 定义为 `ClassT == void ? TJS_STATICMEMBER : 0`。本实例
  `ClassT=PSBFile`，因此编译期常量是 `0`，没有运行时判断。
- `cpp/core/plugin/ncbind.hpp:1325-1358`，`ncbNativeClassMethod<CommandT>`：第
  1357 行目标模板 override
  `FlagsT GetFlags() const { return CommandT::Flags; }`，其 `PSBFile::Load`
  specialization 正好编译为 Android 的 `MOV W0, WZR; RET`。
- `cpp/core/plugin/ncbind.hpp:1714-1718`，`ncbRegistClass::Method`：创建上述
  `ncbNativeClassMethod<InvokeCommand<PSBFile, ...>>` 并把其 method-interface
  交给 `DoItem`；第 1731 行在未显式给 tag 时选择 `Normal`。
- `cpp/core/plugin/ncbind.hpp:895-904`，`ncbNativeClassMethodBase::iMethod`：第
  899 行 interface `GetFlags()` 转调 `_this->GetFlags()`；
  `cpp/core/plugin/ncbind.hpp:1879-1887` 的 `RegistItem` 第 1885 行调用
  `item->GetFlags()` 并把结果传入 `TJSNativeClassRegisterNCM`。这与 Android
  `@59AEEC` 的两级虚分派数据流一致。
- `cpp/core/plugin/ncbind.hpp:28-35,835-844`：`FlagsT` 是 `tjs_uint32`，
  `ncbIMethodObject::GetFlags() const` 是接口虚槽；Android 写 `W0` 返回零与该返回类型
  一致。

逐行对应：伪代码第 1 行对应本地 `ncbind.hpp:899,1357` 的 const、无状态 getter 与
`iMethod` 转调；伪代码第 2 行对应 `ncbind.hpp:1224-1226,1357` 的编译期
`CommandT::Flags == 0`。注册和调用归属由 `main.cpp:754`、
`ncbind.hpp:1716-1718,1885` 对应 Android `@597F38/@59AEEC`，但这些上下文不在本
getter 内产生额外调用或状态。

## 六维审计

| 维度 | 状态 | Android 与本地逐项证据 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 保留 load typed Method wrapper 的独立 `GetFlags` vslot，vtable `0x1A0B918` 指向一个 8-byte constant getter；本地仍由 `ncbNativeClassMethod<CommandT>` 的 virtual override 生成该独立 specialization，没有改写为注册点手填常量或业务层分支。精确 mangled/source 拼写不可由 stripped 二进制恢复，故名称保留 `_guess`，但不影响可证明的 emitted 结构一致性。 |
| 数据流 | `MATCH` | Android 不读取 `this` 或任何外部数据，只把 `WZR` 写入返回寄存器；本地 `CommandT::Flags` 对非 static `PSBFile::Load` 在编译期为 0，同样没有运行时输入或中间状态。 |
| 调用链 | `MATCH` | Android 注册体构造 `L"load"` wrapper，RegistItem 通过 interface 取得 flags，再由 wrapper vtable vdispatch 到本入口；目标无 code xref、无 callee。`0x59B570/0x59B6DC/0x59B700` 是同 wrapper sibling vslots。当前本地链为 `Method("load", &PSBFile::Load) -> iMethod::GetFlags -> ncbNativeClassMethod::GetFlags`，同样不是 FuncCall 或 destructor 对本函数的直接调用。 |
| 对象生命周期 | `N/A` | 本入口不构造、retain、release、销毁或修改任何对象；只返回常量。wrapper 的分配、注册 Release 与删除属于其他 emitted 入口。 |
| 内部容器实现 | `N/A` | 本入口不访问 NCB autoreg 链、member table、TJS Array、PSB 容器或 STL 容器。 |
| 边界行为 | `MATCH` | Android 对所有隐式/显式调用状态均无条件返回 0；没有 null gate、错误码、默认替代、异常或副作用。当前本地 compile-time constant getter 保持相同完整边界。 |

## 总判定

`ALIGNED`

在本轮 fresh target decompile、完整两指令 disasm、唯一 vtable data xref、连续 vtable
原始字节、Android NCB 构造/注册上下文与本地模板逐行对照可证明范围内，六维实现一致。

## 确定 GAP

无。

证据边界：Android stripped/O3 二进制不能恢复该 specialization 的精确 C++ mangled 名与
源码拼写，因此 canonical 名称保留 `_guess`；ARM64 vtable 字节偏移是 ABI 结果，不要求
wasm32 硬凑。

## TASK_TREE 直接子节点与 cross-reference

- 本节点是 leaf；尚待派发的 canonical 直接子节点：空。阶段 A 未 spawn 任何 agent。
- canonical parent `@59B570` 仅因 spanning tree 的 `[vtable-member]` 归属而位于上层；
  Android 中它是同一 vtable 的 `FuncCall` sibling，不调用 `@59B700`。
- `@59B6DC` 是同一 wrapper 的 deleting-destructor sibling vslot，也不调用本函数；不得
  把二者伪装为 direct-call 或为本节点重复派发。
- `@597F38` 构造并以字面量 `L"load"` 绑定 wrapper，`@59AEEC` 在注册期经 interface
  读取 flags；二者只是非 canonical 上下文 cross-reference，各自由其唯一 function
  subagent 审计。
- `@59B708` 是 `@59B570 FuncCall` 的 typed Invoke/argument helper，与本 getter无调用
  关系；本节点不为它派发重复 agent。

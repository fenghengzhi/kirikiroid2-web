# `0x597E98` — `ncbClassInfo_PSBFile_GetName_guess`

## 身份与归属

- canonical 地址：`0x597E98`
- canonical 名称：`ncbClassInfo_PSBFile_GetName_guess`
- 分组：C / PSBFile typed NCB 前段 / class-info helper
- TASK_TREE parent：`0x597F24 ncbClassInfo_PSBFile_InfoCtor_guess`，关系为
  `[helper]`。这表示本 getter 与 parent 所构造的 class-info 状态之间的逻辑归属；Android
  对 `0x597E98` 的 fresh xref 查询为 0，parent 也不直接调用本入口，因此不得把该边写成
  `[direct-call]`。
- 审计 agent path：`/root/f_42cef8/f_597f24/f_597e98`
- Android vtable / NCB / 调用链归属：本入口不是 vtable 槽、TJS member wrapper、注册
  callback 或对象方法 dispatch；它是 `ncbClassInfo<PSBFile>` 的 template-emitted 静态
  getter，读取 PSBFile class-info 共享状态中的 name 指针。该共享字段位于
  `0x1AB5100`，由 class-info 静态初始化建立默认值，并由独立的 Set/Clear/注册生命周期入口
  读写；那些函数在本报告中仅作状态归属上下文，不替它们下审计结论。
- 唯一权威来源为 Android kirikiroid2 `libkrkr2.so` 内嵌实现；本轮未查看、引用或从外部
  `psbfile.dll` 推导。

## 本轮 fresh Android 证据

2026-07-25 在本新会话由 `/root/f_42cef8/f_597f24/f_597e98` fresh 执行了
`decompile(0x597E98)`；随后执行完整 `disasm(0x597E98)`（3 条指令）、
`xrefs_to(0x597E98)`、`xref_query(to, 0x597E98)`、`lookup_funcs`、class-info 全局符号查询及
name 字段 xref 查询复核。此次记录不是沿用旧报告：本轮开始时目标报告不存在。

- fresh decompile 当前 IDA 标签为 `PSBFile_ncbClassInfo_GetName_guess`，函数范围为
  `0x597E98..0x597EA7`；TASK_TREE canonical 名称采用
  `ncbClassInfo_PSBFile_GetName_guess`。
- fresh decompile 的完整有效语义是
  `return PSBFile_ncbClassInfo_name;`，返回字段地址为 `0x1AB5100`。
- `0x597E98: ADRL X8, PSBFile_ncbClassInfo_initialized`：取得同一 class-info 静态状态块的
  基址；这是地址形成，不读取 `initialized` 的值。
- `0x597EA0: LDR X0, [X8,#8]`：从 `0x1AB5100`
  `PSBFile_ncbClassInfo_name` 载入一只 64 位指针作为返回值。
- `0x597EA4: RET`：原样返回该指针。
- 完整函数没有参数、条件分支、`BL`、null 检查、替代默认值、分配、引用计数、异常路径或
  显式失败返回。
- `xrefs_to` 与独立 `xref_query` 都报告函数入口的 incoming xref 总数为 0；这只证明当前
  Android emitted body 没有 retained caller，不从 negative xref 推断模板为何被保留。
- 共享字段上下文：全局符号查询把 name 定位到 `0x1AB5100`；字段 xref 显示本入口在
  `0x597EA0` 读取它。`PSBFile_ncbClassInfo_static_init@0x42CEF8` 的上下文反汇编在
  `0x42CF14` 把同一字段写为 null，因此默认状态或 Clear 后的 null 会被本 getter 原样返回；
  本入口自身不生成默认值。

## Android 伪代码（4 行）

```text
NameT GetName():
    return PSBFile_ncbClassInfo_name    // _info.name；当前值可为 null
// 无条件分支、fallback、callee、所有权变更、错误码或异常处理
```

## 本地精确对照

- name 的共通类型：
  [cpp/core/plugin/ncbind.hpp](../../../cpp/core/plugin/ncbind.hpp) 第 28–35 行，符号
  `ncbTypedefs::NameT`；第 29 行将其定义为 `tjs_char const *`。
- class-info 模板与本函数精确源码：同文件第 73–85 行，符号
  `ncbClassInfo<T>::GetName()`；第 82 行完整实现为
  `static inline NameT GetName() { return _info.name; }`。
- 共享状态定义与默认值：同文件第 103–114 行，符号
  `ncbClassInfo<T>::InfoT`、`ncbClassInfo<T>::_info`；第 104 行把 `name` 初始化为
  `nullptr`，第 107 行保存 `NameT name`，第 114 行定义模板静态对象。
- 显式模板实例化：同文件第 2160–2164 行，符号 `NCB_REGISTER_CLASS_COMMON`；第 2161 行
  `template struct ncbClassInfo<cls>` 会为具体 native class 实例化 class-info helpers。
- PSBFile 展开点：
  [cpp/plugins/psbfile/main.cpp](../../../cpp/plugins/psbfile/main.cpp) 第 751-755 行，符号
  `NCB_REGISTER_CLASS(PSBFile)`。

逐项对应：

1. Android `ADRL + LDR + RET` 与本地第 82 行“读取 `_info.name` 并按值返回”一一对应；
   两边均不先读取 `initialized`，也不合成 fallback。
2. Android AArch64 上 `NameT` 表现为 64 位载入；本地用 `tjs_char const *` 表达相同源码
   类型，让 wasm32 / 其他目标 ABI 自行决定指针宽度，没有硬凑 Android 字节布局。
3. Android 本入口只借出当前指针，不复制字符串、不 AddRef/Release、不清空状态；本地原始
   const 指针按值返回具有同样的非 owning 数据流。
4. Android getter 不负责 name 的初始化、设置或清理；本地也把这些职责留在 `InfoT`
   构造器及 `Set`/`Clear`，未把生命周期逻辑折入 getter。

## 六维判定

| 维度 | 状态 | 证据与结论 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android 是零参数、单字段读取的 class-info 静态 getter；本地 `ncbClassInfo<T>::GetName()` 保留相同模板层级、静态 getter 形状和 `_info.name` 字段访问。stripped 二进制不能证明模板/成员原始拼写，`MATCH` 只覆盖可证结构。 |
| 数据流 | `MATCH` | Android 从共享状态 `0x1AB5100` 载入 name 指针到返回寄存器并原样返回；本地直接返回 `_info.name`，没有中间转换、拷贝或 fallback。 |
| 调用链 | `MATCH` | Android emitted body 无 callee，且两种 fresh incoming-xref 查询均为 0；本地 getter 本体同样无 helper/callback 调用。TASK_TREE 的 parent 只是 `[helper]` 逻辑归属，没有伪装成 retained direct call。 |
| 对象生命周期 | `MATCH` | Android 不分配、不复制、不 AddRef/Release，也不修改共享状态，只返回当前借用指针；本地 `NameT=tjs_char const *` 按值返回且 getter 无写操作，所有权与生命周期行为一致。 |
| 内部容器实现 | `N/A` | 本入口只读取一个原始指针字段，不创建、遍历或修改任何容器。 |
| 边界行为 | `MATCH` | Android 不检查 initialized/name，也不提供错误路径；name 为 null 时返回 null，非 null 时原样返回。静态状态本身保证字段地址有效，本地直接字段返回保留相同无 guard 边界。 |

## 总判定

`ALIGNED`

在本轮 fresh Android decompile、完整 3 指令反汇编、双 xref 查询和共享字段初始化证据可证明
范围内，本地 `ncbClassInfo<PSBFile>::GetName()` 与 `0x597E98` 的六维行为一致。

## 确定 GAP

无。

## TASK_TREE 直接子节点与 cross-reference

- 本节点没有 canonical 直接子节点；尚待派发列表为空，本 turn 未 spawn child。
- TASK_TREE 的非 canonical Cross-reference 节没有 `0x597E98` 条目。
- `0x597F24` 仅是本节点的 canonical `[helper]` parent，不是 Android retained caller；不为
  parent 或共享字段的其他读写入口创建重复函数 agent。

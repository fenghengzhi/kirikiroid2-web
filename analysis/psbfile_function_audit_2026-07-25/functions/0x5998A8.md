# `0x5998A8` — `PSBMedia_GetName_guess`

## 节点身份与审计边界

| 项目 | 值 |
| --- | --- |
| canonical 地址 | `0x5998A8` |
| canonical 名称 | `PSBMedia_GetName_guess`（stripped 二进制未保留精确原始 C++ 标识符） |
| MANIFEST 分组 | E |
| TASK_TREE parent | `0x59849C` `PSBFile_preRegister_guess_initPsbFile` |
| canonical 边 | `[vtable-member]`（vtable 归属，不是直接调用） |
| 审计 agent path | `/root/f_42cf28/f_59849c/f_5998a8` |
| 本轮日期 | `2026-07-25` |
| 本轮范围 | 只审计 `0x5998A8`；未审计或派发第二个函数，未修改 `cpp/` |

唯一权威来源是 Android kirikiroid2 `libkrkr2.so` 的内嵌实现。本报告未查看、引用或
从外部 `psbfile.dll` 推导任何结论；二进制中的 `"PSBFile.dll"` 仅是模块名字面量。

## 本轮 fresh IDA 证据

- 本轮新会话 fresh 调用
  `mcp__ida_mcp_pro__decompile(addr="0x5998A8")`；Hex-Rays 得到
  `void PSBMedia_GetName_guess(PSBMedia *self, ttstr *name)`，函数体只在
  `0x5998B8` 执行 `sub_54DEFC(name, L"psb")`。
- 补充 fresh `disasm(addr="0x5998A8")`：函数共 4 条指令、大小 `0x14`：
  `ADRL X8, apsb` → `MOV X0, X1` → `MOV X1, X8` →
  `B sub_54DEFC`。这是尾调用，没有本函数栈帧、条件跳转、返回值加工或异常处理。
- `get_bytes(0x14C900C, 16)` 返回
  `70 00 73 00 62 00 00 00 ...`，独立确认传入的是 UTF-16LE
  `L"psb"`，而不是依据本地变量名或 PSB 键名猜测。
- `xrefs_to(0x5998A8)` 只有数据引用 `0x1A0B530`。读取
  `0x1A0B510` 起的 Android vtable qword 序列后，偏移 `+0x20` 的第 5 项
  正是 `0x5998A8`；按 emitted 槽编号为 `PSBMedia` vslot 04。
- 为确认间接调用归属，只把通用引擎函数 `sub_8E881C` 作为上下文读取：
  `0x8E8858` 通过 `*(vptr + 32)` 调用 media 的名称访问器，恰好命中上述
  vslot 04。该通用函数不在 114-address MANIFEST 中，本报告不替它下审计结论。
- 仅为识别尾调用语义而上下文反编译 `sub_54DEFC`：它是
  `ttstr` 对 UTF-16 字面量的赋值路径，按目标当前是否为空/共享而分配、独立化或
  重置底层字符串；该通用字符串函数也不属于本节点的函数审计。

## Android vtable / NCB / 调用链归属

`0x5998A8` 属于 `PSBMedia` vtable `0x1A0B510` 的 vslot 04（字节偏移
`+0x20`），不是 NCB method/property wrapper，也没有直接代码 caller。canonical
parent `0x59849C` 首次构造进程期 `PSBMedia` 并把该 vtable 安装到对象上；这是一条
`[vtable-member]` 归属边，不能伪装成 `0x59849C` 对本函数的直接调用。

实际消费路径由通用 storage-media manager 经接口虚分派进入：Android
`sub_8E881C+0x3C @ 0x8E8858` 调用 `vptr+32`，本函数随即把固定 media 名
`psb` 写入调用者提供的 `ttstr`。本函数不读 `self` 的任何字段，也不参与
`PSBFile` 的 NCB member 注册。

## Android 关键伪代码（4 行）

```text
PSBMedia_GetName(self, out_name):
  literal = UTF16("psb")
  ttstr_assign(out_name, literal)          // tail-call；self 未使用
  // 无条件、无默认分支/状态码/null guard；赋值失败或异常原样向调用者传播
```

## 本地实现与逐行对照

精确位置：

- `cpp/plugins/psbfile/PSBMedia.h:10,25` — `PSBMedia` 的继承关系及
  `PSBMedia::GetName(ttstr &)` 唯一实现；
- `cpp/core/base/StorageIntf.h:114-122` — `iTVPStorageMedia::GetName` 接口位置；
- `cpp/core/tjs2/tjsString.h:147-157` —
  `tTJSString::operator=(const tjs_char *)`（`ttstr` 的字面量赋值路径）；
- `cpp/core/base/StorageIntf.cpp:281-283` — storage-media manager 的
  `media->GetName(medianame)` 虚调用点。

| Android 伪代码步骤 | 本地精确复刻 |
| --- | --- |
| 行 1，接口成员接收 `self` 与 caller-owned 输出字符串 | `PSBMedia.h:10,25` 以 `PSBMedia : iTVPStorageMedia` 的 `override` 成员接收 `ttstr &name`；没有额外 wrapper 或返回状态。 |
| 行 2，固定 UTF-16 `psb` 字面量 | `PSBMedia.h:25` 使用 `TJS_W("psb")`；Android 原始字节独立证明同一 3 个 UTF-16 code unit。 |
| 行 3，仅把字面量赋给输出，且不读取对象状态 | `PSBMedia.h:25` 的完整方法体只有 `name = TJS_W("psb")`；`tjsString.h:147-157` 提供同型的 `ttstr` 字面量赋值语义。 |
| 行 4，无本地判断或错误转换 | 本地该 inline 方法没有条件、fallback、catch 或 null 检查；底层赋值的分配/异常语义直接向上传递。 |
| Android vslot `+0x20` 间接调用 | `StorageIntf.h:114-122` 的虚接口顺序与 `PSBMedia.h:25` 的 `override` 保留该接口边界；`StorageIntf.cpp:281-283` 通过基类指针虚调用，而非绕开 vdispatch。 |

## 六维审计

| 维度 | 状态 | Android 证据与本地对照 |
| --- | --- | --- |
| 源代码结构 | `MATCH` | Android emitted 入口是一个无分支的 `PSBMedia` vtable member，仅尾调用 `ttstr` 字面量赋值；本地 `PSBMedia.h:25` 同样是单一 inline `override` 赋值体。是否原始源码也写成 inline 无法由 stripped/O3 唯一证明，但当前 emitted 结构不存在确定差异。 |
| 数据流 | `MATCH` | Android 抛弃 `self`，把第二参数重排为赋值目标并把唯一常量 `L"psb"` 作为源；本地只写 `name`，不读取 `_ref`、`_file` 或 `_container`。 |
| 调用链 | `MATCH` | Android 地址位于 `0x1A0B510+0x20`，通用 manager 在 `0x8E8858` 经 `vptr+32` 调用；本地保留 `iTVPStorageMedia::GetName` → `PSBMedia::GetName` 的虚分派，manager 在 `StorageIntf.cpp:283` 经接口调用。canonical parent 仅表示 vtable 归属。 |
| 对象生命周期 | `MATCH` | 本函数不改变 `PSBMedia` 引用计数或所有权；唯一生命周期效应是用通用 `ttstr` 赋值路径替换 caller-owned 输出字符串的旧内容。Android 尾调用 `sub_54DEFC`，本地调用 `tTJSString::operator=(const tjs_char *)`，均没有额外临时 owner 或吞异常逻辑。 |
| 内部容器实现 | `N/A` | 该 emitted 入口不创建、读取、遍历或修改任何容器；仅执行字符串赋值。storage manager 的哈希表属于外部调用上下文，不属于 `0x5998A8`。 |
| 边界行为 | `MATCH` | Android 目标函数本身没有条件、默认值、null guard 或失败状态码；非空固定字面量覆盖空/非空输出，底层分配失败不在此捕获。本文所对照本地方法具备相同边界。 |

## 总判定

`ALIGNED`

在本轮 fresh decompile、4 指令反汇编、vtable 原始数据、UTF-16 原始字节及间接调用点
可以证明的范围内，未发现 `0x5998A8` 与本地 `PSBMedia::GetName` 的确定六维偏差。
canonical 名称中的 `_guess` 只表示原始 C++ 标识符未保留，不构成 GAP。

## 所有确定 GAP

无。

## Cross-reference 与阶段 B 子节点

- TASK_TREE 的 cross-reference 列表没有涉及 `@5998A8` 的非 canonical 边。
- `0x1A0B530` 是 vtable 数据槽，不是直接调用边；`0x8E8858` 是 MANIFEST 外通用
  storage-media manager 的间接调用上下文，二者均不得据此创建重复 function agent。
- TASK_TREE 中本节点尚待派发的直接 canonical child：**无**。

